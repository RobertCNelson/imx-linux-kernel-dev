From 4e91cf8265a7e15e3dca4413467f7dfeeef00bc3 Mon Sep 17 00:00:00 2001
From: Robert Nelson <robertcnelson@gmail.com>
Date: Thu, 17 Sep 2015 15:45:47 -0500
Subject: [PATCH 89/97] adjust backports config symbols we port

---
 backports/compat/Kconfig                           |  72 +--
 backports/drivers/bcma/Kconfig                     |  76 +--
 backports/drivers/bluetooth/Kconfig                | 121 +++--
 backports/drivers/media/Kconfig                    |  74 +--
 backports/drivers/media/common/Kconfig             |  14 +-
 backports/drivers/media/common/b2c2/Kconfig        |  38 +-
 backports/drivers/media/common/saa7146/Kconfig     |  12 +-
 backports/drivers/media/common/siano/Kconfig       |  25 +-
 backports/drivers/media/dvb-core/Kconfig           |  10 +-
 backports/drivers/media/dvb-frontends/Kconfig      | 522 ++++++++++++---------
 .../drivers/media/dvb-frontends/drx39xyj/Kconfig   |   5 +-
 backports/drivers/media/firewire/Kconfig           |  12 +-
 backports/drivers/media/i2c/Kconfig                | 399 +++++++++-------
 backports/drivers/media/i2c/cx25840/Kconfig        |   5 +-
 backports/drivers/media/i2c/m5mols/Kconfig         |   7 +-
 backports/drivers/media/i2c/smiapp/Kconfig         |   9 +-
 backports/drivers/media/i2c/soc_camera/Kconfig     |  70 +--
 backports/drivers/media/mmc/siano/Kconfig          |  11 +-
 backports/drivers/media/pci/Kconfig                |  15 +-
 backports/drivers/media/pci/b2c2/Kconfig           |  12 +-
 backports/drivers/media/pci/bt8xx/Kconfig          |  48 +-
 backports/drivers/media/pci/cx18/Kconfig           |  32 +-
 backports/drivers/media/pci/cx23885/Kconfig        |  88 ++--
 backports/drivers/media/pci/cx25821/Kconfig        |  12 +-
 backports/drivers/media/pci/cx88/Kconfig           |  85 ++--
 backports/drivers/media/pci/ddbridge/Kconfig       |  15 +-
 backports/drivers/media/pci/dm1105/Kconfig         |  23 +-
 backports/drivers/media/pci/ivtv/Kconfig           |  47 +-
 backports/drivers/media/pci/mantis/Kconfig         |  39 +-
 backports/drivers/media/pci/meye/Kconfig           |   5 +-
 backports/drivers/media/pci/ngene/Kconfig          |  19 +-
 backports/drivers/media/pci/pluto2/Kconfig         |   7 +-
 backports/drivers/media/pci/pt1/Kconfig            |   5 +-
 backports/drivers/media/pci/pt3/Kconfig            |  11 +-
 backports/drivers/media/pci/saa7134/Kconfig        |  79 ++--
 backports/drivers/media/pci/saa7146/Kconfig        |  31 +-
 backports/drivers/media/pci/saa7164/Kconfig        |  17 +-
 backports/drivers/media/pci/smipcie/Kconfig        |  15 +-
 backports/drivers/media/pci/solo6x10/Kconfig       |   9 +-
 backports/drivers/media/pci/sta2x11/Kconfig        |  11 +-
 backports/drivers/media/pci/ttpci/Kconfig          | 144 +++---
 backports/drivers/media/pci/tw68/Kconfig           |   7 +-
 backports/drivers/media/pci/zoran/Kconfig          |  58 ++-
 backports/drivers/media/platform/Kconfig           | 190 ++++----
 backports/drivers/media/platform/am437x/Kconfig    |   7 +-
 backports/drivers/media/platform/blackfin/Kconfig  |  14 +-
 backports/drivers/media/platform/davinci/Kconfig   |  50 +-
 .../drivers/media/platform/exynos4-is/Kconfig      |  43 +-
 .../drivers/media/platform/marvell-ccic/Kconfig    |  22 +-
 backports/drivers/media/platform/omap/Kconfig      |  12 +-
 backports/drivers/media/platform/s5p-tv/Kconfig    |  56 ++-
 .../drivers/media/platform/soc_camera/Kconfig      |  79 ++--
 backports/drivers/media/platform/vivid/Kconfig     |   7 +-
 backports/drivers/media/platform/xilinx/Kconfig    |  21 +-
 backports/drivers/media/radio/Kconfig              | 238 ++++++----
 backports/drivers/media/radio/si470x/Kconfig       |  10 +-
 backports/drivers/media/radio/si4713/Kconfig       |  19 +-
 backports/drivers/media/radio/wl128x/Kconfig       |   5 +-
 backports/drivers/media/rc/Kconfig                 | 184 +++++---
 backports/drivers/media/rc/img-ir/Kconfig          |  52 +-
 backports/drivers/media/rc/keymaps/Kconfig         |   5 +-
 backports/drivers/media/tuners/Kconfig             | 206 ++++----
 backports/drivers/media/usb/Kconfig                |  15 +-
 backports/drivers/media/usb/airspy/Kconfig         |   7 +-
 backports/drivers/media/usb/as102/Kconfig          |   5 +-
 backports/drivers/media/usb/au0828/Kconfig         |  33 +-
 backports/drivers/media/usb/b2c2/Kconfig           |  12 +-
 backports/drivers/media/usb/cpia2/Kconfig          |   5 +-
 backports/drivers/media/usb/cx231xx/Kconfig        |  52 +-
 backports/drivers/media/usb/dvb-usb-v2/Kconfig     | 195 ++++----
 backports/drivers/media/usb/dvb-usb/Kconfig        | 311 ++++++------
 backports/drivers/media/usb/em28xx/Kconfig         |  85 ++--
 backports/drivers/media/usb/go7007/Kconfig         |  38 +-
 backports/drivers/media/usb/gspca/Kconfig          | 237 ++++++----
 backports/drivers/media/usb/gspca/gl860/Kconfig    |   5 +-
 backports/drivers/media/usb/gspca/m5602/Kconfig    |   5 +-
 backports/drivers/media/usb/gspca/stv06xx/Kconfig  |   5 +-
 backports/drivers/media/usb/hackrf/Kconfig         |   7 +-
 backports/drivers/media/usb/hdpvr/Kconfig          |   5 +-
 backports/drivers/media/usb/msi2500/Kconfig        |   9 +-
 backports/drivers/media/usb/pvrusb2/Kconfig        |  50 +-
 backports/drivers/media/usb/pwc/Kconfig            |  17 +-
 backports/drivers/media/usb/s2255/Kconfig          |   7 +-
 backports/drivers/media/usb/siano/Kconfig          |  11 +-
 backports/drivers/media/usb/stk1160/Kconfig        |  19 +-
 backports/drivers/media/usb/stkwebcam/Kconfig      |   5 +-
 backports/drivers/media/usb/tm6000/Kconfig         |  25 +-
 backports/drivers/media/usb/ttusb-budget/Kconfig   |  19 +-
 backports/drivers/media/usb/ttusb-dec/Kconfig      |   5 +-
 backports/drivers/media/usb/usbtv/Kconfig          |   7 +-
 backports/drivers/media/usb/usbvision/Kconfig      |   9 +-
 backports/drivers/media/usb/uvc/Kconfig            |  14 +-
 backports/drivers/media/usb/zr364xx/Kconfig        |   9 +-
 backports/drivers/media/v4l2-core/Kconfig          |  85 ++--
 backports/drivers/net/ethernet/Kconfig             |  39 +-
 backports/drivers/net/ethernet/atheros/Kconfig     |  24 +-
 backports/drivers/net/ethernet/broadcom/Kconfig    |  79 ++--
 backports/drivers/net/ethernet/intel/Kconfig       |  92 ++--
 backports/drivers/net/ieee802154/Kconfig           |  19 +-
 backports/drivers/net/usb/Kconfig                  | 205 ++++----
 backports/drivers/net/wireless/Kconfig             | 102 ++--
 backports/drivers/net/wireless/ath/Kconfig         |  28 +-
 backports/drivers/net/wireless/ath/ar5523/Kconfig  |   9 +-
 backports/drivers/net/wireless/ath/ath10k/Kconfig  |  32 +-
 backports/drivers/net/wireless/ath/ath5k/Kconfig   |  34 +-
 backports/drivers/net/wireless/ath/ath6kl/Kconfig  |  32 +-
 backports/drivers/net/wireless/ath/ath9k/Kconfig   | 104 ++--
 .../drivers/net/wireless/ath/carl9170/Kconfig      |  29 +-
 backports/drivers/net/wireless/ath/wcn36xx/Kconfig |  10 +-
 backports/drivers/net/wireless/ath/wil6210/Kconfig |  15 +-
 backports/drivers/net/wireless/b43/Kconfig         | 139 +++---
 backports/drivers/net/wireless/b43legacy/Kconfig   |  71 +--
 backports/drivers/net/wireless/brcm80211/Kconfig   |  76 +--
 backports/drivers/net/wireless/cw1200/Kconfig      |  25 +-
 backports/drivers/net/wireless/ipw2x00/Kconfig     |  81 ++--
 backports/drivers/net/wireless/iwlegacy/Kconfig    |  31 +-
 backports/drivers/net/wireless/iwlwifi/Kconfig     |  57 ++-
 backports/drivers/net/wireless/libertas/Kconfig    |  37 +-
 backports/drivers/net/wireless/mwifiex/Kconfig     |  20 +-
 backports/drivers/net/wireless/orinoco/Kconfig     |  59 ++-
 backports/drivers/net/wireless/p54/Kconfig         |  30 +-
 backports/drivers/net/wireless/rsi/Kconfig         |  20 +-
 backports/drivers/net/wireless/rt2x00/Kconfig      | 173 ++++---
 backports/drivers/net/wireless/rtl818x/Kconfig     |  21 +-
 backports/drivers/net/wireless/rtlwifi/Kconfig     | 123 ++---
 backports/drivers/net/wireless/ti/Kconfig          |  14 +-
 backports/drivers/net/wireless/ti/wl1251/Kconfig   |  15 +-
 backports/drivers/net/wireless/ti/wl12xx/Kconfig   |   7 +-
 backports/drivers/net/wireless/ti/wl18xx/Kconfig   |   7 +-
 backports/drivers/net/wireless/ti/wlcore/Kconfig   |  21 +-
 backports/drivers/net/wireless/zd1211rw/Kconfig    |  10 +-
 backports/drivers/nfc/Kconfig                      |  42 +-
 backports/drivers/nfc/microread/Kconfig            |  19 +-
 backports/drivers/nfc/nfcmrvl/Kconfig              |  22 +-
 backports/drivers/nfc/nxp-nci/Kconfig              |  12 +-
 backports/drivers/nfc/pn544/Kconfig                |  17 +-
 backports/drivers/nfc/st21nfca/Kconfig             |  12 +-
 backports/drivers/nfc/st21nfcb/Kconfig             |  12 +-
 backports/drivers/ssb/Kconfig                      | 125 +++--
 backports/drivers/usb/class/Kconfig                |  12 +-
 backports/net/6lowpan/Kconfig                      |  31 +-
 backports/net/bluetooth/Kconfig                    |  38 +-
 backports/net/bluetooth/bnep/Kconfig               |  15 +-
 backports/net/bluetooth/cmtp/Kconfig               |   5 +-
 backports/net/bluetooth/hidp/Kconfig               |   5 +-
 backports/net/bluetooth/rfcomm/Kconfig             |  10 +-
 backports/net/ieee802154/6lowpan/Kconfig           |   5 +-
 backports/net/ieee802154/Kconfig                   |   8 +-
 backports/net/mac80211/Kconfig                     | 164 ++++---
 backports/net/mac802154/Kconfig                    |   5 +-
 backports/net/nfc/Kconfig                          |  16 +-
 backports/net/nfc/hci/Kconfig                      |  12 +-
 backports/net/nfc/nci/Kconfig                      |  10 +-
 backports/net/wireless/Kconfig                     |  77 +--
 154 files changed, 4283 insertions(+), 3130 deletions(-)

diff --git a/backports/compat/Kconfig b/backports/compat/Kconfig
index 8fcdb8a..bdb24c3 100644
--- a/backports/compat/Kconfig
+++ b/backports/compat/Kconfig
@@ -29,7 +29,7 @@
 #	default m if BPAUTO_FOO
 #
 #	# or for build-testing (BPAUTO_USERSEL_BUILD_ALL is enabled)
-#	default m if BPAUTO_USERSEL_BUILD_ALL
+#	default m if BACKPORT_BPAUTO_USERSEL_BUILD_ALL
 #
 # config BPAUTO_FOO
 #	bool
@@ -53,33 +53,39 @@
 # Variations are obviously possible.
 #
 
-config BPAUTO_BUILD_CORDIC
+config BACKPORT_BPAUTO_BUILD_CORDIC
 	tristate
+	depends on !BPAUTO_BUILD_CORDIC
 	depends on !CORDIC
-	default m if BPAUTO_CORDIC
-	default m if BPAUTO_USERSEL_BUILD_ALL
+	default m if BACKPORT_BPAUTO_CORDIC
+	default m if BACKPORT_BPAUTO_USERSEL_BUILD_ALL
 	#module-name cordic
 	#c-file lib/cordic.c
 
-config BPAUTO_CORDIC
+config BACKPORT_BPAUTO_CORDIC
 	bool
+	depends on !BPAUTO_CORDIC
 
-config BPAUTO_BUILD_AVERAGE
+config BACKPORT_BPAUTO_BUILD_AVERAGE
 	bool
+	depends on !BPAUTO_BUILD_AVERAGE
 	depends on !AVERAGE
-	default y if BPAUTO_USERSEL_BUILD_ALL
-	default y if BPAUTO_AVERAGE
+	default y if BACKPORT_BPAUTO_USERSEL_BUILD_ALL
+	default y if BACKPORT_BPAUTO_AVERAGE
 	#h-file linux/average.h
 	#c-file lib/average.c
 
-config BPAUTO_AVERAGE
+config BACKPORT_BPAUTO_AVERAGE
 	bool
+	depends on !BPAUTO_AVERAGE
 
-config BPAUTO_MII
+config BACKPORT_BPAUTO_MII
 	bool
+	depends on !BPAUTO_MII
 
-config BPAUTO_BUILD_DMA_SHARED_HELPERS
+config BACKPORT_BPAUTO_BUILD_DMA_SHARED_HELPERS
 	bool
+	depends on !BPAUTO_BUILD_DMA_SHARED_HELPERS
 	depends on HAS_DMA
 	# Build on other kernels kernels < 3.9 if HAVE_GENERIC_DMA_COHERENT is
 	# not set. Kernels >= 3.8 have this if HAS_DMA is set.
@@ -87,26 +93,31 @@ config BPAUTO_BUILD_DMA_SHARED_HELPERS
 	# Always build if on 3.3 - 3.5
 	default y if (KERNEL_3_4 || KERNEL_3_5 || KERNEL_3_6)
 	# build for testing
-	default y if BPAUTO_USERSEL_BUILD_ALL
+	default y if BACKPORT_BPAUTO_USERSEL_BUILD_ALL
 
-config BPAUTO_BUILD_LEDS
+config BACKPORT_BPAUTO_BUILD_LEDS
 	bool
+	depends on !BPAUTO_BUILD_LEDS
 	depends on !NEW_LEDS || LEDS_CLASS=n || !LEDS_TRIGGERS
-	default y if BPAUTO_NEW_LEDS
-	default y if BPAUTO_LEDS_CLASS
-	default y if BPAUTO_LEDS_TRIGGERS
+	default y if BACKPORT_BPAUTO_NEW_LEDS
+	default y if BACKPORT_BPAUTO_LEDS_CLASS
+	default y if BACKPORT_BPAUTO_LEDS_TRIGGERS
 
-config BPAUTO_NEW_LEDS
+config BACKPORT_BPAUTO_NEW_LEDS
 	bool
+	depends on !BPAUTO_NEW_LEDS
 
-config BPAUTO_LEDS_CLASS
+config BACKPORT_BPAUTO_LEDS_CLASS
 	bool
+	depends on !BPAUTO_LEDS_CLASS
 
-config BPAUTO_LEDS_TRIGGERS
+config BACKPORT_BPAUTO_LEDS_TRIGGERS
 	bool
+	depends on !BPAUTO_LEDS_TRIGGERS
 
-config BPAUTO_USERSEL_BUILD_ALL
+config BACKPORT_BPAUTO_USERSEL_BUILD_ALL
 	bool "Build all compat code"
+	depends on !BPAUTO_USERSEL_BUILD_ALL
 	help
 	  This option selects all the compat code options
 	  that would otherwise only be selected by drivers.
@@ -114,33 +125,38 @@ config BPAUTO_USERSEL_BUILD_ALL
 	  It's only really useful for compat testing, so
 	  you probably shouldn't enable it.
 
-config BPAUTO_CRYPTO_CCM
+config BACKPORT_BPAUTO_CRYPTO_CCM
 	depends on CRYPTO_AEAD
+	depends on !BPAUTO_CRYPTO_CCM
 	depends on CRYPTO_CTR
 	bool
 
-config BPAUTO_BUILD_CRYPTO_CCM
+config BACKPORT_BPAUTO_BUILD_CRYPTO_CCM
 	bool
+	depends on !BPAUTO_BUILD_CRYPTO_CCM
 	default n if CRYPTO_CCM
-	default y if BPAUTO_CRYPTO_CCM
+	default y if BACKPORT_BPAUTO_CRYPTO_CCM
 	#c-file crypto/ccm.c
 
-config BPAUTO_WANT_DEV_COREDUMP
+config BACKPORT_BPAUTO_WANT_DEV_COREDUMP
 	bool
+	depends on !BPAUTO_WANT_DEV_COREDUMP
 
-config BPAUTO_BUILD_WANT_DEV_COREDUMP
+config BACKPORT_BPAUTO_BUILD_WANT_DEV_COREDUMP
 	bool
+	depends on !BPAUTO_BUILD_WANT_DEV_COREDUMP
 	default n if DEV_COREDUMP
 	default n if DISABLE_DEV_COREDUMP
-	default y if BPAUTO_WANT_DEV_COREDUMP
+	default y if BACKPORT_BPAUTO_WANT_DEV_COREDUMP
 	#h-file linux/devcoredump.h
 	#c-file drivers/base/devcoredump.c
 
-config BPAUTO_RHASHTABLE
+config BACKPORT_BPAUTO_RHASHTABLE
 	bool
+	depends on !BPAUTO_RHASHTABLE
 	# current API of rhashtable was introduced in version 4.1
 	depends on KERNEL_4_1
 	# not very nice - but better than always having it
-	default y if MAC80211
+	default y if BACKPORT_MAC80211
 	#h-file linux/rhashtable.h
 	#c-file lib/rhashtable.c
diff --git a/backports/drivers/bcma/Kconfig b/backports/drivers/bcma/Kconfig
index fc6ffcf..7bdb10f 100644
--- a/backports/drivers/bcma/Kconfig
+++ b/backports/drivers/bcma/Kconfig
@@ -1,43 +1,50 @@
-config BCMA_POSSIBLE
+config BACKPORT_BCMA_POSSIBLE
 	bool
+	depends on !BCMA_POSSIBLE
 	depends on HAS_IOMEM && HAS_DMA
 	default y
 
 menu "Broadcom specific AMBA"
-	depends on BCMA_POSSIBLE
+	depends on BACKPORT_BCMA_POSSIBLE
 
-config BCMA
+config BACKPORT_BCMA
 	tristate "BCMA support"
-	depends on BCMA_POSSIBLE
+	depends on !BCMA
+	depends on BACKPORT_BCMA_POSSIBLE
 	help
 	  Bus driver for Broadcom specific Advanced Microcontroller Bus
 	  Architecture.
 
 # Support for Block-I/O. SELECT this from the driver that needs it.
-config BCMA_BLOCKIO
+config BACKPORT_BCMA_BLOCKIO
 	bool
-	depends on BCMA
+	depends on !BCMA_BLOCKIO
+	depends on BACKPORT_BCMA
 
-config BCMA_HOST_PCI_POSSIBLE
+config BACKPORT_BCMA_HOST_PCI_POSSIBLE
 	bool
-	depends on BCMA && PCI = y
+	depends on !BCMA_HOST_PCI_POSSIBLE
+	depends on BACKPORT_BCMA && PCI = y
 	default y
 
-config BCMA_HOST_PCI
+config BACKPORT_BCMA_HOST_PCI
 	bool "Support for BCMA on PCI-host bus"
-	depends on BCMA_HOST_PCI_POSSIBLE
-	select BCMA_DRIVER_PCI
+	depends on !BCMA_HOST_PCI
+	depends on BACKPORT_BCMA_HOST_PCI_POSSIBLE
+	select BACKPORT_BCMA_DRIVER_PCI
 	default y
 
-config BCMA_DRIVER_PCI_HOSTMODE
+config BACKPORT_BCMA_DRIVER_PCI_HOSTMODE
 	bool "Driver for PCI core working in hostmode"
-	depends on BCMA && MIPS && BCMA_HOST_PCI
+	depends on !BCMA_DRIVER_PCI_HOSTMODE
+	depends on BACKPORT_BCMA && MIPS && BACKPORT_BCMA_HOST_PCI
 	help
 	  PCI core hostmode operation (external PCI bus).
 
-config BCMA_HOST_SOC
+config BACKPORT_BCMA_HOST_SOC
 	bool "Support for BCMA in a SoC"
-	depends on BCMA
+	depends on !BCMA_HOST_SOC
+	depends on BACKPORT_BCMA
 	help
 	  Host interface for a Broadcom AIX bus directly mapped into
 	  the memory. This only works with the Broadcom SoCs from the
@@ -45,12 +52,13 @@ config BCMA_HOST_SOC
 
 	  If unsure, say N
 
-config BCMA_DRIVER_PCI
+config BACKPORT_BCMA_DRIVER_PCI
 	bool "BCMA Broadcom PCI core driver"
-	depends on BCMA && PCI
+	depends on !BCMA_DRIVER_PCI
+	depends on BACKPORT_BCMA && PCI
 	default y
 	help
-	  BCMA bus may have many versions of PCIe core. This driver
+	  BACKPORT_BCMA bus may have many versions of PCIe core. This driver
 	  supports:
 	  1) PCIe core working in clientmode
 	  2) PCIe Gen 2 clientmode core
@@ -61,46 +69,52 @@ config BCMA_DRIVER_PCI
 	  This driver is also prerequisite for a hostmode PCIe core
 	  support.
 
-config BCMA_DRIVER_MIPS
+config BACKPORT_BCMA_DRIVER_MIPS
 	bool "BCMA Broadcom MIPS core driver"
-	depends on BCMA && MIPS
+	depends on !BCMA_DRIVER_MIPS
+	depends on BACKPORT_BCMA && MIPS
 	help
 	  Driver for the Broadcom MIPS core attached to Broadcom specific
 	  Advanced Microcontroller Bus.
 
 	  If unsure, say N
 
-config BCMA_SFLASH
+config BACKPORT_BCMA_SFLASH
 	bool
-	depends on BCMA_DRIVER_MIPS
+	depends on !BCMA_SFLASH
+	depends on BACKPORT_BCMA_DRIVER_MIPS
 	default y
 
-config BCMA_NFLASH
+config BACKPORT_BCMA_NFLASH
 	bool
-	depends on BCMA_DRIVER_MIPS
+	depends on !BCMA_NFLASH
+	depends on BACKPORT_BCMA_DRIVER_MIPS
 	default y
 
-config BCMA_DRIVER_GMAC_CMN
+config BACKPORT_BCMA_DRIVER_GMAC_CMN
 	bool "BCMA Broadcom GBIT MAC COMMON core driver"
-	depends on BCMA
+	depends on !BCMA_DRIVER_GMAC_CMN
+	depends on BACKPORT_BCMA
 	help
 	  Driver for the Broadcom GBIT MAC COMMON core attached to Broadcom
 	  specific Advanced Microcontroller Bus.
 
 	  If unsure, say N
 
-config BCMA_DRIVER_GPIO
+config BACKPORT_BCMA_DRIVER_GPIO
 	bool "BCMA GPIO driver"
-	depends on BCMA && GPIOLIB
-	select IRQ_DOMAIN if BCMA_HOST_SOC
+	depends on !BCMA_DRIVER_GPIO
+	depends on BACKPORT_BCMA && GPIOLIB
+	select IRQ_DOMAIN if BACKPORT_BCMA_HOST_SOC
 	help
 	  Driver to provide access to the GPIO pins of the bcma bus.
 
 	  If unsure, say N
 
-config BCMA_DEBUG
+config BACKPORT_BCMA_DEBUG
 	bool "BCMA debugging"
-	depends on BCMA
+	depends on !BCMA_DEBUG
+	depends on BACKPORT_BCMA
 	help
 	  This turns on additional debugging messages.
 
diff --git a/backports/drivers/bluetooth/Kconfig b/backports/drivers/bluetooth/Kconfig
index ed5c273..d92ba5f 100644
--- a/backports/drivers/bluetooth/Kconfig
+++ b/backports/drivers/bluetooth/Kconfig
@@ -1,18 +1,21 @@
 
 menu "Bluetooth device drivers"
-	depends on BT
+	depends on BACKPORT_BT
 
-config BT_INTEL
+config BACKPORT_BT_INTEL
 	tristate
+	depends on !BT_INTEL
 
-config BT_BCM
+config BACKPORT_BT_BCM
 	tristate
+	depends on !BT_BCM
 	select FW_LOADER
 
-config BT_HCIBTUSB
+config BACKPORT_BT_HCIBTUSB
 	tristate "HCI USB driver"
+	depends on !BT_HCIBTUSB
 	depends on USB
-	select BT_INTEL
+	select BACKPORT_BT_INTEL
 	help
 	  Bluetooth HCI USB driver.
 	  This driver is required if you want to use Bluetooth devices with
@@ -21,10 +24,11 @@ config BT_HCIBTUSB
 	  Say Y here to compile support for Bluetooth USB devices into the
 	  kernel or say M to compile it as module (btusb).
 
-config BT_HCIBTUSB_BCM
+config BACKPORT_BT_HCIBTUSB_BCM
 	bool "Broadcom protocol support"
-	depends on BT_HCIBTUSB
-	select BT_BCM
+	depends on !BT_HCIBTUSB_BCM
+	depends on BACKPORT_BT_HCIBTUSB
+	select BACKPORT_BT_BCM
 	default y
 	help
 	  The Broadcom protocol support enables firmware and patchram
@@ -32,8 +36,9 @@ config BT_HCIBTUSB_BCM
 
 	  Say Y here to compile support for Broadcom protocol.
 
-config BT_HCIBTSDIO
+config BACKPORT_BT_HCIBTSDIO
 	tristate "HCI SDIO driver"
+	depends on !BT_HCIBTSDIO
 	depends on MMC
 	help
 	  Bluetooth HCI SDIO driver.
@@ -43,8 +48,9 @@ config BT_HCIBTSDIO
 	  Say Y here to compile support for Bluetooth SDIO devices into the
 	  kernel or say M to compile it as module (btsdio).
 
-config BT_HCIUART
+config BACKPORT_BT_HCIUART
 	tristate "HCI UART driver"
+	depends on !BT_HCIUART
 	depends on TTY
 	help
 	  Bluetooth HCI UART driver.
@@ -56,9 +62,10 @@ config BT_HCIUART
 	  Say Y here to compile support for Bluetooth UART devices into the
 	  kernel or say M to compile it as module (hci_uart).
 
-config BT_HCIUART_H4
+config BACKPORT_BT_HCIUART_H4
 	bool "UART (H4) protocol support"
-	depends on BT_HCIUART
+	depends on !BT_HCIUART_H4
+	depends on BACKPORT_BT_HCIUART
 	help
 	  UART (H4) is serial protocol for communication between Bluetooth
 	  device and host. This protocol is required for most Bluetooth devices
@@ -66,9 +73,10 @@ config BT_HCIUART_H4
 
 	  Say Y here to compile support for HCI UART (H4) protocol.
 
-config BT_HCIUART_BCSP
+config BACKPORT_BT_HCIUART_BCSP
 	bool "BCSP protocol support"
-	depends on BT_HCIUART
+	depends on !BT_HCIUART_BCSP
+	depends on BACKPORT_BT_HCIUART
 	select BITREVERSE
 	help
 	  BCSP (BlueCore Serial Protocol) is serial protocol for communication
@@ -78,10 +86,11 @@ config BT_HCIUART_BCSP
 
 	  Say Y here to compile support for HCI BCSP protocol.
 
-config BT_HCIUART_ATH3K
+config BACKPORT_BT_HCIUART_ATH3K
 	bool "Atheros AR300x serial support"
-	depends on BT_HCIUART
-	select BT_HCIUART_H4
+	depends on !BT_HCIUART_ATH3K
+	depends on BACKPORT_BT_HCIUART
+	select BACKPORT_BT_HCIUART_H4
 	help
 	  HCIATH3K (HCI Atheros AR300x) is a serial protocol for
 	  communication between host and Atheros AR300x Bluetooth devices.
@@ -91,9 +100,10 @@ config BT_HCIUART_ATH3K
 
 	  Say Y here to compile support for HCI UART ATH3K protocol.
 
-config BT_HCIUART_LL
+config BACKPORT_BT_HCIUART_LL
 	bool "HCILL protocol support"
-	depends on BT_HCIUART
+	depends on !BT_HCIUART_LL
+	depends on BACKPORT_BT_HCIUART
 	help
 	  HCILL (HCI Low Level) is a serial protocol for communication
 	  between Bluetooth device and host. This protocol is required for
@@ -102,9 +112,10 @@ config BT_HCIUART_LL
 
 	  Say Y here to compile support for HCILL protocol.
 
-config BT_HCIUART_3WIRE
+config BACKPORT_BT_HCIUART_3WIRE
 	bool "Three-wire UART (H5) protocol support"
-	depends on BT_HCIUART
+	depends on !BT_HCIUART_3WIRE
+	depends on BACKPORT_BT_HCIUART
 	help
 	  The HCI Three-wire UART Transport Layer makes it possible to
 	  user the Bluetooth HCI over a serial port interface. The HCI
@@ -114,29 +125,32 @@ config BT_HCIUART_3WIRE
 
 	  Say Y here to compile support for Three-wire UART protocol.
 
-config BT_HCIUART_INTEL
+config BACKPORT_BT_HCIUART_INTEL
 	bool "Intel protocol support"
-	depends on BT_HCIUART
-	select BT_INTEL
+	depends on !BT_HCIUART_INTEL
+	depends on BACKPORT_BT_HCIUART
+	select BACKPORT_BT_INTEL
 	help
 	  The Intel protocol support enables Bluetooth HCI over serial
 	  port interface for Intel Bluetooth controllers.
 
 	  Say Y here to compile support for Intel protocol.
 
-config BT_HCIUART_BCM
+config BACKPORT_BT_HCIUART_BCM
 	bool "Broadcom protocol support"
-	depends on BT_HCIUART
-	select BT_HCIUART_H4
-	select BT_BCM
+	depends on !BT_HCIUART_BCM
+	depends on BACKPORT_BT_HCIUART
+	select BACKPORT_BT_HCIUART_H4
+	select BACKPORT_BT_BCM
 	help
 	  The Broadcom protocol support enables Bluetooth HCI over serial
 	  port interface for Broadcom Bluetooth controllers.
 
 	  Say Y here to compile support for Broadcom protocol.
 
-config BT_HCIBCM203X
+config BACKPORT_BT_HCIBCM203X
 	tristate "HCI BCM203x USB driver"
+	depends on !BT_HCIBCM203X
 	depends on USB
 	select FW_LOADER
 	help
@@ -147,8 +161,9 @@ config BT_HCIBCM203X
 	  Say Y here to compile support for HCI BCM203x devices into the
 	  kernel or say M to compile it as module (bcm203x).
 
-config BT_HCIBPA10X
+config BACKPORT_BT_HCIBPA10X
 	tristate "HCI BPA10x USB driver"
+	depends on !BT_HCIBPA10X
 	depends on USB
 	help
 	  Bluetooth HCI BPA10x USB driver.
@@ -158,8 +173,9 @@ config BT_HCIBPA10X
 	  Say Y here to compile support for HCI BPA10x devices into the
 	  kernel or say M to compile it as module (bpa10x).
 
-config BT_HCIBFUSB
+config BACKPORT_BT_HCIBFUSB
 	tristate "HCI BlueFRITZ! USB driver"
+	depends on !BT_HCIBFUSB
 	depends on USB
 	select FW_LOADER
 	help
@@ -171,8 +187,9 @@ config BT_HCIBFUSB
 	  Say Y here to compile support for HCI BFUSB devices into the
 	  kernel or say M to compile it as module (bfusb).
 
-config BT_HCIDTL1
+config BACKPORT_BT_HCIDTL1
 	tristate "HCI DTL1 (PC Card) driver"
+	depends on !BT_HCIDTL1
 	depends on PCMCIA
 	help
 	  Bluetooth HCI DTL1 (PC Card) driver.
@@ -184,22 +201,24 @@ config BT_HCIDTL1
 	  Say Y here to compile support for HCI DTL1 devices into the
 	  kernel or say M to compile it as module (dtl1_cs).
 
-config BT_HCIBT3C
+config BACKPORT_BT_HCIBT3C
 	tristate "HCI BT3C (PC Card) driver"
+	depends on !BT_HCIBT3C
 	depends on PCMCIA
 	select FW_LOADER
 	help
-	  Bluetooth HCI BT3C (PC Card) driver.
+	  Bluetooth HCI BACKPORT_BT3C (PC Card) driver.
 	  This driver provides support for Bluetooth PCMCIA devices with
-	  3Com BT3C interface:
+	  3Com BACKPORT_BT3C interface:
 	     3Com Bluetooth Card (3CRWB6096)
 	     HP Bluetooth Card
 
-	  Say Y here to compile support for HCI BT3C devices into the
+	  Say Y here to compile support for HCI BACKPORT_BT3C devices into the
 	  kernel or say M to compile it as module (bt3c_cs).
 
-config BT_HCIBLUECARD
+config BACKPORT_BT_HCIBLUECARD
 	tristate "HCI BlueCard (PC Card) driver"
+	depends on !BT_HCIBLUECARD
 	depends on PCMCIA
 	help
 	  Bluetooth HCI BlueCard (PC Card) driver.
@@ -211,8 +230,9 @@ config BT_HCIBLUECARD
 	  Say Y here to compile support for HCI BlueCard devices into the
 	  kernel or say M to compile it as module (bluecard_cs).
 
-config BT_HCIBTUART
+config BACKPORT_BT_HCIBTUART
 	tristate "HCI UART (PC Card) device driver"
+	depends on !BT_HCIBTUART
 	depends on PCMCIA
 	help
 	  Bluetooth HCI UART (PC Card) driver.
@@ -227,8 +247,9 @@ config BT_HCIBTUART
 	  Say Y here to compile support for HCI UART devices into the
 	  kernel or say M to compile it as module (btuart_cs).
 
-config BT_HCIVHCI
+config BACKPORT_BT_HCIVHCI
 	tristate "HCI VHCI (Virtual HCI device) driver"
+	depends on !BT_HCIVHCI
 	help
 	  Bluetooth Virtual HCI device driver.
 	  This driver is required if you want to use HCI Emulation software.
@@ -236,8 +257,9 @@ config BT_HCIVHCI
 	  Say Y here to compile support for virtual HCI devices into the
 	  kernel or say M to compile it as module (hci_vhci).
 
-config BT_MRVL
+config BACKPORT_BT_MRVL
 	tristate "Marvell Bluetooth driver support"
+	depends on !BT_MRVL
 	help
 	  The core driver to support Marvell Bluetooth devices.
 
@@ -247,9 +269,10 @@ config BT_MRVL
 	  Say Y here to compile Marvell Bluetooth driver
 	  into the kernel or say M to compile it as module.
 
-config BT_MRVL_SDIO
+config BACKPORT_BT_MRVL_SDIO
 	tristate "Marvell BT-over-SDIO driver"
-	depends on BT_MRVL && MMC
+	depends on !BT_MRVL_SDIO
+	depends on BACKPORT_BT_MRVL && MMC
 	select FW_LOADER
 	select WANT_DEV_COREDUMP
 	help
@@ -259,12 +282,13 @@ config BT_MRVL_SDIO
 	  devices with SDIO interface. Currently SD8688/SD8787/SD8797/SD8887/SD8897
 	  chipsets are supported.
 
-	  Say Y here to compile support for Marvell BT-over-SDIO driver
+	  Say Y here to compile support for Marvell BACKPORT_BT-over-SDIO driver
 	  into the kernel or say M to compile it as module.
 
-config BT_ATH3K
+config BACKPORT_BT_ATH3K
 	tristate "Atheros firmware download driver"
-	depends on BT_HCIBTUSB
+	depends on !BT_ATH3K
+	depends on BACKPORT_BT_HCIBTUSB
 	select FW_LOADER
 	help
 	  Bluetooth firmware download driver.
@@ -274,13 +298,14 @@ config BT_ATH3K
 	  Say Y here to compile support for "Atheros firmware download driver"
 	  into the kernel or say M to compile it as module (ath3k).
 
-config BT_WILINK
+config BACKPORT_BT_WILINK
 	tristate "Texas Instruments WiLink7 driver"
+	depends on !BT_WILINK
 	depends on TI_ST
 	help
-	  This enables the Bluetooth driver for Texas Instrument's BT/FM/GPS
+	  This enables the Bluetooth driver for Texas Instrument's BACKPORT_BT/FM/GPS
 	  combo devices. This makes use of shared transport line discipline
-	  core driver to communicate with the BT core of the combo chip.
+	  core driver to communicate with the BACKPORT_BT core of the combo chip.
 
 	  Say Y here to compile support for Texas Instrument's WiLink7 driver
 	  into the kernel or say M to compile it as module (btwilink).
diff --git a/backports/drivers/media/Kconfig b/backports/drivers/media/Kconfig
index 5b3ea39..1c2d5b1 100644
--- a/backports/drivers/media/Kconfig
+++ b/backports/drivers/media/Kconfig
@@ -2,8 +2,9 @@
 # Multimedia device configuration
 #
 
-menuconfig MEDIA_SUPPORT
+menuconfig BACKPORT_MEDIA_SUPPORT
 	tristate "Multimedia support"
+	depends on !MEDIA_SUPPORT
 	depends on HAS_IOMEM
 	help
 	  If you want to use Webcams, Video grabber devices and/or TV devices
@@ -11,22 +12,24 @@ menuconfig MEDIA_SUPPORT
 	  Additional info and docs are available on the web at
 	  <http://linuxtv.org>
 
-if MEDIA_SUPPORT
+if BACKPORT_MEDIA_SUPPORT
 
 comment "Multimedia core support"
 
 #
 # Multimedia support - automatically enable V4L2 and DVB core
 #
-config MEDIA_CAMERA_SUPPORT
+config BACKPORT_MEDIA_CAMERA_SUPPORT
 	bool "Cameras/video grabbers support"
+	depends on !MEDIA_CAMERA_SUPPORT
 	---help---
 	  Enable support for webcams and video grabbers.
 
 	  Say Y when you have a webcam or a video capture grabber board.
 
-config MEDIA_ANALOG_TV_SUPPORT
+config BACKPORT_MEDIA_ANALOG_TV_SUPPORT
 	bool "Analog TV support"
+	depends on !MEDIA_ANALOG_TV_SUPPORT
 	---help---
 	  Enable analog TV support.
 
@@ -37,16 +40,18 @@ config MEDIA_ANALOG_TV_SUPPORT
 		support both analog and digital TV. Disabling this option
 		will disable support for them.
 
-config MEDIA_DIGITAL_TV_SUPPORT
+config BACKPORT_MEDIA_DIGITAL_TV_SUPPORT
 	bool "Digital TV support"
+	depends on !MEDIA_DIGITAL_TV_SUPPORT
 	---help---
 	  Enable digital TV support.
 
 	  Say Y when you have a board with digital support or a board with
 	  hybrid digital TV and analog TV.
 
-config MEDIA_RADIO_SUPPORT
+config BACKPORT_MEDIA_RADIO_SUPPORT
 	bool "AM/FM radio receivers/transmitters support"
+	depends on !MEDIA_RADIO_SUPPORT
 	---help---
 	  Enable AM/FM radio support.
 
@@ -59,15 +64,17 @@ config MEDIA_RADIO_SUPPORT
 		support radio reception. Disabling this option will
 		disable support for them.
 
-config MEDIA_SDR_SUPPORT
+config BACKPORT_MEDIA_SDR_SUPPORT
 	bool "Software defined radio support"
+	depends on !MEDIA_SDR_SUPPORT
 	---help---
 	  Enable software defined radio support.
 
 	  Say Y when you have a software defined radio device.
 
-config MEDIA_RC_SUPPORT
+config BACKPORT_MEDIA_RC_SUPPORT
 	bool "Remote Controller support"
+	depends on !MEDIA_RC_SUPPORT
 	depends on INPUT
 	---help---
 	  Enable support for Remote Controllers on Linux. This is
@@ -85,18 +92,20 @@ config MEDIA_RC_SUPPORT
 #	Selectable only for webcam/grabbers, as other drivers don't use it
 #
 
-config MEDIA_CONTROLLER
+config BACKPORT_MEDIA_CONTROLLER
 	bool "Media Controller API"
-	depends on MEDIA_CAMERA_SUPPORT || MEDIA_ANALOG_TV_SUPPORT || MEDIA_DIGITAL_TV_SUPPORT
+	depends on !MEDIA_CONTROLLER
+	depends on BACKPORT_MEDIA_CAMERA_SUPPORT || MEDIA_ANALOG_TV_SUPPORT || MEDIA_DIGITAL_TV_SUPPORT
 	---help---
 	  Enable the media controller API used to query media devices internal
 	  topology and configure it dynamically.
 
 	  This API is mostly used by camera interfaces in embedded platforms.
 
-config MEDIA_CONTROLLER_DVB
+config BACKPORT_MEDIA_CONTROLLER_DVB
 	bool "Enable Media controller for DVB"
-	depends on MEDIA_CONTROLLER
+	depends on !MEDIA_CONTROLLER_DVB
+	depends on BACKPORT_MEDIA_CONTROLLER
 	depends on BROKEN
 	---help---
 	  Enable the media controller API support for DVB.
@@ -108,15 +117,17 @@ config MEDIA_CONTROLLER_DVB
 #	Only enables if one of the V4L2 types (ATV, webcam, radio) is selected
 #
 
-config VIDEO_DEV
+config BACKPORT_VIDEO_DEV
 	tristate
-	depends on MEDIA_SUPPORT
-	depends on MEDIA_CAMERA_SUPPORT || MEDIA_ANALOG_TV_SUPPORT || MEDIA_RADIO_SUPPORT || MEDIA_SDR_SUPPORT
+	depends on !VIDEO_DEV
+	depends on BACKPORT_MEDIA_SUPPORT
+	depends on BACKPORT_MEDIA_CAMERA_SUPPORT || MEDIA_ANALOG_TV_SUPPORT || MEDIA_RADIO_SUPPORT || MEDIA_SDR_SUPPORT
 	default y
 
-config VIDEO_V4L2_SUBDEV_API
+config BACKPORT_VIDEO_V4L2_SUBDEV_API
 	bool "V4L2 sub-device userspace API"
-	depends on VIDEO_DEV && MEDIA_CONTROLLER
+	depends on !VIDEO_V4L2_SUBDEV_API
+	depends on VIDEO_DEV && BACKPORT_MEDIA_CONTROLLER
 	---help---
 	  Enables the V4L2 sub-device pad-level userspace API used to configure
 	  video format, size and frame rate between hardware blocks.
@@ -130,17 +141,19 @@ source "$BACKPORT_DIR/drivers/media/v4l2-core/Kconfig"
 #	Only enables if one of DTV is selected
 #
 
-config DVB_CORE
+config BACKPORT_DVB_CORE
 	tristate
-	depends on MEDIA_SUPPORT
-	depends on MEDIA_DIGITAL_TV_SUPPORT
+	depends on !DVB_CORE
+	depends on BACKPORT_MEDIA_SUPPORT
+	depends on BACKPORT_MEDIA_DIGITAL_TV_SUPPORT
 	default y
 	select CRC32
 
-config DVB_NET
+config BACKPORT_DVB_NET
 	bool "DVB Network Support"
+	depends on !DVB_NET
 	default (NET && INET)
-	depends on NET && INET && DVB_CORE
+	depends on NET && INET && BACKPORT_DVB_CORE
 	help
 	  This option enables DVB Network Support which is a part of the DVB
 	  standard. It is used, for example, by automatic firmware updates used
@@ -151,8 +164,9 @@ config DVB_NET
 	  unsure say Y.
 
 # This Kconfig option is used by both PCI and USB drivers
-config TTPCI_EEPROM
+config BACKPORT_TTPCI_EEPROM
 	tristate
+	depends on !TTPCI_EEPROM
 	depends on I2C
 	default n
 
@@ -172,7 +186,7 @@ source "$BACKPORT_DIR/drivers/media/mmc/Kconfig"
 source "$BACKPORT_DIR/drivers/media/radio/Kconfig"
 
 comment "Supported FireWire (IEEE 1394) Adapters"
-	depends on DVB_CORE && FIREWIRE
+	depends on BACKPORT_DVB_CORE && FIREWIRE
 source "$BACKPORT_DIR/drivers/media/firewire/Kconfig"
 
 # Common driver options
@@ -184,9 +198,10 @@ comment "Media ancillary drivers (tuners, sensors, i2c, frontends)"
 # Ancillary drivers (tuners, i2c, frontends)
 #
 
-config MEDIA_SUBDRV_AUTOSELECT
+config BACKPORT_MEDIA_SUBDRV_AUTOSELECT
 	bool "Autoselect ancillary drivers (tuners, sensors, i2c, frontends)"
-	depends on MEDIA_ANALOG_TV_SUPPORT || MEDIA_DIGITAL_TV_SUPPORT || MEDIA_CAMERA_SUPPORT || MEDIA_SDR_SUPPORT
+	depends on !MEDIA_SUBDRV_AUTOSELECT
+	depends on MEDIA_ANALOG_TV_SUPPORT || MEDIA_DIGITAL_TV_SUPPORT || BACKPORT_MEDIA_CAMERA_SUPPORT || MEDIA_SDR_SUPPORT
 	depends on HAS_IOMEM
 	select I2C
 	select I2C_MUX
@@ -207,9 +222,10 @@ config MEDIA_SUBDRV_AUTOSELECT
 
 	  If unsure say Y.
 
-config MEDIA_ATTACH
+config BACKPORT_MEDIA_ATTACH
 	bool
-	depends on MEDIA_ANALOG_TV_SUPPORT || MEDIA_DIGITAL_TV_SUPPORT || MEDIA_RADIO_SUPPORT
+	depends on !MEDIA_ATTACH
+	depends on BACKPORT_MEDIA_ANALOG_TV_SUPPORT || MEDIA_DIGITAL_TV_SUPPORT || MEDIA_RADIO_SUPPORT
 	depends on MODULES
 	default MODULES
 
@@ -217,4 +233,4 @@ source "$BACKPORT_DIR/drivers/media/i2c/Kconfig"
 source "$BACKPORT_DIR/drivers/media/tuners/Kconfig"
 source "$BACKPORT_DIR/drivers/media/dvb-frontends/Kconfig"
 
-endif # MEDIA_SUPPORT
+endif # BACKPORT_MEDIA_SUPPORT
diff --git a/backports/drivers/media/common/Kconfig b/backports/drivers/media/common/Kconfig
index aef3f11..be4bb03 100644
--- a/backports/drivers/media/common/Kconfig
+++ b/backports/drivers/media/common/Kconfig
@@ -1,19 +1,23 @@
 # Used by common drivers, when they need to ask questions
-config MEDIA_COMMON_OPTIONS
+config BACKPORT_MEDIA_COMMON_OPTIONS
 	bool
+	depends on !MEDIA_COMMON_OPTIONS
 
 comment "common driver options"
-	depends on MEDIA_COMMON_OPTIONS
+	depends on BACKPORT_MEDIA_COMMON_OPTIONS
 
-config VIDEO_CX2341X
+config BACKPORT_VIDEO_CX2341X
 	tristate
+	depends on !VIDEO_CX2341X
 
-config VIDEO_TVEEPROM
+config BACKPORT_VIDEO_TVEEPROM
 	tristate
+	depends on !VIDEO_TVEEPROM
 	depends on I2C
 
-config CYPRESS_FIRMWARE
+config BACKPORT_CYPRESS_FIRMWARE
 	tristate "Cypress firmware helper routines"
+	depends on !CYPRESS_FIRMWARE
 	depends on USB
 
 source "$BACKPORT_DIR/drivers/media/common/b2c2/Kconfig"
diff --git a/backports/drivers/media/common/b2c2/Kconfig b/backports/drivers/media/common/b2c2/Kconfig
index a8c6cdf..a15bb38 100644
--- a/backports/drivers/media/common/b2c2/Kconfig
+++ b/backports/drivers/media/common/b2c2/Kconfig
@@ -1,23 +1,25 @@
-config DVB_B2C2_FLEXCOP
+config BACKPORT_DVB_B2C2_FLEXCOP
 	tristate
-	depends on DVB_CORE && I2C
-	depends on DVB_B2C2_FLEXCOP_PCI || DVB_B2C2_FLEXCOP_USB
+	depends on !DVB_B2C2_FLEXCOP
+	depends on BACKPORT_DVB_CORE && I2C
+	depends on DVB_B2C2_FLEXCOP_PCI || BACKPORT_DVB_B2C2_FLEXCOP_USB
 	default y
-	select DVB_PLL if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_STV0299 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_MT352 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_MT312 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_NXT200X if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_STV0297 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_BCM3510 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_LGDT330X if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_S5H1420 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_TUNER_ITD1000 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_ISL6421 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_CX24123 if MEDIA_SUBDRV_AUTOSELECT
-	select MEDIA_TUNER_SIMPLE if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_TUNER_CX24113 if MEDIA_SUBDRV_AUTOSELECT
+	select DVB_PLL if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_STV0299 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_MT352 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_MT312 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_NXT200X if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_STV0297 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_BCM3510 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_LGDT330X if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_S5H1420 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_TUNER_ITD1000 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_ISL6421 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_CX24123 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select MEDIA_TUNER_SIMPLE if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_TUNER_CX24113 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
 
 # Selected via the PCI or USB flexcop drivers
-config DVB_B2C2_FLEXCOP_DEBUG
+config BACKPORT_DVB_B2C2_FLEXCOP_DEBUG
 	bool
+	depends on !DVB_B2C2_FLEXCOP_DEBUG
diff --git a/backports/drivers/media/common/saa7146/Kconfig b/backports/drivers/media/common/saa7146/Kconfig
index 769c6f8..ddf5b40 100644
--- a/backports/drivers/media/common/saa7146/Kconfig
+++ b/backports/drivers/media/common/saa7146/Kconfig
@@ -1,9 +1,11 @@
-config VIDEO_SAA7146
+config BACKPORT_VIDEO_SAA7146
 	tristate
+	depends on !VIDEO_SAA7146
 	depends on I2C && PCI
 
-config VIDEO_SAA7146_VV
+config BACKPORT_VIDEO_SAA7146_VV
 	tristate
-	depends on VIDEO_V4L2
-	select VIDEOBUF_DMA_SG
-	select VIDEO_SAA7146
+	depends on !VIDEO_SAA7146_VV
+	depends on BACKPORT_VIDEO_V4L2
+	select BACKPORT_VIDEOBUF_DMA_SG
+	select BACKPORT_VIDEO_SAA7146
diff --git a/backports/drivers/media/common/siano/Kconfig b/backports/drivers/media/common/siano/Kconfig
index 4bfbd5f..d253948 100644
--- a/backports/drivers/media/common/siano/Kconfig
+++ b/backports/drivers/media/common/siano/Kconfig
@@ -2,27 +2,30 @@
 # Siano Mobile Silicon Digital TV device configuration
 #
 
-config SMS_SIANO_MDTV
+config BACKPORT_SMS_SIANO_MDTV
 	tristate
-	depends on DVB_CORE && HAS_DMA
-	depends on !RC_CORE || RC_CORE
-	depends on SMS_USB_DRV || SMS_SDIO_DRV
+	depends on !SMS_SIANO_MDTV
+	depends on BACKPORT_DVB_CORE && HAS_DMA
+	depends on !RC_CORE || BACKPORT_RC_CORE
+	depends on BACKPORT_SMS_USB_DRV || SMS_SDIO_DRV
 	default y
 
-config SMS_SIANO_RC
+config BACKPORT_SMS_SIANO_RC
 	bool "Enable Remote Controller support for Siano devices"
-	depends on SMS_SIANO_MDTV && RC_CORE
-	depends on SMS_USB_DRV || SMS_SDIO_DRV
-	depends on MEDIA_COMMON_OPTIONS
+	depends on !SMS_SIANO_RC
+	depends on SMS_SIANO_MDTV && BACKPORT_RC_CORE
+	depends on BACKPORT_SMS_USB_DRV || SMS_SDIO_DRV
+	depends on BACKPORT_MEDIA_COMMON_OPTIONS
 	default y
 	---help---
 	  Choose Y to select Remote Controller support for Siano driver.
 
-config SMS_SIANO_DEBUGFS
+config BACKPORT_SMS_SIANO_DEBUGFS
 	bool "Enable debugfs for smsdvb"
-	depends on SMS_SIANO_MDTV
+	depends on !SMS_SIANO_DEBUGFS
+	depends on BACKPORT_SMS_SIANO_MDTV
 	depends on DEBUG_FS
-	depends on SMS_USB_DRV = SMS_SDIO_DRV
+	depends on BACKPORT_SMS_USB_DRV = SMS_SDIO_DRV
 
 	---help---
 	  Choose Y to enable visualizing a dump of the frontend
diff --git a/backports/drivers/media/dvb-core/Kconfig b/backports/drivers/media/dvb-core/Kconfig
index fa7a249..85cf9fe 100644
--- a/backports/drivers/media/dvb-core/Kconfig
+++ b/backports/drivers/media/dvb-core/Kconfig
@@ -2,9 +2,10 @@
 # DVB device configuration
 #
 
-config DVB_MAX_ADAPTERS
+config BACKPORT_DVB_MAX_ADAPTERS
 	int "maximum number of DVB/ATSC adapters"
-	depends on DVB_CORE
+	depends on !DVB_MAX_ADAPTERS
+	depends on BACKPORT_DVB_CORE
 	default 8
 	range 1 255
 	help
@@ -15,9 +16,10 @@ config DVB_MAX_ADAPTERS
 
 	  If you are unsure about this, use the default value 8
 
-config DVB_DYNAMIC_MINORS
+config BACKPORT_DVB_DYNAMIC_MINORS
 	bool "Dynamic DVB minor allocation"
-	depends on DVB_CORE
+	depends on !DVB_DYNAMIC_MINORS
+	depends on BACKPORT_DVB_CORE
 	default n
 	help
 	  If you say Y here, the DVB subsystem will use dynamic minor
diff --git a/backports/drivers/media/dvb-frontends/Kconfig b/backports/drivers/media/dvb-frontends/Kconfig
index 4e4fc0a9..6295ab0 100644
--- a/backports/drivers/media/dvb-frontends/Kconfig
+++ b/backports/drivers/media/dvb-frontends/Kconfig
@@ -2,70 +2,78 @@ menu "Customise DVB Frontends"
 	visible if !MEDIA_SUBDRV_AUTOSELECT
 
 comment "Multistandard (satellite) frontends"
-	depends on DVB_CORE
+	depends on BACKPORT_DVB_CORE
 
-config DVB_STB0899
+config BACKPORT_DVB_STB0899
 	tristate "STB0899 based"
-	depends on DVB_CORE && I2C
+	depends on !DVB_STB0899
+	depends on BACKPORT_DVB_CORE && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  A DVB-S/S2/DSS Multistandard demodulator. Say Y when you want
 	  to support this demodulator based frontends
 
-config DVB_STB6100
+config BACKPORT_DVB_STB6100
 	tristate "STB6100 based tuners"
-	depends on DVB_CORE && I2C
+	depends on !DVB_STB6100
+	depends on BACKPORT_DVB_CORE && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  A Silicon tuner from ST used in conjunction with the STB0899
 	  demodulator. Say Y when you want to support this tuner.
 
-config DVB_STV090x
+config BACKPORT_DVB_STV090x
 	tristate "STV0900/STV0903(A/B) based"
-	depends on DVB_CORE && I2C
+	depends on !DVB_STV090x
+	depends on BACKPORT_DVB_CORE && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  DVB-S/S2/DSS Multistandard Professional/Broadcast demodulators.
 	  Say Y when you want to support these frontends.
 
-config DVB_STV6110x
+config BACKPORT_DVB_STV6110x
 	tristate "STV6110/(A) based tuners"
-	depends on DVB_CORE && I2C
+	depends on !DVB_STV6110x
+	depends on BACKPORT_DVB_CORE && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  A Silicon tuner that supports DVB-S and DVB-S2 modes
 
-config DVB_M88DS3103
+config BACKPORT_DVB_M88DS3103
 	tristate "Montage M88DS3103"
-	depends on DVB_CORE && I2C && I2C_MUX
+	depends on !DVB_M88DS3103
+	depends on BACKPORT_DVB_CORE && I2C && I2C_MUX
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  Say Y when you want to support this frontend.
 
 comment "Multistandard (cable + terrestrial) frontends"
-	depends on DVB_CORE
+	depends on BACKPORT_DVB_CORE
 
-config DVB_DRXK
+config BACKPORT_DVB_DRXK
 	tristate "Micronas DRXK based"
-	depends on DVB_CORE && I2C
+	depends on !DVB_DRXK
+	depends on BACKPORT_DVB_CORE && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  Micronas DRX-K DVB-C/T demodulator.
 
 	  Say Y when you want to support this frontend.
 
-config DVB_TDA18271C2DD
+config BACKPORT_DVB_TDA18271C2DD
 	tristate "NXP TDA18271C2 silicon tuner"
-	depends on DVB_CORE && I2C
+	depends on !DVB_TDA18271C2DD
+	depends on BACKPORT_DVB_CORE && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  NXP TDA18271 silicon tuner.
 
 	  Say Y when you want to support this tuner.
 
-config DVB_SI2165
+config BACKPORT_DVB_SI2165
 	tristate "Silicon Labs si2165 based"
-	depends on DVB_CORE && I2C
+	depends on !DVB_SI2165
+	depends on BACKPORT_DVB_CORE && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  A DVB-C/T demodulator.
@@ -73,198 +81,225 @@ config DVB_SI2165
 	  Say Y when you want to support this frontend.
 
 comment "DVB-S (satellite) frontends"
-	depends on DVB_CORE
+	depends on BACKPORT_DVB_CORE
 
-config DVB_CX24110
+config BACKPORT_DVB_CX24110
 	tristate "Conexant CX24110 based"
-	depends on DVB_CORE && I2C
+	depends on !DVB_CX24110
+	depends on BACKPORT_DVB_CORE && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  A DVB-S tuner module. Say Y when you want to support this frontend.
 
-config DVB_CX24123
+config BACKPORT_DVB_CX24123
 	tristate "Conexant CX24123 based"
-	depends on DVB_CORE && I2C
+	depends on !DVB_CX24123
+	depends on BACKPORT_DVB_CORE && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  A DVB-S tuner module. Say Y when you want to support this frontend.
 
-config DVB_MT312
+config BACKPORT_DVB_MT312
 	tristate "Zarlink VP310/MT312/ZL10313 based"
-	depends on DVB_CORE && I2C
+	depends on !DVB_MT312
+	depends on BACKPORT_DVB_CORE && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  A DVB-S tuner module. Say Y when you want to support this frontend.
 
-config DVB_ZL10036
+config BACKPORT_DVB_ZL10036
 	tristate "Zarlink ZL10036 silicon tuner"
-	depends on DVB_CORE && I2C
+	depends on !DVB_ZL10036
+	depends on BACKPORT_DVB_CORE && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  A DVB-S tuner module. Say Y when you want to support this frontend.
 
-config DVB_ZL10039
+config BACKPORT_DVB_ZL10039
 	tristate "Zarlink ZL10039 silicon tuner"
-	depends on DVB_CORE && I2C
+	depends on !DVB_ZL10039
+	depends on BACKPORT_DVB_CORE && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  A DVB-S tuner module. Say Y when you want to support this frontend.
 
-config DVB_S5H1420
+config BACKPORT_DVB_S5H1420
 	tristate "Samsung S5H1420 based"
-	depends on DVB_CORE && I2C
+	depends on !DVB_S5H1420
+	depends on BACKPORT_DVB_CORE && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  A DVB-S tuner module. Say Y when you want to support this frontend.
 
-config DVB_STV0288
+config BACKPORT_DVB_STV0288
 	tristate "ST STV0288 based"
-	depends on DVB_CORE && I2C
+	depends on !DVB_STV0288
+	depends on BACKPORT_DVB_CORE && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  A DVB-S tuner module. Say Y when you want to support this frontend.
 
-config DVB_STB6000
+config BACKPORT_DVB_STB6000
 	tristate "ST STB6000 silicon tuner"
-	depends on DVB_CORE && I2C
+	depends on !DVB_STB6000
+	depends on BACKPORT_DVB_CORE && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	  help
 	  A DVB-S silicon tuner module. Say Y when you want to support this tuner.
 
-config DVB_STV0299
+config BACKPORT_DVB_STV0299
 	tristate "ST STV0299 based"
-	depends on DVB_CORE && I2C
+	depends on !DVB_STV0299
+	depends on BACKPORT_DVB_CORE && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  A DVB-S tuner module. Say Y when you want to support this frontend.
 
-config DVB_STV6110
+config BACKPORT_DVB_STV6110
 	tristate "ST STV6110 silicon tuner"
-	depends on DVB_CORE && I2C
+	depends on !DVB_STV6110
+	depends on BACKPORT_DVB_CORE && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	  help
 	  A DVB-S silicon tuner module. Say Y when you want to support this tuner.
 
-config DVB_STV0900
+config BACKPORT_DVB_STV0900
 	tristate "ST STV0900 based"
-	depends on DVB_CORE && I2C
+	depends on !DVB_STV0900
+	depends on BACKPORT_DVB_CORE && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  A DVB-S/S2 demodulator. Say Y when you want to support this frontend.
 
-config DVB_TDA8083
+config BACKPORT_DVB_TDA8083
 	tristate "Philips TDA8083 based"
-	depends on DVB_CORE && I2C
+	depends on !DVB_TDA8083
+	depends on BACKPORT_DVB_CORE && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  A DVB-S tuner module. Say Y when you want to support this frontend.
 
-config DVB_TDA10086
+config BACKPORT_DVB_TDA10086
 	tristate "Philips TDA10086 based"
-	depends on DVB_CORE && I2C
+	depends on !DVB_TDA10086
+	depends on BACKPORT_DVB_CORE && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  A DVB-S tuner module. Say Y when you want to support this frontend.
 
-config DVB_TDA8261
+config BACKPORT_DVB_TDA8261
 	tristate "Philips TDA8261 based"
-	depends on DVB_CORE && I2C
+	depends on !DVB_TDA8261
+	depends on BACKPORT_DVB_CORE && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  A DVB-S tuner module. Say Y when you want to support this frontend.
 
-config DVB_VES1X93
+config BACKPORT_DVB_VES1X93
 	tristate "VLSI VES1893 or VES1993 based"
-	depends on DVB_CORE && I2C
+	depends on !DVB_VES1X93
+	depends on BACKPORT_DVB_CORE && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  A DVB-S tuner module. Say Y when you want to support this frontend.
 
-config DVB_TUNER_ITD1000
+config BACKPORT_DVB_TUNER_ITD1000
 	tristate "Integrant ITD1000 Zero IF tuner for DVB-S/DSS"
-	depends on DVB_CORE && I2C
+	depends on !DVB_TUNER_ITD1000
+	depends on BACKPORT_DVB_CORE && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  A DVB-S tuner module. Say Y when you want to support this frontend.
 
-config DVB_TUNER_CX24113
+config BACKPORT_DVB_TUNER_CX24113
 	tristate "Conexant CX24113/CX24128 tuner for DVB-S/DSS"
-	depends on DVB_CORE && I2C
+	depends on !DVB_TUNER_CX24113
+	depends on BACKPORT_DVB_CORE && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  A DVB-S tuner module. Say Y when you want to support this frontend.
 
 
-config DVB_TDA826X
+config BACKPORT_DVB_TDA826X
 	tristate "Philips TDA826X silicon tuner"
-	depends on DVB_CORE && I2C
+	depends on !DVB_TDA826X
+	depends on BACKPORT_DVB_CORE && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  A DVB-S silicon tuner module. Say Y when you want to support this tuner.
 
-config DVB_TUA6100
+config BACKPORT_DVB_TUA6100
 	tristate "Infineon TUA6100 PLL"
-	depends on DVB_CORE && I2C
+	depends on !DVB_TUA6100
+	depends on BACKPORT_DVB_CORE && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  A DVB-S PLL chip.
 
-config DVB_CX24116
+config BACKPORT_DVB_CX24116
 	tristate "Conexant CX24116 based"
-	depends on DVB_CORE && I2C
+	depends on !DVB_CX24116
+	depends on BACKPORT_DVB_CORE && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  A DVB-S/S2 tuner module. Say Y when you want to support this frontend.
 
-config DVB_CX24117
+config BACKPORT_DVB_CX24117
 	tristate "Conexant CX24117 based"
-	depends on DVB_CORE && I2C
+	depends on !DVB_CX24117
+	depends on BACKPORT_DVB_CORE && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  A Dual DVB-S/S2 tuner module. Say Y when you want to support this frontend.
 
-config DVB_SI21XX
+config BACKPORT_DVB_SI21XX
 	tristate "Silicon Labs SI21XX based"
-	depends on DVB_CORE && I2C
+	depends on !DVB_SI21XX
+	depends on BACKPORT_DVB_CORE && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  A DVB-S tuner module. Say Y when you want to support this frontend.
 
-config DVB_TS2020
+config BACKPORT_DVB_TS2020
 	tristate "Montage Tehnology TS2020 based tuners"
-	depends on DVB_CORE && I2C
+	depends on !DVB_TS2020
+	depends on BACKPORT_DVB_CORE && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  A DVB-S/S2 silicon tuner. Say Y when you want to support this tuner.
 
-config DVB_DS3000
+config BACKPORT_DVB_DS3000
 	tristate "Montage Tehnology DS3000 based"
-	depends on DVB_CORE && I2C
+	depends on !DVB_DS3000
+	depends on BACKPORT_DVB_CORE && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  A DVB-S/S2 tuner module. Say Y when you want to support this frontend.
 
-config DVB_MB86A16
+config BACKPORT_DVB_MB86A16
 	tristate "Fujitsu MB86A16 based"
-	depends on DVB_CORE && I2C
+	depends on !DVB_MB86A16
+	depends on BACKPORT_DVB_CORE && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  A DVB-S/DSS Direct Conversion reveiver.
 	  Say Y when you want to support this frontend.
 
-config DVB_TDA10071
+config BACKPORT_DVB_TDA10071
 	tristate "NXP TDA10071"
-	depends on DVB_CORE && I2C
+	depends on !DVB_TDA10071
+	depends on BACKPORT_DVB_CORE && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  Say Y when you want to support this frontend.
 
 comment "DVB-T (terrestrial) frontends"
-	depends on DVB_CORE
+	depends on BACKPORT_DVB_CORE
 
-config DVB_SP8870
+config BACKPORT_DVB_SP8870
 	tristate "Spase sp8870 based"
-	depends on DVB_CORE && I2C
+	depends on !DVB_SP8870
+	depends on BACKPORT_DVB_CORE && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  A DVB-T tuner module. Say Y when you want to support this frontend.
@@ -274,9 +309,10 @@ config DVB_SP8870
 	  download/extract it, and then copy it to /usr/lib/hotplug/firmware
 	  or /lib/firmware (depending on configuration of firmware hotplug).
 
-config DVB_SP887X
+config BACKPORT_DVB_SP887X
 	tristate "Spase sp887x based"
-	depends on DVB_CORE && I2C
+	depends on !DVB_SP887X
+	depends on BACKPORT_DVB_CORE && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  A DVB-T tuner module. Say Y when you want to support this frontend.
@@ -286,30 +322,34 @@ config DVB_SP887X
 	  download/extract it, and then copy it to /usr/lib/hotplug/firmware
 	  or /lib/firmware (depending on configuration of firmware hotplug).
 
-config DVB_CX22700
+config BACKPORT_DVB_CX22700
 	tristate "Conexant CX22700 based"
-	depends on DVB_CORE && I2C
+	depends on !DVB_CX22700
+	depends on BACKPORT_DVB_CORE && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  A DVB-T tuner module. Say Y when you want to support this frontend.
 
-config DVB_CX22702
+config BACKPORT_DVB_CX22702
 	tristate "Conexant cx22702 demodulator (OFDM)"
-	depends on DVB_CORE && I2C
+	depends on !DVB_CX22702
+	depends on BACKPORT_DVB_CORE && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  A DVB-T tuner module. Say Y when you want to support this frontend.
 
-config DVB_S5H1432
+config BACKPORT_DVB_S5H1432
 	tristate "Samsung s5h1432 demodulator (OFDM)"
-	depends on DVB_CORE && I2C
+	depends on !DVB_S5H1432
+	depends on BACKPORT_DVB_CORE && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  A DVB-T tuner module. Say Y when you want to support this frontend.
 
-config DVB_DRXD
+config BACKPORT_DVB_DRXD
 	tristate "Micronas DRXD driver"
-	depends on DVB_CORE && I2C
+	depends on !DVB_DRXD
+	depends on BACKPORT_DVB_CORE && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  A DVB-T tuner module. Say Y when you want to support this frontend.
@@ -318,16 +358,18 @@ config DVB_DRXD
 	  under the GPL) as opposed to the existing drx397xd driver, which
 	  was written via reverse engineering.
 
-config DVB_L64781
+config BACKPORT_DVB_L64781
 	tristate "LSI L64781"
-	depends on DVB_CORE && I2C
+	depends on !DVB_L64781
+	depends on BACKPORT_DVB_CORE && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  A DVB-T tuner module. Say Y when you want to support this frontend.
 
-config DVB_TDA1004X
+config BACKPORT_DVB_TDA1004X
 	tristate "Philips TDA10045H/TDA10046H based"
-	depends on DVB_CORE && I2C
+	depends on !DVB_TDA1004X
+	depends on BACKPORT_DVB_CORE && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  A DVB-T tuner module. Say Y when you want to support this frontend.
@@ -338,183 +380,207 @@ config DVB_TDA1004X
 	  download/extract them, and then copy them to /usr/lib/hotplug/firmware
 	  or /lib/firmware (depending on configuration of firmware hotplug).
 
-config DVB_NXT6000
+config BACKPORT_DVB_NXT6000
 	tristate "NxtWave Communications NXT6000 based"
-	depends on DVB_CORE && I2C
+	depends on !DVB_NXT6000
+	depends on BACKPORT_DVB_CORE && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  A DVB-T tuner module. Say Y when you want to support this frontend.
 
-config DVB_MT352
+config BACKPORT_DVB_MT352
 	tristate "Zarlink MT352 based"
-	depends on DVB_CORE && I2C
+	depends on !DVB_MT352
+	depends on BACKPORT_DVB_CORE && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  A DVB-T tuner module. Say Y when you want to support this frontend.
 
-config DVB_ZL10353
+config BACKPORT_DVB_ZL10353
 	tristate "Zarlink ZL10353 based"
-	depends on DVB_CORE && I2C
+	depends on !DVB_ZL10353
+	depends on BACKPORT_DVB_CORE && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  A DVB-T tuner module. Say Y when you want to support this frontend.
 
-config DVB_DIB3000MB
+config BACKPORT_DVB_DIB3000MB
 	tristate "DiBcom 3000M-B"
-	depends on DVB_CORE && I2C
+	depends on !DVB_DIB3000MB
+	depends on BACKPORT_DVB_CORE && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  A DVB-T tuner module. Designed for mobile usage. Say Y when you want
 	  to support this frontend.
 
-config DVB_DIB3000MC
+config BACKPORT_DVB_DIB3000MC
 	tristate "DiBcom 3000P/M-C"
-	depends on DVB_CORE && I2C
+	depends on !DVB_DIB3000MC
+	depends on BACKPORT_DVB_CORE && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  A DVB-T tuner module. Designed for mobile usage. Say Y when you want
 	  to support this frontend.
 
-config DVB_DIB7000M
+config BACKPORT_DVB_DIB7000M
 	tristate "DiBcom 7000MA/MB/PA/PB/MC"
-	depends on DVB_CORE && I2C
+	depends on !DVB_DIB7000M
+	depends on BACKPORT_DVB_CORE && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  A DVB-T tuner module. Designed for mobile usage. Say Y when you want
 	  to support this frontend.
 
-config DVB_DIB7000P
+config BACKPORT_DVB_DIB7000P
 	tristate "DiBcom 7000PC"
-	depends on DVB_CORE && I2C
+	depends on !DVB_DIB7000P
+	depends on BACKPORT_DVB_CORE && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  A DVB-T tuner module. Designed for mobile usage. Say Y when you want
 	  to support this frontend.
 
-config DVB_DIB9000
+config BACKPORT_DVB_DIB9000
 	tristate "DiBcom 9000"
-	depends on DVB_CORE && I2C
+	depends on !DVB_DIB9000
+	depends on BACKPORT_DVB_CORE && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  A DVB-T tuner module. Designed for mobile usage. Say Y when you want
 	  to support this frontend.
 
-config DVB_TDA10048
+config BACKPORT_DVB_TDA10048
 	tristate "Philips TDA10048HN based"
-	depends on DVB_CORE && I2C
+	depends on !DVB_TDA10048
+	depends on BACKPORT_DVB_CORE && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  A DVB-T tuner module. Say Y when you want to support this frontend.
 
-config DVB_AF9013
+config BACKPORT_DVB_AF9013
 	tristate "Afatech AF9013 demodulator"
-	depends on DVB_CORE && I2C
+	depends on !DVB_AF9013
+	depends on BACKPORT_DVB_CORE && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  Say Y when you want to support this frontend.
 
-config DVB_EC100
+config BACKPORT_DVB_EC100
 	tristate "E3C EC100"
-	depends on DVB_CORE && I2C
+	depends on !DVB_EC100
+	depends on BACKPORT_DVB_CORE && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  Say Y when you want to support this frontend.
 
-config DVB_HD29L2
+config BACKPORT_DVB_HD29L2
 	tristate "HDIC HD29L2"
-	depends on DVB_CORE && I2C
+	depends on !DVB_HD29L2
+	depends on BACKPORT_DVB_CORE && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  Say Y when you want to support this frontend.
 
-config DVB_STV0367
+config BACKPORT_DVB_STV0367
 	tristate "ST STV0367 based"
-	depends on DVB_CORE && I2C
+	depends on !DVB_STV0367
+	depends on BACKPORT_DVB_CORE && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  A DVB-T/C tuner module. Say Y when you want to support this frontend.
 
-config DVB_CXD2820R
+config BACKPORT_DVB_CXD2820R
 	tristate "Sony CXD2820R"
-	depends on DVB_CORE && I2C
+	depends on !DVB_CXD2820R
+	depends on BACKPORT_DVB_CORE && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  Say Y when you want to support this frontend.
 
-config DVB_RTL2830
+config BACKPORT_DVB_RTL2830
 	tristate "Realtek RTL2830 DVB-T"
-	depends on DVB_CORE && I2C && I2C_MUX
+	depends on !DVB_RTL2830
+	depends on BACKPORT_DVB_CORE && I2C && I2C_MUX
 	select REGMAP
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  Say Y when you want to support this frontend.
 
-config DVB_RTL2832
+config BACKPORT_DVB_RTL2832
 	tristate "Realtek RTL2832 DVB-T"
-	depends on DVB_CORE && I2C && I2C_MUX
+	depends on !DVB_RTL2832
+	depends on BACKPORT_DVB_CORE && I2C && I2C_MUX
 	select REGMAP
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  Say Y when you want to support this frontend.
 
-config DVB_RTL2832_SDR
+config BACKPORT_DVB_RTL2832_SDR
 	tristate "Realtek RTL2832 SDR"
-	depends on DVB_CORE && I2C && I2C_MUX && VIDEO_V4L2 && MEDIA_SDR_SUPPORT && USB
-	select DVB_RTL2832
-	select VIDEOBUF2_VMALLOC
+	depends on !DVB_RTL2832_SDR
+	depends on DVB_CORE && I2C && I2C_MUX && VIDEO_V4L2 && BACKPORT_MEDIA_SDR_SUPPORT && USB
+	select BACKPORT_DVB_RTL2832
+	select BACKPORT_VIDEOBUF2_VMALLOC
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  Say Y when you want to support this SDR module.
 
-config DVB_SI2168
+config BACKPORT_DVB_SI2168
 	tristate "Silicon Labs Si2168"
-	depends on DVB_CORE && I2C && I2C_MUX
+	depends on !DVB_SI2168
+	depends on BACKPORT_DVB_CORE && I2C && I2C_MUX
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  Say Y when you want to support this frontend.
 
-config DVB_AS102_FE
+config BACKPORT_DVB_AS102_FE
 	tristate
-	depends on DVB_CORE
-	default DVB_AS102
+	depends on !DVB_AS102_FE
+	depends on BACKPORT_DVB_CORE
+	default BACKPORT_DVB_AS102
 
 comment "DVB-C (cable) frontends"
-	depends on DVB_CORE
+	depends on BACKPORT_DVB_CORE
 
-config DVB_VES1820
+config BACKPORT_DVB_VES1820
 	tristate "VLSI VES1820 based"
-	depends on DVB_CORE && I2C
+	depends on !DVB_VES1820
+	depends on BACKPORT_DVB_CORE && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  A DVB-C tuner module. Say Y when you want to support this frontend.
 
-config DVB_TDA10021
+config BACKPORT_DVB_TDA10021
 	tristate "Philips TDA10021 based"
-	depends on DVB_CORE && I2C
+	depends on !DVB_TDA10021
+	depends on BACKPORT_DVB_CORE && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  A DVB-C tuner module. Say Y when you want to support this frontend.
 
-config DVB_TDA10023
+config BACKPORT_DVB_TDA10023
 	tristate "Philips TDA10023 based"
-	depends on DVB_CORE && I2C
+	depends on !DVB_TDA10023
+	depends on BACKPORT_DVB_CORE && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  A DVB-C tuner module. Say Y when you want to support this frontend.
 
-config DVB_STV0297
+config BACKPORT_DVB_STV0297
 	tristate "ST STV0297 based"
-	depends on DVB_CORE && I2C
+	depends on !DVB_STV0297
+	depends on BACKPORT_DVB_CORE && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  A DVB-C tuner module. Say Y when you want to support this frontend.
 
 comment "ATSC (North American/Korean Terrestrial/Cable DTV) frontends"
-	depends on DVB_CORE
+	depends on BACKPORT_DVB_CORE
 
-config DVB_NXT200X
+config BACKPORT_DVB_NXT200X
 	tristate "NxtWave Communications NXT2002/NXT2004 based"
-	depends on DVB_CORE && I2C
+	depends on !DVB_NXT200X
+	depends on BACKPORT_DVB_CORE && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  An ATSC 8VSB and QAM64/256 tuner module. Say Y when you want
@@ -526,9 +592,10 @@ config DVB_NXT200X
 	  download/extract them, and then copy them to /usr/lib/hotplug/firmware
 	  or /lib/firmware (depending on configuration of firmware hotplug).
 
-config DVB_OR51211
+config BACKPORT_DVB_OR51211
 	tristate "Oren OR51211 based"
-	depends on DVB_CORE && I2C
+	depends on !DVB_OR51211
+	depends on BACKPORT_DVB_CORE && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  An ATSC 8VSB tuner module. Say Y when you want to support this frontend.
@@ -538,9 +605,10 @@ config DVB_OR51211
 	  download it, and then copy it to /usr/lib/hotplug/firmware
 	  or /lib/firmware (depending on configuration of firmware hotplug).
 
-config DVB_OR51132
+config BACKPORT_DVB_OR51132
 	tristate "Oren OR51132 based"
-	depends on DVB_CORE && I2C
+	depends on !DVB_OR51132
+	depends on BACKPORT_DVB_CORE && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  An ATSC 8VSB and QAM64/256 tuner module. Say Y when you want
@@ -553,135 +621,151 @@ config DVB_OR51132
 	  /usr/lib/hotplug/firmware or /lib/firmware (depending on
 	  configuration of firmware hotplug).
 
-config DVB_BCM3510
+config BACKPORT_DVB_BCM3510
 	tristate "Broadcom BCM3510"
-	depends on DVB_CORE && I2C
+	depends on !DVB_BCM3510
+	depends on BACKPORT_DVB_CORE && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  An ATSC 8VSB/16VSB and QAM64/256 tuner module. Say Y when you want to
 	  support this frontend.
 
-config DVB_LGDT330X
+config BACKPORT_DVB_LGDT330X
 	tristate "LG Electronics LGDT3302/LGDT3303 based"
-	depends on DVB_CORE && I2C
+	depends on !DVB_LGDT330X
+	depends on BACKPORT_DVB_CORE && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  An ATSC 8VSB and QAM64/256 tuner module. Say Y when you want
 	  to support this frontend.
 
-config DVB_LGDT3305
+config BACKPORT_DVB_LGDT3305
 	tristate "LG Electronics LGDT3304 and LGDT3305 based"
-	depends on DVB_CORE && I2C
+	depends on !DVB_LGDT3305
+	depends on BACKPORT_DVB_CORE && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  An ATSC 8VSB and QAM64/256 tuner module. Say Y when you want
 	  to support this frontend.
 
-config DVB_LGDT3306A
+config BACKPORT_DVB_LGDT3306A
 	tristate "LG Electronics LGDT3306A based"
-	depends on DVB_CORE && I2C
+	depends on !DVB_LGDT3306A
+	depends on BACKPORT_DVB_CORE && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  An ATSC 8VSB and QAM-B 64/256 demodulator module. Say Y when you want
 	  to support this frontend.
 
-config DVB_LG2160
+config BACKPORT_DVB_LG2160
 	tristate "LG Electronics LG216x based"
-	depends on DVB_CORE && I2C
+	depends on !DVB_LG2160
+	depends on BACKPORT_DVB_CORE && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  An ATSC/MH demodulator module. Say Y when you want
 	  to support this frontend.
 
-config DVB_S5H1409
+config BACKPORT_DVB_S5H1409
 	tristate "Samsung S5H1409 based"
-	depends on DVB_CORE && I2C
+	depends on !DVB_S5H1409
+	depends on BACKPORT_DVB_CORE && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  An ATSC 8VSB and QAM64/256 tuner module. Say Y when you want
 	  to support this frontend.
 
-config DVB_AU8522
+config BACKPORT_DVB_AU8522
 	depends on I2C
+	depends on !DVB_AU8522
 	tristate
 
-config DVB_AU8522_DTV
+config BACKPORT_DVB_AU8522_DTV
 	tristate "Auvitek AU8522 based DTV demod"
-	depends on DVB_CORE && I2C
-	select DVB_AU8522
+	depends on !DVB_AU8522_DTV
+	depends on BACKPORT_DVB_CORE && I2C
+	select BACKPORT_DVB_AU8522
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  An ATSC 8VSB, QAM64/256 & NTSC demodulator module. Say Y when
 	  you want to enable DTV demodulation support for this frontend.
 
-config DVB_AU8522_V4L
+config BACKPORT_DVB_AU8522_V4L
 	tristate "Auvitek AU8522 based ATV demod"
-	depends on VIDEO_V4L2 && I2C
-	select DVB_AU8522
+	depends on !DVB_AU8522_V4L
+	depends on BACKPORT_VIDEO_V4L2 && I2C
+	select BACKPORT_DVB_AU8522
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  An ATSC 8VSB, QAM64/256 & NTSC demodulator module. Say Y when
 	  you want to enable ATV demodulation support for this frontend.
 
-config DVB_S5H1411
+config BACKPORT_DVB_S5H1411
 	tristate "Samsung S5H1411 based"
-	depends on DVB_CORE && I2C
+	depends on !DVB_S5H1411
+	depends on BACKPORT_DVB_CORE && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  An ATSC 8VSB and QAM64/256 tuner module. Say Y when you want
 	  to support this frontend.
 
 comment "ISDB-T (terrestrial) frontends"
-	depends on DVB_CORE
+	depends on BACKPORT_DVB_CORE
 
-config DVB_S921
+config BACKPORT_DVB_S921
 	tristate "Sharp S921 frontend"
-	depends on DVB_CORE && I2C
+	depends on !DVB_S921
+	depends on BACKPORT_DVB_CORE && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  AN ISDB-T DQPSK, QPSK, 16QAM and 64QAM 1seg tuner module.
 	  Say Y when you want to support this frontend.
 
-config DVB_DIB8000
+config BACKPORT_DVB_DIB8000
 	tristate "DiBcom 8000MB/MC"
-	depends on DVB_CORE && I2C
+	depends on !DVB_DIB8000
+	depends on BACKPORT_DVB_CORE && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  A driver for DiBcom's DiB8000 ISDB-T/ISDB-Tsb demodulator.
 	  Say Y when you want to support this frontend.
 
-config DVB_MB86A20S
+config BACKPORT_DVB_MB86A20S
 	tristate "Fujitsu mb86a20s"
-	depends on DVB_CORE && I2C
+	depends on !DVB_MB86A20S
+	depends on BACKPORT_DVB_CORE && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  A driver for Fujitsu mb86a20s ISDB-T/ISDB-Tsb demodulator.
 	  Say Y when you want to support this frontend.
 
 comment "ISDB-S (satellite) & ISDB-T (terrestrial) frontends"
-	depends on DVB_CORE
+	depends on BACKPORT_DVB_CORE
 
-config DVB_TC90522
+config BACKPORT_DVB_TC90522
 	tristate "Toshiba TC90522"
-	depends on DVB_CORE && I2C
+	depends on !DVB_TC90522
+	depends on BACKPORT_DVB_CORE && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  Toshiba TC90522 2xISDB-S 8PSK + 2xISDB-T OFDM demodulator.
 	  Say Y when you want to support this frontend.
 
 comment "Digital terrestrial only tuners/PLL"
-	depends on DVB_CORE
+	depends on BACKPORT_DVB_CORE
 
-config DVB_PLL
+config BACKPORT_DVB_PLL
 	tristate "Generic I2C PLL based tuners"
-	depends on DVB_CORE && I2C
+	depends on !DVB_PLL
+	depends on BACKPORT_DVB_CORE && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  This module drives a number of tuners based on PLL chips with a
 	  common I2C interface. Say Y when you want to support these tuners.
 
-config DVB_TUNER_DIB0070
+config BACKPORT_DVB_TUNER_DIB0070
 	tristate "DiBcom DiB0070 silicon base-band tuner"
+	depends on !DVB_TUNER_DIB0070
 	depends on I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
@@ -689,8 +773,9 @@ config DVB_TUNER_DIB0070
 	  This device is only used inside a SiP called together with a
 	  demodulator for now.
 
-config DVB_TUNER_DIB0090
+config BACKPORT_DVB_TUNER_DIB0090
 	tristate "DiBcom DiB0090 silicon base-band tuner"
+	depends on !DVB_TUNER_DIB0090
 	depends on I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
@@ -699,20 +784,22 @@ config DVB_TUNER_DIB0090
 	  demodulator for now.
 
 comment "SEC control devices for DVB-S"
-	depends on DVB_CORE
+	depends on BACKPORT_DVB_CORE
 
 source "$BACKPORT_DIR/drivers/media/dvb-frontends/drx39xyj/Kconfig"
 
-config DVB_LNBP21
+config BACKPORT_DVB_LNBP21
 	tristate "LNBP21/LNBH24 SEC controllers"
-	depends on DVB_CORE && I2C
+	depends on !DVB_LNBP21
+	depends on BACKPORT_DVB_CORE && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  An SEC control chips.
 
-config DVB_LNBP22
+config BACKPORT_DVB_LNBP22
 	tristate "LNBP22 SEC controllers"
-	depends on DVB_CORE && I2C
+	depends on !DVB_LNBP22
+	depends on BACKPORT_DVB_CORE && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  LNB power supply and control voltage
@@ -720,64 +807,73 @@ config DVB_LNBP22
 	  and I2C interface.
 	  Say Y when you want to support this chip.
 
-config DVB_ISL6405
+config BACKPORT_DVB_ISL6405
 	tristate "ISL6405 SEC controller"
-	depends on DVB_CORE && I2C
+	depends on !DVB_ISL6405
+	depends on BACKPORT_DVB_CORE && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  An SEC control chip.
 
-config DVB_ISL6421
+config BACKPORT_DVB_ISL6421
 	tristate "ISL6421 SEC controller"
-	depends on DVB_CORE && I2C
+	depends on !DVB_ISL6421
+	depends on BACKPORT_DVB_CORE && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  An SEC control chip.
 
-config DVB_ISL6423
+config BACKPORT_DVB_ISL6423
 	tristate "ISL6423 SEC controller"
-	depends on DVB_CORE && I2C
+	depends on !DVB_ISL6423
+	depends on BACKPORT_DVB_CORE && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  A SEC controller chip from Intersil
 
-config DVB_A8293
+config BACKPORT_DVB_A8293
 	tristate "Allegro A8293"
-	depends on DVB_CORE && I2C
+	depends on !DVB_A8293
+	depends on BACKPORT_DVB_CORE && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 
-config DVB_SP2
+config BACKPORT_DVB_SP2
 	tristate "CIMaX SP2"
-	depends on DVB_CORE && I2C
+	depends on !DVB_SP2
+	depends on BACKPORT_DVB_CORE && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  CIMaX SP2/SP2HF Common Interface module.
 
-config DVB_LGS8GL5
+config BACKPORT_DVB_LGS8GL5
 	tristate "Silicon Legend LGS-8GL5 demodulator (OFDM)"
-	depends on DVB_CORE && I2C
+	depends on !DVB_LGS8GL5
+	depends on BACKPORT_DVB_CORE && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  A DMB-TH tuner module. Say Y when you want to support this frontend.
 
-config DVB_LGS8GXX
+config BACKPORT_DVB_LGS8GXX
 	tristate "Legend Silicon LGS8913/LGS8GL5/LGS8GXX DMB-TH demodulator"
-	depends on DVB_CORE && I2C
+	depends on !DVB_LGS8GXX
+	depends on BACKPORT_DVB_CORE && I2C
 	select FW_LOADER
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  A DMB-TH tuner module. Say Y when you want to support this frontend.
 
-config DVB_ATBM8830
+config BACKPORT_DVB_ATBM8830
 	tristate "AltoBeam ATBM8830/8831 DMB-TH demodulator"
-	depends on DVB_CORE && I2C
+	depends on !DVB_ATBM8830
+	depends on BACKPORT_DVB_CORE && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  A DMB-TH tuner module. Say Y when you want to support this frontend.
 
-config DVB_TDA665x
+config BACKPORT_DVB_TDA665x
 	tristate "TDA665x tuner"
-	depends on DVB_CORE && I2C
+	depends on !DVB_TDA665x
+	depends on BACKPORT_DVB_CORE && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  Support for tuner modules based on Philips TDA6650/TDA6651 chips.
@@ -786,29 +882,33 @@ config DVB_TDA665x
 	  Currently supported tuners:
 	  * Panasonic ENV57H12D5 (ET-50DT)
 
-config DVB_IX2505V
+config BACKPORT_DVB_IX2505V
 	tristate "Sharp IX2505V silicon tuner"
-	depends on DVB_CORE && I2C
+	depends on !DVB_IX2505V
+	depends on BACKPORT_DVB_CORE && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  A DVB-S tuner module. Say Y when you want to support this frontend.
 
-config DVB_M88RS2000
+config BACKPORT_DVB_M88RS2000
 	tristate "M88RS2000 DVB-S demodulator and tuner"
-	depends on DVB_CORE && I2C
+	depends on !DVB_M88RS2000
+	depends on BACKPORT_DVB_CORE && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  A DVB-S tuner module.
 	  Say Y when you want to support this frontend.
 
-config DVB_AF9033
+config BACKPORT_DVB_AF9033
 	tristate "Afatech AF9033 DVB-T demodulator"
-	depends on DVB_CORE && I2C
+	depends on !DVB_AF9033
+	depends on BACKPORT_DVB_CORE && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 
 comment "Tools to develop new frontends"
 
-config DVB_DUMMY_FE
+config BACKPORT_DVB_DUMMY_FE
 	tristate "Dummy frontend driver"
+	depends on !DVB_DUMMY_FE
 	default n
 endmenu
diff --git a/backports/drivers/media/dvb-frontends/drx39xyj/Kconfig b/backports/drivers/media/dvb-frontends/drx39xyj/Kconfig
index 6c2ccb6..0d75e12 100644
--- a/backports/drivers/media/dvb-frontends/drx39xyj/Kconfig
+++ b/backports/drivers/media/dvb-frontends/drx39xyj/Kconfig
@@ -1,6 +1,7 @@
-config DVB_DRX39XYJ
+config BACKPORT_DVB_DRX39XYJ
 	tristate "Micronas DRX-J demodulator"
-	depends on DVB_CORE && I2C
+	depends on !DVB_DRX39XYJ
+	depends on BACKPORT_DVB_CORE && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  An ATSC 8VSB and QAM64/256 tuner module. Say Y when you want
diff --git a/backports/drivers/media/firewire/Kconfig b/backports/drivers/media/firewire/Kconfig
index f3e9448..356059a 100644
--- a/backports/drivers/media/firewire/Kconfig
+++ b/backports/drivers/media/firewire/Kconfig
@@ -1,6 +1,7 @@
-config DVB_FIREDTV
+config BACKPORT_DVB_FIREDTV
 	tristate "FireDTV and FloppyDTV"
-	depends on DVB_CORE && FIREWIRE
+	depends on !DVB_FIREDTV
+	depends on BACKPORT_DVB_CORE && FIREWIRE
 	help
 	  Support for DVB receivers from Digital Everywhere
 	  which are connected via IEEE 1394 (FireWire).
@@ -11,9 +12,10 @@ config DVB_FIREDTV
 	  To compile this driver as a module, say M here:
 	  the module will be called firedtv.
 
-if DVB_FIREDTV
+if BACKPORT_DVB_FIREDTV
 
-config DVB_FIREDTV_INPUT
+config BACKPORT_DVB_FIREDTV_INPUT
 	def_bool INPUT = y || (INPUT = m && DVB_FIREDTV = m)
+	depends on !DVB_FIREDTV_INPUT
 
-endif # DVB_FIREDTV
+endif # BACKPORT_DVB_FIREDTV
diff --git a/backports/drivers/media/i2c/Kconfig b/backports/drivers/media/i2c/Kconfig
index 36095a1..e80b661 100644
--- a/backports/drivers/media/i2c/Kconfig
+++ b/backports/drivers/media/i2c/Kconfig
@@ -2,11 +2,12 @@
 # Multimedia Video device configuration
 #
 
-if VIDEO_V4L2
+if BACKPORT_VIDEO_V4L2
 
-config VIDEO_IR_I2C
+config BACKPORT_VIDEO_IR_I2C
 	tristate "I2C module for IR" if !MEDIA_SUBDRV_AUTOSELECT
-	depends on I2C && RC_CORE
+	depends on !VIDEO_IR_I2C
+	depends on I2C && BACKPORT_RC_CORE
 	default y
 	---help---
 	  Most boards have an IR chip directly connected via GPIO. However,
@@ -26,9 +27,10 @@ menu "Encoders, decoders, sensors and other helper chips"
 
 comment "Audio decoders, processors and mixers"
 
-config VIDEO_TVAUDIO
+config BACKPORT_VIDEO_TVAUDIO
 	tristate "Simple audio decoder chips"
-	depends on VIDEO_V4L2 && I2C
+	depends on !VIDEO_TVAUDIO
+	depends on BACKPORT_VIDEO_V4L2 && I2C
 	---help---
 	  Support for several audio decoder chips found on some bt8xx boards:
 	  Philips: tda9840, tda9873h, tda9874h/a, tda9850, tda985x, tea6300,
@@ -38,17 +40,19 @@ config VIDEO_TVAUDIO
 	  To compile this driver as a module, choose M here: the
 	  module will be called tvaudio.
 
-config VIDEO_TDA7432
+config BACKPORT_VIDEO_TDA7432
 	tristate "Philips TDA7432 audio processor"
-	depends on VIDEO_V4L2 && I2C
+	depends on !VIDEO_TDA7432
+	depends on BACKPORT_VIDEO_V4L2 && I2C
 	---help---
 	  Support for tda7432 audio decoder chip found on some bt8xx boards.
 
 	  To compile this driver as a module, choose M here: the
 	  module will be called tda7432.
 
-config VIDEO_TDA9840
+config BACKPORT_VIDEO_TDA9840
 	tristate "Philips TDA9840 audio processor"
+	depends on !VIDEO_TDA9840
 	depends on I2C
 	---help---
 	  Support for tda9840 audio decoder chip found on some Zoran boards.
@@ -56,8 +60,9 @@ config VIDEO_TDA9840
 	  To compile this driver as a module, choose M here: the
 	  module will be called tda9840.
 
-config VIDEO_TEA6415C
+config BACKPORT_VIDEO_TEA6415C
 	tristate "Philips TEA6415C audio processor"
+	depends on !VIDEO_TEA6415C
 	depends on I2C
 	---help---
 	  Support for tea6415c audio decoder chip found on some bt8xx boards.
@@ -65,8 +70,9 @@ config VIDEO_TEA6415C
 	  To compile this driver as a module, choose M here: the
 	  module will be called tea6415c.
 
-config VIDEO_TEA6420
+config BACKPORT_VIDEO_TEA6420
 	tristate "Philips TEA6420 audio processor"
+	depends on !VIDEO_TEA6420
 	depends on I2C
 	---help---
 	  Support for tea6420 audio decoder chip found on some bt8xx boards.
@@ -74,18 +80,20 @@ config VIDEO_TEA6420
 	  To compile this driver as a module, choose M here: the
 	  module will be called tea6420.
 
-config VIDEO_MSP3400
+config BACKPORT_VIDEO_MSP3400
 	tristate "Micronas MSP34xx audio decoders"
-	depends on VIDEO_V4L2 && I2C
+	depends on !VIDEO_MSP3400
+	depends on BACKPORT_VIDEO_V4L2 && I2C
 	---help---
 	  Support for the Micronas MSP34xx series of audio decoders.
 
 	  To compile this driver as a module, choose M here: the
 	  module will be called msp3400.
 
-config VIDEO_CS5345
+config BACKPORT_VIDEO_CS5345
 	tristate "Cirrus Logic CS5345 audio ADC"
-	depends on VIDEO_V4L2 && I2C
+	depends on !VIDEO_CS5345
+	depends on BACKPORT_VIDEO_V4L2 && I2C
 	---help---
 	  Support for the Cirrus Logic CS5345 24-bit, 192 kHz
 	  stereo A/D converter.
@@ -93,9 +101,10 @@ config VIDEO_CS5345
 	  To compile this driver as a module, choose M here: the
 	  module will be called cs5345.
 
-config VIDEO_CS53L32A
+config BACKPORT_VIDEO_CS53L32A
 	tristate "Cirrus Logic CS53L32A audio ADC"
-	depends on VIDEO_V4L2 && I2C
+	depends on !VIDEO_CS53L32A
+	depends on BACKPORT_VIDEO_V4L2 && I2C
 	---help---
 	  Support for the Cirrus Logic CS53L32A low voltage
 	  stereo A/D converter.
@@ -103,27 +112,30 @@ config VIDEO_CS53L32A
 	  To compile this driver as a module, choose M here: the
 	  module will be called cs53l32a.
 
-config VIDEO_TLV320AIC23B
+config BACKPORT_VIDEO_TLV320AIC23B
 	tristate "Texas Instruments TLV320AIC23B audio codec"
-	depends on VIDEO_V4L2 && I2C
+	depends on !VIDEO_TLV320AIC23B
+	depends on BACKPORT_VIDEO_V4L2 && I2C
 	---help---
 	  Support for the Texas Instruments TLV320AIC23B audio codec.
 
 	  To compile this driver as a module, choose M here: the
 	  module will be called tlv320aic23b.
 
-config VIDEO_UDA1342
+config BACKPORT_VIDEO_UDA1342
 	tristate "Philips UDA1342 audio codec"
-	depends on VIDEO_V4L2 && I2C
+	depends on !VIDEO_UDA1342
+	depends on BACKPORT_VIDEO_V4L2 && I2C
 	---help---
 	  Support for the Philips UDA1342 audio codec.
 
 	  To compile this driver as a module, choose M here: the
 	  module will be called uda1342.
 
-config VIDEO_WM8775
+config BACKPORT_VIDEO_WM8775
 	tristate "Wolfson Microelectronics WM8775 audio ADC with input mixer"
-	depends on VIDEO_V4L2 && I2C
+	depends on !VIDEO_WM8775
+	depends on BACKPORT_VIDEO_V4L2 && I2C
 	---help---
 	  Support for the Wolfson Microelectronics WM8775 high
 	  performance stereo A/D Converter with a 4 channel input mixer.
@@ -131,9 +143,10 @@ config VIDEO_WM8775
 	  To compile this driver as a module, choose M here: the
 	  module will be called wm8775.
 
-config VIDEO_WM8739
+config BACKPORT_VIDEO_WM8739
 	tristate "Wolfson Microelectronics WM8739 stereo audio ADC"
-	depends on VIDEO_V4L2 && I2C
+	depends on !VIDEO_WM8739
+	depends on BACKPORT_VIDEO_V4L2 && I2C
 	---help---
 	  Support for the Wolfson Microelectronics WM8739
 	  stereo A/D Converter.
@@ -141,29 +154,32 @@ config VIDEO_WM8739
 	  To compile this driver as a module, choose M here: the
 	  module will be called wm8739.
 
-config VIDEO_VP27SMPX
+config BACKPORT_VIDEO_VP27SMPX
 	tristate "Panasonic VP27's internal MPX"
-	depends on VIDEO_V4L2 && I2C
+	depends on !VIDEO_VP27SMPX
+	depends on BACKPORT_VIDEO_V4L2 && I2C
 	---help---
 	  Support for the internal MPX of the Panasonic VP27s tuner.
 
 	  To compile this driver as a module, choose M here: the
 	  module will be called vp27smpx.
 
-config VIDEO_SONY_BTF_MPX
+config BACKPORT_VIDEO_SONY_BTF_MPX
 	tristate "Sony BTF's internal MPX"
-	depends on VIDEO_V4L2 && I2C
+	depends on !VIDEO_SONY_BTF_MPX
+	depends on BACKPORT_VIDEO_V4L2 && I2C
 	help
-	  Support for the internal MPX of the Sony BTF-PG472Z tuner.
+	  Support for the internal MPX of the Sony BACKPORT_BTF-PG472Z tuner.
 
 	  To compile this driver as a module, choose M here: the
 	  module will be called sony-btf-mpx.
 
 comment "RDS decoders"
 
-config VIDEO_SAA6588
+config BACKPORT_VIDEO_SAA6588
 	tristate "SAA6588 Radio Chip RDS decoder support"
-	depends on VIDEO_V4L2 && I2C
+	depends on !VIDEO_SAA6588
+	depends on BACKPORT_VIDEO_V4L2 && I2C
 
 	help
 	  Support for this Radio Data System (RDS) decoder. This allows
@@ -175,18 +191,20 @@ config VIDEO_SAA6588
 
 comment "Video decoders"
 
-config VIDEO_ADV7180
+config BACKPORT_VIDEO_ADV7180
 	tristate "Analog Devices ADV7180 decoder"
-	depends on VIDEO_V4L2 && I2C && VIDEO_V4L2_SUBDEV_API
+	depends on !VIDEO_ADV7180
+	depends on VIDEO_V4L2 && I2C && BACKPORT_VIDEO_V4L2_SUBDEV_API
 	---help---
 	  Support for the Analog Devices ADV7180 video decoder.
 
 	  To compile this driver as a module, choose M here: the
 	  module will be called adv7180.
 
-config VIDEO_ADV7183
+config BACKPORT_VIDEO_ADV7183
 	tristate "Analog Devices ADV7183 decoder"
-	depends on VIDEO_V4L2 && I2C
+	depends on !VIDEO_ADV7183
+	depends on BACKPORT_VIDEO_V4L2 && I2C
 	---help---
 	  V4l2 subdevice driver for the Analog Devices
 	  ADV7183 video decoder.
@@ -194,9 +212,10 @@ config VIDEO_ADV7183
 	  To compile this driver as a module, choose M here: the
 	  module will be called adv7183.
 
-config VIDEO_ADV7604
+config BACKPORT_VIDEO_ADV7604
 	tristate "Analog Devices ADV7604 decoder"
-	depends on VIDEO_V4L2 && I2C && VIDEO_V4L2_SUBDEV_API
+	depends on !VIDEO_ADV7604
+	depends on VIDEO_V4L2 && I2C && BACKPORT_VIDEO_V4L2_SUBDEV_API
 	---help---
 	  Support for the Analog Devices ADV7604 video decoder.
 
@@ -206,9 +225,10 @@ config VIDEO_ADV7604
 	  To compile this driver as a module, choose M here: the
 	  module will be called adv7604.
 
-config VIDEO_ADV7842
+config BACKPORT_VIDEO_ADV7842
 	tristate "Analog Devices ADV7842 decoder"
-	depends on VIDEO_V4L2 && I2C && VIDEO_V4L2_SUBDEV_API
+	depends on !VIDEO_ADV7842
+	depends on VIDEO_V4L2 && I2C && BACKPORT_VIDEO_V4L2_SUBDEV_API
 	select HDMI
 	---help---
 	  Support for the Analog Devices ADV7842 video decoder.
@@ -219,36 +239,40 @@ config VIDEO_ADV7842
 	  To compile this driver as a module, choose M here: the
 	  module will be called adv7842.
 
-config VIDEO_BT819
+config BACKPORT_VIDEO_BT819
 	tristate "BT819A VideoStream decoder"
-	depends on VIDEO_V4L2 && I2C
+	depends on !VIDEO_BT819
+	depends on BACKPORT_VIDEO_V4L2 && I2C
 	---help---
-	  Support for BT819A video decoder.
+	  Support for BACKPORT_BT819A video decoder.
 
 	  To compile this driver as a module, choose M here: the
 	  module will be called bt819.
 
-config VIDEO_BT856
+config BACKPORT_VIDEO_BT856
 	tristate "BT856 VideoStream decoder"
-	depends on VIDEO_V4L2 && I2C
+	depends on !VIDEO_BT856
+	depends on BACKPORT_VIDEO_V4L2 && I2C
 	---help---
-	  Support for BT856 video decoder.
+	  Support for BACKPORT_BT856 video decoder.
 
 	  To compile this driver as a module, choose M here: the
 	  module will be called bt856.
 
-config VIDEO_BT866
+config BACKPORT_VIDEO_BT866
 	tristate "BT866 VideoStream decoder"
-	depends on VIDEO_V4L2 && I2C
+	depends on !VIDEO_BT866
+	depends on BACKPORT_VIDEO_V4L2 && I2C
 	---help---
-	  Support for BT866 video decoder.
+	  Support for BACKPORT_BT866 video decoder.
 
 	  To compile this driver as a module, choose M here: the
 	  module will be called bt866.
 
-config VIDEO_KS0127
+config BACKPORT_VIDEO_KS0127
 	tristate "KS0127 video decoder"
-	depends on VIDEO_V4L2 && I2C
+	depends on !VIDEO_KS0127
+	depends on BACKPORT_VIDEO_V4L2 && I2C
 	---help---
 	  Support for KS0127 video decoder.
 
@@ -258,36 +282,40 @@ config VIDEO_KS0127
 	  To compile this driver as a module, choose M here: the
 	  module will be called ks0127.
 
-config VIDEO_ML86V7667
+config BACKPORT_VIDEO_ML86V7667
 	tristate "OKI ML86V7667 video decoder"
-	depends on VIDEO_V4L2 && I2C
+	depends on !VIDEO_ML86V7667
+	depends on BACKPORT_VIDEO_V4L2 && I2C
 	---help---
 	  Support for the OKI Semiconductor ML86V7667 video decoder.
 
 	  To compile this driver as a module, choose M here: the
 	  module will be called ml86v7667.
 
-config VIDEO_SAA7110
+config BACKPORT_VIDEO_SAA7110
 	tristate "Philips SAA7110 video decoder"
-	depends on VIDEO_V4L2 && I2C
+	depends on !VIDEO_SAA7110
+	depends on BACKPORT_VIDEO_V4L2 && I2C
 	---help---
 	  Support for the Philips SAA7110 video decoders.
 
 	  To compile this driver as a module, choose M here: the
 	  module will be called saa7110.
 
-config VIDEO_SAA711X
+config BACKPORT_VIDEO_SAA711X
 	tristate "Philips SAA7111/3/4/5 video decoders"
-	depends on VIDEO_V4L2 && I2C
+	depends on !VIDEO_SAA711X
+	depends on BACKPORT_VIDEO_V4L2 && I2C
 	---help---
 	  Support for the Philips SAA7111/3/4/5 video decoders.
 
 	  To compile this driver as a module, choose M here: the
 	  module will be called saa7115.
 
-config VIDEO_TVP514X
+config BACKPORT_VIDEO_TVP514X
 	tristate "Texas Instruments TVP514x video decoder"
-	depends on VIDEO_V4L2 && I2C
+	depends on !VIDEO_TVP514X
+	depends on BACKPORT_VIDEO_V4L2 && I2C
 	---help---
 	  This is a Video4Linux2 sensor-level driver for the TI TVP5146/47
 	  decoder. It is currently working with the TI OMAP3 camera
@@ -296,36 +324,40 @@ config VIDEO_TVP514X
 	  To compile this driver as a module, choose M here: the
 	  module will be called tvp514x.
 
-config VIDEO_TVP5150
+config BACKPORT_VIDEO_TVP5150
 	tristate "Texas Instruments TVP5150 video decoder"
-	depends on VIDEO_V4L2 && I2C
+	depends on !VIDEO_TVP5150
+	depends on BACKPORT_VIDEO_V4L2 && I2C
 	---help---
 	  Support for the Texas Instruments TVP5150 video decoder.
 
 	  To compile this driver as a module, choose M here: the
 	  module will be called tvp5150.
 
-config VIDEO_TVP7002
+config BACKPORT_VIDEO_TVP7002
 	tristate "Texas Instruments TVP7002 video decoder"
-	depends on VIDEO_V4L2 && I2C
+	depends on !VIDEO_TVP7002
+	depends on BACKPORT_VIDEO_V4L2 && I2C
 	---help---
 	  Support for the Texas Instruments TVP7002 video decoder.
 
 	  To compile this driver as a module, choose M here: the
 	  module will be called tvp7002.
 
-config VIDEO_TW2804
+config BACKPORT_VIDEO_TW2804
 	tristate "Techwell TW2804 multiple video decoder"
-	depends on VIDEO_V4L2 && I2C
+	depends on !VIDEO_TW2804
+	depends on BACKPORT_VIDEO_V4L2 && I2C
 	---help---
 	  Support for the Techwell tw2804 multiple video decoder.
 
 	  To compile this driver as a module, choose M here: the
 	  module will be called tw2804.
 
-config VIDEO_TW9903
+config BACKPORT_VIDEO_TW9903
 	tristate "Techwell TW9903 video decoder"
-	depends on VIDEO_V4L2 && I2C
+	depends on !VIDEO_TW9903
+	depends on BACKPORT_VIDEO_V4L2 && I2C
 	---help---
 	  Support for the Techwell tw9903 multi-standard video decoder
 	  with high quality down scaler.
@@ -333,9 +365,10 @@ config VIDEO_TW9903
 	  To compile this driver as a module, choose M here: the
 	  module will be called tw9903.
 
-config VIDEO_TW9906
+config BACKPORT_VIDEO_TW9906
 	tristate "Techwell TW9906 video decoder"
-	depends on VIDEO_V4L2 && I2C
+	depends on !VIDEO_TW9906
+	depends on BACKPORT_VIDEO_V4L2 && I2C
 	---help---
 	  Support for the Techwell tw9906 enhanced multi-standard comb filter
 	  video decoder with YCbCr input support.
@@ -343,9 +376,10 @@ config VIDEO_TW9906
 	  To compile this driver as a module, choose M here: the
 	  module will be called tw9906.
 
-config VIDEO_VPX3220
+config BACKPORT_VIDEO_VPX3220
 	tristate "vpx3220a, vpx3216b & vpx3214c video decoders"
-	depends on VIDEO_V4L2 && I2C
+	depends on !VIDEO_VPX3220
+	depends on BACKPORT_VIDEO_V4L2 && I2C
 	---help---
 	  Support for VPX322x video decoders.
 
@@ -354,9 +388,10 @@ config VIDEO_VPX3220
 
 comment "Video and audio decoders"
 
-config VIDEO_SAA717X
+config BACKPORT_VIDEO_SAA717X
 	tristate "Philips SAA7171/3/4 audio/video decoders"
-	depends on VIDEO_V4L2 && I2C
+	depends on !VIDEO_SAA717X
+	depends on BACKPORT_VIDEO_V4L2 && I2C
 	---help---
 	  Support for the Philips SAA7171/3/4 audio/video decoders.
 
@@ -367,44 +402,49 @@ source "$BACKPORT_DIR/drivers/media/i2c/cx25840/Kconfig"
 
 comment "Video encoders"
 
-config VIDEO_SAA7127
+config BACKPORT_VIDEO_SAA7127
 	tristate "Philips SAA7127/9 digital video encoders"
-	depends on VIDEO_V4L2 && I2C
+	depends on !VIDEO_SAA7127
+	depends on BACKPORT_VIDEO_V4L2 && I2C
 	---help---
 	  Support for the Philips SAA7127/9 digital video encoders.
 
 	  To compile this driver as a module, choose M here: the
 	  module will be called saa7127.
 
-config VIDEO_SAA7185
+config BACKPORT_VIDEO_SAA7185
 	tristate "Philips SAA7185 video encoder"
-	depends on VIDEO_V4L2 && I2C
+	depends on !VIDEO_SAA7185
+	depends on BACKPORT_VIDEO_V4L2 && I2C
 	---help---
 	  Support for the Philips SAA7185 video encoder.
 
 	  To compile this driver as a module, choose M here: the
 	  module will be called saa7185.
 
-config VIDEO_ADV7170
+config BACKPORT_VIDEO_ADV7170
 	tristate "Analog Devices ADV7170 video encoder"
-	depends on VIDEO_V4L2 && I2C
+	depends on !VIDEO_ADV7170
+	depends on BACKPORT_VIDEO_V4L2 && I2C
 	---help---
 	  Support for the Analog Devices ADV7170 video encoder driver
 
 	  To compile this driver as a module, choose M here: the
 	  module will be called adv7170.
 
-config VIDEO_ADV7175
+config BACKPORT_VIDEO_ADV7175
 	tristate "Analog Devices ADV7175 video encoder"
-	depends on VIDEO_V4L2 && I2C
+	depends on !VIDEO_ADV7175
+	depends on BACKPORT_VIDEO_V4L2 && I2C
 	---help---
 	  Support for the Analog Devices ADV7175 video encoder driver
 
 	  To compile this driver as a module, choose M here: the
 	  module will be called adv7175.
 
-config VIDEO_ADV7343
+config BACKPORT_VIDEO_ADV7343
 	tristate "ADV7343 video encoder"
+	depends on !VIDEO_ADV7343
 	depends on I2C
 	help
 	  Support for Analog Devices I2C bus based ADV7343 encoder.
@@ -412,8 +452,9 @@ config VIDEO_ADV7343
 	  To compile this driver as a module, choose M here: the
 	  module will be called adv7343.
 
-config VIDEO_ADV7393
+config BACKPORT_VIDEO_ADV7393
 	tristate "ADV7393 video encoder"
+	depends on !VIDEO_ADV7393
 	depends on I2C
 	help
 	  Support for Analog Devices I2C bus based ADV7393 encoder.
@@ -421,9 +462,10 @@ config VIDEO_ADV7393
 	  To compile this driver as a module, choose M here: the
 	  module will be called adv7393.
 
-config VIDEO_ADV7511
+config BACKPORT_VIDEO_ADV7511
 	tristate "Analog Devices ADV7511 encoder"
-	depends on VIDEO_V4L2 && I2C && VIDEO_V4L2_SUBDEV_API
+	depends on !VIDEO_ADV7511
+	depends on VIDEO_V4L2 && I2C && BACKPORT_VIDEO_V4L2_SUBDEV_API
 	---help---
 	  Support for the Analog Devices ADV7511 video encoder.
 
@@ -432,9 +474,10 @@ config VIDEO_ADV7511
 	  To compile this driver as a module, choose M here: the
 	  module will be called adv7511.
 
-config VIDEO_AD9389B
+config BACKPORT_VIDEO_AD9389B
 	tristate "Analog Devices AD9389B encoder"
-	depends on VIDEO_V4L2 && I2C && VIDEO_V4L2_SUBDEV_API
+	depends on !VIDEO_AD9389B
+	depends on VIDEO_V4L2 && I2C && BACKPORT_VIDEO_V4L2_SUBDEV_API
 	---help---
 	  Support for the Analog Devices AD9389B video encoder.
 
@@ -443,15 +486,17 @@ config VIDEO_AD9389B
 	  To compile this driver as a module, choose M here: the
 	  module will be called ad9389b.
 
-config VIDEO_AK881X
+config BACKPORT_VIDEO_AK881X
 	tristate "AK8813/AK8814 video encoders"
+	depends on !VIDEO_AK881X
 	depends on I2C
 	help
 	  Video output driver for AKM AK8813 and AK8814 TV encoders
 
-config VIDEO_THS8200
+config BACKPORT_VIDEO_THS8200
 	tristate "Texas Instruments THS8200 video encoder"
-	depends on VIDEO_V4L2 && I2C
+	depends on !VIDEO_THS8200
+	depends on BACKPORT_VIDEO_V4L2 && I2C
 	---help---
 	  Support for the Texas Instruments THS8200 video encoder.
 
@@ -460,16 +505,19 @@ config VIDEO_THS8200
 
 comment "Camera sensor devices"
 
-config VIDEO_APTINA_PLL
+config BACKPORT_VIDEO_APTINA_PLL
 	tristate
+	depends on !VIDEO_APTINA_PLL
 
-config VIDEO_SMIAPP_PLL
+config BACKPORT_VIDEO_SMIAPP_PLL
 	tristate
+	depends on !VIDEO_SMIAPP_PLL
 
-config VIDEO_OV2659
+config BACKPORT_VIDEO_OV2659
 	tristate "OmniVision OV2659 sensor support"
-	depends on VIDEO_V4L2 && I2C
-	depends on MEDIA_CAMERA_SUPPORT
+	depends on !VIDEO_OV2659
+	depends on BACKPORT_VIDEO_V4L2 && I2C
+	depends on BACKPORT_MEDIA_CAMERA_SUPPORT
 	---help---
 	  This is a Video4Linux2 sensor-level driver for the OmniVision
 	  OV2659 camera.
@@ -477,10 +525,11 @@ config VIDEO_OV2659
 	  To compile this driver as a module, choose M here: the
 	  module will be called ov2659.
 
-config VIDEO_OV7640
+config BACKPORT_VIDEO_OV7640
 	tristate "OmniVision OV7640 sensor support"
-	depends on I2C && VIDEO_V4L2
-	depends on MEDIA_CAMERA_SUPPORT
+	depends on !VIDEO_OV7640
+	depends on I2C && BACKPORT_VIDEO_V4L2
+	depends on BACKPORT_MEDIA_CAMERA_SUPPORT
 	---help---
 	  This is a Video4Linux2 sensor-level driver for the OmniVision
 	  OV7640 camera.
@@ -488,26 +537,29 @@ config VIDEO_OV7640
 	  To compile this driver as a module, choose M here: the
 	  module will be called ov7640.
 
-config VIDEO_OV7670
+config BACKPORT_VIDEO_OV7670
 	tristate "OmniVision OV7670 sensor support"
-	depends on I2C && VIDEO_V4L2
-	depends on MEDIA_CAMERA_SUPPORT
+	depends on !VIDEO_OV7670
+	depends on I2C && BACKPORT_VIDEO_V4L2
+	depends on BACKPORT_MEDIA_CAMERA_SUPPORT
 	---help---
 	  This is a Video4Linux2 sensor-level driver for the OmniVision
 	  OV7670 VGA camera.  It currently only works with the M88ALP01
 	  controller.
 
-config VIDEO_OV9650
+config BACKPORT_VIDEO_OV9650
 	tristate "OmniVision OV9650/OV9652 sensor support"
-	depends on I2C && VIDEO_V4L2 && VIDEO_V4L2_SUBDEV_API
+	depends on !VIDEO_OV9650
+	depends on I2C && VIDEO_V4L2 && BACKPORT_VIDEO_V4L2_SUBDEV_API
 	---help---
 	  This is a V4L2 sensor-level driver for the Omnivision
 	  OV9650 and OV9652 camera sensors.
 
-config VIDEO_VS6624
+config BACKPORT_VIDEO_VS6624
 	tristate "ST VS6624 sensor support"
-	depends on VIDEO_V4L2 && I2C
-	depends on MEDIA_CAMERA_SUPPORT
+	depends on !VIDEO_VS6624
+	depends on BACKPORT_VIDEO_V4L2 && I2C
+	depends on BACKPORT_MEDIA_CAMERA_SUPPORT
 	---help---
 	  This is a Video4Linux2 sensor-level driver for the ST VS6624
 	  camera.
@@ -515,136 +567,152 @@ config VIDEO_VS6624
 	  To compile this driver as a module, choose M here: the
 	  module will be called vs6624.
 
-config VIDEO_MT9M032
+config BACKPORT_VIDEO_MT9M032
 	tristate "MT9M032 camera sensor support"
-	depends on I2C && VIDEO_V4L2 && VIDEO_V4L2_SUBDEV_API
-	depends on MEDIA_CAMERA_SUPPORT
-	select VIDEO_APTINA_PLL
+	depends on !VIDEO_MT9M032
+	depends on I2C && VIDEO_V4L2 && BACKPORT_VIDEO_V4L2_SUBDEV_API
+	depends on BACKPORT_MEDIA_CAMERA_SUPPORT
+	select BACKPORT_VIDEO_APTINA_PLL
 	---help---
 	  This driver supports MT9M032 camera sensors from Aptina, monochrome
 	  models only.
 
-config VIDEO_MT9P031
+config BACKPORT_VIDEO_MT9P031
 	tristate "Aptina MT9P031 support"
-	depends on I2C && VIDEO_V4L2 && VIDEO_V4L2_SUBDEV_API
-	depends on MEDIA_CAMERA_SUPPORT
-	select VIDEO_APTINA_PLL
+	depends on !VIDEO_MT9P031
+	depends on I2C && VIDEO_V4L2 && BACKPORT_VIDEO_V4L2_SUBDEV_API
+	depends on BACKPORT_MEDIA_CAMERA_SUPPORT
+	select BACKPORT_VIDEO_APTINA_PLL
 	---help---
 	  This is a Video4Linux2 sensor-level driver for the Aptina
 	  (Micron) mt9p031 5 Mpixel camera.
 
-config VIDEO_MT9T001
+config BACKPORT_VIDEO_MT9T001
 	tristate "Aptina MT9T001 support"
-	depends on I2C && VIDEO_V4L2 && VIDEO_V4L2_SUBDEV_API
-	depends on MEDIA_CAMERA_SUPPORT
+	depends on !VIDEO_MT9T001
+	depends on I2C && VIDEO_V4L2 && BACKPORT_VIDEO_V4L2_SUBDEV_API
+	depends on BACKPORT_MEDIA_CAMERA_SUPPORT
 	---help---
 	  This is a Video4Linux2 sensor-level driver for the Aptina
 	  (Micron) mt0t001 3 Mpixel camera.
 
-config VIDEO_MT9V011
+config BACKPORT_VIDEO_MT9V011
 	tristate "Micron mt9v011 sensor support"
-	depends on I2C && VIDEO_V4L2
-	depends on MEDIA_CAMERA_SUPPORT
+	depends on !VIDEO_MT9V011
+	depends on I2C && BACKPORT_VIDEO_V4L2
+	depends on BACKPORT_MEDIA_CAMERA_SUPPORT
 	---help---
 	  This is a Video4Linux2 sensor-level driver for the Micron
 	  mt0v011 1.3 Mpixel camera.  It currently only works with the
 	  em28xx driver.
 
-config VIDEO_MT9V032
+config BACKPORT_VIDEO_MT9V032
 	tristate "Micron MT9V032 sensor support"
-	depends on I2C && VIDEO_V4L2 && VIDEO_V4L2_SUBDEV_API
-	depends on MEDIA_CAMERA_SUPPORT
+	depends on !VIDEO_MT9V032
+	depends on I2C && VIDEO_V4L2 && BACKPORT_VIDEO_V4L2_SUBDEV_API
+	depends on BACKPORT_MEDIA_CAMERA_SUPPORT
 	select REGMAP_I2C
 	---help---
 	  This is a Video4Linux2 sensor-level driver for the Micron
 	  MT9V032 752x480 CMOS sensor.
 
-config VIDEO_SR030PC30
+config BACKPORT_VIDEO_SR030PC30
 	tristate "Siliconfile SR030PC30 sensor support"
-	depends on I2C && VIDEO_V4L2
-	depends on MEDIA_CAMERA_SUPPORT
+	depends on !VIDEO_SR030PC30
+	depends on I2C && BACKPORT_VIDEO_V4L2
+	depends on BACKPORT_MEDIA_CAMERA_SUPPORT
 	---help---
 	  This driver supports SR030PC30 VGA camera from Siliconfile
 
-config VIDEO_NOON010PC30
+config BACKPORT_VIDEO_NOON010PC30
 	tristate "Siliconfile NOON010PC30 sensor support"
-	depends on I2C && VIDEO_V4L2 && VIDEO_V4L2_SUBDEV_API
-	depends on MEDIA_CAMERA_SUPPORT
+	depends on !VIDEO_NOON010PC30
+	depends on I2C && VIDEO_V4L2 && BACKPORT_VIDEO_V4L2_SUBDEV_API
+	depends on BACKPORT_MEDIA_CAMERA_SUPPORT
 	---help---
 	  This driver supports NOON010PC30 CIF camera from Siliconfile
 
 source "$BACKPORT_DIR/drivers/media/i2c/m5mols/Kconfig"
 
-config VIDEO_S5K6AA
+config BACKPORT_VIDEO_S5K6AA
 	tristate "Samsung S5K6AAFX sensor support"
-	depends on MEDIA_CAMERA_SUPPORT
-	depends on I2C && VIDEO_V4L2 && VIDEO_V4L2_SUBDEV_API
+	depends on !VIDEO_S5K6AA
+	depends on BACKPORT_MEDIA_CAMERA_SUPPORT
+	depends on I2C && VIDEO_V4L2 && BACKPORT_VIDEO_V4L2_SUBDEV_API
 	---help---
 	  This is a V4L2 sensor-level driver for Samsung S5K6AA(FX) 1.3M
 	  camera sensor with an embedded SoC image signal processor.
 
-config VIDEO_S5K6A3
+config BACKPORT_VIDEO_S5K6A3
 	tristate "Samsung S5K6A3 sensor support"
-	depends on MEDIA_CAMERA_SUPPORT
-	depends on I2C && VIDEO_V4L2 && VIDEO_V4L2_SUBDEV_API
+	depends on !VIDEO_S5K6A3
+	depends on BACKPORT_MEDIA_CAMERA_SUPPORT
+	depends on I2C && VIDEO_V4L2 && BACKPORT_VIDEO_V4L2_SUBDEV_API
 	---help---
 	  This is a V4L2 sensor-level driver for Samsung S5K6A3 raw
 	  camera sensor.
 
-config VIDEO_S5K4ECGX
+config BACKPORT_VIDEO_S5K4ECGX
         tristate "Samsung S5K4ECGX sensor support"
-        depends on I2C && VIDEO_V4L2 && VIDEO_V4L2_SUBDEV_API
+	depends on !VIDEO_S5K4ECGX
+        depends on I2C && VIDEO_V4L2 && BACKPORT_VIDEO_V4L2_SUBDEV_API
         ---help---
           This is a V4L2 sensor-level driver for Samsung S5K4ECGX 5M
           camera sensor with an embedded SoC image signal processor.
 
-config VIDEO_S5K5BAF
+config BACKPORT_VIDEO_S5K5BAF
 	tristate "Samsung S5K5BAF sensor support"
-	depends on I2C && VIDEO_V4L2 && VIDEO_V4L2_SUBDEV_API
+	depends on !VIDEO_S5K5BAF
+	depends on I2C && VIDEO_V4L2 && BACKPORT_VIDEO_V4L2_SUBDEV_API
 	---help---
 	  This is a V4L2 sensor-level driver for Samsung S5K5BAF 2M
 	  camera sensor with an embedded SoC image signal processor.
 
 source "$BACKPORT_DIR/drivers/media/i2c/smiapp/Kconfig"
 
-config VIDEO_S5C73M3
+config BACKPORT_VIDEO_S5C73M3
 	tristate "Samsung S5C73M3 sensor support"
-	depends on I2C && SPI && VIDEO_V4L2 && VIDEO_V4L2_SUBDEV_API
+	depends on !VIDEO_S5C73M3
+	depends on I2C && SPI && VIDEO_V4L2 && BACKPORT_VIDEO_V4L2_SUBDEV_API
 	---help---
 	  This is a V4L2 sensor-level driver for Samsung S5C73M3
 	  8 Mpixel camera.
 
 comment "Flash devices"
 
-config VIDEO_ADP1653
+config BACKPORT_VIDEO_ADP1653
 	tristate "ADP1653 flash support"
-	depends on I2C && VIDEO_V4L2 && MEDIA_CONTROLLER
-	depends on MEDIA_CAMERA_SUPPORT
+	depends on !VIDEO_ADP1653
+	depends on I2C && VIDEO_V4L2 && BACKPORT_MEDIA_CONTROLLER
+	depends on BACKPORT_MEDIA_CAMERA_SUPPORT
 	---help---
 	  This is a driver for the ADP1653 flash controller. It is used for
 	  example in Nokia N900.
 
-config VIDEO_AS3645A
+config BACKPORT_VIDEO_AS3645A
 	tristate "AS3645A flash driver support"
-	depends on I2C && VIDEO_V4L2 && MEDIA_CONTROLLER
-	depends on MEDIA_CAMERA_SUPPORT
+	depends on !VIDEO_AS3645A
+	depends on I2C && VIDEO_V4L2 && BACKPORT_MEDIA_CONTROLLER
+	depends on BACKPORT_MEDIA_CAMERA_SUPPORT
 	---help---
 	  This is a driver for the AS3645A and LM3555 flash controllers. It has
 	  build in control for flash, torch and indicator LEDs.
 
-config VIDEO_LM3560
+config BACKPORT_VIDEO_LM3560
 	tristate "LM3560 dual flash driver support"
-	depends on I2C && VIDEO_V4L2 && MEDIA_CONTROLLER
-	depends on MEDIA_CAMERA_SUPPORT
+	depends on !VIDEO_LM3560
+	depends on I2C && VIDEO_V4L2 && BACKPORT_MEDIA_CONTROLLER
+	depends on BACKPORT_MEDIA_CAMERA_SUPPORT
 	select REGMAP_I2C
 	---help---
 	  This is a driver for the lm3560 dual flash controllers. It controls
 	  flash, torch LEDs.
 
-config VIDEO_LM3646
+config BACKPORT_VIDEO_LM3646
 	tristate "LM3646 dual flash driver support"
-	depends on I2C && VIDEO_V4L2 && MEDIA_CONTROLLER
-	depends on MEDIA_CAMERA_SUPPORT
+	depends on !VIDEO_LM3646
+	depends on I2C && VIDEO_V4L2 && BACKPORT_MEDIA_CONTROLLER
+	depends on BACKPORT_MEDIA_CAMERA_SUPPORT
 	select REGMAP_I2C
 	---help---
 	  This is a driver for the lm3646 dual flash controllers. It controls
@@ -652,9 +720,10 @@ config VIDEO_LM3646
 
 comment "Video improvement chips"
 
-config VIDEO_UPD64031A
+config BACKPORT_VIDEO_UPD64031A
 	tristate "NEC Electronics uPD64031A Ghost Reduction"
-	depends on VIDEO_V4L2 && I2C
+	depends on !VIDEO_UPD64031A
+	depends on BACKPORT_VIDEO_V4L2 && I2C
 	---help---
 	  Support for the NEC Electronics uPD64031A Ghost Reduction
 	  video chip. It is most often found in NTSC TV cards made for
@@ -664,9 +733,10 @@ config VIDEO_UPD64031A
 	  To compile this driver as a module, choose M here: the
 	  module will be called upd64031a.
 
-config VIDEO_UPD64083
+config BACKPORT_VIDEO_UPD64083
 	tristate "NEC Electronics uPD64083 3-Dimensional Y/C separation"
-	depends on VIDEO_V4L2 && I2C
+	depends on !VIDEO_UPD64083
+	depends on BACKPORT_VIDEO_V4L2 && I2C
 	---help---
 	  Support for the NEC Electronics uPD64083 3-Dimensional Y/C
 	  separation video chip. It is used to improve the quality of
@@ -677,9 +747,10 @@ config VIDEO_UPD64083
 
 comment "Audio/Video compression chips"
 
-config VIDEO_SAA6752HS
+config BACKPORT_VIDEO_SAA6752HS
 	tristate "Philips SAA6752HS MPEG-2 Audio/Video Encoder"
-	depends on VIDEO_V4L2 && I2C
+	depends on !VIDEO_SAA6752HS
+	depends on BACKPORT_VIDEO_V4L2 && I2C
 	select CRC32
 	---help---
 	  Support for the Philips SAA6752HS MPEG-2 video and MPEG-audio/AC-3
@@ -690,18 +761,20 @@ config VIDEO_SAA6752HS
 
 comment "Miscellaneous helper chips"
 
-config VIDEO_THS7303
+config BACKPORT_VIDEO_THS7303
 	tristate "THS7303/53 Video Amplifier"
-	depends on VIDEO_V4L2 && I2C
+	depends on !VIDEO_THS7303
+	depends on BACKPORT_VIDEO_V4L2 && I2C
 	help
 	  Support for TI THS7303/53 video amplifier
 
 	  To compile this driver as a module, choose M here: the
 	  module will be called ths7303.
 
-config VIDEO_M52790
+config BACKPORT_VIDEO_M52790
 	tristate "Mitsubishi M52790 A/V switch"
-	depends on VIDEO_V4L2 && I2C
+	depends on !VIDEO_M52790
+	depends on BACKPORT_VIDEO_V4L2 && I2C
 	---help---
 	 Support for the Mitsubishi M52790 A/V switch.
 
@@ -711,7 +784,7 @@ endmenu
 
 menu "Sensors used on soc_camera driver"
 
-if SOC_CAMERA
+if BACKPORT_SOC_CAMERA
 source "$BACKPORT_DIR/drivers/media/i2c/soc_camera/Kconfig"
 endif
 
diff --git a/backports/drivers/media/i2c/cx25840/Kconfig b/backports/drivers/media/i2c/cx25840/Kconfig
index 451133a..4842af5 100644
--- a/backports/drivers/media/i2c/cx25840/Kconfig
+++ b/backports/drivers/media/i2c/cx25840/Kconfig
@@ -1,6 +1,7 @@
-config VIDEO_CX25840
+config BACKPORT_VIDEO_CX25840
 	tristate "Conexant CX2584x audio/video decoders"
-	depends on VIDEO_V4L2 && I2C
+	depends on !VIDEO_CX25840
+	depends on BACKPORT_VIDEO_V4L2 && I2C
 	---help---
 	  Support for the Conexant CX2584x audio/video decoders.
 
diff --git a/backports/drivers/media/i2c/m5mols/Kconfig b/backports/drivers/media/i2c/m5mols/Kconfig
index dc8c250..ae12a8e 100644
--- a/backports/drivers/media/i2c/m5mols/Kconfig
+++ b/backports/drivers/media/i2c/m5mols/Kconfig
@@ -1,6 +1,7 @@
-config VIDEO_M5MOLS
+config BACKPORT_VIDEO_M5MOLS
 	tristate "Fujitsu M-5MOLS 8MP sensor support"
-	depends on I2C && VIDEO_V4L2 && VIDEO_V4L2_SUBDEV_API
-	depends on MEDIA_CAMERA_SUPPORT
+	depends on !VIDEO_M5MOLS
+	depends on I2C && VIDEO_V4L2 && BACKPORT_VIDEO_V4L2_SUBDEV_API
+	depends on BACKPORT_MEDIA_CAMERA_SUPPORT
 	---help---
 	  This driver supports Fujitsu M-5MOLS camera sensor with ISP
diff --git a/backports/drivers/media/i2c/smiapp/Kconfig b/backports/drivers/media/i2c/smiapp/Kconfig
index 3149cda..3ffa558 100644
--- a/backports/drivers/media/i2c/smiapp/Kconfig
+++ b/backports/drivers/media/i2c/smiapp/Kconfig
@@ -1,7 +1,8 @@
-config VIDEO_SMIAPP
+config BACKPORT_VIDEO_SMIAPP
 	tristate "SMIA++/SMIA sensor support"
-	depends on I2C && VIDEO_V4L2 && VIDEO_V4L2_SUBDEV_API && HAVE_CLK
-	depends on MEDIA_CAMERA_SUPPORT
-	select VIDEO_SMIAPP_PLL
+	depends on !VIDEO_SMIAPP
+	depends on I2C && VIDEO_V4L2 && BACKPORT_VIDEO_V4L2_SUBDEV_API && HAVE_CLK
+	depends on BACKPORT_MEDIA_CAMERA_SUPPORT
+	select BACKPORT_VIDEO_SMIAPP_PLL
 	---help---
 	  This is a generic driver for SMIA++/SMIA camera modules.
diff --git a/backports/drivers/media/i2c/soc_camera/Kconfig b/backports/drivers/media/i2c/soc_camera/Kconfig
index 23d352f..bdf0f9d 100644
--- a/backports/drivers/media/i2c/soc_camera/Kconfig
+++ b/backports/drivers/media/i2c/soc_camera/Kconfig
@@ -1,87 +1,101 @@
 comment "soc_camera sensor drivers"
 
-config SOC_CAMERA_IMX074
+config BACKPORT_SOC_CAMERA_IMX074
 	tristate "imx074 support"
-	depends on SOC_CAMERA && I2C
+	depends on !SOC_CAMERA_IMX074
+	depends on BACKPORT_SOC_CAMERA && I2C
 	help
 	  This driver supports IMX074 cameras from Sony
 
-config SOC_CAMERA_MT9M001
+config BACKPORT_SOC_CAMERA_MT9M001
 	tristate "mt9m001 support"
-	depends on SOC_CAMERA && I2C
+	depends on !SOC_CAMERA_MT9M001
+	depends on BACKPORT_SOC_CAMERA && I2C
 	help
 	  This driver supports MT9M001 cameras from Micron, monochrome
 	  and colour models.
 
-config SOC_CAMERA_MT9M111
+config BACKPORT_SOC_CAMERA_MT9M111
 	tristate "mt9m111, mt9m112 and mt9m131 support"
-	depends on SOC_CAMERA && I2C
+	depends on !SOC_CAMERA_MT9M111
+	depends on BACKPORT_SOC_CAMERA && I2C
 	help
 	  This driver supports MT9M111, MT9M112 and MT9M131 cameras from
 	  Micron/Aptina
 
-config SOC_CAMERA_MT9T031
+config BACKPORT_SOC_CAMERA_MT9T031
 	tristate "mt9t031 support"
-	depends on SOC_CAMERA && I2C
+	depends on !SOC_CAMERA_MT9T031
+	depends on BACKPORT_SOC_CAMERA && I2C
 	help
 	  This driver supports MT9T031 cameras from Micron.
 
-config SOC_CAMERA_MT9T112
+config BACKPORT_SOC_CAMERA_MT9T112
 	tristate "mt9t112 support"
-	depends on SOC_CAMERA && I2C
+	depends on !SOC_CAMERA_MT9T112
+	depends on BACKPORT_SOC_CAMERA && I2C
 	help
 	  This driver supports MT9T112 cameras from Aptina.
 
-config SOC_CAMERA_MT9V022
+config BACKPORT_SOC_CAMERA_MT9V022
 	tristate "mt9v022 and mt9v024 support"
-	depends on SOC_CAMERA && I2C
+	depends on !SOC_CAMERA_MT9V022
+	depends on BACKPORT_SOC_CAMERA && I2C
 	help
 	  This driver supports MT9V022 cameras from Micron
 
-config SOC_CAMERA_OV2640
+config BACKPORT_SOC_CAMERA_OV2640
 	tristate "ov2640 camera support"
-	depends on SOC_CAMERA && I2C
+	depends on !SOC_CAMERA_OV2640
+	depends on BACKPORT_SOC_CAMERA && I2C
 	help
 	  This is a ov2640 camera driver
 
-config SOC_CAMERA_OV5642
+config BACKPORT_SOC_CAMERA_OV5642
 	tristate "ov5642 camera support"
-	depends on SOC_CAMERA && I2C
+	depends on !SOC_CAMERA_OV5642
+	depends on BACKPORT_SOC_CAMERA && I2C
 	help
 	  This is a V4L2 camera driver for the OmniVision OV5642 sensor
 
-config SOC_CAMERA_OV6650
+config BACKPORT_SOC_CAMERA_OV6650
 	tristate "ov6650 sensor support"
-	depends on SOC_CAMERA && I2C
+	depends on !SOC_CAMERA_OV6650
+	depends on BACKPORT_SOC_CAMERA && I2C
 	---help---
 	  This is a V4L2 SoC camera driver for the OmniVision OV6650 sensor
 
-config SOC_CAMERA_OV772X
+config BACKPORT_SOC_CAMERA_OV772X
 	tristate "ov772x camera support"
-	depends on SOC_CAMERA && I2C
+	depends on !SOC_CAMERA_OV772X
+	depends on BACKPORT_SOC_CAMERA && I2C
 	help
 	  This is a ov772x camera driver
 
-config SOC_CAMERA_OV9640
+config BACKPORT_SOC_CAMERA_OV9640
 	tristate "ov9640 camera support"
-	depends on SOC_CAMERA && I2C
+	depends on !SOC_CAMERA_OV9640
+	depends on BACKPORT_SOC_CAMERA && I2C
 	help
 	  This is a ov9640 camera driver
 
-config SOC_CAMERA_OV9740
+config BACKPORT_SOC_CAMERA_OV9740
 	tristate "ov9740 camera support"
-	depends on SOC_CAMERA && I2C
+	depends on !SOC_CAMERA_OV9740
+	depends on BACKPORT_SOC_CAMERA && I2C
 	help
 	  This is a ov9740 camera driver
 
-config SOC_CAMERA_RJ54N1
+config BACKPORT_SOC_CAMERA_RJ54N1
 	tristate "rj54n1cb0c support"
-	depends on SOC_CAMERA && I2C
+	depends on !SOC_CAMERA_RJ54N1
+	depends on BACKPORT_SOC_CAMERA && I2C
 	help
 	  This is a rj54n1cb0c video driver
 
-config SOC_CAMERA_TW9910
+config BACKPORT_SOC_CAMERA_TW9910
 	tristate "tw9910 support"
-	depends on SOC_CAMERA && I2C
+	depends on !SOC_CAMERA_TW9910
+	depends on BACKPORT_SOC_CAMERA && I2C
 	help
 	  This is a tw9910 video driver
diff --git a/backports/drivers/media/mmc/siano/Kconfig b/backports/drivers/media/mmc/siano/Kconfig
index 7693487..8538f0c 100644
--- a/backports/drivers/media/mmc/siano/Kconfig
+++ b/backports/drivers/media/mmc/siano/Kconfig
@@ -2,12 +2,13 @@
 # Siano Mobile Silicon Digital TV device configuration
 #
 
-config SMS_SDIO_DRV
+config BACKPORT_SMS_SDIO_DRV
 	tristate "Siano SMS1xxx based MDTV via SDIO interface"
-	depends on DVB_CORE && HAS_DMA
+	depends on !SMS_SDIO_DRV
+	depends on BACKPORT_DVB_CORE && HAS_DMA
 	depends on MMC
-	depends on !RC_CORE || RC_CORE
-	select MEDIA_COMMON_OPTIONS
-	select SMS_SIANO_MDTV
+	depends on !RC_CORE || BACKPORT_RC_CORE
+	select BACKPORT_MEDIA_COMMON_OPTIONS
+	select BACKPORT_SMS_SIANO_MDTV
 	---help---
 	  Choose if you would like to have Siano's support for SDIO interface
diff --git a/backports/drivers/media/pci/Kconfig b/backports/drivers/media/pci/Kconfig
index 51509a2d..71748f1 100644
--- a/backports/drivers/media/pci/Kconfig
+++ b/backports/drivers/media/pci/Kconfig
@@ -1,20 +1,21 @@
-if PCI && MEDIA_SUPPORT
+if PCI && BACKPORT_MEDIA_SUPPORT
 
-menuconfig MEDIA_PCI_SUPPORT
+menuconfig BACKPORT_MEDIA_PCI_SUPPORT
 	bool "Media PCI Adapters"
+	depends on !MEDIA_PCI_SUPPORT
 	help
 	  Enable media drivers for PCI/PCIe bus.
 	  If you have such devices, say Y.
 
-if MEDIA_PCI_SUPPORT
+if BACKPORT_MEDIA_PCI_SUPPORT
 
-if MEDIA_CAMERA_SUPPORT
+if BACKPORT_MEDIA_CAMERA_SUPPORT
 	comment "Media capture support"
 source "$BACKPORT_DIR/drivers/media/pci/meye/Kconfig"
 source "$BACKPORT_DIR/drivers/media/pci/sta2x11/Kconfig"
 endif
 
-if MEDIA_ANALOG_TV_SUPPORT
+if BACKPORT_MEDIA_ANALOG_TV_SUPPORT
 	comment "Media capture/analog TV support"
 source "$BACKPORT_DIR/drivers/media/pci/ivtv/Kconfig"
 source "$BACKPORT_DIR/drivers/media/pci/zoran/Kconfig"
@@ -23,7 +24,7 @@ source "$BACKPORT_DIR/drivers/media/pci/solo6x10/Kconfig"
 source "$BACKPORT_DIR/drivers/media/pci/tw68/Kconfig"
 endif
 
-if MEDIA_ANALOG_TV_SUPPORT || MEDIA_DIGITAL_TV_SUPPORT
+if BACKPORT_MEDIA_ANALOG_TV_SUPPORT || MEDIA_DIGITAL_TV_SUPPORT
 	comment "Media capture/analog/hybrid TV support"
 source "$BACKPORT_DIR/drivers/media/pci/cx18/Kconfig"
 source "$BACKPORT_DIR/drivers/media/pci/cx23885/Kconfig"
@@ -35,7 +36,7 @@ source "$BACKPORT_DIR/drivers/media/pci/saa7164/Kconfig"
 
 endif
 
-if MEDIA_DIGITAL_TV_SUPPORT
+if BACKPORT_MEDIA_DIGITAL_TV_SUPPORT
 	comment "Media digital TV PCI Adapters"
 source "$BACKPORT_DIR/drivers/media/pci/ttpci/Kconfig"
 source "$BACKPORT_DIR/drivers/media/pci/b2c2/Kconfig"
diff --git a/backports/drivers/media/pci/b2c2/Kconfig b/backports/drivers/media/pci/b2c2/Kconfig
index 58761a2..911ee79 100644
--- a/backports/drivers/media/pci/b2c2/Kconfig
+++ b/backports/drivers/media/pci/b2c2/Kconfig
@@ -1,15 +1,17 @@
-config DVB_B2C2_FLEXCOP_PCI
+config BACKPORT_DVB_B2C2_FLEXCOP_PCI
 	tristate "Technisat/B2C2 Air/Sky/Cable2PC PCI"
-	depends on DVB_CORE && I2C
+	depends on !DVB_B2C2_FLEXCOP_PCI
+	depends on BACKPORT_DVB_CORE && I2C
 	help
 	  Support for the Air/Sky/CableStar2 PCI card (DVB/ATSC) by Technisat/B2C2.
 
 	  Say Y if you own such a device and want to use it.
 
-config DVB_B2C2_FLEXCOP_PCI_DEBUG
+config BACKPORT_DVB_B2C2_FLEXCOP_PCI_DEBUG
 	bool "Enable debug for the B2C2 FlexCop drivers"
-	depends on DVB_B2C2_FLEXCOP_PCI
-	select DVB_B2C2_FLEXCOP_DEBUG
+	depends on !DVB_B2C2_FLEXCOP_PCI_DEBUG
+	depends on BACKPORT_DVB_B2C2_FLEXCOP_PCI
+	select BACKPORT_DVB_B2C2_FLEXCOP_DEBUG
 	help
 	Say Y if you want to enable the module option to control debug messages
 	of all B2C2 FlexCop drivers.
diff --git a/backports/drivers/media/pci/bt8xx/Kconfig b/backports/drivers/media/pci/bt8xx/Kconfig
index 4a93f6d..31508e8 100644
--- a/backports/drivers/media/pci/bt8xx/Kconfig
+++ b/backports/drivers/media/pci/bt8xx/Kconfig
@@ -1,37 +1,39 @@
-config VIDEO_BT848
+config BACKPORT_VIDEO_BT848
 	tristate "BT848 Video For Linux"
-	depends on VIDEO_DEV && PCI && I2C && VIDEO_V4L2
+	depends on !VIDEO_BT848
+	depends on BACKPORT_VIDEO_DEV && PCI && I2C && VIDEO_V4L2
 	select I2C_ALGOBIT
-	select VIDEOBUF_DMA_SG
-	depends on RC_CORE
-	depends on MEDIA_RADIO_SUPPORT
-	select VIDEO_TUNER
-	select VIDEO_TVEEPROM
-	select VIDEO_MSP3400 if MEDIA_SUBDRV_AUTOSELECT
-	select VIDEO_TVAUDIO if MEDIA_SUBDRV_AUTOSELECT
-	select VIDEO_TDA7432 if MEDIA_SUBDRV_AUTOSELECT
-	select VIDEO_SAA6588 if MEDIA_SUBDRV_AUTOSELECT
-	select RADIO_ADAPTERS
-	select RADIO_TEA575X
+	select BACKPORT_VIDEOBUF_DMA_SG
+	depends on BACKPORT_RC_CORE
+	depends on BACKPORT_MEDIA_RADIO_SUPPORT
+	select BACKPORT_VIDEO_TUNER
+	select BACKPORT_VIDEO_TVEEPROM
+	select VIDEO_MSP3400 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select VIDEO_TVAUDIO if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select VIDEO_TDA7432 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select VIDEO_SAA6588 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select BACKPORT_RADIO_ADAPTERS
+	select BACKPORT_RADIO_TEA575X
 	---help---
-	  Support for BT848 based frame grabber/overlay boards. This includes
+	  Support for BACKPORT_BT848 based frame grabber/overlay boards. This includes
 	  the Miro, Hauppauge and STB boards. Please read the material in
 	  <file:Documentation/video4linux/bttv/> for more information.
 
 	  To compile this driver as a module, choose M here: the
 	  module will be called bttv.
 
-config DVB_BT8XX
+config BACKPORT_DVB_BT8XX
 	tristate "DVB/ATSC Support for bt878 based TV cards"
+	depends on !DVB_BT8XX
 	depends on DVB_CORE && PCI && I2C && VIDEO_BT848
-	select DVB_MT352 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_SP887X if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_NXT6000 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_CX24110 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_OR51211 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_LGDT330X if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_ZL10353 if MEDIA_SUBDRV_AUTOSELECT
-	select MEDIA_TUNER_SIMPLE if MEDIA_SUBDRV_AUTOSELECT
+	select DVB_MT352 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_SP887X if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_NXT6000 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_CX24110 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_OR51211 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_LGDT330X if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_ZL10353 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select MEDIA_TUNER_SIMPLE if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
 	help
 	  Support for PCI cards based on the Bt8xx PCI bridge. Examples are
 	  the Nebula cards, the Pinnacle PCTV cards, the Twinhan DST cards,
diff --git a/backports/drivers/media/pci/cx18/Kconfig b/backports/drivers/media/pci/cx18/Kconfig
index c675b83..44dad9d 100644
--- a/backports/drivers/media/pci/cx18/Kconfig
+++ b/backports/drivers/media/pci/cx18/Kconfig
@@ -1,18 +1,19 @@
-config VIDEO_CX18
+config BACKPORT_VIDEO_CX18
 	tristate "Conexant cx23418 MPEG encoder support"
-	depends on VIDEO_V4L2 && DVB_CORE && PCI && I2C
+	depends on !VIDEO_CX18
+	depends on VIDEO_V4L2 && BACKPORT_DVB_CORE && PCI && I2C
 	select I2C_ALGOBIT
-	select VIDEOBUF_VMALLOC
-	depends on RC_CORE
-	select VIDEO_TUNER
-	select VIDEO_TVEEPROM
-	select VIDEO_CX2341X
-	select VIDEO_CS5345
-	select DVB_S5H1409 if MEDIA_SUBDRV_AUTOSELECT
-	select MEDIA_TUNER_MXL5005S if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_S5H1411 if MEDIA_SUBDRV_AUTOSELECT
-	select MEDIA_TUNER_TDA18271 if MEDIA_SUBDRV_AUTOSELECT
-	select MEDIA_TUNER_TDA8290 if MEDIA_SUBDRV_AUTOSELECT
+	select BACKPORT_VIDEOBUF_VMALLOC
+	depends on BACKPORT_RC_CORE
+	select BACKPORT_VIDEO_TUNER
+	select BACKPORT_VIDEO_TVEEPROM
+	select BACKPORT_VIDEO_CX2341X
+	select BACKPORT_VIDEO_CS5345
+	select DVB_S5H1409 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select MEDIA_TUNER_MXL5005S if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_S5H1411 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select MEDIA_TUNER_TDA18271 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select MEDIA_TUNER_TDA8290 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
 	---help---
 	  This is a video4linux driver for Conexant cx23418 based
 	  PCI combo video recorder devices.
@@ -23,9 +24,10 @@ config VIDEO_CX18
 	  To compile this driver as a module, choose M here: the
 	  module will be called cx18.
 
-config VIDEO_CX18_ALSA
+config BACKPORT_VIDEO_CX18_ALSA
 	tristate "Conexant 23418 DMA audio support"
-	depends on VIDEO_CX18 && SND
+	depends on !VIDEO_CX18_ALSA
+	depends on BACKPORT_VIDEO_CX18 && SND
 	select SND_PCM
 	---help---
 	  This is a video4linux driver for direct (DMA) audio on
diff --git a/backports/drivers/media/pci/cx23885/Kconfig b/backports/drivers/media/pci/cx23885/Kconfig
index 2e1b88c..c80ba38 100644
--- a/backports/drivers/media/pci/cx23885/Kconfig
+++ b/backports/drivers/media/pci/cx23885/Kconfig
@@ -1,47 +1,48 @@
-config VIDEO_CX23885
+config BACKPORT_VIDEO_CX23885
 	tristate "Conexant cx23885 (2388x successor) support"
-	depends on DVB_CORE && VIDEO_DEV && PCI && I2C && INPUT && SND
+	depends on !VIDEO_CX23885
+	depends on DVB_CORE && BACKPORT_VIDEO_DEV && PCI && I2C && INPUT && SND
 	select SND_PCM
 	select I2C_ALGOBIT
-	select VIDEO_TUNER
-	select VIDEO_TVEEPROM
-	depends on RC_CORE
-	select VIDEOBUF2_DVB
-	select VIDEOBUF2_DMA_SG
-	select VIDEO_CX25840
-	select VIDEO_CX2341X
-	select DVB_DIB7000P if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_DRXK if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_S5H1409 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_S5H1411 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_LGDT330X if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_ZL10353 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_TDA10048 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_LNBP21 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_STV090x if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_STB6100 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_STV6110 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_CX24116 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_CX24117 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_STV0900 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_DS3000 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_TS2020 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_STV0367 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_TDA10071 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_A8293 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_MB86A20S if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_SI2165 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_SI2168 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_M88DS3103 if MEDIA_SUBDRV_AUTOSELECT
-	select MEDIA_TUNER_MT2063 if MEDIA_SUBDRV_AUTOSELECT
-	select MEDIA_TUNER_MT2131 if MEDIA_SUBDRV_AUTOSELECT
-	select MEDIA_TUNER_XC2028 if MEDIA_SUBDRV_AUTOSELECT
-	select MEDIA_TUNER_TDA8290 if MEDIA_SUBDRV_AUTOSELECT
-	select MEDIA_TUNER_TDA18271 if MEDIA_SUBDRV_AUTOSELECT
-	select MEDIA_TUNER_XC5000 if MEDIA_SUBDRV_AUTOSELECT
-	select MEDIA_TUNER_SI2157 if MEDIA_SUBDRV_AUTOSELECT
-	select MEDIA_TUNER_M88RS6000T if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_TUNER_DIB0070 if MEDIA_SUBDRV_AUTOSELECT
+	select BACKPORT_VIDEO_TUNER
+	select BACKPORT_VIDEO_TVEEPROM
+	depends on BACKPORT_RC_CORE
+	select BACKPORT_VIDEOBUF2_DVB
+	select BACKPORT_VIDEOBUF2_DMA_SG
+	select BACKPORT_VIDEO_CX25840
+	select BACKPORT_VIDEO_CX2341X
+	select DVB_DIB7000P if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_DRXK if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_S5H1409 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_S5H1411 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_LGDT330X if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_ZL10353 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_TDA10048 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_LNBP21 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_STV090x if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_STB6100 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_STV6110 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_CX24116 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_CX24117 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_STV0900 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_DS3000 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_TS2020 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_STV0367 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_TDA10071 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_A8293 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_MB86A20S if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_SI2165 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_SI2168 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_M88DS3103 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select MEDIA_TUNER_MT2063 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select MEDIA_TUNER_MT2131 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select MEDIA_TUNER_XC2028 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select MEDIA_TUNER_TDA8290 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select MEDIA_TUNER_TDA18271 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select MEDIA_TUNER_XC5000 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select MEDIA_TUNER_SI2157 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select MEDIA_TUNER_M88RS6000T if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_TUNER_DIB0070 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
 	---help---
 	  This is a video4linux driver for Conexant 23885 based
 	  TV cards.
@@ -49,9 +50,10 @@ config VIDEO_CX23885
 	  To compile this driver as a module, choose M here: the
 	  module will be called cx23885
 
-config MEDIA_ALTERA_CI
+config BACKPORT_MEDIA_ALTERA_CI
 	tristate "Altera FPGA based CI module"
-	depends on VIDEO_CX23885 && DVB_CORE
+	depends on !MEDIA_ALTERA_CI
+	depends on VIDEO_CX23885 && BACKPORT_DVB_CORE
 	select ALTERA_STAPL
 	---help---
 	  An Altera FPGA CI module for NetUP Dual DVB-T/C RF CI card.
diff --git a/backports/drivers/media/pci/cx25821/Kconfig b/backports/drivers/media/pci/cx25821/Kconfig
index aa68ada..46a7dcf 100644
--- a/backports/drivers/media/pci/cx25821/Kconfig
+++ b/backports/drivers/media/pci/cx25821/Kconfig
@@ -1,8 +1,9 @@
-config VIDEO_CX25821
+config BACKPORT_VIDEO_CX25821
 	tristate "Conexant cx25821 support"
-	depends on VIDEO_DEV && PCI && I2C
+	depends on !VIDEO_CX25821
+	depends on BACKPORT_VIDEO_DEV && PCI && I2C
 	select I2C_ALGOBIT
-	select VIDEOBUF2_DMA_SG
+	select BACKPORT_VIDEOBUF2_DMA_SG
 	---help---
 	  This is a video4linux driver for Conexant 25821 based
 	  TV cards.
@@ -10,9 +11,10 @@ config VIDEO_CX25821
 	  To compile this driver as a module, choose M here: the
 	  module will be called cx25821
 
-config VIDEO_CX25821_ALSA
+config BACKPORT_VIDEO_CX25821_ALSA
 	tristate "Conexant 25821 DMA audio support"
-	depends on VIDEO_CX25821 && SND
+	depends on !VIDEO_CX25821_ALSA
+	depends on BACKPORT_VIDEO_CX25821 && SND
 	select SND_PCM
 	---help---
 	  This is a video4linux driver for direct (DMA) audio on
diff --git a/backports/drivers/media/pci/cx88/Kconfig b/backports/drivers/media/pci/cx88/Kconfig
index 14b813d6..b75fb4d 100644
--- a/backports/drivers/media/pci/cx88/Kconfig
+++ b/backports/drivers/media/pci/cx88/Kconfig
@@ -1,11 +1,12 @@
-config VIDEO_CX88
+config BACKPORT_VIDEO_CX88
 	tristate "Conexant 2388x (bt878 successor) support"
-	depends on VIDEO_DEV && PCI && I2C && RC_CORE
+	depends on !VIDEO_CX88
+	depends on BACKPORT_VIDEO_DEV && PCI && I2C && RC_CORE
 	select I2C_ALGOBIT
-	select VIDEOBUF2_DMA_SG
-	select VIDEO_TUNER
-	select VIDEO_TVEEPROM
-	select VIDEO_WM8775 if MEDIA_SUBDRV_AUTOSELECT
+	select BACKPORT_VIDEOBUF2_DMA_SG
+	select BACKPORT_VIDEO_TUNER
+	select BACKPORT_VIDEO_TVEEPROM
+	select VIDEO_WM8775 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
 	---help---
 	  This is a video4linux driver for Conexant 2388x based
 	  TV cards.
@@ -13,9 +14,10 @@ config VIDEO_CX88
 	  To compile this driver as a module, choose M here: the
 	  module will be called cx8800
 
-config VIDEO_CX88_ALSA
+config BACKPORT_VIDEO_CX88_ALSA
 	tristate "Conexant 2388x DMA audio support"
-	depends on VIDEO_CX88 && SND
+	depends on !VIDEO_CX88_ALSA
+	depends on BACKPORT_VIDEO_CX88 && SND
 	select SND_PCM
 	---help---
 	  This is a video4linux driver for direct (DMA) audio on
@@ -29,10 +31,11 @@ config VIDEO_CX88_ALSA
 	  To compile this driver as a module, choose M here: the
 	  module will be called cx88-alsa.
 
-config VIDEO_CX88_BLACKBIRD
+config BACKPORT_VIDEO_CX88_BLACKBIRD
 	tristate "Blackbird MPEG encoder support (cx2388x + cx23416)"
-	depends on VIDEO_CX88
-	select VIDEO_CX2341X
+	depends on !VIDEO_CX88_BLACKBIRD
+	depends on BACKPORT_VIDEO_CX88
+	select BACKPORT_VIDEO_CX2341X
 	---help---
 	  This adds support for MPEG encoder cards based on the
 	  Blackbird reference design, using the Conexant 2388x
@@ -41,29 +44,30 @@ config VIDEO_CX88_BLACKBIRD
 	  To compile this driver as a module, choose M here: the
 	  module will be called cx88-blackbird.
 
-config VIDEO_CX88_DVB
+config BACKPORT_VIDEO_CX88_DVB
 	tristate "DVB/ATSC Support for cx2388x based TV cards"
-	depends on VIDEO_CX88 && DVB_CORE
-	select VIDEOBUF2_DVB
-	select DVB_PLL if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_MT352 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_ZL10353 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_OR51132 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_CX22702 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_LGDT330X if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_NXT200X if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_CX24123 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_ISL6421 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_S5H1411 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_CX24116 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_STV0299 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_STV0288 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_STB6000 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_STV0900 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_STB6100 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_DS3000 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_TS2020 if MEDIA_SUBDRV_AUTOSELECT
-	select MEDIA_TUNER_SIMPLE if MEDIA_SUBDRV_AUTOSELECT
+	depends on !VIDEO_CX88_DVB
+	depends on VIDEO_CX88 && BACKPORT_DVB_CORE
+	select BACKPORT_VIDEOBUF2_DVB
+	select DVB_PLL if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_MT352 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_ZL10353 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_OR51132 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_CX22702 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_LGDT330X if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_NXT200X if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_CX24123 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_ISL6421 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_S5H1411 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_CX24116 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_STV0299 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_STV0288 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_STB6000 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_STV0900 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_STB6100 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_DS3000 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_TS2020 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select MEDIA_TUNER_SIMPLE if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
 	---help---
 	  This adds support for DVB/ATSC cards based on the
 	  Conexant 2388x chip.
@@ -71,22 +75,25 @@ config VIDEO_CX88_DVB
 	  To compile this driver as a module, choose M here: the
 	  module will be called cx88-dvb.
 
-config VIDEO_CX88_ENABLE_VP3054
+config BACKPORT_VIDEO_CX88_ENABLE_VP3054
 	bool "VP-3054 Secondary I2C Bus Support"
+	depends on !VIDEO_CX88_ENABLE_VP3054
 	default y
-	depends on VIDEO_CX88_DVB && DVB_MT352
+	depends on BACKPORT_VIDEO_CX88_DVB && DVB_MT352
 	---help---
 	  This adds DVB-T support for cards based on the
 	  Conexant 2388x chip and the MT352 demodulator,
 	  which also require support for the VP-3054
 	  Secondary I2C bus, such at DNTV Live! DVB-T Pro.
 
-config VIDEO_CX88_VP3054
+config BACKPORT_VIDEO_CX88_VP3054
 	tristate
-	depends on VIDEO_CX88_DVB && VIDEO_CX88_ENABLE_VP3054
+	depends on !VIDEO_CX88_VP3054
+	depends on BACKPORT_VIDEO_CX88_DVB && BACKPORT_VIDEO_CX88_ENABLE_VP3054
 	default y
 
-config VIDEO_CX88_MPEG
+config BACKPORT_VIDEO_CX88_MPEG
 	tristate
-	depends on VIDEO_CX88_DVB || VIDEO_CX88_BLACKBIRD
+	depends on !VIDEO_CX88_MPEG
+	depends on BACKPORT_VIDEO_CX88_DVB || BACKPORT_VIDEO_CX88_BLACKBIRD
 	default y
diff --git a/backports/drivers/media/pci/ddbridge/Kconfig b/backports/drivers/media/pci/ddbridge/Kconfig
index 44e5dc1..17a7750 100644
--- a/backports/drivers/media/pci/ddbridge/Kconfig
+++ b/backports/drivers/media/pci/ddbridge/Kconfig
@@ -1,11 +1,12 @@
-config DVB_DDBRIDGE
+config BACKPORT_DVB_DDBRIDGE
 	tristate "Digital Devices bridge support"
-	depends on DVB_CORE && PCI && I2C
-	select DVB_LNBP21 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_STV6110x if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_STV090x if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_DRXK if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_TDA18271C2DD if MEDIA_SUBDRV_AUTOSELECT
+	depends on !DVB_DDBRIDGE
+	depends on BACKPORT_DVB_CORE && PCI && I2C
+	select DVB_LNBP21 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_STV6110x if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_STV090x if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_DRXK if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_TDA18271C2DD if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
 	---help---
 	  Support for cards with the Digital Devices PCI express bridge:
 	  - Octopus PCIe Bridge
diff --git a/backports/drivers/media/pci/dm1105/Kconfig b/backports/drivers/media/pci/dm1105/Kconfig
index 173daf0..3c9b7a4 100644
--- a/backports/drivers/media/pci/dm1105/Kconfig
+++ b/backports/drivers/media/pci/dm1105/Kconfig
@@ -1,15 +1,16 @@
-config DVB_DM1105
+config BACKPORT_DVB_DM1105
 	tristate "SDMC DM1105 based PCI cards"
-	depends on DVB_CORE && PCI && I2C
-	select DVB_PLL if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_STV0299 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_STV0288 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_STB6000 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_CX24116 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_SI21XX if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_DS3000 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_TS2020 if MEDIA_SUBDRV_AUTOSELECT
-	depends on RC_CORE
+	depends on !DVB_DM1105
+	depends on BACKPORT_DVB_CORE && PCI && I2C
+	select DVB_PLL if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_STV0299 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_STV0288 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_STB6000 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_CX24116 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_SI21XX if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_DS3000 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_TS2020 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	depends on BACKPORT_RC_CORE
 	help
 	  Support for cards based on the SDMC DM1105 PCI chip like
 	  DvbWorld 2002
diff --git a/backports/drivers/media/pci/ivtv/Kconfig b/backports/drivers/media/pci/ivtv/Kconfig
index dd6ee57e..1193083 100644
--- a/backports/drivers/media/pci/ivtv/Kconfig
+++ b/backports/drivers/media/pci/ivtv/Kconfig
@@ -1,23 +1,24 @@
-config VIDEO_IVTV
+config BACKPORT_VIDEO_IVTV
 	tristate "Conexant cx23416/cx23415 MPEG encoder/decoder support"
-	depends on VIDEO_V4L2 && PCI && I2C
+	depends on !VIDEO_IVTV
+	depends on BACKPORT_VIDEO_V4L2 && PCI && I2C
 	select I2C_ALGOBIT
-	depends on RC_CORE
-	select VIDEO_TUNER
-	select VIDEO_TVEEPROM
-	select VIDEO_CX2341X
-	select VIDEO_CX25840
-	select VIDEO_MSP3400
-	select VIDEO_SAA711X
-	select VIDEO_SAA717X
-	select VIDEO_SAA7127
-	select VIDEO_CS53L32A
-	select VIDEO_M52790
-	select VIDEO_WM8775
-	select VIDEO_WM8739
-	select VIDEO_VP27SMPX
-	select VIDEO_UPD64031A
-	select VIDEO_UPD64083
+	depends on BACKPORT_RC_CORE
+	select BACKPORT_VIDEO_TUNER
+	select BACKPORT_VIDEO_TVEEPROM
+	select BACKPORT_VIDEO_CX2341X
+	select BACKPORT_VIDEO_CX25840
+	select BACKPORT_VIDEO_MSP3400
+	select BACKPORT_VIDEO_SAA711X
+	select BACKPORT_VIDEO_SAA717X
+	select BACKPORT_VIDEO_SAA7127
+	select BACKPORT_VIDEO_CS53L32A
+	select BACKPORT_VIDEO_M52790
+	select BACKPORT_VIDEO_WM8775
+	select BACKPORT_VIDEO_WM8739
+	select BACKPORT_VIDEO_VP27SMPX
+	select BACKPORT_VIDEO_UPD64031A
+	select BACKPORT_VIDEO_UPD64083
 	---help---
 	  This is a video4linux driver for Conexant cx23416 or cx23415 based
 	  PCI personal video recorder devices.
@@ -28,9 +29,10 @@ config VIDEO_IVTV
 	  To compile this driver as a module, choose M here: the
 	  module will be called ivtv.
 
-config VIDEO_IVTV_ALSA
+config BACKPORT_VIDEO_IVTV_ALSA
 	tristate "Conexant cx23415/cx23416 ALSA interface for PCM audio capture"
-	depends on VIDEO_IVTV && SND
+	depends on !VIDEO_IVTV_ALSA
+	depends on BACKPORT_VIDEO_IVTV && SND
 	select SND_PCM
 	---help---
 	  This driver provides an ALSA interface as another method for user
@@ -44,9 +46,10 @@ config VIDEO_IVTV_ALSA
 	  To compile this driver as a module, choose M here: the
 	  module will be called ivtv-alsa.
 
-config VIDEO_FB_IVTV
+config BACKPORT_VIDEO_FB_IVTV
 	tristate "Conexant cx23415 framebuffer support"
-	depends on VIDEO_IVTV && FB
+	depends on !VIDEO_FB_IVTV
+	depends on BACKPORT_VIDEO_IVTV && FB
 	select FB_CFB_FILLRECT
 	select FB_CFB_COPYAREA
 	select FB_CFB_IMAGEBLIT
diff --git a/backports/drivers/media/pci/mantis/Kconfig b/backports/drivers/media/pci/mantis/Kconfig
index d3cc216..8ee7b84 100644
--- a/backports/drivers/media/pci/mantis/Kconfig
+++ b/backports/drivers/media/pci/mantis/Kconfig
@@ -1,36 +1,39 @@
-config MANTIS_CORE
+config BACKPORT_MANTIS_CORE
 	tristate "Mantis/Hopper PCI bridge based devices"
-	depends on PCI && I2C && INPUT && RC_CORE
+	depends on !MANTIS_CORE
+	depends on PCI && I2C && INPUT && BACKPORT_RC_CORE
 
 	help
 	  Support for PCI cards based on the Mantis and Hopper PCi bridge.
 
 	  Say Y if you own such a device and want to use it.
 
-config DVB_MANTIS
+config BACKPORT_DVB_MANTIS
 	tristate "MANTIS based cards"
-	depends on MANTIS_CORE && DVB_CORE && PCI && I2C
-	select DVB_MB86A16 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_ZL10353 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_STV0299 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_LNBP21 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_STB0899 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_STB6100 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_TDA665x if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_TDA10021 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_TDA10023 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_PLL
+	depends on !DVB_MANTIS
+	depends on MANTIS_CORE && BACKPORT_DVB_CORE && PCI && I2C
+	select DVB_MB86A16 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_ZL10353 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_STV0299 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_LNBP21 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_STB0899 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_STB6100 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_TDA665x if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_TDA10021 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_TDA10023 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select BACKPORT_DVB_PLL
 	help
 	  Support for PCI cards based on the Mantis PCI bridge.
 	  Say Y when you have a Mantis based DVB card and want to use it.
 
 	  If unsure say N.
 
-config DVB_HOPPER
+config BACKPORT_DVB_HOPPER
 	tristate "HOPPER based cards"
-	depends on MANTIS_CORE && DVB_CORE && PCI && I2C
-	select DVB_ZL10353 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_PLL
+	depends on !DVB_HOPPER
+	depends on MANTIS_CORE && BACKPORT_DVB_CORE && PCI && I2C
+	select DVB_ZL10353 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select BACKPORT_DVB_PLL
 	help
 	  Support for PCI cards based on the Hopper  PCI bridge.
 	  Say Y when you have a Hopper based DVB card and want to use it.
diff --git a/backports/drivers/media/pci/meye/Kconfig b/backports/drivers/media/pci/meye/Kconfig
index b4bf848..6fb4ea8 100644
--- a/backports/drivers/media/pci/meye/Kconfig
+++ b/backports/drivers/media/pci/meye/Kconfig
@@ -1,6 +1,7 @@
-config VIDEO_MEYE
+config BACKPORT_VIDEO_MEYE
 	tristate "Sony Vaio Picturebook Motion Eye Video For Linux"
-	depends on PCI && SONY_LAPTOP && VIDEO_V4L2
+	depends on !VIDEO_MEYE
+	depends on PCI && SONY_LAPTOP && BACKPORT_VIDEO_V4L2
 	---help---
 	  This is the video4linux driver for the Motion Eye camera found
 	  in the Vaio Picturebook laptops. Please read the material in
diff --git a/backports/drivers/media/pci/ngene/Kconfig b/backports/drivers/media/pci/ngene/Kconfig
index fdb1bb0..a5152bf 100644
--- a/backports/drivers/media/pci/ngene/Kconfig
+++ b/backports/drivers/media/pci/ngene/Kconfig
@@ -1,12 +1,13 @@
-config DVB_NGENE
+config BACKPORT_DVB_NGENE
 	tristate "Micronas nGene support"
-	depends on DVB_CORE && PCI && I2C
-	select DVB_LNBP21 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_STV6110x if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_STV090x if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_LGDT330X if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_DRXK if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_TDA18271C2DD if MEDIA_SUBDRV_AUTOSELECT
-	select MEDIA_TUNER_MT2131 if MEDIA_SUBDRV_AUTOSELECT
+	depends on !DVB_NGENE
+	depends on BACKPORT_DVB_CORE && PCI && I2C
+	select DVB_LNBP21 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_STV6110x if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_STV090x if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_LGDT330X if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_DRXK if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_TDA18271C2DD if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select MEDIA_TUNER_MT2131 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
 	---help---
 	  Support for Micronas PCI express cards with nGene bridge.
diff --git a/backports/drivers/media/pci/pluto2/Kconfig b/backports/drivers/media/pci/pluto2/Kconfig
index c17dbf2..3862ea8 100644
--- a/backports/drivers/media/pci/pluto2/Kconfig
+++ b/backports/drivers/media/pci/pluto2/Kconfig
@@ -1,8 +1,9 @@
-config DVB_PLUTO2
+config BACKPORT_DVB_PLUTO2
 	tristate "Pluto2 cards"
-	depends on DVB_CORE && PCI && I2C
+	depends on !DVB_PLUTO2
+	depends on BACKPORT_DVB_CORE && PCI && I2C
 	select I2C_ALGOBIT
-	select DVB_TDA1004X
+	select BACKPORT_DVB_TDA1004X
 	help
 	  Support for PCI cards based on the Pluto2 FPGA like the Satelco
 	  Easywatch Mobile Terrestrial DVB-T Receiver.
diff --git a/backports/drivers/media/pci/pt1/Kconfig b/backports/drivers/media/pci/pt1/Kconfig
index 1478489..fa7568c1 100644
--- a/backports/drivers/media/pci/pt1/Kconfig
+++ b/backports/drivers/media/pci/pt1/Kconfig
@@ -1,6 +1,7 @@
-config DVB_PT1
+config BACKPORT_DVB_PT1
 	tristate "PT1 cards"
-	depends on DVB_CORE && PCI && I2C
+	depends on !DVB_PT1
+	depends on BACKPORT_DVB_CORE && PCI && I2C
 	help
 	  Support for Earthsoft PT1 PCI cards.
 
diff --git a/backports/drivers/media/pci/pt3/Kconfig b/backports/drivers/media/pci/pt3/Kconfig
index 16c208a..b373130 100644
--- a/backports/drivers/media/pci/pt3/Kconfig
+++ b/backports/drivers/media/pci/pt3/Kconfig
@@ -1,9 +1,10 @@
-config DVB_PT3
+config BACKPORT_DVB_PT3
 	tristate "Earthsoft PT3 cards"
-	depends on DVB_CORE && PCI && I2C
-	select DVB_TC90522 if MEDIA_SUBDRV_AUTOSELECT
-	select MEDIA_TUNER_QM1D1C0042 if MEDIA_SUBDRV_AUTOSELECT
-	select MEDIA_TUNER_MXL301RF if MEDIA_SUBDRV_AUTOSELECT
+	depends on !DVB_PT3
+	depends on BACKPORT_DVB_CORE && PCI && I2C
+	select DVB_TC90522 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select MEDIA_TUNER_QM1D1C0042 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select MEDIA_TUNER_MXL301RF if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
 	help
 	  Support for Earthsoft PT3 PCIe cards.
 
diff --git a/backports/drivers/media/pci/saa7134/Kconfig b/backports/drivers/media/pci/saa7134/Kconfig
index b44e0d7..94204f2 100644
--- a/backports/drivers/media/pci/saa7134/Kconfig
+++ b/backports/drivers/media/pci/saa7134/Kconfig
@@ -1,12 +1,13 @@
-config VIDEO_SAA7134
+config BACKPORT_VIDEO_SAA7134
 	tristate "Philips SAA7134 support"
-	depends on VIDEO_DEV && PCI && I2C
-	select VIDEOBUF2_DMA_SG
-	select VIDEO_TUNER
-	select VIDEO_TVEEPROM
+	depends on !VIDEO_SAA7134
+	depends on BACKPORT_VIDEO_DEV && PCI && I2C
+	select BACKPORT_VIDEOBUF2_DMA_SG
+	select BACKPORT_VIDEO_TUNER
+	select BACKPORT_VIDEO_TVEEPROM
 	select CRC32
-	select VIDEO_SAA6588 if MEDIA_SUBDRV_AUTOSELECT
-	select VIDEO_SAA6752HS if MEDIA_SUBDRV_AUTOSELECT
+	select VIDEO_SAA6588 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select VIDEO_SAA6752HS if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
 	---help---
 	  This is a video4linux driver for Philips SAA713x based
 	  TV cards.
@@ -14,9 +15,10 @@ config VIDEO_SAA7134
 	  To compile this driver as a module, choose M here: the
 	  module will be called saa7134.
 
-config VIDEO_SAA7134_ALSA
+config BACKPORT_VIDEO_SAA7134_ALSA
 	tristate "Philips SAA7134 DMA audio support"
-	depends on VIDEO_SAA7134 && SND
+	depends on !VIDEO_SAA7134_ALSA
+	depends on BACKPORT_VIDEO_SAA7134 && SND
 	select SND_PCM
 	---help---
 	  This is a video4linux driver for direct (DMA) audio in
@@ -25,38 +27,40 @@ config VIDEO_SAA7134_ALSA
 	  To compile this driver as a module, choose M here: the
 	  module will be called saa7134-alsa.
 
-config VIDEO_SAA7134_RC
+config BACKPORT_VIDEO_SAA7134_RC
 	bool "Philips SAA7134 Remote Controller support"
-	depends on RC_CORE
-	depends on VIDEO_SAA7134
+	depends on !VIDEO_SAA7134_RC
+	depends on BACKPORT_RC_CORE
+	depends on BACKPORT_VIDEO_SAA7134
 	depends on !(RC_CORE=m && VIDEO_SAA7134=y)
 	default y
 	---help---
 	  Enables Remote Controller support on saa7134 driver.
 
-config VIDEO_SAA7134_DVB
+config BACKPORT_VIDEO_SAA7134_DVB
 	tristate "DVB/ATSC Support for saa7134 based TV cards"
-	depends on VIDEO_SAA7134 && DVB_CORE
-	select VIDEOBUF2_DVB
-	select DVB_PLL if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_MT352 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_TDA1004X if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_NXT200X if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_TDA10086 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_TDA826X if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_ISL6421 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_ISL6405 if MEDIA_SUBDRV_AUTOSELECT
-	select MEDIA_TUNER_TDA827X if MEDIA_SUBDRV_AUTOSELECT
-	select MEDIA_TUNER_SIMPLE if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_ZL10036 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_MT312 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_LNBP21 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_ZL10353 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_LGDT3305 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_TDA10048 if MEDIA_SUBDRV_AUTOSELECT
-	select MEDIA_TUNER_TDA18271 if MEDIA_SUBDRV_AUTOSELECT
-	select MEDIA_TUNER_TDA8290 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_ZL10039 if MEDIA_SUBDRV_AUTOSELECT
+	depends on !VIDEO_SAA7134_DVB
+	depends on VIDEO_SAA7134 && BACKPORT_DVB_CORE
+	select BACKPORT_VIDEOBUF2_DVB
+	select DVB_PLL if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_MT352 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_TDA1004X if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_NXT200X if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_TDA10086 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_TDA826X if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_ISL6421 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_ISL6405 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select MEDIA_TUNER_TDA827X if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select MEDIA_TUNER_SIMPLE if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_ZL10036 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_MT312 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_LNBP21 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_ZL10353 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_LGDT3305 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_TDA10048 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select MEDIA_TUNER_TDA18271 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select MEDIA_TUNER_TDA8290 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_ZL10039 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
 	---help---
 	  This adds support for DVB cards based on the
 	  Philips saa7134 chip.
@@ -64,10 +68,11 @@ config VIDEO_SAA7134_DVB
 	  To compile this driver as a module, choose M here: the
 	  module will be called saa7134-dvb.
 
-config VIDEO_SAA7134_GO7007
+config BACKPORT_VIDEO_SAA7134_GO7007
 	tristate "go7007 support for saa7134 based TV cards"
-	depends on VIDEO_SAA7134
-	depends on VIDEO_GO7007
+	depends on !VIDEO_SAA7134_GO7007
+	depends on BACKPORT_VIDEO_SAA7134
+	depends on BACKPORT_VIDEO_GO7007
 	---help---
 	  Enables saa7134 driver support for boards with go7007
 	  MPEG encoder (WIS Voyager or compatible).
diff --git a/backports/drivers/media/pci/saa7146/Kconfig b/backports/drivers/media/pci/saa7146/Kconfig
index da88b77..1f72c2f 100644
--- a/backports/drivers/media/pci/saa7146/Kconfig
+++ b/backports/drivers/media/pci/saa7146/Kconfig
@@ -1,7 +1,8 @@
-config VIDEO_HEXIUM_GEMINI
+config BACKPORT_VIDEO_HEXIUM_GEMINI
 	tristate "Hexium Gemini frame grabber"
-	depends on PCI && VIDEO_V4L2 && I2C
-	select VIDEO_SAA7146_VV
+	depends on !VIDEO_HEXIUM_GEMINI
+	depends on PCI && BACKPORT_VIDEO_V4L2 && I2C
+	select BACKPORT_VIDEO_SAA7146_VV
 	---help---
 	  This is a video4linux driver for the Hexium Gemini frame
 	  grabber card by Hexium. Please note that the Gemini Dual
@@ -10,10 +11,11 @@ config VIDEO_HEXIUM_GEMINI
 	  To compile this driver as a module, choose M here: the
 	  module will be called hexium_gemini.
 
-config VIDEO_HEXIUM_ORION
+config BACKPORT_VIDEO_HEXIUM_ORION
 	tristate "Hexium HV-PCI6 and Orion frame grabber"
-	depends on PCI && VIDEO_V4L2 && I2C
-	select VIDEO_SAA7146_VV
+	depends on !VIDEO_HEXIUM_ORION
+	depends on PCI && BACKPORT_VIDEO_V4L2 && I2C
+	select BACKPORT_VIDEO_SAA7146_VV
 	---help---
 	  This is a video4linux driver for the Hexium HV-PCI6 and
 	  Orion frame grabber cards by Hexium.
@@ -21,15 +23,16 @@ config VIDEO_HEXIUM_ORION
 	  To compile this driver as a module, choose M here: the
 	  module will be called hexium_orion.
 
-config VIDEO_MXB
+config BACKPORT_VIDEO_MXB
 	tristate "Siemens-Nixdorf 'Multimedia eXtension Board'"
-	depends on PCI && VIDEO_V4L2 && I2C
-	select VIDEO_SAA7146_VV
-	select VIDEO_TUNER
-	select VIDEO_SAA711X if MEDIA_SUBDRV_AUTOSELECT
-	select VIDEO_TDA9840 if MEDIA_SUBDRV_AUTOSELECT
-	select VIDEO_TEA6415C if MEDIA_SUBDRV_AUTOSELECT
-	select VIDEO_TEA6420 if MEDIA_SUBDRV_AUTOSELECT
+	depends on !VIDEO_MXB
+	depends on PCI && BACKPORT_VIDEO_V4L2 && I2C
+	select BACKPORT_VIDEO_SAA7146_VV
+	select BACKPORT_VIDEO_TUNER
+	select VIDEO_SAA711X if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select VIDEO_TDA9840 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select VIDEO_TEA6415C if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select VIDEO_TEA6420 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
 	---help---
 	  This is a video4linux driver for the 'Multimedia eXtension Board'
 	  TV card by Siemens-Nixdorf.
diff --git a/backports/drivers/media/pci/saa7164/Kconfig b/backports/drivers/media/pci/saa7164/Kconfig
index a85c8f0..a629693 100644
--- a/backports/drivers/media/pci/saa7164/Kconfig
+++ b/backports/drivers/media/pci/saa7164/Kconfig
@@ -1,14 +1,15 @@
-config VIDEO_SAA7164
+config BACKPORT_VIDEO_SAA7164
 	tristate "NXP SAA7164 support"
-	depends on DVB_CORE && VIDEO_DEV && PCI && I2C
+	depends on !VIDEO_SAA7164
+	depends on DVB_CORE && BACKPORT_VIDEO_DEV && PCI && I2C
 	select I2C_ALGOBIT
 	select FW_LOADER
-	select VIDEO_TUNER
-	select VIDEO_TVEEPROM
-	select VIDEOBUF_DVB
-	select DVB_TDA10048 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_S5H1411 if MEDIA_SUBDRV_AUTOSELECT
-	select MEDIA_TUNER_TDA18271 if MEDIA_SUBDRV_AUTOSELECT
+	select BACKPORT_VIDEO_TUNER
+	select BACKPORT_VIDEO_TVEEPROM
+	select BACKPORT_VIDEOBUF_DVB
+	select DVB_TDA10048 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_S5H1411 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select MEDIA_TUNER_TDA18271 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
 	---help---
 	  This is a video4linux driver for NXP SAA7164 based
 	  TV cards.
diff --git a/backports/drivers/media/pci/smipcie/Kconfig b/backports/drivers/media/pci/smipcie/Kconfig
index 21a1583..a4f8ff6 100644
--- a/backports/drivers/media/pci/smipcie/Kconfig
+++ b/backports/drivers/media/pci/smipcie/Kconfig
@@ -1,12 +1,13 @@
-config DVB_SMIPCIE
+config BACKPORT_DVB_SMIPCIE
 	tristate "SMI PCIe DVBSky cards"
-	depends on DVB_CORE && PCI && I2C
+	depends on !DVB_SMIPCIE
+	depends on BACKPORT_DVB_CORE && PCI && I2C
 	select I2C_ALGOBIT
-	select DVB_M88DS3103 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_SI2168 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_TS2020 if MEDIA_SUBDRV_AUTOSELECT
-	select MEDIA_TUNER_M88RS6000T if MEDIA_SUBDRV_AUTOSELECT
-	select MEDIA_TUNER_SI2157 if MEDIA_SUBDRV_AUTOSELECT
+	select DVB_M88DS3103 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_SI2168 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_TS2020 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select MEDIA_TUNER_M88RS6000T if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select MEDIA_TUNER_SI2157 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
 	help
 	  Support for cards with SMI PCIe bridge:
 	  - DVBSky S950 V3
diff --git a/backports/drivers/media/pci/solo6x10/Kconfig b/backports/drivers/media/pci/solo6x10/Kconfig
index 0fb91dc..eeb9482 100644
--- a/backports/drivers/media/pci/solo6x10/Kconfig
+++ b/backports/drivers/media/pci/solo6x10/Kconfig
@@ -1,12 +1,13 @@
-config VIDEO_SOLO6X10
+config BACKPORT_VIDEO_SOLO6X10
 	tristate "Bluecherry / Softlogic 6x10 capture cards (MPEG-4/H.264)"
-	depends on PCI && VIDEO_DEV && SND && I2C
+	depends on !VIDEO_SOLO6X10
+	depends on PCI && BACKPORT_VIDEO_DEV && SND && I2C
 	depends on HAS_DMA
 	select BITREVERSE
 	select FONT_SUPPORT
 	select FONT_8x16
-	select VIDEOBUF2_DMA_SG
-	select VIDEOBUF2_DMA_CONTIG
+	select BACKPORT_VIDEOBUF2_DMA_SG
+	select BACKPORT_VIDEOBUF2_DMA_CONTIG
 	select SND_PCM
 	select FONT_8x16
 	---help---
diff --git a/backports/drivers/media/pci/sta2x11/Kconfig b/backports/drivers/media/pci/sta2x11/Kconfig
index e03587b..4e42118 100644
--- a/backports/drivers/media/pci/sta2x11/Kconfig
+++ b/backports/drivers/media/pci/sta2x11/Kconfig
@@ -1,11 +1,12 @@
-config STA2X11_VIP
+config BACKPORT_STA2X11_VIP
 	tristate "STA2X11 VIP Video For Linux"
+	depends on !STA2X11_VIP
 	depends on STA2X11
 	depends on HAS_DMA
-	select VIDEO_ADV7180 if MEDIA_SUBDRV_AUTOSELECT
-	select VIDEOBUF2_DMA_CONTIG
-	depends on PCI && VIDEO_V4L2 && VIRT_TO_BUS
-	depends on VIDEO_V4L2_SUBDEV_API
+	select VIDEO_ADV7180 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select BACKPORT_VIDEOBUF2_DMA_CONTIG
+	depends on PCI && BACKPORT_VIDEO_V4L2 && VIRT_TO_BUS
+	depends on BACKPORT_VIDEO_V4L2_SUBDEV_API
 	depends on I2C
 	help
 	  Say Y for support for STA2X11 VIP (Video Input Port) capture
diff --git a/backports/drivers/media/pci/ttpci/Kconfig b/backports/drivers/media/pci/ttpci/Kconfig
index 7b83151..75285cf 100644
--- a/backports/drivers/media/pci/ttpci/Kconfig
+++ b/backports/drivers/media/pci/ttpci/Kconfig
@@ -1,21 +1,23 @@
-config DVB_AV7110_IR
+config BACKPORT_DVB_AV7110_IR
 	bool
+	depends on !DVB_AV7110_IR
 
-config DVB_AV7110
+config BACKPORT_DVB_AV7110
 	tristate "AV7110 cards"
-	depends on DVB_CORE && PCI && I2C
-	select TTPCI_EEPROM
-	select VIDEO_SAA7146_VV
-	select DVB_AV7110_IR if INPUT_EVDEV=y || INPUT_EVDEV=DVB_AV7110
-	depends on VIDEO_DEV	# dependencies of VIDEO_SAA7146_VV
-	select DVB_VES1820 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_VES1X93 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_STV0299 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_TDA8083 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_SP8870 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_STV0297 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_L64781 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_LNBP21 if MEDIA_SUBDRV_AUTOSELECT
+	depends on !DVB_AV7110
+	depends on BACKPORT_DVB_CORE && PCI && I2C
+	select BACKPORT_TTPCI_EEPROM
+	select BACKPORT_VIDEO_SAA7146_VV
+	select BACKPORT_DVB_AV7110_IR if INPUT_EVDEV=y || INPUT_EVDEV=DVB_AV7110
+	depends on BACKPORT_VIDEO_DEV	# dependencies of VIDEO_SAA7146_VV
+	select DVB_VES1820 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_VES1X93 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_STV0299 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_TDA8083 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_SP8870 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_STV0297 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_L64781 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_LNBP21 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
 	help
 	  Support for SAA7146 and AV7110 based DVB cards as produced
 	  by Fujitsu-Siemens, Technotrend, Hauppauge and others.
@@ -35,10 +37,11 @@ config DVB_AV7110
 
 	  Say Y if you own such a card and want to use it.
 
-config DVB_AV7110_OSD
+config BACKPORT_DVB_AV7110_OSD
 	bool "AV7110 OSD support"
-	depends on DVB_AV7110
-	default y if DVB_AV7110=y || DVB_AV7110=m
+	depends on !DVB_AV7110_OSD
+	depends on BACKPORT_DVB_AV7110
+	default y if BACKPORT_DVB_AV7110=y || BACKPORT_DVB_AV7110=m
 	help
 	  The AV7110 firmware provides some code to generate an OnScreenDisplay
 	  on the video output. This is kind of nonstandard and not guaranteed to
@@ -49,32 +52,34 @@ config DVB_AV7110_OSD
 
 	  All other people say N.
 
-config DVB_BUDGET_CORE
+config BACKPORT_DVB_BUDGET_CORE
 	tristate "SAA7146 DVB cards (aka Budget, Nova-PCI)"
-	depends on DVB_CORE && PCI && I2C
-	select VIDEO_SAA7146
-	select TTPCI_EEPROM
+	depends on !DVB_BUDGET_CORE
+	depends on BACKPORT_DVB_CORE && PCI && I2C
+	select BACKPORT_VIDEO_SAA7146
+	select BACKPORT_TTPCI_EEPROM
 	help
 	  Support for simple SAA7146 based DVB cards
 	  (so called Budget- or Nova-PCI cards) without onboard
 	  MPEG2 decoder.
 
-config DVB_BUDGET
+config BACKPORT_DVB_BUDGET
 	tristate "Budget cards"
-	depends on DVB_BUDGET_CORE && I2C
-	select DVB_STV0299 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_VES1X93 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_VES1820 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_L64781 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_TDA8083 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_S5H1420 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_TDA10086 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_TDA826X if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_LNBP21 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_TDA1004X if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_ISL6423 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_STV090x if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_STV6110x if MEDIA_SUBDRV_AUTOSELECT
+	depends on !DVB_BUDGET
+	depends on BACKPORT_DVB_BUDGET_CORE && I2C
+	select DVB_STV0299 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_VES1X93 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_VES1820 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_L64781 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_TDA8083 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_S5H1420 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_TDA10086 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_TDA826X if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_LNBP21 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_TDA1004X if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_ISL6423 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_STV090x if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_STV6110x if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
 	help
 	  Support for simple SAA7146 based DVB cards (so called Budget-
 	  or Nova-PCI cards) without onboard MPEG2 decoder, and without
@@ -85,20 +90,21 @@ config DVB_BUDGET
 	  To compile this driver as a module, choose M here: the
 	  module will be called budget.
 
-config DVB_BUDGET_CI
+config BACKPORT_DVB_BUDGET_CI
 	tristate "Budget cards with onboard CI connector"
-	depends on DVB_BUDGET_CORE && I2C
-	select DVB_STV0297 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_STV0299 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_TDA1004X if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_STB0899 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_STB6100 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_LNBP21 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_STV0288 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_STB6000 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_TDA10023 if MEDIA_SUBDRV_AUTOSELECT
-	select MEDIA_TUNER_TDA827X if MEDIA_SUBDRV_AUTOSELECT
-	depends on RC_CORE
+	depends on !DVB_BUDGET_CI
+	depends on BACKPORT_DVB_BUDGET_CORE && I2C
+	select DVB_STV0297 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_STV0299 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_TDA1004X if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_STB0899 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_STB6100 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_LNBP21 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_STV0288 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_STB6000 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_TDA10023 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select MEDIA_TUNER_TDA827X if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	depends on BACKPORT_RC_CORE
 	help
 	  Support for simple SAA7146 based DVB cards
 	  (so called Budget- or Nova-PCI cards) without onboard
@@ -112,19 +118,20 @@ config DVB_BUDGET_CI
 	  To compile this driver as a module, choose M here: the
 	  module will be called budget-ci.
 
-config DVB_BUDGET_AV
+config BACKPORT_DVB_BUDGET_AV
 	tristate "Budget cards with analog video inputs"
-	depends on DVB_BUDGET_CORE && I2C
-	select VIDEO_SAA7146_VV
-	depends on VIDEO_DEV	# dependencies of VIDEO_SAA7146_VV
-	select DVB_PLL if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_STV0299 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_TDA1004X if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_TDA10021 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_TDA10023 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_STB0899 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_TDA8261 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_TUA6100 if MEDIA_SUBDRV_AUTOSELECT
+	depends on !DVB_BUDGET_AV
+	depends on BACKPORT_DVB_BUDGET_CORE && I2C
+	select BACKPORT_VIDEO_SAA7146_VV
+	depends on BACKPORT_VIDEO_DEV	# dependencies of VIDEO_SAA7146_VV
+	select DVB_PLL if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_STV0299 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_TDA1004X if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_TDA10021 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_TDA10023 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_STB0899 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_TDA8261 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_TUA6100 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
 	help
 	  Support for simple SAA7146 based DVB cards
 	  (so called Budget- or Nova-PCI cards) without onboard
@@ -135,13 +142,14 @@ config DVB_BUDGET_AV
 	  To compile this driver as a module, choose M here: the
 	  module will be called budget-av.
 
-config DVB_BUDGET_PATCH
+config BACKPORT_DVB_BUDGET_PATCH
 	tristate "AV7110 cards with Budget Patch"
-	depends on DVB_BUDGET_CORE && I2C
-	depends on DVB_AV7110
-	select DVB_STV0299 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_VES1X93 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_TDA8083 if MEDIA_SUBDRV_AUTOSELECT
+	depends on !DVB_BUDGET_PATCH
+	depends on BACKPORT_DVB_BUDGET_CORE && I2C
+	depends on BACKPORT_DVB_AV7110
+	select DVB_STV0299 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_VES1X93 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_TDA8083 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
 	help
 	  Support for Budget Patch (full TS) modification on
 	  SAA7146+AV7110 based cards (DVB-S cards). This
diff --git a/backports/drivers/media/pci/tw68/Kconfig b/backports/drivers/media/pci/tw68/Kconfig
index 95d5d52..5c6b21e 100644
--- a/backports/drivers/media/pci/tw68/Kconfig
+++ b/backports/drivers/media/pci/tw68/Kconfig
@@ -1,7 +1,8 @@
-config VIDEO_TW68
+config BACKPORT_VIDEO_TW68
 	tristate "Techwell tw68x Video For Linux"
-	depends on VIDEO_DEV && PCI && VIDEO_V4L2
-	select VIDEOBUF2_DMA_SG
+	depends on !VIDEO_TW68
+	depends on BACKPORT_VIDEO_DEV && PCI && VIDEO_V4L2
+	select BACKPORT_VIDEOBUF2_DMA_SG
 	---help---
 	  Support for Techwell tw68xx based frame grabber boards.
 
diff --git a/backports/drivers/media/pci/zoran/Kconfig b/backports/drivers/media/pci/zoran/Kconfig
index 39ec35b..4609267 100644
--- a/backports/drivers/media/pci/zoran/Kconfig
+++ b/backports/drivers/media/pci/zoran/Kconfig
@@ -1,6 +1,7 @@
-config VIDEO_ZORAN
+config BACKPORT_VIDEO_ZORAN
 	tristate "Zoran ZR36057/36067 Video For Linux"
-	depends on PCI && I2C_ALGOBIT && VIDEO_V4L2 && VIRT_TO_BUS
+	depends on !VIDEO_ZORAN
+	depends on PCI && I2C_ALGOBIT && BACKPORT_VIDEO_V4L2 && VIRT_TO_BUS
 	depends on !ALPHA
 	help
 	  Say Y for support for MJPEG capture cards based on the Zoran
@@ -12,64 +13,71 @@ config VIDEO_ZORAN
 	  To compile this driver as a module, choose M here: the
 	  module will be called zr36067.
 
-config VIDEO_ZORAN_DC30
+config BACKPORT_VIDEO_ZORAN_DC30
 	tristate "Pinnacle/Miro DC30(+) support"
-	depends on VIDEO_ZORAN
-	select VIDEO_ADV7175 if MEDIA_SUBDRV_AUTOSELECT
-	select VIDEO_VPX3220 if MEDIA_SUBDRV_AUTOSELECT
+	depends on !VIDEO_ZORAN_DC30
+	depends on BACKPORT_VIDEO_ZORAN
+	select VIDEO_ADV7175 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select VIDEO_VPX3220 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
 	help
 	  Support for the Pinnacle/Miro DC30(+) MJPEG capture/playback
 	  card. This also supports really old DC10 cards based on the
 	  zr36050 MJPEG codec and zr36016 VFE.
 
-config VIDEO_ZORAN_ZR36060
+config BACKPORT_VIDEO_ZORAN_ZR36060
 	tristate "Zoran ZR36060"
-	depends on VIDEO_ZORAN
+	depends on !VIDEO_ZORAN_ZR36060
+	depends on BACKPORT_VIDEO_ZORAN
 	help
 	  Say Y to support Zoran boards based on 36060 chips.
 	  This includes Iomega Buz, Pinnacle DC10, Linux media Labs 33
 	  and 33 R10 and AverMedia 6 boards.
 
-config VIDEO_ZORAN_BUZ
+config BACKPORT_VIDEO_ZORAN_BUZ
 	tristate "Iomega Buz support"
-	depends on VIDEO_ZORAN_ZR36060
-	select VIDEO_SAA711X if MEDIA_SUBDRV_AUTOSELECT
-	select VIDEO_SAA7185 if MEDIA_SUBDRV_AUTOSELECT
+	depends on !VIDEO_ZORAN_BUZ
+	depends on BACKPORT_VIDEO_ZORAN_ZR36060
+	select VIDEO_SAA711X if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select VIDEO_SAA7185 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
 	help
 	  Support for the Iomega Buz MJPEG capture/playback card.
 
-config VIDEO_ZORAN_DC10
+config BACKPORT_VIDEO_ZORAN_DC10
 	tristate "Pinnacle/Miro DC10(+) support"
-	depends on VIDEO_ZORAN_ZR36060
-	select VIDEO_SAA7110 if MEDIA_SUBDRV_AUTOSELECT
-	select VIDEO_ADV7175 if MEDIA_SUBDRV_AUTOSELECT
+	depends on !VIDEO_ZORAN_DC10
+	depends on BACKPORT_VIDEO_ZORAN_ZR36060
+	select VIDEO_SAA7110 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select VIDEO_ADV7175 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
 	help
 	  Support for the Pinnacle/Miro DC10(+) MJPEG capture/playback
 	  card.
 
-config VIDEO_ZORAN_LML33
+config BACKPORT_VIDEO_ZORAN_LML33
 	tristate "Linux Media Labs LML33 support"
-	depends on VIDEO_ZORAN_ZR36060
+	depends on !VIDEO_ZORAN_LML33
+	depends on BACKPORT_VIDEO_ZORAN_ZR36060
 	select VIDEO_BT819 if MEDIA_SUBDRV_AUTOSELECT
 	select VIDEO_BT856 if MEDIA_SUBDRV_AUTOSELECT
 	help
 	  Support for the Linux Media Labs LML33 MJPEG capture/playback
 	  card.
 
-config VIDEO_ZORAN_LML33R10
+config BACKPORT_VIDEO_ZORAN_LML33R10
 	tristate "Linux Media Labs LML33R10 support"
-	depends on VIDEO_ZORAN_ZR36060
-	select VIDEO_SAA711X if MEDIA_SUBDRV_AUTOSELECT
-	select VIDEO_ADV7170 if MEDIA_SUBDRV_AUTOSELECT
+	depends on !VIDEO_ZORAN_LML33R10
+	depends on BACKPORT_VIDEO_ZORAN_ZR36060
+	select VIDEO_SAA711X if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select VIDEO_ADV7170 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
 	help
 	  support for the Linux Media Labs LML33R10 MJPEG capture/playback
 	  card.
 
-config VIDEO_ZORAN_AVS6EYES
+config BACKPORT_VIDEO_ZORAN_AVS6EYES
 	tristate "AverMedia 6 Eyes support"
-	depends on VIDEO_ZORAN_ZR36060
+	depends on !VIDEO_ZORAN_AVS6EYES
+	depends on BACKPORT_VIDEO_ZORAN_ZR36060
 	select VIDEO_BT856 if MEDIA_SUBDRV_AUTOSELECT
 	select VIDEO_BT866 if MEDIA_SUBDRV_AUTOSELECT
-	select VIDEO_KS0127 if MEDIA_SUBDRV_AUTOSELECT
+	select VIDEO_KS0127 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
 	help
 	  Support for the AverMedia 6 Eyes video surveillance card.
diff --git a/backports/drivers/media/platform/Kconfig b/backports/drivers/media/platform/Kconfig
index 61402d1..d296751 100644
--- a/backports/drivers/media/platform/Kconfig
+++ b/backports/drivers/media/platform/Kconfig
@@ -2,22 +2,24 @@
 # Platform drivers
 #	All drivers here are currently for webcam support
 
-menuconfig V4L_PLATFORM_DRIVERS
+menuconfig BACKPORT_V4L_PLATFORM_DRIVERS
 	bool "V4L platform devices"
-	depends on MEDIA_CAMERA_SUPPORT
+	depends on !V4L_PLATFORM_DRIVERS
+	depends on BACKPORT_MEDIA_CAMERA_SUPPORT
 	default n
 	---help---
 	  Say Y here to enable support for platform-specific V4L drivers.
 
-if V4L_PLATFORM_DRIVERS
+if BACKPORT_V4L_PLATFORM_DRIVERS
 
 source "$BACKPORT_DIR/drivers/media/platform/marvell-ccic/Kconfig"
 
-config VIDEO_VIA_CAMERA
+config BACKPORT_VIDEO_VIA_CAMERA
 	tristate "VIAFB camera controller support"
+	depends on !VIDEO_VIA_CAMERA
 	depends on FB_VIA
-	select VIDEOBUF_DMA_SG
-	select VIDEO_OV7670
+	select BACKPORT_VIDEOBUF_DMA_SG
+	select BACKPORT_VIDEO_OV7670
 	help
 	   Driver support for the integrated camera controller in VIA
 	   Chrome9 chipsets.  Currently only tested on OLPC xo-1.5 systems
@@ -33,19 +35,21 @@ source "$BACKPORT_DIR/drivers/media/platform/omap/Kconfig"
 
 source "$BACKPORT_DIR/drivers/media/platform/blackfin/Kconfig"
 
-config VIDEO_SH_VOU
+config BACKPORT_VIDEO_SH_VOU
 	tristate "SuperH VOU video output driver"
-	depends on MEDIA_CAMERA_SUPPORT
-	depends on VIDEO_DEV && I2C && HAS_DMA
+	depends on !VIDEO_SH_VOU
+	depends on BACKPORT_MEDIA_CAMERA_SUPPORT
+	depends on BACKPORT_VIDEO_DEV && I2C && HAS_DMA
 	depends on ARCH_SHMOBILE || COMPILE_TEST
-	select VIDEOBUF_DMA_CONTIG
+	select BACKPORT_VIDEOBUF_DMA_CONTIG
 	help
 	  Support for the Video Output Unit (VOU) on SuperH SoCs.
 
-config VIDEO_VIU
+config BACKPORT_VIDEO_VIU
 	tristate "Freescale VIU Video Driver"
-	depends on VIDEO_V4L2 && PPC_MPC512x
-	select VIDEOBUF_DMA_CONTIG
+	depends on !VIDEO_VIU
+	depends on BACKPORT_VIDEO_V4L2 && PPC_MPC512x
+	select BACKPORT_VIDEOBUF_DMA_CONTIG
 	default y
 	---help---
 	  Support for Freescale VIU video driver. This device captures
@@ -54,27 +58,30 @@ config VIDEO_VIU
 	  Say Y here if you want to enable VIU device on MPC5121e Rev2+.
 	  In doubt, say N.
 
-config VIDEO_TIMBERDALE
+config BACKPORT_VIDEO_TIMBERDALE
 	tristate "Support for timberdale Video In/LogiWIN"
-	depends on VIDEO_V4L2 && I2C && VIDEO_V4L2_SUBDEV_API && HAS_DMA
+	depends on !VIDEO_TIMBERDALE
+	depends on VIDEO_V4L2 && I2C && BACKPORT_VIDEO_V4L2_SUBDEV_API && HAS_DMA
 	depends on (MFD_TIMBERDALE && TIMB_DMA) || COMPILE_TEST
-	select VIDEO_ADV7180
-	select VIDEOBUF_DMA_CONTIG
+	select BACKPORT_VIDEO_ADV7180
+	select BACKPORT_VIDEOBUF_DMA_CONTIG
 	---help---
 	  Add support for the Video In peripherial of the timberdale FPGA.
 
-config VIDEO_M32R_AR
+config BACKPORT_VIDEO_M32R_AR
 	tristate "AR devices"
-	depends on VIDEO_V4L2
+	depends on !VIDEO_M32R_AR
+	depends on BACKPORT_VIDEO_V4L2
 	depends on M32R || COMPILE_TEST
 	---help---
 	  This is a video4linux driver for the Renesas AR (Artificial Retina)
 	  camera module.
 
-config VIDEO_M32R_AR_M64278
+config BACKPORT_VIDEO_M32R_AR_M64278
 	tristate "AR device with color module M64278(VGA)"
+	depends on !VIDEO_M32R_AR_M64278
 	depends on PLAT_M32700UT
-	select VIDEO_M32R_AR
+	select BACKPORT_VIDEO_M32R_AR
 	---help---
 	  This is a video4linux driver for the Renesas AR (Artificial
 	  Retina) with M64278E-800 camera module.
@@ -83,30 +90,33 @@ config VIDEO_M32R_AR_M64278
 	  To compile this driver as a module, choose M here: the
 	  module will be called arv.
 
-config VIDEO_OMAP3
+config BACKPORT_VIDEO_OMAP3
 	tristate "OMAP 3 Camera support"
-	depends on VIDEO_V4L2 && I2C && VIDEO_V4L2_SUBDEV_API && ARCH_OMAP3
+	depends on !VIDEO_OMAP3
+	depends on VIDEO_V4L2 && I2C && BACKPORT_VIDEO_V4L2_SUBDEV_API && ARCH_OMAP3
 	depends on HAS_DMA
 	select ARM_DMA_USE_IOMMU
 	select OMAP_IOMMU
-	select VIDEOBUF2_DMA_CONTIG
+	select BACKPORT_VIDEOBUF2_DMA_CONTIG
 	select MFD_SYSCON
 	---help---
 	  Driver for an OMAP 3 camera controller.
 
-config VIDEO_OMAP3_DEBUG
+config BACKPORT_VIDEO_OMAP3_DEBUG
 	bool "OMAP 3 Camera debug messages"
-	depends on VIDEO_OMAP3
+	depends on !VIDEO_OMAP3_DEBUG
+	depends on BACKPORT_VIDEO_OMAP3
 	---help---
 	  Enable debug messages on OMAP 3 camera controller driver.
 
-config VIDEO_S3C_CAMIF
+config BACKPORT_VIDEO_S3C_CAMIF
 	tristate "Samsung S3C24XX/S3C64XX SoC Camera Interface driver"
-	depends on VIDEO_V4L2 && I2C && VIDEO_V4L2_SUBDEV_API
+	depends on !VIDEO_S3C_CAMIF
+	depends on VIDEO_V4L2 && I2C && BACKPORT_VIDEO_V4L2_SUBDEV_API
 	depends on PM
 	depends on ARCH_S3C64XX || PLAT_S3C24XX || COMPILE_TEST
 	depends on HAS_DMA
-	select VIDEOBUF2_DMA_CONTIG
+	select BACKPORT_VIDEOBUF2_DMA_CONTIG
 	---help---
 	  This is a v4l2 driver for s3c24xx and s3c64xx SoC series camera
 	  host interface (CAMIF).
@@ -120,12 +130,13 @@ source "$BACKPORT_DIR/drivers/media/platform/s5p-tv/Kconfig"
 source "$BACKPORT_DIR/drivers/media/platform/am437x/Kconfig"
 source "$BACKPORT_DIR/drivers/media/platform/xilinx/Kconfig"
 
-endif # V4L_PLATFORM_DRIVERS
+endif # BACKPORT_V4L_PLATFORM_DRIVERS
 
-menuconfig V4L_MEM2MEM_DRIVERS
+menuconfig BACKPORT_V4L_MEM2MEM_DRIVERS
 	bool "Memory-to-memory multimedia devices"
-	depends on VIDEO_V4L2
-	depends on MEDIA_CAMERA_SUPPORT
+	depends on !V4L_MEM2MEM_DRIVERS
+	depends on BACKPORT_VIDEO_V4L2
+	depends on BACKPORT_MEDIA_CAMERA_SUPPORT
 	default n
 	---help---
 	  Say Y here to enable selecting drivers for V4L devices that
@@ -133,138 +144,151 @@ menuconfig V4L_MEM2MEM_DRIVERS
 	  to capture and output drivers, which use memory buffers for just
 	  one of those.
 
-if V4L_MEM2MEM_DRIVERS
+if BACKPORT_V4L_MEM2MEM_DRIVERS
 
-config VIDEO_CODA
+config BACKPORT_VIDEO_CODA
 	tristate "Chips&Media Coda multi-standard codec IP"
-	depends on VIDEO_DEV && VIDEO_V4L2 && ARCH_MXC
+	depends on !VIDEO_CODA
+	depends on BACKPORT_VIDEO_DEV && VIDEO_V4L2 && ARCH_MXC
 	depends on HAS_DMA
 	select SRAM
-	select VIDEOBUF2_DMA_CONTIG
-	select VIDEOBUF2_VMALLOC
-	select V4L2_MEM2MEM_DEV
+	select BACKPORT_VIDEOBUF2_DMA_CONTIG
+	select BACKPORT_VIDEOBUF2_VMALLOC
+	select BACKPORT_V4L2_MEM2MEM_DEV
 	select GENERIC_ALLOCATOR
 	---help---
 	   Coda is a range of video codec IPs that supports
 	   H.264, MPEG-4, and other video formats.
 
-config VIDEO_MEM2MEM_DEINTERLACE
+config BACKPORT_VIDEO_MEM2MEM_DEINTERLACE
 	tristate "Deinterlace support"
-	depends on VIDEO_DEV && VIDEO_V4L2 && DMA_ENGINE
+	depends on !VIDEO_MEM2MEM_DEINTERLACE
+	depends on BACKPORT_VIDEO_DEV && VIDEO_V4L2 && DMA_ENGINE
 	depends on HAS_DMA
-	select VIDEOBUF2_DMA_CONTIG
-	select V4L2_MEM2MEM_DEV
+	select BACKPORT_VIDEOBUF2_DMA_CONTIG
+	select BACKPORT_V4L2_MEM2MEM_DEV
 	help
 	    Generic deinterlacing V4L2 driver.
 
-config VIDEO_SAMSUNG_S5P_G2D
+config BACKPORT_VIDEO_SAMSUNG_S5P_G2D
 	tristate "Samsung S5P and EXYNOS4 G2D 2d graphics accelerator driver"
-	depends on VIDEO_DEV && VIDEO_V4L2
+	depends on !VIDEO_SAMSUNG_S5P_G2D
+	depends on BACKPORT_VIDEO_DEV && VIDEO_V4L2
 	depends on ARCH_S5PV210 || ARCH_EXYNOS || COMPILE_TEST
 	depends on HAS_DMA
-	select VIDEOBUF2_DMA_CONTIG
-	select V4L2_MEM2MEM_DEV
+	select BACKPORT_VIDEOBUF2_DMA_CONTIG
+	select BACKPORT_V4L2_MEM2MEM_DEV
 	default n
 	---help---
 	  This is a v4l2 driver for Samsung S5P and EXYNOS4 G2D
 	  2d graphics accelerator.
 
-config VIDEO_SAMSUNG_S5P_JPEG
+config BACKPORT_VIDEO_SAMSUNG_S5P_JPEG
 	tristate "Samsung S5P/Exynos3250/Exynos4 JPEG codec driver"
-	depends on VIDEO_DEV && VIDEO_V4L2
+	depends on !VIDEO_SAMSUNG_S5P_JPEG
+	depends on BACKPORT_VIDEO_DEV && VIDEO_V4L2
 	depends on ARCH_S5PV210 || ARCH_EXYNOS || COMPILE_TEST
 	depends on HAS_DMA
-	select VIDEOBUF2_DMA_CONTIG
-	select V4L2_MEM2MEM_DEV
+	select BACKPORT_VIDEOBUF2_DMA_CONTIG
+	select BACKPORT_V4L2_MEM2MEM_DEV
 	---help---
 	  This is a v4l2 driver for Samsung S5P, EXYNOS3250
 	  and EXYNOS4 JPEG codec
 
-config VIDEO_SAMSUNG_S5P_MFC
+config BACKPORT_VIDEO_SAMSUNG_S5P_MFC
 	tristate "Samsung S5P MFC Video Codec"
-	depends on VIDEO_DEV && VIDEO_V4L2
+	depends on !VIDEO_SAMSUNG_S5P_MFC
+	depends on BACKPORT_VIDEO_DEV && VIDEO_V4L2
 	depends on ARCH_S5PV210 || ARCH_EXYNOS || COMPILE_TEST
 	depends on HAS_DMA
-	select VIDEOBUF2_DMA_CONTIG
+	select BACKPORT_VIDEOBUF2_DMA_CONTIG
 	default n
 	help
 	    MFC 5.1 and 6.x driver for V4L2
 
-config VIDEO_MX2_EMMAPRP
+config BACKPORT_VIDEO_MX2_EMMAPRP
 	tristate "MX2 eMMa-PrP support"
-	depends on VIDEO_DEV && VIDEO_V4L2
+	depends on !VIDEO_MX2_EMMAPRP
+	depends on BACKPORT_VIDEO_DEV && VIDEO_V4L2
 	depends on SOC_IMX27 || COMPILE_TEST
 	depends on HAS_DMA
-	select VIDEOBUF2_DMA_CONTIG
-	select V4L2_MEM2MEM_DEV
+	select BACKPORT_VIDEOBUF2_DMA_CONTIG
+	select BACKPORT_V4L2_MEM2MEM_DEV
 	help
 	    MX2X chips have a PrP that can be used to process buffers from
 	    memory to memory. Operations include resizing and format
 	    conversion.
 
-config VIDEO_SAMSUNG_EXYNOS_GSC
+config BACKPORT_VIDEO_SAMSUNG_EXYNOS_GSC
 	tristate "Samsung Exynos G-Scaler driver"
-	depends on VIDEO_DEV && VIDEO_V4L2
+	depends on !VIDEO_SAMSUNG_EXYNOS_GSC
+	depends on BACKPORT_VIDEO_DEV && VIDEO_V4L2
 	depends on ARCH_EXYNOS5 || COMPILE_TEST
 	depends on HAS_DMA
-	select VIDEOBUF2_DMA_CONTIG
-	select V4L2_MEM2MEM_DEV
+	select BACKPORT_VIDEOBUF2_DMA_CONTIG
+	select BACKPORT_V4L2_MEM2MEM_DEV
 	help
 	  This is a v4l2 driver for Samsung EXYNOS5 SoC G-Scaler.
 
-config VIDEO_SH_VEU
+config BACKPORT_VIDEO_SH_VEU
 	tristate "SuperH VEU mem2mem video processing driver"
-	depends on VIDEO_DEV && VIDEO_V4L2 && HAS_DMA
-	select VIDEOBUF2_DMA_CONTIG
-	select V4L2_MEM2MEM_DEV
+	depends on !VIDEO_SH_VEU
+	depends on BACKPORT_VIDEO_DEV && VIDEO_V4L2 && HAS_DMA
+	select BACKPORT_VIDEOBUF2_DMA_CONTIG
+	select BACKPORT_V4L2_MEM2MEM_DEV
 	help
 	    Support for the Video Engine Unit (VEU) on SuperH and
 	    SH-Mobile SoCs.
 
-config VIDEO_RENESAS_VSP1
+config BACKPORT_VIDEO_RENESAS_VSP1
 	tristate "Renesas VSP1 Video Processing Engine"
-	depends on VIDEO_V4L2 && VIDEO_V4L2_SUBDEV_API && HAS_DMA
+	depends on !VIDEO_RENESAS_VSP1
+	depends on VIDEO_V4L2 && BACKPORT_VIDEO_V4L2_SUBDEV_API && HAS_DMA
 	depends on (ARCH_SHMOBILE && OF) || COMPILE_TEST
-	select VIDEOBUF2_DMA_CONTIG
+	select BACKPORT_VIDEOBUF2_DMA_CONTIG
 	---help---
 	  This is a V4L2 driver for the Renesas VSP1 video processing engine.
 
 	  To compile this driver as a module, choose M here: the module
 	  will be called vsp1.
 
-config VIDEO_TI_VPE
+config BACKPORT_VIDEO_TI_VPE
 	tristate "TI VPE (Video Processing Engine) driver"
-	depends on VIDEO_DEV && VIDEO_V4L2
+	depends on !VIDEO_TI_VPE
+	depends on BACKPORT_VIDEO_DEV && VIDEO_V4L2
 	depends on SOC_DRA7XX || COMPILE_TEST
 	depends on HAS_DMA
-	select VIDEOBUF2_DMA_CONTIG
-	select V4L2_MEM2MEM_DEV
+	select BACKPORT_VIDEOBUF2_DMA_CONTIG
+	select BACKPORT_V4L2_MEM2MEM_DEV
 	default n
 	---help---
 	  Support for the TI VPE(Video Processing Engine) block
 	  found on DRA7XX SoC.
 
-config VIDEO_TI_VPE_DEBUG
+config BACKPORT_VIDEO_TI_VPE_DEBUG
 	bool "VPE debug messages"
-	depends on VIDEO_TI_VPE
+	depends on !VIDEO_TI_VPE_DEBUG
+	depends on BACKPORT_VIDEO_TI_VPE
 	---help---
 	  Enable debug messages on VPE driver.
 
-endif # V4L_MEM2MEM_DRIVERS
+endif # BACKPORT_V4L_MEM2MEM_DRIVERS
 
-menuconfig V4L_TEST_DRIVERS
+menuconfig BACKPORT_V4L_TEST_DRIVERS
 	bool "Media test drivers"
-	depends on MEDIA_CAMERA_SUPPORT
+	depends on !V4L_TEST_DRIVERS
+	depends on BACKPORT_MEDIA_CAMERA_SUPPORT
 
-if V4L_TEST_DRIVERS
+if BACKPORT_V4L_TEST_DRIVERS
 
 source "$BACKPORT_DIR/drivers/media/platform/vivid/Kconfig"
 
-config VIDEO_VIM2M
+config BACKPORT_VIDEO_VIM2M
 	tristate "Virtual Memory-to-Memory Driver"
-	depends on VIDEO_DEV && VIDEO_V4L2
-	select VIDEOBUF2_VMALLOC
-	select V4L2_MEM2MEM_DEV
+	depends on !VIDEO_VIM2M
+	depends on BACKPORT_VIDEO_DEV && VIDEO_V4L2
+	select BACKPORT_VIDEOBUF2_VMALLOC
+	select BACKPORT_V4L2_MEM2MEM_DEV
 	default n
 	---help---
 	  This is a virtual test device for the memory-to-memory driver
diff --git a/backports/drivers/media/platform/am437x/Kconfig b/backports/drivers/media/platform/am437x/Kconfig
index 42d9c18..965411f 100644
--- a/backports/drivers/media/platform/am437x/Kconfig
+++ b/backports/drivers/media/platform/am437x/Kconfig
@@ -1,8 +1,9 @@
-config VIDEO_AM437X_VPFE
+config BACKPORT_VIDEO_AM437X_VPFE
 	tristate "TI AM437x VPFE video capture driver"
-	depends on VIDEO_V4L2 && VIDEO_V4L2_SUBDEV_API && HAS_DMA
+	depends on !VIDEO_AM437X_VPFE
+	depends on VIDEO_V4L2 && BACKPORT_VIDEO_V4L2_SUBDEV_API && HAS_DMA
 	depends on SOC_AM43XX || COMPILE_TEST
-	select VIDEOBUF2_DMA_CONTIG
+	select BACKPORT_VIDEOBUF2_DMA_CONTIG
 	help
 	   Support for AM437x Video Processing Front End based Video
 	   Capture Driver.
diff --git a/backports/drivers/media/platform/blackfin/Kconfig b/backports/drivers/media/platform/blackfin/Kconfig
index 68fa901..57efd1a 100644
--- a/backports/drivers/media/platform/blackfin/Kconfig
+++ b/backports/drivers/media/platform/blackfin/Kconfig
@@ -1,8 +1,9 @@
-config VIDEO_BLACKFIN_CAPTURE
+config BACKPORT_VIDEO_BLACKFIN_CAPTURE
 	tristate "Blackfin Video Capture Driver"
-	depends on VIDEO_V4L2 && BLACKFIN && I2C
+	depends on !VIDEO_BLACKFIN_CAPTURE
+	depends on BACKPORT_VIDEO_V4L2 && BLACKFIN && I2C
 	depends on HAS_DMA
-	select VIDEOBUF2_DMA_CONTIG
+	select BACKPORT_VIDEOBUF2_DMA_CONTIG
 	help
 	  V4L2 bridge driver for Blackfin video capture device.
 	  Choose PPI or EPPI as its interface.
@@ -10,7 +11,8 @@ config VIDEO_BLACKFIN_CAPTURE
 	  To compile this driver as a module, choose M here: the
 	  module will be called bfin_capture.
 
-config VIDEO_BLACKFIN_PPI
+config BACKPORT_VIDEO_BLACKFIN_PPI
 	tristate
-	depends on VIDEO_BLACKFIN_CAPTURE
-	default VIDEO_BLACKFIN_CAPTURE
+	depends on !VIDEO_BLACKFIN_PPI
+	depends on BACKPORT_VIDEO_BLACKFIN_CAPTURE
+	default BACKPORT_VIDEO_BLACKFIN_CAPTURE
diff --git a/backports/drivers/media/platform/davinci/Kconfig b/backports/drivers/media/platform/davinci/Kconfig
index 469e9d2..0bdc0ec 100644
--- a/backports/drivers/media/platform/davinci/Kconfig
+++ b/backports/drivers/media/platform/davinci/Kconfig
@@ -1,11 +1,12 @@
-config VIDEO_DAVINCI_VPIF_DISPLAY
+config BACKPORT_VIDEO_DAVINCI_VPIF_DISPLAY
 	tristate "TI DaVinci VPIF V4L2-Display driver"
-	depends on VIDEO_V4L2
+	depends on !VIDEO_DAVINCI_VPIF_DISPLAY
+	depends on BACKPORT_VIDEO_V4L2
 	depends on ARCH_DAVINCI || COMPILE_TEST
 	depends on HAS_DMA
-	select VIDEOBUF2_DMA_CONTIG
-	select VIDEO_ADV7343 if MEDIA_SUBDRV_AUTOSELECT
-	select VIDEO_THS7303 if MEDIA_SUBDRV_AUTOSELECT
+	select BACKPORT_VIDEOBUF2_DMA_CONTIG
+	select VIDEO_ADV7343 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select VIDEO_THS7303 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
 	help
 	  Enables Davinci VPIF module used for display devices.
 	  This module is used for display on TI DM6467/DA850/OMAPL138
@@ -14,12 +15,13 @@ config VIDEO_DAVINCI_VPIF_DISPLAY
 	  To compile this driver as a module, choose M here. There will
 	  be two modules called vpif.ko and vpif_display.ko
 
-config VIDEO_DAVINCI_VPIF_CAPTURE
+config BACKPORT_VIDEO_DAVINCI_VPIF_CAPTURE
 	tristate "TI DaVinci VPIF video capture driver"
-	depends on VIDEO_V4L2
+	depends on !VIDEO_DAVINCI_VPIF_CAPTURE
+	depends on BACKPORT_VIDEO_V4L2
 	depends on ARCH_DAVINCI || COMPILE_TEST
 	depends on HAS_DMA
-	select VIDEOBUF2_DMA_CONTIG
+	select BACKPORT_VIDEOBUF2_DMA_CONTIG
 	help
 	  Enables Davinci VPIF module used for capture devices.
 	  This module is used for capture on TI DM6467/DA850/OMAPL138
@@ -28,15 +30,16 @@ config VIDEO_DAVINCI_VPIF_CAPTURE
 	  To compile this driver as a module, choose M here. There will
 	  be two modules called vpif.ko and vpif_capture.ko
 
-config VIDEO_DM6446_CCDC
+config BACKPORT_VIDEO_DM6446_CCDC
 	tristate "TI DM6446 CCDC video capture driver"
-	depends on VIDEO_V4L2
+	depends on !VIDEO_DM6446_CCDC
+	depends on BACKPORT_VIDEO_V4L2
 	depends on ARCH_DAVINCI || COMPILE_TEST
 	depends on HAS_DMA
-	select VIDEOBUF_DMA_CONTIG
+	select BACKPORT_VIDEOBUF_DMA_CONTIG
 	help
 	   Enables DaVinci CCD hw module. DaVinci CCDC hw interfaces
-	   with decoder modules such as TVP5146 over BT656 or
+	   with decoder modules such as TVP5146 over BACKPORT_BT656 or
 	   sensor module such as MT9T001 over a raw interface. This
 	   module configures the interface and CCDC/ISIF to do
 	   video frame capture from slave decoders.
@@ -44,15 +47,16 @@ config VIDEO_DM6446_CCDC
 	   To compile this driver as a module, choose M here. There will
 	   be three modules called vpfe_capture.ko, vpss.ko and dm644x_ccdc.ko
 
-config VIDEO_DM355_CCDC
+config BACKPORT_VIDEO_DM355_CCDC
 	tristate "TI DM355 CCDC video capture driver"
-	depends on VIDEO_V4L2
+	depends on !VIDEO_DM355_CCDC
+	depends on BACKPORT_VIDEO_V4L2
 	depends on ARCH_DAVINCI || COMPILE_TEST
 	depends on HAS_DMA
-	select VIDEOBUF_DMA_CONTIG
+	select BACKPORT_VIDEOBUF_DMA_CONTIG
 	help
 	   Enables DM355 CCD hw module. DM355 CCDC hw interfaces
-	   with decoder modules such as TVP5146 over BT656 or
+	   with decoder modules such as TVP5146 over BACKPORT_BT656 or
 	   sensor module such as MT9T001 over a raw interface. This
 	   module configures the interface and CCDC/ISIF to do
 	   video frame capture from a slave decoders
@@ -60,11 +64,12 @@ config VIDEO_DM355_CCDC
 	   To compile this driver as a module, choose M here. There will
 	   be three modules called vpfe_capture.ko, vpss.ko and dm355_ccdc.ko
 
-config VIDEO_DM365_ISIF
+config BACKPORT_VIDEO_DM365_ISIF
 	tristate "TI DM365 ISIF video capture driver"
-	depends on VIDEO_V4L2 && ARCH_DAVINCI
+	depends on !VIDEO_DM365_ISIF
+	depends on BACKPORT_VIDEO_V4L2 && ARCH_DAVINCI
 	depends on HAS_DMA
-	select VIDEOBUF_DMA_CONTIG
+	select BACKPORT_VIDEOBUF_DMA_CONTIG
 	help
 	   Enables ISIF hw module. This is the hardware module for
 	   configuring ISIF in VPFE to capture Raw Bayer RGB data from
@@ -73,11 +78,12 @@ config VIDEO_DM365_ISIF
 	   To compile this driver as a module, choose M here. There will
 	   be three modules called vpfe_capture.ko, vpss.ko and isif.ko
 
-config VIDEO_DAVINCI_VPBE_DISPLAY
+config BACKPORT_VIDEO_DAVINCI_VPBE_DISPLAY
 	tristate "TI DaVinci VPBE V4L2-Display driver"
-	depends on VIDEO_V4L2 && ARCH_DAVINCI
+	depends on !VIDEO_DAVINCI_VPBE_DISPLAY
+	depends on BACKPORT_VIDEO_V4L2 && ARCH_DAVINCI
 	depends on HAS_DMA
-	select VIDEOBUF2_DMA_CONTIG
+	select BACKPORT_VIDEOBUF2_DMA_CONTIG
 	help
 	    Enables Davinci VPBE module used for display devices.
 	    This module is used for display on TI DM644x/DM365/DM355
diff --git a/backports/drivers/media/platform/exynos4-is/Kconfig b/backports/drivers/media/platform/exynos4-is/Kconfig
index b7b2e472..0772f578 100644
--- a/backports/drivers/media/platform/exynos4-is/Kconfig
+++ b/backports/drivers/media/platform/exynos4-is/Kconfig
@@ -1,26 +1,29 @@
 
-config VIDEO_SAMSUNG_EXYNOS4_IS
+config BACKPORT_VIDEO_SAMSUNG_EXYNOS4_IS
 	bool "Samsung S5P/EXYNOS4 SoC series Camera Subsystem driver"
-	depends on VIDEO_V4L2 && VIDEO_V4L2_SUBDEV_API
+	depends on !VIDEO_SAMSUNG_EXYNOS4_IS
+	depends on VIDEO_V4L2 && BACKPORT_VIDEO_V4L2_SUBDEV_API
 	depends on ARCH_S5PV210 || ARCH_EXYNOS || COMPILE_TEST
 	depends on OF && COMMON_CLK
 	help
 	  Say Y here to enable camera host interface devices for
 	  Samsung S5P and EXYNOS SoC series.
 
-if VIDEO_SAMSUNG_EXYNOS4_IS
+if BACKPORT_VIDEO_SAMSUNG_EXYNOS4_IS
 
-config VIDEO_EXYNOS4_IS_COMMON
+config BACKPORT_VIDEO_EXYNOS4_IS_COMMON
        tristate
+	depends on !VIDEO_EXYNOS4_IS_COMMON
 
-config VIDEO_S5P_FIMC
+config BACKPORT_VIDEO_S5P_FIMC
 	tristate "S5P/EXYNOS4 FIMC/CAMIF camera interface driver"
+	depends on !VIDEO_S5P_FIMC
 	depends on I2C
 	depends on HAS_DMA
-	select VIDEOBUF2_DMA_CONTIG
-	select V4L2_MEM2MEM_DEV
+	select BACKPORT_VIDEOBUF2_DMA_CONTIG
+	select BACKPORT_V4L2_MEM2MEM_DEV
 	select MFD_SYSCON
-	select VIDEO_EXYNOS4_IS_COMMON
+	select BACKPORT_VIDEO_EXYNOS4_IS_COMMON
 	help
 	  This is a V4L2 driver for Samsung S5P and EXYNOS4 SoC camera host
 	  interface and video postprocessor (FIMC) devices.
@@ -28,8 +31,9 @@ config VIDEO_S5P_FIMC
 	  To compile this driver as a module, choose M here: the
 	  module will be called s5p-fimc.
 
-config VIDEO_S5P_MIPI_CSIS
+config BACKPORT_VIDEO_S5P_MIPI_CSIS
 	tristate "S5P/EXYNOS MIPI-CSI2 receiver (MIPI-CSIS) driver"
+	depends on !VIDEO_S5P_MIPI_CSIS
 	depends on REGULATOR
 	select GENERIC_PHY
 	help
@@ -41,12 +45,13 @@ config VIDEO_S5P_MIPI_CSIS
 
 if SOC_EXYNOS4212 || SOC_EXYNOS4412 || SOC_EXYNOS5250
 
-config VIDEO_EXYNOS_FIMC_LITE
+config BACKPORT_VIDEO_EXYNOS_FIMC_LITE
 	tristate "EXYNOS FIMC-LITE camera interface driver"
+	depends on !VIDEO_EXYNOS_FIMC_LITE
 	depends on I2C
 	depends on HAS_DMA
-	select VIDEOBUF2_DMA_CONTIG
-	select VIDEO_EXYNOS4_IS_COMMON
+	select BACKPORT_VIDEOBUF2_DMA_CONTIG
+	select BACKPORT_VIDEO_EXYNOS4_IS_COMMON
 	help
 	  This is a V4L2 driver for Samsung EXYNOS4/5 SoC FIMC-LITE camera
 	  host interface.
@@ -55,10 +60,11 @@ config VIDEO_EXYNOS_FIMC_LITE
 	  module will be called exynos-fimc-lite.
 endif
 
-config VIDEO_EXYNOS4_FIMC_IS
+config BACKPORT_VIDEO_EXYNOS4_FIMC_IS
 	tristate "EXYNOS4x12 FIMC-IS (Imaging Subsystem) driver"
+	depends on !VIDEO_EXYNOS4_FIMC_IS
 	depends on HAS_DMA
-	select VIDEOBUF2_DMA_CONTIG
+	select BACKPORT_VIDEOBUF2_DMA_CONTIG
 	depends on OF
 	select FW_LOADER
 	help
@@ -68,13 +74,14 @@ config VIDEO_EXYNOS4_FIMC_IS
 	  To compile this driver as a module, choose M here: the
 	  module will be called exynos4-fimc-is.
 
-config VIDEO_EXYNOS4_ISP_DMA_CAPTURE
+config BACKPORT_VIDEO_EXYNOS4_ISP_DMA_CAPTURE
 	bool "EXYNOS4x12 FIMC-IS ISP Direct DMA capture support"
-	depends on VIDEO_EXYNOS4_FIMC_IS
-	select VIDEO_EXYNOS4_IS_COMMON
+	depends on !VIDEO_EXYNOS4_ISP_DMA_CAPTURE
+	depends on BACKPORT_VIDEO_EXYNOS4_FIMC_IS
+	select BACKPORT_VIDEO_EXYNOS4_IS_COMMON
 	default y
 	  help
 	  This option enables an additional video device node exposing a V4L2
 	  video capture interface for the FIMC-IS ISP raw (Bayer) capture DMA.
 
-endif # VIDEO_SAMSUNG_EXYNOS4_IS
+endif # BACKPORT_VIDEO_SAMSUNG_EXYNOS4_IS
diff --git a/backports/drivers/media/platform/marvell-ccic/Kconfig b/backports/drivers/media/platform/marvell-ccic/Kconfig
index 7797b89..338b433 100644
--- a/backports/drivers/media/platform/marvell-ccic/Kconfig
+++ b/backports/drivers/media/platform/marvell-ccic/Kconfig
@@ -1,23 +1,25 @@
-config VIDEO_CAFE_CCIC
+config BACKPORT_VIDEO_CAFE_CCIC
 	tristate "Marvell 88ALP01 (Cafe) CMOS Camera Controller support"
-	depends on PCI && I2C && VIDEO_V4L2
+	depends on !VIDEO_CAFE_CCIC
+	depends on PCI && I2C && BACKPORT_VIDEO_V4L2
 	depends on HAS_DMA
-	select VIDEO_OV7670
-	select VIDEOBUF2_VMALLOC
-	select VIDEOBUF2_DMA_CONTIG
-	select VIDEOBUF2_DMA_SG
+	select BACKPORT_VIDEO_OV7670
+	select BACKPORT_VIDEOBUF2_VMALLOC
+	select BACKPORT_VIDEOBUF2_DMA_CONTIG
+	select BACKPORT_VIDEOBUF2_DMA_SG
 	---help---
 	  This is a video4linux2 driver for the Marvell 88ALP01 integrated
 	  CMOS camera controller.  This is the controller found on first-
 	  generation OLPC systems.
 
-config VIDEO_MMP_CAMERA
+config BACKPORT_VIDEO_MMP_CAMERA
 	tristate "Marvell Armada 610 integrated camera controller support"
-	depends on ARCH_MMP && I2C && VIDEO_V4L2
+	depends on !VIDEO_MMP_CAMERA
+	depends on ARCH_MMP && I2C && BACKPORT_VIDEO_V4L2
 	depends on HAS_DMA && BROKEN
-	select VIDEO_OV7670
+	select BACKPORT_VIDEO_OV7670
 	select I2C_GPIO
-	select VIDEOBUF2_DMA_SG
+	select BACKPORT_VIDEOBUF2_DMA_SG
 	---help---
 	  This is a Video4Linux2 driver for the integrated camera
 	  controller found on Marvell Armada 610 application
diff --git a/backports/drivers/media/platform/omap/Kconfig b/backports/drivers/media/platform/omap/Kconfig
index dc2aaab..09fad85 100644
--- a/backports/drivers/media/platform/omap/Kconfig
+++ b/backports/drivers/media/platform/omap/Kconfig
@@ -1,15 +1,17 @@
-config VIDEO_OMAP2_VOUT_VRFB
+config BACKPORT_VIDEO_OMAP2_VOUT_VRFB
 	bool
+	depends on !VIDEO_OMAP2_VOUT_VRFB
 
-config VIDEO_OMAP2_VOUT
+config BACKPORT_VIDEO_OMAP2_VOUT
 	tristate "OMAP2/OMAP3 V4L2-Display driver"
+	depends on !VIDEO_OMAP2_VOUT
 	depends on MMU
 	depends on ARCH_OMAP2 || ARCH_OMAP3
-	select VIDEOBUF_GEN
-	select VIDEOBUF_DMA_CONTIG
+	select BACKPORT_VIDEOBUF_GEN
+	select BACKPORT_VIDEOBUF_DMA_CONTIG
 	select OMAP2_DSS if HAS_IOMEM && ARCH_OMAP2PLUS
 	select OMAP2_VRFB if ARCH_OMAP2 || ARCH_OMAP3
-	select VIDEO_OMAP2_VOUT_VRFB if VIDEO_OMAP2_VOUT && OMAP2_VRFB
+	select BACKPORT_VIDEO_OMAP2_VOUT_VRFB if VIDEO_OMAP2_VOUT && OMAP2_VRFB
 	default n
 	---help---
 	  V4L2 Display driver support for OMAP2/3 based boards.
diff --git a/backports/drivers/media/platform/s5p-tv/Kconfig b/backports/drivers/media/platform/s5p-tv/Kconfig
index 697aaed..88a411f 100644
--- a/backports/drivers/media/platform/s5p-tv/Kconfig
+++ b/backports/drivers/media/platform/s5p-tv/Kconfig
@@ -6,8 +6,9 @@
 #
 # Licensed under GPL
 
-config VIDEO_SAMSUNG_S5P_TV
+config BACKPORT_VIDEO_SAMSUNG_S5P_TV
 	bool "Samsung TV driver for S5P platform"
+	depends on !VIDEO_SAMSUNG_S5P_TV
 	depends on PM
 	depends on ARCH_S5PV210 || ARCH_EXYNOS || COMPILE_TEST
 	default n
@@ -15,14 +16,15 @@ config VIDEO_SAMSUNG_S5P_TV
 	  Say Y here to enable selecting the TV output devices for
 	  Samsung S5P platform.
 
-if VIDEO_SAMSUNG_S5P_TV
+if BACKPORT_VIDEO_SAMSUNG_S5P_TV
 
-config VIDEO_SAMSUNG_S5P_HDMI
+config BACKPORT_VIDEO_SAMSUNG_S5P_HDMI
 	tristate "Samsung HDMI Driver"
-	depends on VIDEO_V4L2
+	depends on !VIDEO_SAMSUNG_S5P_HDMI
+	depends on BACKPORT_VIDEO_V4L2
 	depends on I2C
-	depends on VIDEO_SAMSUNG_S5P_TV
-	select VIDEO_SAMSUNG_S5P_HDMIPHY
+	depends on BACKPORT_VIDEO_SAMSUNG_S5P_TV
+	select BACKPORT_VIDEO_SAMSUNG_S5P_HDMIPHY
 	help
 	  Say Y here if you want support for the HDMI output
 	  interface in S5P Samsung SoC. The driver can be compiled
@@ -30,37 +32,41 @@ config VIDEO_SAMSUNG_S5P_HDMI
 	  subdev for use by other drivers. This driver requires
 	  hdmiphy driver to work correctly.
 
-config VIDEO_SAMSUNG_S5P_HDMI_DEBUG
+config BACKPORT_VIDEO_SAMSUNG_S5P_HDMI_DEBUG
 	bool "Enable debug for HDMI Driver"
-	depends on VIDEO_SAMSUNG_S5P_HDMI
+	depends on !VIDEO_SAMSUNG_S5P_HDMI_DEBUG
+	depends on BACKPORT_VIDEO_SAMSUNG_S5P_HDMI
 	default n
 	help
 	  Enables debugging for HDMI driver.
 
-config VIDEO_SAMSUNG_S5P_HDMIPHY
+config BACKPORT_VIDEO_SAMSUNG_S5P_HDMIPHY
 	tristate "Samsung HDMIPHY Driver"
-	depends on VIDEO_DEV && VIDEO_V4L2 && I2C
-	depends on VIDEO_SAMSUNG_S5P_TV
+	depends on !VIDEO_SAMSUNG_S5P_HDMIPHY
+	depends on BACKPORT_VIDEO_DEV && VIDEO_V4L2 && I2C
+	depends on BACKPORT_VIDEO_SAMSUNG_S5P_TV
 	help
 	  Say Y here if you want support for the physical HDMI
 	  interface in S5P Samsung SoC. The driver can be compiled
 	  as module. It is an I2C driver, that exposes a V4L2
 	  subdev for use by other drivers.
 
-config VIDEO_SAMSUNG_S5P_SII9234
+config BACKPORT_VIDEO_SAMSUNG_S5P_SII9234
 	tristate "Samsung SII9234 Driver"
-	depends on VIDEO_DEV && VIDEO_V4L2 && I2C
-	depends on VIDEO_SAMSUNG_S5P_TV
+	depends on !VIDEO_SAMSUNG_S5P_SII9234
+	depends on BACKPORT_VIDEO_DEV && VIDEO_V4L2 && I2C
+	depends on BACKPORT_VIDEO_SAMSUNG_S5P_TV
 	help
 	  Say Y here if you want support for the MHL interface
 	  in S5P Samsung SoC. The driver can be compiled
 	  as module. It is an I2C driver, that exposes a V4L2
 	  subdev for use by other drivers.
 
-config VIDEO_SAMSUNG_S5P_SDO
+config BACKPORT_VIDEO_SAMSUNG_S5P_SDO
 	tristate "Samsung Analog TV Driver"
-	depends on VIDEO_DEV && VIDEO_V4L2
-	depends on VIDEO_SAMSUNG_S5P_TV
+	depends on !VIDEO_SAMSUNG_S5P_SDO
+	depends on BACKPORT_VIDEO_DEV && VIDEO_V4L2
+	depends on BACKPORT_VIDEO_SAMSUNG_S5P_TV
 	help
 	  Say Y here if you want support for the analog TV output
 	  interface in S5P Samsung SoC. The driver can be compiled
@@ -68,21 +74,23 @@ config VIDEO_SAMSUNG_S5P_SDO
 	  subdev for use by other drivers. This driver requires
 	  hdmiphy driver to work correctly.
 
-config VIDEO_SAMSUNG_S5P_MIXER
+config BACKPORT_VIDEO_SAMSUNG_S5P_MIXER
 	tristate "Samsung Mixer and Video Processor Driver"
-	depends on VIDEO_DEV && VIDEO_V4L2
-	depends on VIDEO_SAMSUNG_S5P_TV
+	depends on !VIDEO_SAMSUNG_S5P_MIXER
+	depends on BACKPORT_VIDEO_DEV && VIDEO_V4L2
+	depends on BACKPORT_VIDEO_SAMSUNG_S5P_TV
 	depends on HAS_DMA
-	select VIDEOBUF2_DMA_CONTIG
+	select BACKPORT_VIDEOBUF2_DMA_CONTIG
 	help
 	  Say Y here if you want support for the Mixer in Samsung S5P SoCs.
 	  This device produce image data to one of output interfaces.
 
-config VIDEO_SAMSUNG_S5P_MIXER_DEBUG
+config BACKPORT_VIDEO_SAMSUNG_S5P_MIXER_DEBUG
 	bool "Enable debug for Mixer Driver"
-	depends on VIDEO_SAMSUNG_S5P_MIXER
+	depends on !VIDEO_SAMSUNG_S5P_MIXER_DEBUG
+	depends on BACKPORT_VIDEO_SAMSUNG_S5P_MIXER
 	default n
 	help
 	  Enables debugging for Mixer driver.
 
-endif # VIDEO_SAMSUNG_S5P_TV
+endif # BACKPORT_VIDEO_SAMSUNG_S5P_TV
diff --git a/backports/drivers/media/platform/soc_camera/Kconfig b/backports/drivers/media/platform/soc_camera/Kconfig
index d89b542..398b4dd 100644
--- a/backports/drivers/media/platform/soc_camera/Kconfig
+++ b/backports/drivers/media/platform/soc_camera/Kconfig
@@ -1,90 +1,101 @@
-config SOC_CAMERA
+config BACKPORT_SOC_CAMERA
 	tristate "SoC camera support"
-	depends on VIDEO_V4L2 && HAS_DMA && I2C
-	select VIDEOBUF_GEN
-	select VIDEOBUF2_CORE
+	depends on !SOC_CAMERA
+	depends on BACKPORT_VIDEO_V4L2 && HAS_DMA && I2C
+	select BACKPORT_VIDEOBUF_GEN
+	select BACKPORT_VIDEOBUF2_CORE
 	help
 	  SoC Camera is a common API to several cameras, not connecting
 	  over a bus like PCI or USB. For example some i2c camera connected
 	  directly to the data bus of an SoC.
 
-config SOC_CAMERA_SCALE_CROP
+config BACKPORT_SOC_CAMERA_SCALE_CROP
 	tristate
+	depends on !SOC_CAMERA_SCALE_CROP
 
-config SOC_CAMERA_PLATFORM
+config BACKPORT_SOC_CAMERA_PLATFORM
 	tristate "platform camera support"
-	depends on SOC_CAMERA
+	depends on !SOC_CAMERA_PLATFORM
+	depends on BACKPORT_SOC_CAMERA
 	help
 	  This is a generic SoC camera platform driver, useful for testing
 
-config VIDEO_MX3
+config BACKPORT_VIDEO_MX3
 	tristate "i.MX3x Camera Sensor Interface driver"
-	depends on VIDEO_DEV && MX3_IPU && SOC_CAMERA
+	depends on !VIDEO_MX3
+	depends on BACKPORT_VIDEO_DEV && MX3_IPU && SOC_CAMERA
 	depends on MX3_IPU || COMPILE_TEST
 	depends on HAS_DMA
-	select VIDEOBUF2_DMA_CONTIG
+	select BACKPORT_VIDEOBUF2_DMA_CONTIG
 	---help---
 	  This is a v4l2 driver for the i.MX3x Camera Sensor Interface
 
-config VIDEO_PXA27x
+config BACKPORT_VIDEO_PXA27x
 	tristate "PXA27x Quick Capture Interface driver"
-	depends on VIDEO_DEV && PXA27x && SOC_CAMERA
-	select VIDEOBUF_DMA_SG
+	depends on !VIDEO_PXA27x
+	depends on BACKPORT_VIDEO_DEV && PXA27x && SOC_CAMERA
+	select BACKPORT_VIDEOBUF_DMA_SG
 	---help---
 	  This is a v4l2 driver for the PXA27x Quick Capture Interface
 
-config VIDEO_RCAR_VIN
+config BACKPORT_VIDEO_RCAR_VIN
 	tristate "R-Car Video Input (VIN) support"
-	depends on VIDEO_DEV && SOC_CAMERA
+	depends on !VIDEO_RCAR_VIN
+	depends on BACKPORT_VIDEO_DEV && SOC_CAMERA
 	depends on ARCH_SHMOBILE || COMPILE_TEST
 	depends on HAS_DMA
-	select VIDEOBUF2_DMA_CONTIG
-	select SOC_CAMERA_SCALE_CROP
+	select BACKPORT_VIDEOBUF2_DMA_CONTIG
+	select BACKPORT_SOC_CAMERA_SCALE_CROP
 	---help---
 	  This is a v4l2 driver for the R-Car VIN Interface
 
-config VIDEO_SH_MOBILE_CSI2
+config BACKPORT_VIDEO_SH_MOBILE_CSI2
 	tristate "SuperH Mobile MIPI CSI-2 Interface driver"
-	depends on VIDEO_DEV && SOC_CAMERA && HAVE_CLK
+	depends on !VIDEO_SH_MOBILE_CSI2
+	depends on BACKPORT_VIDEO_DEV && SOC_CAMERA && HAVE_CLK
 	depends on ARCH_SHMOBILE || SUPERH || COMPILE_TEST
 	---help---
 	  This is a v4l2 driver for the SuperH MIPI CSI-2 Interface
 
-config VIDEO_SH_MOBILE_CEU
+config BACKPORT_VIDEO_SH_MOBILE_CEU
 	tristate "SuperH Mobile CEU Interface driver"
-	depends on VIDEO_DEV && SOC_CAMERA && HAS_DMA && HAVE_CLK
+	depends on !VIDEO_SH_MOBILE_CEU
+	depends on BACKPORT_VIDEO_DEV && SOC_CAMERA && HAS_DMA && HAVE_CLK
 	depends on ARCH_SHMOBILE || SUPERH || COMPILE_TEST
 	depends on HAS_DMA
-	select VIDEOBUF2_DMA_CONTIG
-	select SOC_CAMERA_SCALE_CROP
+	select BACKPORT_VIDEOBUF2_DMA_CONTIG
+	select BACKPORT_SOC_CAMERA_SCALE_CROP
 	---help---
 	  This is a v4l2 driver for the SuperH Mobile CEU Interface
 
-config VIDEO_OMAP1
+config BACKPORT_VIDEO_OMAP1
 	tristate "OMAP1 Camera Interface driver"
-	depends on VIDEO_DEV && SOC_CAMERA
+	depends on !VIDEO_OMAP1
+	depends on BACKPORT_VIDEO_DEV && SOC_CAMERA
 	depends on ARCH_OMAP1
 	depends on HAS_DMA
-	select VIDEOBUF_DMA_CONTIG
-	select VIDEOBUF_DMA_SG
+	select BACKPORT_VIDEOBUF_DMA_CONTIG
+	select BACKPORT_VIDEOBUF_DMA_SG
 	---help---
 	  This is a v4l2 driver for the TI OMAP1 camera interface
 
-config VIDEO_MX2
+config BACKPORT_VIDEO_MX2
 	tristate "i.MX27 Camera Sensor Interface driver"
-	depends on VIDEO_DEV && SOC_CAMERA
+	depends on !VIDEO_MX2
+	depends on BACKPORT_VIDEO_DEV && SOC_CAMERA
 	depends on SOC_IMX27 || COMPILE_TEST
 	depends on HAS_DMA
-	select VIDEOBUF2_DMA_CONTIG
+	select BACKPORT_VIDEOBUF2_DMA_CONTIG
 	---help---
 	  This is a v4l2 driver for the i.MX27 Camera Sensor Interface
 
-config VIDEO_ATMEL_ISI
+config BACKPORT_VIDEO_ATMEL_ISI
 	tristate "ATMEL Image Sensor Interface (ISI) support"
-	depends on VIDEO_DEV && SOC_CAMERA
+	depends on !VIDEO_ATMEL_ISI
+	depends on BACKPORT_VIDEO_DEV && SOC_CAMERA
 	depends on ARCH_AT91 || COMPILE_TEST
 	depends on HAS_DMA
-	select VIDEOBUF2_DMA_CONTIG
+	select BACKPORT_VIDEOBUF2_DMA_CONTIG
 	---help---
-	  This module makes the ATMEL Image Sensor Interface available
+	  This module makes the BACKPORT_ATMEL Image Sensor Interface available
 	  as a v4l2 device.
diff --git a/backports/drivers/media/platform/vivid/Kconfig b/backports/drivers/media/platform/vivid/Kconfig
index c309093..05e4383 100644
--- a/backports/drivers/media/platform/vivid/Kconfig
+++ b/backports/drivers/media/platform/vivid/Kconfig
@@ -1,12 +1,13 @@
-config VIDEO_VIVID
+config BACKPORT_VIDEO_VIVID
 	tristate "Virtual Video Test Driver"
-	depends on VIDEO_DEV && VIDEO_V4L2 && !SPARC32 && !SPARC64 && FB
+	depends on !VIDEO_VIVID
+	depends on BACKPORT_VIDEO_DEV && VIDEO_V4L2 && !SPARC32 && !SPARC64 && FB
 	select FONT_SUPPORT
 	select FONT_8x16
 	select FB_CFB_FILLRECT
 	select FB_CFB_COPYAREA
 	select FB_CFB_IMAGEBLIT
-	select VIDEOBUF2_VMALLOC
+	select BACKPORT_VIDEOBUF2_VMALLOC
 	default n
 	---help---
 	  Enables a virtual video driver. This driver emulates a webcam,
diff --git a/backports/drivers/media/platform/xilinx/Kconfig b/backports/drivers/media/platform/xilinx/Kconfig
index d7324c7..39f9d7f 100644
--- a/backports/drivers/media/platform/xilinx/Kconfig
+++ b/backports/drivers/media/platform/xilinx/Kconfig
@@ -1,22 +1,25 @@
-config VIDEO_XILINX
+config BACKPORT_VIDEO_XILINX
 	tristate "Xilinx Video IP (EXPERIMENTAL)"
-	depends on VIDEO_V4L2 && VIDEO_V4L2_SUBDEV_API && OF
-	select VIDEOBUF2_DMA_CONTIG
+	depends on !VIDEO_XILINX
+	depends on VIDEO_V4L2 && BACKPORT_VIDEO_V4L2_SUBDEV_API && OF
+	select BACKPORT_VIDEOBUF2_DMA_CONTIG
 	---help---
 	  Driver for Xilinx Video IP Pipelines
 
-if VIDEO_XILINX
+if BACKPORT_VIDEO_XILINX
 
-config VIDEO_XILINX_TPG
+config BACKPORT_VIDEO_XILINX_TPG
 	tristate "Xilinx Video Test Pattern Generator"
-	depends on VIDEO_XILINX
-	select VIDEO_XILINX_VTC
+	depends on !VIDEO_XILINX_TPG
+	depends on BACKPORT_VIDEO_XILINX
+	select BACKPORT_VIDEO_XILINX_VTC
 	---help---
 	   Driver for the Xilinx Video Test Pattern Generator
 
-config VIDEO_XILINX_VTC
+config BACKPORT_VIDEO_XILINX_VTC
 	tristate "Xilinx Video Timing Controller"
-	depends on VIDEO_XILINX
+	depends on !VIDEO_XILINX_VTC
+	depends on BACKPORT_VIDEO_XILINX
 	---help---
 	   Driver for the Xilinx Video Timing Controller
 
diff --git a/backports/drivers/media/radio/Kconfig b/backports/drivers/media/radio/Kconfig
index 5badccc..1cb52ec 100644
--- a/backports/drivers/media/radio/Kconfig
+++ b/backports/drivers/media/radio/Kconfig
@@ -2,34 +2,39 @@
 # Multimedia Video device configuration
 #
 
-menuconfig RADIO_ADAPTERS
+menuconfig BACKPORT_RADIO_ADAPTERS
 	bool "Radio Adapters"
-	depends on VIDEO_V4L2
-	depends on MEDIA_RADIO_SUPPORT
+	depends on !RADIO_ADAPTERS
+	depends on BACKPORT_VIDEO_V4L2
+	depends on BACKPORT_MEDIA_RADIO_SUPPORT
 	default y
 	---help---
 	  Say Y here to enable selecting AM/FM radio adapters.
 
-if RADIO_ADAPTERS && VIDEO_V4L2
+if RADIO_ADAPTERS && BACKPORT_VIDEO_V4L2
 
-config RADIO_TEA575X
+config BACKPORT_RADIO_TEA575X
 	tristate
+	depends on !RADIO_TEA575X
 
-config RADIO_SI470X
+config BACKPORT_RADIO_SI470X
 	bool "Silicon Labs Si470x FM Radio Receiver support"
-	depends on VIDEO_V4L2
+	depends on !RADIO_SI470X
+	depends on BACKPORT_VIDEO_V4L2
 
 source "$BACKPORT_DIR/drivers/media/radio/si470x/Kconfig"
 
-config RADIO_SI4713
+config BACKPORT_RADIO_SI4713
 	tristate "Silicon Labs Si4713 FM Radio with RDS Transmitter support"
-	depends on VIDEO_V4L2
+	depends on !RADIO_SI4713
+	depends on BACKPORT_VIDEO_V4L2
 
 source "$BACKPORT_DIR/drivers/media/radio/si4713/Kconfig"
 
-config RADIO_SI476X
+config BACKPORT_RADIO_SI476X
 	tristate "Silicon Laboratories Si476x I2C FM Radio"
-	depends on I2C && VIDEO_V4L2
+	depends on !RADIO_SI476X
+	depends on I2C && BACKPORT_VIDEO_V4L2
 	depends on MFD_SI476X_CORE
 	depends on SND_SOC
 	select SND_SOC_SI476X
@@ -44,9 +49,10 @@ config RADIO_SI476X
 	  To compile this driver as a module, choose M here: the
 	  module will be called radio-si476x.
 
-config USB_MR800
+config BACKPORT_USB_MR800
 	tristate "AverMedia MR 800 USB FM radio support"
-	depends on USB && VIDEO_V4L2
+	depends on !USB_MR800
+	depends on USB && BACKPORT_VIDEO_V4L2
 	---help---
 	  Say Y here if you want to connect this type of radio to your
 	  computer's USB port. Note that the audio is not digital, and
@@ -56,9 +62,10 @@ config USB_MR800
 	  To compile this driver as a module, choose M here: the
 	  module will be called radio-mr800.
 
-config USB_DSBR
+config BACKPORT_USB_DSBR
 	tristate "D-Link/GemTek USB FM radio support"
-	depends on USB && VIDEO_V4L2
+	depends on !USB_DSBR
+	depends on USB && BACKPORT_VIDEO_V4L2
 	---help---
 	  Say Y here if you want to connect this type of radio to your
 	  computer's USB port. Note that the audio is not digital, and
@@ -68,10 +75,11 @@ config USB_DSBR
 	  To compile this driver as a module, choose M here: the
 	  module will be called dsbr100.
 
-config RADIO_MAXIRADIO
+config BACKPORT_RADIO_MAXIRADIO
 	tristate "Guillemot MAXI Radio FM 2000 radio"
-	depends on VIDEO_V4L2 && PCI
-	select RADIO_TEA575X
+	depends on !RADIO_MAXIRADIO
+	depends on BACKPORT_VIDEO_V4L2 && PCI
+	select BACKPORT_RADIO_TEA575X
 	---help---
 	  Choose Y here if you have this radio card.  This card may also be
 	  found as Gemtek PCI FM.
@@ -84,10 +92,11 @@ config RADIO_MAXIRADIO
 	  To compile this driver as a module, choose M here: the
 	  module will be called radio-maxiradio.
 
-config RADIO_SHARK
+config BACKPORT_RADIO_SHARK
 	tristate "Griffin radioSHARK USB radio receiver"
+	depends on !RADIO_SHARK
 	depends on USB
-	select RADIO_TEA575X
+	select BACKPORT_RADIO_TEA575X
 	---help---
 	  Choose Y here if you have this radio receiver.
 
@@ -102,8 +111,9 @@ config RADIO_SHARK
 	  To compile this driver as a module, choose M here: the
 	  module will be called radio-shark.
 
-config RADIO_SHARK2
+config BACKPORT_RADIO_SHARK2
 	tristate "Griffin radioSHARK2 USB radio receiver"
+	depends on !RADIO_SHARK2
 	depends on USB
 	---help---
 	  Choose Y here if you have this radio receiver.
@@ -119,9 +129,10 @@ config RADIO_SHARK2
 	  To compile this driver as a module, choose M here: the
 	  module will be called radio-shark2.
 
-config USB_KEENE
+config BACKPORT_USB_KEENE
 	tristate "Keene FM Transmitter USB support"
-	depends on USB && VIDEO_V4L2
+	depends on !USB_KEENE
+	depends on USB && BACKPORT_VIDEO_V4L2
 	---help---
 	  Say Y here if you want to connect this type of FM transmitter
 	  to your computer's USB port.
@@ -129,9 +140,10 @@ config USB_KEENE
 	  To compile this driver as a module, choose M here: the
 	  module will be called radio-keene.
 
-config USB_RAREMONO
+config BACKPORT_USB_RAREMONO
 	tristate "Thanko's Raremono AM/FM/SW radio support"
-	depends on USB && VIDEO_V4L2
+	depends on !USB_RAREMONO
+	depends on USB && BACKPORT_VIDEO_V4L2
 	---help---
 	  The 'Thanko's Raremono' device contains the Si4734 chip from Silicon Labs Inc.
 	  It is one of the very few or perhaps the only consumer USB radio device
@@ -143,9 +155,10 @@ config USB_RAREMONO
 	  To compile this driver as a module, choose M here: the
 	  module will be called radio-raremono.
 
-config USB_MA901
+config BACKPORT_USB_MA901
 	tristate "Masterkit MA901 USB FM radio support"
-	depends on USB && VIDEO_V4L2
+	depends on !USB_MA901
+	depends on USB && BACKPORT_VIDEO_V4L2
 	---help---
 	  Say Y here if you want to connect this type of radio to your
 	  computer's USB port. Note that the audio is not digital, and
@@ -155,9 +168,10 @@ config USB_MA901
 	  To compile this driver as a module, choose M here: the
 	  module will be called radio-ma901.
 
-config RADIO_TEA5764
+config BACKPORT_RADIO_TEA5764
 	tristate "TEA5764 I2C FM radio support"
-	depends on I2C && VIDEO_V4L2
+	depends on !RADIO_TEA5764
+	depends on I2C && BACKPORT_VIDEO_V4L2
 	---help---
 	  Say Y here if you want to use the TEA5764 FM chip found in
 	  EZX phones. This FM chip is present in EZX phones from Motorola,
@@ -166,17 +180,19 @@ config RADIO_TEA5764
 	  To compile this driver as a module, choose M here: the
 	  module will be called radio-tea5764.
 
-config RADIO_TEA5764_XTAL
+config BACKPORT_RADIO_TEA5764_XTAL
 	bool "TEA5764 crystal reference"
-	depends on RADIO_TEA5764=y
+	depends on !RADIO_TEA5764_XTAL
+	depends on BACKPORT_RADIO_TEA5764=y
 	default y
 	help
 	  Say Y here if TEA5764 have a 32768 Hz crystal in circuit, say N
 	  here if TEA5764 reference frequency is connected in FREQIN.
 
-config RADIO_SAA7706H
+config BACKPORT_RADIO_SAA7706H
 	tristate "SAA7706H Car Radio DSP"
-	depends on I2C && VIDEO_V4L2
+	depends on !RADIO_SAA7706H
+	depends on I2C && BACKPORT_VIDEO_V4L2
 	---help---
 	  Say Y here if you want to use the SAA7706H Car radio Digital
 	  Signal Processor, found for instance on the Russellville development
@@ -186,9 +202,10 @@ config RADIO_SAA7706H
 	  To compile this driver as a module, choose M here: the
 	  module will be called SAA7706H.
 
-config RADIO_TEF6862
+config BACKPORT_RADIO_TEF6862
 	tristate "TEF6862 Car Radio Enhanced Selectivity Tuner"
-	depends on I2C && VIDEO_V4L2
+	depends on !RADIO_TEF6862
+	depends on I2C && BACKPORT_VIDEO_V4L2
 	---help---
 	  Say Y here if you want to use the TEF6862 Car Radio Enhanced
 	  Selectivity Tuner, found for instance on the Russellville development
@@ -198,20 +215,22 @@ config RADIO_TEF6862
 	  To compile this driver as a module, choose M here: the
 	  module will be called TEF6862.
 
-config RADIO_TIMBERDALE
+config BACKPORT_RADIO_TIMBERDALE
 	tristate "Enable the Timberdale radio driver"
-	depends on MFD_TIMBERDALE && VIDEO_V4L2
-	depends on I2C	# for RADIO_SAA7706H
-	select RADIO_TEF6862
-	select RADIO_SAA7706H
+	depends on !RADIO_TIMBERDALE
+	depends on MFD_TIMBERDALE && BACKPORT_VIDEO_V4L2
+	depends on I2C	# for BACKPORT_RADIO_SAA7706H
+	select BACKPORT_RADIO_TEF6862
+	select BACKPORT_RADIO_SAA7706H
 	---help---
 	  This is a kind of umbrella driver for the Radio Tuner and DSP
 	  found behind the Timberdale FPGA on the Russellville board.
 	  Enabling this driver will automatically select the DSP and tuner.
 
-config RADIO_WL1273
+config BACKPORT_RADIO_WL1273
 	tristate "Texas Instruments WL1273 I2C FM Radio"
-	depends on I2C && VIDEO_V4L2
+	depends on !RADIO_WL1273
+	depends on I2C && BACKPORT_VIDEO_V4L2
 	select MFD_CORE
 	select MFD_WL1273_CORE
 	select FW_LOADER
@@ -233,22 +252,25 @@ source "$BACKPORT_DIR/drivers/media/radio/wl128x/Kconfig"
 # ISA drivers configuration
 #
 
-menuconfig V4L_RADIO_ISA_DRIVERS
+menuconfig BACKPORT_V4L_RADIO_ISA_DRIVERS
 	bool "ISA radio devices"
+	depends on !V4L_RADIO_ISA_DRIVERS
 	depends on ISA
 	default n
 	---help---
 	  Say Y here to enable support for these ISA drivers.
 
-if V4L_RADIO_ISA_DRIVERS
+if BACKPORT_V4L_RADIO_ISA_DRIVERS
 
-config RADIO_ISA
+config BACKPORT_RADIO_ISA
 	depends on ISA
+	depends on !RADIO_ISA
 	tristate
 
-config RADIO_CADET
+config BACKPORT_RADIO_CADET
 	tristate "ADS Cadet AM/FM Tuner"
-	depends on ISA && VIDEO_V4L2
+	depends on !RADIO_CADET
+	depends on ISA && BACKPORT_VIDEO_V4L2
 	---help---
 	  Choose Y here if you have one of these AM/FM radio cards, and then
 	  fill in the port address below.
@@ -256,10 +278,11 @@ config RADIO_CADET
 	  To compile this driver as a module, choose M here: the
 	  module will be called radio-cadet.
 
-config RADIO_RTRACK
+config BACKPORT_RADIO_RTRACK
 	tristate "AIMSlab RadioTrack (aka RadioReveal) support"
-	depends on ISA && VIDEO_V4L2
-	select RADIO_ISA
+	depends on !RADIO_RTRACK
+	depends on ISA && BACKPORT_VIDEO_V4L2
+	select BACKPORT_RADIO_ISA
 	---help---
 	  Choose Y here if you have one of these FM radio cards, and then fill
 	  in the port address below.
@@ -279,18 +302,20 @@ config RADIO_RTRACK
 	  To compile this driver as a module, choose M here: the
 	  module will be called radio-aimslab.
 
-config RADIO_RTRACK_PORT
+config BACKPORT_RADIO_RTRACK_PORT
 	hex "RadioTrack i/o port (0x20f or 0x30f)"
-	depends on RADIO_RTRACK=y
+	depends on !RADIO_RTRACK_PORT
+	depends on BACKPORT_RADIO_RTRACK=y
 	default "30f"
 	help
 	  Enter either 0x30f or 0x20f here.  The card default is 0x30f, if you
 	  haven't changed the jumper setting on the card.
 
-config RADIO_RTRACK2
+config BACKPORT_RADIO_RTRACK2
 	tristate "AIMSlab RadioTrack II support"
-	depends on ISA && VIDEO_V4L2
-	select RADIO_ISA
+	depends on !RADIO_RTRACK2
+	depends on ISA && BACKPORT_VIDEO_V4L2
+	select BACKPORT_RADIO_ISA
 	---help---
 	  Choose Y here if you have this FM radio card, and then fill in the
 	  port address below.
@@ -302,18 +327,20 @@ config RADIO_RTRACK2
 	  To compile this driver as a module, choose M here: the
 	  module will be called radio-rtrack2.
 
-config RADIO_RTRACK2_PORT
+config BACKPORT_RADIO_RTRACK2_PORT
 	hex "RadioTrack II i/o port (0x20c or 0x30c)"
-	depends on RADIO_RTRACK2=y
+	depends on !RADIO_RTRACK2_PORT
+	depends on BACKPORT_RADIO_RTRACK2=y
 	default "30c"
 	help
 	  Enter either 0x30c or 0x20c here.  The card default is 0x30c, if you
 	  haven't changed the jumper setting on the card.
 
-config RADIO_AZTECH
+config BACKPORT_RADIO_AZTECH
 	tristate "Aztech/Packard Bell Radio"
-	depends on ISA && VIDEO_V4L2
-	select RADIO_ISA
+	depends on !RADIO_AZTECH
+	depends on ISA && BACKPORT_VIDEO_V4L2
+	select BACKPORT_RADIO_ISA
 	---help---
 	  Choose Y here if you have one of these FM radio cards, and then fill
 	  in the port address below.
@@ -321,19 +348,21 @@ config RADIO_AZTECH
 	  To compile this driver as a module, choose M here: the
 	  module will be called radio-aztech.
 
-config RADIO_AZTECH_PORT
+config BACKPORT_RADIO_AZTECH_PORT
 	hex "Aztech/Packard Bell I/O port (0x350 or 0x358)"
-	depends on RADIO_AZTECH=y
+	depends on !RADIO_AZTECH_PORT
+	depends on BACKPORT_RADIO_AZTECH=y
 	default "350"
 	help
 	  Enter either 0x350 or 0x358 here.  The card default is 0x350, if you
 	  haven't changed the setting of jumper JP3 on the card.  Removing the
 	  jumper sets the card to 0x358.
 
-config RADIO_GEMTEK
+config BACKPORT_RADIO_GEMTEK
 	tristate "GemTek Radio card (or compatible) support"
-	depends on ISA && VIDEO_V4L2
-	select RADIO_ISA
+	depends on !RADIO_GEMTEK
+	depends on ISA && BACKPORT_VIDEO_V4L2
+	select BACKPORT_RADIO_ISA
 	---help---
 	  Choose Y here if you have this FM radio card, and then fill in the
 	  I/O port address and settings below. The following cards either have
@@ -346,9 +375,10 @@ config RADIO_GEMTEK
 	  To compile this driver as a module, choose M here: the
 	  module will be called radio-gemtek.
 
-config RADIO_GEMTEK_PORT
+config BACKPORT_RADIO_GEMTEK_PORT
 	hex "Fixed I/O port (0x20c, 0x30c, 0x24c, 0x34c, 0x248 or 0x28c)"
-	depends on RADIO_GEMTEK=y
+	depends on !RADIO_GEMTEK_PORT
+	depends on BACKPORT_RADIO_GEMTEK=y
 	default "34c"
 	help
 	  Enter either 0x20c, 0x30c, 0x24c, 0x34c, 0x248 or 0x28c here. The
@@ -361,18 +391,20 @@ config RADIO_GEMTEK_PORT
 	  If automatic I/O port probing is enabled this port will be used only
 	  in case of automatic probing failure, ie. as a fallback.
 
-config RADIO_GEMTEK_PROBE
+config BACKPORT_RADIO_GEMTEK_PROBE
 	bool "Automatic I/O port probing"
-	depends on RADIO_GEMTEK=y
+	depends on !RADIO_GEMTEK_PROBE
+	depends on BACKPORT_RADIO_GEMTEK=y
 	default y
 	help
 	  Say Y here to enable automatic probing for GemTek Radio card. The
 	  following ports will be probed: 0x20c, 0x30c, 0x24c, 0x34c, 0x248 and
 	  0x28c.
 
-config RADIO_MIROPCM20
+config BACKPORT_RADIO_MIROPCM20
 	tristate "miroSOUND PCM20 radio"
-	depends on ISA && ISA_DMA_API && VIDEO_V4L2 && SND
+	depends on !RADIO_MIROPCM20
+	depends on ISA && ISA_DMA_API && BACKPORT_VIDEO_V4L2 && SND
 	select SND_ISA
 	select SND_MIRO
 	---help---
@@ -384,29 +416,32 @@ config RADIO_MIROPCM20
 	  To compile this driver as a module, choose M here: the
 	  module will be called radio-miropcm20.
 
-config RADIO_SF16FMI
+config BACKPORT_RADIO_SF16FMI
 	tristate "SF16-FMI/SF16-FMP/SF16-FMD Radio"
-	depends on ISA && VIDEO_V4L2
+	depends on !RADIO_SF16FMI
+	depends on ISA && BACKPORT_VIDEO_V4L2
 	---help---
 	  Choose Y here if you have one of these FM radio cards.
 
 	  To compile this driver as a module, choose M here: the
 	  module will be called radio-sf16fmi.
 
-config RADIO_SF16FMR2
+config BACKPORT_RADIO_SF16FMR2
 	tristate "SF16-FMR2/SF16-FMD2 Radio"
-	depends on ISA && VIDEO_V4L2
-	select RADIO_TEA575X
+	depends on !RADIO_SF16FMR2
+	depends on ISA && BACKPORT_VIDEO_V4L2
+	select BACKPORT_RADIO_TEA575X
 	---help---
 	  Choose Y here if you have one of these FM radio cards.
 
 	  To compile this driver as a module, choose M here: the
 	  module will be called radio-sf16fmr2.
 
-config RADIO_TERRATEC
+config BACKPORT_RADIO_TERRATEC
 	tristate "TerraTec ActiveRadio ISA Standalone"
-	depends on ISA && VIDEO_V4L2
-	select RADIO_ISA
+	depends on !RADIO_TERRATEC
+	depends on ISA && BACKPORT_VIDEO_V4L2
+	select BACKPORT_RADIO_ISA
 	---help---
 	  Choose Y here if you have this FM radio card.
 
@@ -417,10 +452,11 @@ config RADIO_TERRATEC
 	  To compile this driver as a module, choose M here: the
 	  module will be called radio-terratec.
 
-config RADIO_TRUST
+config BACKPORT_RADIO_TRUST
 	tristate "Trust FM radio card"
-	depends on ISA && VIDEO_V4L2
-	select RADIO_ISA
+	depends on !RADIO_TRUST
+	depends on ISA && BACKPORT_VIDEO_V4L2
+	select BACKPORT_RADIO_ISA
 	help
 	  This is a driver for the Trust FM radio cards. Say Y if you have
 	  such a card and want to use it under Linux.
@@ -432,18 +468,20 @@ config RADIO_TRUST
 	  To compile this driver as a module, choose M here: the
 	  module will be called radio-trust.
 
-config RADIO_TRUST_PORT
+config BACKPORT_RADIO_TRUST_PORT
 	hex "Trust i/o port (usually 0x350 or 0x358)"
-	depends on RADIO_TRUST=y
+	depends on !RADIO_TRUST_PORT
+	depends on BACKPORT_RADIO_TRUST=y
 	default "350"
 	help
 	  Enter the I/O port of your Trust FM radio card. If unsure, try the
 	  values "0x350" or "0x358".
 
-config RADIO_TYPHOON
+config BACKPORT_RADIO_TYPHOON
 	tristate "Typhoon Radio (a.k.a. EcoRadio)"
-	depends on ISA && VIDEO_V4L2
-	select RADIO_ISA
+	depends on !RADIO_TYPHOON
+	depends on ISA && BACKPORT_VIDEO_V4L2
+	select BACKPORT_RADIO_ISA
 	---help---
 	  Choose Y here if you have one of these FM radio cards, and then fill
 	  in the port address and the frequency used for muting below.
@@ -455,16 +493,18 @@ config RADIO_TYPHOON
 	  To compile this driver as a module, choose M here: the
 	  module will be called radio-typhoon.
 
-config RADIO_TYPHOON_PORT
+config BACKPORT_RADIO_TYPHOON_PORT
 	hex "Typhoon I/O port (0x316 or 0x336)"
-	depends on RADIO_TYPHOON=y
+	depends on !RADIO_TYPHOON_PORT
+	depends on BACKPORT_RADIO_TYPHOON=y
 	default "316"
 	help
 	  Enter the I/O port of your Typhoon or EcoRadio radio card.
 
-config RADIO_TYPHOON_MUTEFREQ
+config BACKPORT_RADIO_TYPHOON_MUTEFREQ
 	int "Typhoon frequency set when muting the device (kHz)"
-	depends on RADIO_TYPHOON=y
+	depends on !RADIO_TYPHOON_MUTEFREQ
+	depends on BACKPORT_RADIO_TYPHOON=y
 	default "87500"
 	help
 	  Enter the frequency used for muting the radio. The device is never
@@ -474,10 +514,11 @@ config RADIO_TYPHOON_MUTEFREQ
 	  the device is muted. There should be no local radio station at that
 	  frequency.
 
-config RADIO_ZOLTRIX
+config BACKPORT_RADIO_ZOLTRIX
 	tristate "Zoltrix Radio"
-	depends on ISA && VIDEO_V4L2
-	select RADIO_ISA
+	depends on !RADIO_ZOLTRIX
+	depends on ISA && BACKPORT_VIDEO_V4L2
+	select BACKPORT_RADIO_ISA
 	---help---
 	  Choose Y here if you have one of these FM radio cards, and then fill
 	  in the port address below.
@@ -489,13 +530,14 @@ config RADIO_ZOLTRIX
 	  To compile this driver as a module, choose M here: the
 	  module will be called radio-zoltrix.
 
-config RADIO_ZOLTRIX_PORT
+config BACKPORT_RADIO_ZOLTRIX_PORT
 	hex "ZOLTRIX I/O port (0x20c or 0x30c)"
-	depends on RADIO_ZOLTRIX=y
+	depends on !RADIO_ZOLTRIX_PORT
+	depends on BACKPORT_RADIO_ZOLTRIX=y
 	default "20c"
 	help
 	  Enter the I/O port of your Zoltrix radio card.
 
-endif # V4L_RADIO_ISA_DRIVERS
+endif # BACKPORT_V4L_RADIO_ISA_DRIVERS
 
-endif # RADIO_ADAPTERS
+endif # BACKPORT_RADIO_ADAPTERS
diff --git a/backports/drivers/media/radio/si470x/Kconfig b/backports/drivers/media/radio/si470x/Kconfig
index a466654..2f2da44 100644
--- a/backports/drivers/media/radio/si470x/Kconfig
+++ b/backports/drivers/media/radio/si470x/Kconfig
@@ -1,6 +1,7 @@
-config USB_SI470X
+config BACKPORT_USB_SI470X
 	tristate "Silicon Labs Si470x FM Radio Receiver support with USB"
-	depends on USB && RADIO_SI470X
+	depends on !USB_SI470X
+	depends on USB && BACKPORT_RADIO_SI470X
 	---help---
 	  This is a driver for USB devices with the Silicon Labs SI470x
 	  chip. Currently these devices are known to work:
@@ -23,9 +24,10 @@ config USB_SI470X
 	  To compile this driver as a module, choose M here: the
 	  module will be called radio-usb-si470x.
 
-config I2C_SI470X
+config BACKPORT_I2C_SI470X
 	tristate "Silicon Labs Si470x FM Radio Receiver support with I2C"
-	depends on I2C && RADIO_SI470X && !USB_SI470X
+	depends on !I2C_SI470X
+	depends on I2C && BACKPORT_RADIO_SI470X && !USB_SI470X
 	---help---
 	  This is a driver for I2C devices with the Silicon Labs SI470x
 	  chip.
diff --git a/backports/drivers/media/radio/si4713/Kconfig b/backports/drivers/media/radio/si4713/Kconfig
index 9c8b887..6439f9a 100644
--- a/backports/drivers/media/radio/si4713/Kconfig
+++ b/backports/drivers/media/radio/si4713/Kconfig
@@ -1,7 +1,8 @@
-config USB_SI4713
+config BACKPORT_USB_SI4713
 	tristate "Silicon Labs Si4713 FM Radio Transmitter support with USB"
-	depends on USB && I2C && RADIO_SI4713
-	select I2C_SI4713
+	depends on !USB_SI4713
+	depends on USB && I2C && BACKPORT_RADIO_SI4713
+	select BACKPORT_I2C_SI4713
 	---help---
 	  This is a driver for USB devices with the Silicon Labs SI4713
 	  chip. Currently these devices are known to work.
@@ -13,10 +14,11 @@ config USB_SI4713
 	  To compile this driver as a module, choose M here: the
 	  module will be called radio-usb-si4713.
 
-config PLATFORM_SI4713
+config BACKPORT_PLATFORM_SI4713
 	tristate "Silicon Labs Si4713 FM Radio Transmitter support with I2C"
-	depends on I2C && RADIO_SI4713
-	select I2C_SI4713
+	depends on !PLATFORM_SI4713
+	depends on I2C && BACKPORT_RADIO_SI4713
+	select BACKPORT_I2C_SI4713
 	---help---
 	  This is a driver for I2C devices with the Silicon Labs SI4713
 	  chip.
@@ -27,9 +29,10 @@ config PLATFORM_SI4713
 	  To compile this driver as a module, choose M here: the
 	  module will be called radio-platform-si4713.
 
-config I2C_SI4713
+config BACKPORT_I2C_SI4713
 	tristate "Silicon Labs Si4713 FM Radio Transmitter support"
-	depends on I2C && RADIO_SI4713
+	depends on !I2C_SI4713
+	depends on I2C && BACKPORT_RADIO_SI4713
 	---help---
 	  Say Y here if you want support to Si4713 FM Radio Transmitter.
 	  This device can transmit audio through FM. It can transmit
diff --git a/backports/drivers/media/radio/wl128x/Kconfig b/backports/drivers/media/radio/wl128x/Kconfig
index 9d6574b..32ca691 100644
--- a/backports/drivers/media/radio/wl128x/Kconfig
+++ b/backports/drivers/media/radio/wl128x/Kconfig
@@ -2,9 +2,10 @@
 # TI's wl128x FM driver based on TI's ST driver.
 #
 menu "Texas Instruments WL128x FM driver (ST based)"
-config RADIO_WL128X
+config BACKPORT_RADIO_WL128X
 	tristate "Texas Instruments WL128x FM Radio"
-	depends on VIDEO_V4L2 && RFKILL && GPIOLIB && TTY
+	depends on !RADIO_WL128X
+	depends on BACKPORT_VIDEO_V4L2 && RFKILL && GPIOLIB && TTY
 	depends on TI_ST
 	help
 	Choose Y here if you have this FM radio chip.
diff --git a/backports/drivers/media/rc/Kconfig b/backports/drivers/media/rc/Kconfig
index 81f6e85..1293b83 100644
--- a/backports/drivers/media/rc/Kconfig
+++ b/backports/drivers/media/rc/Kconfig
@@ -1,42 +1,47 @@
-config RC_CORE
+config BACKPORT_RC_CORE
 	tristate
-	depends on MEDIA_RC_SUPPORT
+	depends on !RC_CORE
+	depends on BACKPORT_MEDIA_RC_SUPPORT
 	depends on INPUT
 	default y
 
 source "$BACKPORT_DIR/drivers/media/rc/keymaps/Kconfig"
 
-menuconfig RC_DECODERS
+menuconfig BACKPORT_RC_DECODERS
         bool "Remote controller decoders"
-	depends on RC_CORE
+	depends on !RC_DECODERS
+	depends on BACKPORT_RC_CORE
 	default y
 
-if RC_DECODERS
-config LIRC
+if BACKPORT_RC_DECODERS
+config BACKPORT_LIRC
 	tristate "LIRC interface driver"
-	depends on RC_CORE
+	depends on !LIRC
+	depends on BACKPORT_RC_CORE
 
 	---help---
 	   Enable this option to build the Linux Infrared Remote
-	   Control (LIRC) core device interface driver. The LIRC
+	   Control (LIRC) core device interface driver. The BACKPORT_LIRC
 	   interface passes raw IR to and from userspace, where the
-	   LIRC daemon handles protocol decoding for IR reception and
+	   BACKPORT_LIRC daemon handles protocol decoding for IR reception and
 	   encoding for IR transmitting (aka "blasting").
 
-config IR_LIRC_CODEC
+config BACKPORT_IR_LIRC_CODEC
 	tristate "Enable IR to LIRC bridge"
-	depends on RC_CORE
-	depends on LIRC
+	depends on !IR_LIRC_CODEC
+	depends on BACKPORT_RC_CORE
+	depends on BACKPORT_LIRC
 	default y
 
 	---help---
 	   Enable this option to pass raw IR to and from userspace via
-	   the LIRC interface.
+	   the BACKPORT_LIRC interface.
 
 
-config IR_NEC_DECODER
+config BACKPORT_IR_NEC_DECODER
 	tristate "Enable IR raw decoder for the NEC protocol"
-	depends on RC_CORE
+	depends on !IR_NEC_DECODER
+	depends on BACKPORT_RC_CORE
 	select BITREVERSE
 	default y
 
@@ -44,9 +49,10 @@ config IR_NEC_DECODER
 	   Enable this option if you have IR with NEC protocol, and
 	   if the IR is decoded in software
 
-config IR_RC5_DECODER
+config BACKPORT_IR_RC5_DECODER
 	tristate "Enable IR raw decoder for the RC-5 protocol"
-	depends on RC_CORE
+	depends on !IR_RC5_DECODER
+	depends on BACKPORT_RC_CORE
 	select BITREVERSE
 	default y
 
@@ -54,9 +60,10 @@ config IR_RC5_DECODER
 	   Enable this option if you have IR with RC-5 protocol, and
 	   if the IR is decoded in software
 
-config IR_RC6_DECODER
+config BACKPORT_IR_RC6_DECODER
 	tristate "Enable IR raw decoder for the RC6 protocol"
-	depends on RC_CORE
+	depends on !IR_RC6_DECODER
+	depends on BACKPORT_RC_CORE
 	select BITREVERSE
 	default y
 
@@ -64,9 +71,10 @@ config IR_RC6_DECODER
 	   Enable this option if you have an infrared remote control which
 	   uses the RC6 protocol, and you need software decoding support.
 
-config IR_JVC_DECODER
+config BACKPORT_IR_JVC_DECODER
 	tristate "Enable IR raw decoder for the JVC protocol"
-	depends on RC_CORE
+	depends on !IR_JVC_DECODER
+	depends on BACKPORT_RC_CORE
 	select BITREVERSE
 	default y
 
@@ -74,9 +82,10 @@ config IR_JVC_DECODER
 	   Enable this option if you have an infrared remote control which
 	   uses the JVC protocol, and you need software decoding support.
 
-config IR_SONY_DECODER
+config BACKPORT_IR_SONY_DECODER
 	tristate "Enable IR raw decoder for the Sony protocol"
-	depends on RC_CORE
+	depends on !IR_SONY_DECODER
+	depends on BACKPORT_RC_CORE
 	select BITREVERSE
 	default y
 
@@ -84,9 +93,10 @@ config IR_SONY_DECODER
 	   Enable this option if you have an infrared remote control which
 	   uses the Sony protocol, and you need software decoding support.
 
-config IR_SANYO_DECODER
+config BACKPORT_IR_SANYO_DECODER
 	tristate "Enable IR raw decoder for the Sanyo protocol"
-	depends on RC_CORE
+	depends on !IR_SANYO_DECODER
+	depends on BACKPORT_RC_CORE
 	default y
 
 	---help---
@@ -94,18 +104,20 @@ config IR_SANYO_DECODER
 	   uses the Sanyo protocol (Sanyo, Aiwa, Chinon remotes),
 	   and you need software decoding support.
 
-config IR_SHARP_DECODER
+config BACKPORT_IR_SHARP_DECODER
 	tristate "Enable IR raw decoder for the Sharp protocol"
-	depends on RC_CORE
+	depends on !IR_SHARP_DECODER
+	depends on BACKPORT_RC_CORE
 	default y
 
 	---help---
 	   Enable this option if you have an infrared remote control which
 	   uses the Sharp protocol, and you need software decoding support.
 
-config IR_MCE_KBD_DECODER
+config BACKPORT_IR_MCE_KBD_DECODER
 	tristate "Enable IR raw decoder for the MCE keyboard/mouse protocol"
-	depends on RC_CORE
+	depends on !IR_MCE_KBD_DECODER
+	depends on BACKPORT_RC_CORE
 	select BITREVERSE
 	default y
 
@@ -114,9 +126,10 @@ config IR_MCE_KBD_DECODER
 	   Windows Media Center Edition, which you would like to use with
 	   a raw IR receiver in your system.
 
-config IR_XMP_DECODER
+config BACKPORT_IR_XMP_DECODER
 	tristate "Enable IR raw decoder for the XMP protocol"
-	depends on RC_CORE
+	depends on !IR_XMP_DECODER
+	depends on BACKPORT_RC_CORE
 	select BITREVERSE
 	default y
 
@@ -125,16 +138,18 @@ config IR_XMP_DECODER
 	   if the IR is decoded in software
 endif #RC_DECODERS
 
-menuconfig RC_DEVICES
+menuconfig BACKPORT_RC_DEVICES
 	bool "Remote Controller devices"
-	depends on RC_CORE
+	depends on !RC_DEVICES
+	depends on BACKPORT_RC_CORE
 
-if RC_DEVICES
+if BACKPORT_RC_DEVICES
 
-config RC_ATI_REMOTE
+config BACKPORT_RC_ATI_REMOTE
 	tristate "ATI / X10 based USB RF remote controls"
+	depends on !RC_ATI_REMOTE
 	depends on USB_ARCH_HAS_HCD
-	depends on RC_CORE
+	depends on BACKPORT_RC_CORE
 	select USB
 	help
 	   Say Y here if you want to use an X10 based USB remote control.
@@ -150,10 +165,11 @@ config RC_ATI_REMOTE
 	   To compile this driver as a module, choose M here: the module will be
 	   called ati_remote.
 
-config IR_ENE
+config BACKPORT_IR_ENE
 	tristate "ENE eHome Receiver/Transceiver (pnp id: ENE0100/ENE02xxx)"
+	depends on !IR_ENE
 	depends on PNP
-	depends on RC_CORE
+	depends on BACKPORT_RC_CORE
 	---help---
 	   Say Y here to enable support for integrated infrared receiver
 	   /transceiver made by ENE.
@@ -164,9 +180,10 @@ config IR_ENE
 	   To compile this driver as a module, choose M here: the
 	   module will be called ene_ir.
 
-config IR_HIX5HD2
+config BACKPORT_IR_HIX5HD2
 	tristate "Hisilicon hix5hd2 IR remote control"
-	depends on RC_CORE
+	depends on !IR_HIX5HD2
+	depends on BACKPORT_RC_CORE
 	help
 	 Say Y here if you want to use hisilicon hix5hd2 remote control.
 	 To compile this driver as a module, choose M here: the module will be
@@ -174,10 +191,11 @@ config IR_HIX5HD2
 
 	 If you're not sure, select N here
 
-config IR_IMON
+config BACKPORT_IR_IMON
 	tristate "SoundGraph iMON Receiver and Display"
+	depends on !IR_IMON
 	depends on USB_ARCH_HAS_HCD
-	depends on RC_CORE
+	depends on BACKPORT_RC_CORE
 	select USB
 	---help---
 	   Say Y here if you want to use a SoundGraph iMON (aka Antec Veris)
@@ -186,10 +204,11 @@ config IR_IMON
 	   To compile this driver as a module, choose M here: the
 	   module will be called imon.
 
-config IR_MCEUSB
+config BACKPORT_IR_MCEUSB
 	tristate "Windows Media Center Ed. eHome Infrared Transceiver"
+	depends on !IR_MCEUSB
 	depends on USB_ARCH_HAS_HCD
-	depends on RC_CORE
+	depends on BACKPORT_RC_CORE
 	select USB
 	---help---
 	   Say Y here if you want to use a Windows Media Center Edition
@@ -198,10 +217,11 @@ config IR_MCEUSB
 	   To compile this driver as a module, choose M here: the
 	   module will be called mceusb.
 
-config IR_ITE_CIR
+config BACKPORT_IR_ITE_CIR
 	tristate "ITE Tech Inc. IT8712/IT8512 Consumer Infrared Transceiver"
+	depends on !IR_ITE_CIR
 	depends on PNP
-	depends on RC_CORE
+	depends on BACKPORT_RC_CORE
 	---help---
 	   Say Y here to enable support for integrated infrared receivers
 	   /transceivers made by ITE Tech Inc. These are found in
@@ -211,10 +231,11 @@ config IR_ITE_CIR
 	   To compile this driver as a module, choose M here: the
 	   module will be called ite-cir.
 
-config IR_FINTEK
+config BACKPORT_IR_FINTEK
 	tristate "Fintek Consumer Infrared Transceiver"
+	depends on !IR_FINTEK
 	depends on PNP
-	depends on RC_CORE
+	depends on BACKPORT_RC_CORE
 	---help---
 	   Say Y here to enable support for integrated infrared receiver
 	   /transciever made by Fintek. This chip is found on assorted
@@ -223,9 +244,10 @@ config IR_FINTEK
 	   To compile this driver as a module, choose M here: the
 	   module will be called fintek-cir.
 
-config IR_MESON
+config BACKPORT_IR_MESON
 	tristate "Amlogic Meson IR remote receiver"
-	depends on RC_CORE
+	depends on !IR_MESON
+	depends on BACKPORT_RC_CORE
 	depends on ARCH_MESON || COMPILE_TEST
 	---help---
 	   Say Y if you want to use the IR remote receiver available
@@ -234,10 +256,11 @@ config IR_MESON
 	   To compile this driver as a module, choose M here: the
 	   module will be called meson-ir.
 
-config IR_NUVOTON
+config BACKPORT_IR_NUVOTON
 	tristate "Nuvoton w836x7hg Consumer Infrared Transceiver"
+	depends on !IR_NUVOTON
 	depends on PNP
-	depends on RC_CORE
+	depends on BACKPORT_RC_CORE
 	---help---
 	   Say Y here to enable support for integrated infrared receiver
 	   /transciever made by Nuvoton (formerly Winbond). This chip is
@@ -247,10 +270,11 @@ config IR_NUVOTON
 	   To compile this driver as a module, choose M here: the
 	   module will be called nuvoton-cir.
 
-config IR_REDRAT3
+config BACKPORT_IR_REDRAT3
 	tristate "RedRat3 IR Transceiver"
+	depends on !IR_REDRAT3
 	depends on USB_ARCH_HAS_HCD
-	depends on RC_CORE
+	depends on BACKPORT_RC_CORE
 	select NEW_LEDS
 	select LEDS_CLASS
 	select USB
@@ -260,10 +284,11 @@ config IR_REDRAT3
 	   To compile this driver as a module, choose M here: the
 	   module will be called redrat3.
 
-config IR_STREAMZAP
+config BACKPORT_IR_STREAMZAP
 	tristate "Streamzap PC Remote IR Receiver"
+	depends on !IR_STREAMZAP
 	depends on USB_ARCH_HAS_HCD
-	depends on RC_CORE
+	depends on BACKPORT_RC_CORE
 	select USB
 	---help---
 	   Say Y here if you want to use a Streamzap PC Remote
@@ -272,10 +297,11 @@ config IR_STREAMZAP
 	   To compile this driver as a module, choose M here: the
 	   module will be called streamzap.
 
-config IR_WINBOND_CIR
+config BACKPORT_IR_WINBOND_CIR
 	tristate "Winbond IR remote control"
+	depends on !IR_WINBOND_CIR
 	depends on X86 && PNP
-	depends on RC_CORE
+	depends on BACKPORT_RC_CORE
 	select NEW_LEDS
 	select LEDS_CLASS
 	select BITREVERSE
@@ -288,10 +314,11 @@ config IR_WINBOND_CIR
 	   To compile this driver as a module, choose M here: the module will
 	   be called winbond_cir.
 
-config IR_IGORPLUGUSB
+config BACKPORT_IR_IGORPLUGUSB
 	tristate "IgorPlug-USB IR Receiver"
+	depends on !IR_IGORPLUGUSB
 	depends on USB_ARCH_HAS_HCD
-	depends on RC_CORE
+	depends on BACKPORT_RC_CORE
 	select USB
 	---help---
 	   Say Y here if you want to use the IgorPlug-USB IR Receiver by
@@ -303,10 +330,11 @@ config IR_IGORPLUGUSB
 	   To compile this driver as a module, choose M here: the module will
 	   be called igorplugusb.
 
-config IR_IGUANA
+config BACKPORT_IR_IGUANA
 	tristate "IguanaWorks USB IR Transceiver"
+	depends on !IR_IGUANA
 	depends on USB_ARCH_HAS_HCD
-	depends on RC_CORE
+	depends on BACKPORT_RC_CORE
 	select USB
 	---help---
 	   Say Y here if you want to use the IguanaWorks USB IR Transceiver.
@@ -319,10 +347,11 @@ config IR_IGUANA
 	   To compile this driver as a module, choose M here: the module will
 	   be called iguanair.
 
-config IR_TTUSBIR
+config BACKPORT_IR_TTUSBIR
 	tristate "TechnoTrend USB IR Receiver"
+	depends on !IR_TTUSBIR
 	depends on USB_ARCH_HAS_HCD
-	depends on RC_CORE
+	depends on BACKPORT_RC_CORE
 	select USB
 	select NEW_LEDS
 	select LEDS_CLASS
@@ -333,9 +362,10 @@ config IR_TTUSBIR
 	   To compile this driver as a module, choose M here: the module will
 	   be called ttusbir.
 
-config IR_RX51
+config BACKPORT_IR_RX51
 	tristate "Nokia N900 IR transmitter diode"
-	depends on OMAP_DM_TIMER && ARCH_OMAP2PLUS && LIRC && !ARCH_MULTIPLATFORM
+	depends on !IR_RX51
+	depends on OMAP_DM_TIMER && ARCH_OMAP2PLUS && BACKPORT_LIRC && !ARCH_MULTIPLATFORM
 	---help---
 	   Say Y or M here if you want to enable support for the IR
 	   transmitter diode built in the Nokia N900 (RX51) device.
@@ -345,9 +375,10 @@ config IR_RX51
 
 source "$BACKPORT_DIR/drivers/media/rc/img-ir/Kconfig"
 
-config RC_LOOPBACK
+config BACKPORT_RC_LOOPBACK
 	tristate "Remote Control Loopback Driver"
-	depends on RC_CORE
+	depends on !RC_LOOPBACK
+	depends on BACKPORT_RC_CORE
 	---help---
 	   Say Y here if you want support for the remote control loopback
 	   driver which allows TX data to be sent back as RX data.
@@ -358,29 +389,32 @@ config RC_LOOPBACK
 	   To compile this driver as a module, choose M here: the module will
 	   be called rc_loopback.
 
-config IR_GPIO_CIR
+config BACKPORT_IR_GPIO_CIR
 	tristate "GPIO IR remote control"
-	depends on RC_CORE
+	depends on !IR_GPIO_CIR
+	depends on BACKPORT_RC_CORE
 	---help---
 	   Say Y if you want to use GPIO based IR Receiver.
 
 	   To compile this driver as a module, choose M here: the module will
 	   be called gpio-ir-recv.
 
-config RC_ST
+config BACKPORT_RC_ST
 	tristate "ST remote control receiver"
-	depends on RC_CORE
+	depends on !RC_ST
+	depends on BACKPORT_RC_CORE
 	depends on ARCH_STI || COMPILE_TEST
 	help
 	 Say Y here if you want support for ST remote control driver
 	 which allows both IR and UHF RX.
-	 The driver passes raw pulse and space information to the LIRC decoder.
+	 The driver passes raw pulse and space information to the BACKPORT_LIRC decoder.
 
 	 If you're not sure, select N here.
 
-config IR_SUNXI
+config BACKPORT_IR_SUNXI
     tristate "SUNXI IR remote control"
-    depends on RC_CORE
+	depends on !IR_SUNXI
+    depends on BACKPORT_RC_CORE
     depends on ARCH_SUNXI || COMPILE_TEST
     ---help---
       Say Y if you want to use sunXi internal IR Controller
diff --git a/backports/drivers/media/rc/img-ir/Kconfig b/backports/drivers/media/rc/img-ir/Kconfig
index a896d3c..d08dd55 100644
--- a/backports/drivers/media/rc/img-ir/Kconfig
+++ b/backports/drivers/media/rc/img-ir/Kconfig
@@ -1,15 +1,17 @@
-config IR_IMG
+config BACKPORT_IR_IMG
 	tristate "ImgTec IR Decoder"
-	depends on RC_CORE
+	depends on !IR_IMG
+	depends on BACKPORT_RC_CORE
 	depends on METAG || MIPS || COMPILE_TEST
-	select IR_IMG_HW if !IR_IMG_RAW
+	select BACKPORT_IR_IMG_HW if !IR_IMG_RAW
 	help
 	   Say Y or M here if you want to use the ImgTec infrared decoder
 	   functionality found in SoCs such as TZ1090.
 
-config IR_IMG_RAW
+config BACKPORT_IR_IMG_RAW
 	bool "Raw decoder"
-	depends on IR_IMG
+	depends on !IR_IMG_RAW
+	depends on BACKPORT_IR_IMG
 	help
 	   Say Y here to enable the raw mode driver which passes raw IR signal
 	   changes to the IR raw decoders for software decoding. This is much
@@ -17,60 +19,68 @@ config IR_IMG_RAW
 	   processing power than using hardware decode, but can be useful for
 	   testing, debug, and to make more protocols available.
 
-config IR_IMG_HW
+config BACKPORT_IR_IMG_HW
 	bool "Hardware decoder"
-	depends on IR_IMG
+	depends on !IR_IMG_HW
+	depends on BACKPORT_IR_IMG
 	help
 	   Say Y here to enable the hardware decode driver which decodes the IR
 	   signals in hardware. This is more reliable, consumes less processing
 	   power since only a single interrupt is received for each scancode,
 	   and allows an IR scancode to be used as a wake event.
 
-config IR_IMG_NEC
+config BACKPORT_IR_IMG_NEC
 	bool "NEC protocol support"
-	depends on IR_IMG_HW
+	depends on !IR_IMG_NEC
+	depends on BACKPORT_IR_IMG_HW
 	help
 	   Say Y here to enable support for the NEC, extended NEC, and 32-bit
 	   NEC protocols in the ImgTec infrared decoder block.
 
-config IR_IMG_JVC
+config BACKPORT_IR_IMG_JVC
 	bool "JVC protocol support"
-	depends on IR_IMG_HW
+	depends on !IR_IMG_JVC
+	depends on BACKPORT_IR_IMG_HW
 	help
 	   Say Y here to enable support for the JVC protocol in the ImgTec
 	   infrared decoder block.
 
-config IR_IMG_SONY
+config BACKPORT_IR_IMG_SONY
 	bool "Sony protocol support"
-	depends on IR_IMG_HW
+	depends on !IR_IMG_SONY
+	depends on BACKPORT_IR_IMG_HW
 	help
 	   Say Y here to enable support for the Sony protocol in the ImgTec
 	   infrared decoder block.
 
-config IR_IMG_SHARP
+config BACKPORT_IR_IMG_SHARP
 	bool "Sharp protocol support"
-	depends on IR_IMG_HW
+	depends on !IR_IMG_SHARP
+	depends on BACKPORT_IR_IMG_HW
 	help
 	   Say Y here to enable support for the Sharp protocol in the ImgTec
 	   infrared decoder block.
 
-config IR_IMG_SANYO
+config BACKPORT_IR_IMG_SANYO
 	bool "Sanyo protocol support"
-	depends on IR_IMG_HW
+	depends on !IR_IMG_SANYO
+	depends on BACKPORT_IR_IMG_HW
 	help
 	   Say Y here to enable support for the Sanyo protocol (used by Sanyo,
 	   Aiwa, Chinon remotes) in the ImgTec infrared decoder block.
 
-config IR_IMG_RC5
+config BACKPORT_IR_IMG_RC5
 	bool "Philips RC5 protocol support"
-	depends on IR_IMG_HW
+	depends on !IR_IMG_RC5
+	depends on BACKPORT_IR_IMG_HW
 	help
 	   Say Y here to enable support for the RC5 protocol in the ImgTec
 	   infrared decoder block.
 
-config IR_IMG_RC6
+config BACKPORT_IR_IMG_RC6
 	bool "Philips RC6 protocol support"
-	depends on IR_IMG_HW
+	depends on !IR_IMG_RC6
+	depends on BACKPORT_IR_IMG_HW
 	help
 	   Say Y here to enable support for the RC6 protocol in the ImgTec
 	   infrared decoder block.
diff --git a/backports/drivers/media/rc/keymaps/Kconfig b/backports/drivers/media/rc/keymaps/Kconfig
index 767423b..8dd65ab 100644
--- a/backports/drivers/media/rc/keymaps/Kconfig
+++ b/backports/drivers/media/rc/keymaps/Kconfig
@@ -1,6 +1,7 @@
-config RC_MAP
+config BACKPORT_RC_MAP
 	tristate "Compile Remote Controller keymap modules"
-	depends on RC_CORE
+	depends on !RC_MAP
+	depends on BACKPORT_RC_CORE
 	default y
 
 	---help---
diff --git a/backports/drivers/media/tuners/Kconfig b/backports/drivers/media/tuners/Kconfig
index 983510d..d7e916e 100644
--- a/backports/drivers/media/tuners/Kconfig
+++ b/backports/drivers/media/tuners/Kconfig
@@ -1,276 +1,312 @@
 # Analog TV tuners, auto-loaded via tuner.ko
-config MEDIA_TUNER
+config BACKPORT_MEDIA_TUNER
 	tristate
+	depends on !MEDIA_TUNER
 	depends on (MEDIA_ANALOG_TV_SUPPORT || MEDIA_DIGITAL_TV_SUPPORT || MEDIA_RADIO_SUPPORT || MEDIA_SDR_SUPPORT) && I2C
 	default y
-	select MEDIA_TUNER_XC2028 if MEDIA_SUBDRV_AUTOSELECT
-	select MEDIA_TUNER_XC5000 if MEDIA_SUBDRV_AUTOSELECT
-	select MEDIA_TUNER_XC4000 if MEDIA_SUBDRV_AUTOSELECT
-	select MEDIA_TUNER_MT20XX if MEDIA_SUBDRV_AUTOSELECT
-	select MEDIA_TUNER_TDA8290 if MEDIA_SUBDRV_AUTOSELECT
-	select MEDIA_TUNER_TEA5761 if MEDIA_SUBDRV_AUTOSELECT && MEDIA_RADIO_SUPPORT
-	select MEDIA_TUNER_TEA5767 if MEDIA_SUBDRV_AUTOSELECT && MEDIA_RADIO_SUPPORT
-	select MEDIA_TUNER_SIMPLE if MEDIA_SUBDRV_AUTOSELECT
-	select MEDIA_TUNER_TDA9887 if MEDIA_SUBDRV_AUTOSELECT
-	select MEDIA_TUNER_MC44S803 if MEDIA_SUBDRV_AUTOSELECT
+	select MEDIA_TUNER_XC2028 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select MEDIA_TUNER_XC5000 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select MEDIA_TUNER_XC4000 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select MEDIA_TUNER_MT20XX if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select MEDIA_TUNER_TDA8290 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select MEDIA_TUNER_TEA5761 if MEDIA_SUBDRV_AUTOSELECT && BACKPORT_MEDIA_RADIO_SUPPORT
+	select MEDIA_TUNER_TEA5767 if MEDIA_SUBDRV_AUTOSELECT && BACKPORT_MEDIA_RADIO_SUPPORT
+	select MEDIA_TUNER_SIMPLE if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select MEDIA_TUNER_TDA9887 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select MEDIA_TUNER_MC44S803 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
 
 menu "Customize TV tuners"
 	visible if !MEDIA_SUBDRV_AUTOSELECT
-	depends on MEDIA_ANALOG_TV_SUPPORT || MEDIA_DIGITAL_TV_SUPPORT || MEDIA_RADIO_SUPPORT || MEDIA_SDR_SUPPORT
+	depends on BACKPORT_MEDIA_ANALOG_TV_SUPPORT || MEDIA_DIGITAL_TV_SUPPORT || MEDIA_RADIO_SUPPORT || MEDIA_SDR_SUPPORT
 
-config MEDIA_TUNER_SIMPLE
+config BACKPORT_MEDIA_TUNER_SIMPLE
 	tristate "Simple tuner support"
-	depends on MEDIA_SUPPORT && I2C
-	select MEDIA_TUNER_TDA9887
+	depends on !MEDIA_TUNER_SIMPLE
+	depends on BACKPORT_MEDIA_SUPPORT && I2C
+	select BACKPORT_MEDIA_TUNER_TDA9887
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  Say Y here to include support for various simple tuners.
 
-config MEDIA_TUNER_TDA8290
+config BACKPORT_MEDIA_TUNER_TDA8290
 	tristate "TDA 8290/8295 + 8275(a)/18271 tuner combo"
-	depends on MEDIA_SUPPORT && I2C
-	select MEDIA_TUNER_TDA827X
-	select MEDIA_TUNER_TDA18271
+	depends on !MEDIA_TUNER_TDA8290
+	depends on BACKPORT_MEDIA_SUPPORT && I2C
+	select BACKPORT_MEDIA_TUNER_TDA827X
+	select BACKPORT_MEDIA_TUNER_TDA18271
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  Say Y here to include support for Philips TDA8290+8275(a) tuner.
 
-config MEDIA_TUNER_TDA827X
+config BACKPORT_MEDIA_TUNER_TDA827X
 	tristate "Philips TDA827X silicon tuner"
-	depends on MEDIA_SUPPORT && I2C
+	depends on !MEDIA_TUNER_TDA827X
+	depends on BACKPORT_MEDIA_SUPPORT && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  A DVB-T silicon tuner module. Say Y when you want to support this tuner.
 
-config MEDIA_TUNER_TDA18271
+config BACKPORT_MEDIA_TUNER_TDA18271
 	tristate "NXP TDA18271 silicon tuner"
-	depends on MEDIA_SUPPORT && I2C
+	depends on !MEDIA_TUNER_TDA18271
+	depends on BACKPORT_MEDIA_SUPPORT && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  A silicon tuner module. Say Y when you want to support this tuner.
 
-config MEDIA_TUNER_TDA9887
+config BACKPORT_MEDIA_TUNER_TDA9887
 	tristate "TDA 9885/6/7 analog IF demodulator"
-	depends on MEDIA_SUPPORT && I2C
+	depends on !MEDIA_TUNER_TDA9887
+	depends on BACKPORT_MEDIA_SUPPORT && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  Say Y here to include support for Philips TDA9885/6/7
 	  analog IF demodulator.
 
-config MEDIA_TUNER_TEA5761
+config BACKPORT_MEDIA_TUNER_TEA5761
 	tristate "TEA 5761 radio tuner"
-	depends on MEDIA_SUPPORT && I2C
+	depends on !MEDIA_TUNER_TEA5761
+	depends on BACKPORT_MEDIA_SUPPORT && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  Say Y here to include support for the Philips TEA5761 radio tuner.
 
-config MEDIA_TUNER_TEA5767
+config BACKPORT_MEDIA_TUNER_TEA5767
 	tristate "TEA 5767 radio tuner"
-	depends on MEDIA_SUPPORT && I2C
+	depends on !MEDIA_TUNER_TEA5767
+	depends on BACKPORT_MEDIA_SUPPORT && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  Say Y here to include support for the Philips TEA5767 radio tuner.
 
-config MEDIA_TUNER_MSI001
+config BACKPORT_MEDIA_TUNER_MSI001
 	tristate "Mirics MSi001"
-	depends on MEDIA_SUPPORT && SPI && VIDEO_V4L2
+	depends on !MEDIA_TUNER_MSI001
+	depends on BACKPORT_MEDIA_SUPPORT && SPI && VIDEO_V4L2
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  Mirics MSi001 silicon tuner driver.
 
-config MEDIA_TUNER_MT20XX
+config BACKPORT_MEDIA_TUNER_MT20XX
 	tristate "Microtune 2032 / 2050 tuners"
-	depends on MEDIA_SUPPORT && I2C
+	depends on !MEDIA_TUNER_MT20XX
+	depends on BACKPORT_MEDIA_SUPPORT && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  Say Y here to include support for the MT2032 / MT2050 tuner.
 
-config MEDIA_TUNER_MT2060
+config BACKPORT_MEDIA_TUNER_MT2060
 	tristate "Microtune MT2060 silicon IF tuner"
-	depends on MEDIA_SUPPORT && I2C
+	depends on !MEDIA_TUNER_MT2060
+	depends on BACKPORT_MEDIA_SUPPORT && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  A driver for the silicon IF tuner MT2060 from Microtune.
 
-config MEDIA_TUNER_MT2063
+config BACKPORT_MEDIA_TUNER_MT2063
 	tristate "Microtune MT2063 silicon IF tuner"
-	depends on MEDIA_SUPPORT && I2C
+	depends on !MEDIA_TUNER_MT2063
+	depends on BACKPORT_MEDIA_SUPPORT && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  A driver for the silicon IF tuner MT2063 from Microtune.
 
-config MEDIA_TUNER_MT2266
+config BACKPORT_MEDIA_TUNER_MT2266
 	tristate "Microtune MT2266 silicon tuner"
-	depends on MEDIA_SUPPORT && I2C
+	depends on !MEDIA_TUNER_MT2266
+	depends on BACKPORT_MEDIA_SUPPORT && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  A driver for the silicon baseband tuner MT2266 from Microtune.
 
-config MEDIA_TUNER_MT2131
+config BACKPORT_MEDIA_TUNER_MT2131
 	tristate "Microtune MT2131 silicon tuner"
-	depends on MEDIA_SUPPORT && I2C
+	depends on !MEDIA_TUNER_MT2131
+	depends on BACKPORT_MEDIA_SUPPORT && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  A driver for the silicon baseband tuner MT2131 from Microtune.
 
-config MEDIA_TUNER_QT1010
+config BACKPORT_MEDIA_TUNER_QT1010
 	tristate "Quantek QT1010 silicon tuner"
-	depends on MEDIA_SUPPORT && I2C
+	depends on !MEDIA_TUNER_QT1010
+	depends on BACKPORT_MEDIA_SUPPORT && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  A driver for the silicon tuner QT1010 from Quantek.
 
-config MEDIA_TUNER_XC2028
+config BACKPORT_MEDIA_TUNER_XC2028
 	tristate "XCeive xc2028/xc3028 tuners"
-	depends on MEDIA_SUPPORT && I2C
+	depends on !MEDIA_TUNER_XC2028
+	depends on BACKPORT_MEDIA_SUPPORT && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  Say Y here to include support for the xc2028/xc3028 tuners.
 
-config MEDIA_TUNER_XC5000
+config BACKPORT_MEDIA_TUNER_XC5000
 	tristate "Xceive XC5000 silicon tuner"
-	depends on MEDIA_SUPPORT && I2C
+	depends on !MEDIA_TUNER_XC5000
+	depends on BACKPORT_MEDIA_SUPPORT && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  A driver for the silicon tuner XC5000 from Xceive.
 	  This device is only used inside a SiP called together with a
 	  demodulator for now.
 
-config MEDIA_TUNER_XC4000
+config BACKPORT_MEDIA_TUNER_XC4000
 	tristate "Xceive XC4000 silicon tuner"
-	depends on MEDIA_SUPPORT && I2C
+	depends on !MEDIA_TUNER_XC4000
+	depends on BACKPORT_MEDIA_SUPPORT && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  A driver for the silicon tuner XC4000 from Xceive.
 	  This device is only used inside a SiP called together with a
 	  demodulator for now.
 
-config MEDIA_TUNER_MXL5005S
+config BACKPORT_MEDIA_TUNER_MXL5005S
 	tristate "MaxLinear MSL5005S silicon tuner"
-	depends on MEDIA_SUPPORT && I2C
+	depends on !MEDIA_TUNER_MXL5005S
+	depends on BACKPORT_MEDIA_SUPPORT && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  A driver for the silicon tuner MXL5005S from MaxLinear.
 
-config MEDIA_TUNER_MXL5007T
+config BACKPORT_MEDIA_TUNER_MXL5007T
 	tristate "MaxLinear MxL5007T silicon tuner"
-	depends on MEDIA_SUPPORT && I2C
+	depends on !MEDIA_TUNER_MXL5007T
+	depends on BACKPORT_MEDIA_SUPPORT && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  A driver for the silicon tuner MxL5007T from MaxLinear.
 
-config MEDIA_TUNER_MC44S803
+config BACKPORT_MEDIA_TUNER_MC44S803
 	tristate "Freescale MC44S803 Low Power CMOS Broadband tuners"
-	depends on MEDIA_SUPPORT && I2C
+	depends on !MEDIA_TUNER_MC44S803
+	depends on BACKPORT_MEDIA_SUPPORT && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  Say Y here to support the Freescale MC44S803 based tuners
 
-config MEDIA_TUNER_MAX2165
+config BACKPORT_MEDIA_TUNER_MAX2165
 	tristate "Maxim MAX2165 silicon tuner"
-	depends on MEDIA_SUPPORT && I2C
+	depends on !MEDIA_TUNER_MAX2165
+	depends on BACKPORT_MEDIA_SUPPORT && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  A driver for the silicon tuner MAX2165 from Maxim.
 
-config MEDIA_TUNER_TDA18218
+config BACKPORT_MEDIA_TUNER_TDA18218
 	tristate "NXP TDA18218 silicon tuner"
-	depends on MEDIA_SUPPORT && I2C
+	depends on !MEDIA_TUNER_TDA18218
+	depends on BACKPORT_MEDIA_SUPPORT && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  NXP TDA18218 silicon tuner driver.
 
-config MEDIA_TUNER_FC0011
+config BACKPORT_MEDIA_TUNER_FC0011
 	tristate "Fitipower FC0011 silicon tuner"
-	depends on MEDIA_SUPPORT && I2C
+	depends on !MEDIA_TUNER_FC0011
+	depends on BACKPORT_MEDIA_SUPPORT && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  Fitipower FC0011 silicon tuner driver.
 
-config MEDIA_TUNER_FC0012
+config BACKPORT_MEDIA_TUNER_FC0012
 	tristate "Fitipower FC0012 silicon tuner"
-	depends on MEDIA_SUPPORT && I2C
+	depends on !MEDIA_TUNER_FC0012
+	depends on BACKPORT_MEDIA_SUPPORT && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  Fitipower FC0012 silicon tuner driver.
 
-config MEDIA_TUNER_FC0013
+config BACKPORT_MEDIA_TUNER_FC0013
 	tristate "Fitipower FC0013 silicon tuner"
-	depends on MEDIA_SUPPORT && I2C
+	depends on !MEDIA_TUNER_FC0013
+	depends on BACKPORT_MEDIA_SUPPORT && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  Fitipower FC0013 silicon tuner driver.
 
-config MEDIA_TUNER_TDA18212
+config BACKPORT_MEDIA_TUNER_TDA18212
 	tristate "NXP TDA18212 silicon tuner"
-	depends on MEDIA_SUPPORT && I2C
+	depends on !MEDIA_TUNER_TDA18212
+	depends on BACKPORT_MEDIA_SUPPORT && I2C
 	select REGMAP_I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  NXP TDA18212 silicon tuner driver.
 
-config MEDIA_TUNER_E4000
+config BACKPORT_MEDIA_TUNER_E4000
 	tristate "Elonics E4000 silicon tuner"
-	depends on MEDIA_SUPPORT && I2C
+	depends on !MEDIA_TUNER_E4000
+	depends on BACKPORT_MEDIA_SUPPORT && I2C
 	select REGMAP_I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  Elonics E4000 silicon tuner driver.
 
-config MEDIA_TUNER_FC2580
+config BACKPORT_MEDIA_TUNER_FC2580
 	tristate "FCI FC2580 silicon tuner"
-	depends on MEDIA_SUPPORT && I2C
+	depends on !MEDIA_TUNER_FC2580
+	depends on BACKPORT_MEDIA_SUPPORT && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  FCI FC2580 silicon tuner driver.
 
-config MEDIA_TUNER_M88RS6000T
+config BACKPORT_MEDIA_TUNER_M88RS6000T
 	tristate "Montage M88RS6000 internal tuner"
-	depends on MEDIA_SUPPORT && I2C
+	depends on !MEDIA_TUNER_M88RS6000T
+	depends on BACKPORT_MEDIA_SUPPORT && I2C
 	select REGMAP_I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  Montage M88RS6000 internal tuner.
 
-config MEDIA_TUNER_TUA9001
+config BACKPORT_MEDIA_TUNER_TUA9001
 	tristate "Infineon TUA 9001 silicon tuner"
-	depends on MEDIA_SUPPORT && I2C
+	depends on !MEDIA_TUNER_TUA9001
+	depends on BACKPORT_MEDIA_SUPPORT && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  Infineon TUA 9001 silicon tuner driver.
 
-config MEDIA_TUNER_SI2157
+config BACKPORT_MEDIA_TUNER_SI2157
 	tristate "Silicon Labs Si2157 silicon tuner"
-	depends on MEDIA_SUPPORT && I2C
+	depends on !MEDIA_TUNER_SI2157
+	depends on BACKPORT_MEDIA_SUPPORT && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  Silicon Labs Si2157 silicon tuner driver.
 
-config MEDIA_TUNER_IT913X
+config BACKPORT_MEDIA_TUNER_IT913X
 	tristate "ITE Tech IT913x silicon tuner"
-	depends on MEDIA_SUPPORT && I2C
+	depends on !MEDIA_TUNER_IT913X
+	depends on BACKPORT_MEDIA_SUPPORT && I2C
 	select REGMAP_I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  ITE Tech IT913x silicon tuner driver.
 
-config MEDIA_TUNER_R820T
+config BACKPORT_MEDIA_TUNER_R820T
 	tristate "Rafael Micro R820T silicon tuner"
-	depends on MEDIA_SUPPORT && I2C
+	depends on !MEDIA_TUNER_R820T
+	depends on BACKPORT_MEDIA_SUPPORT && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  Rafael Micro R820T silicon tuner driver.
 
-config MEDIA_TUNER_MXL301RF
+config BACKPORT_MEDIA_TUNER_MXL301RF
 	tristate "MaxLinear MxL301RF tuner"
-	depends on MEDIA_SUPPORT && I2C
+	depends on !MEDIA_TUNER_MXL301RF
+	depends on BACKPORT_MEDIA_SUPPORT && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  MaxLinear MxL301RF OFDM tuner driver.
 
-config MEDIA_TUNER_QM1D1C0042
+config BACKPORT_MEDIA_TUNER_QM1D1C0042
 	tristate "Sharp QM1D1C0042 tuner"
-	depends on MEDIA_SUPPORT && I2C
+	depends on !MEDIA_TUNER_QM1D1C0042
+	depends on BACKPORT_MEDIA_SUPPORT && I2C
 	default m if !MEDIA_SUBDRV_AUTOSELECT
 	help
 	  Sharp QM1D1C0042 trellis coded 8PSK tuner driver.
diff --git a/backports/drivers/media/usb/Kconfig b/backports/drivers/media/usb/Kconfig
index 154b12a..aa7ee55 100644
--- a/backports/drivers/media/usb/Kconfig
+++ b/backports/drivers/media/usb/Kconfig
@@ -1,14 +1,15 @@
-if USB && MEDIA_SUPPORT
+if USB && BACKPORT_MEDIA_SUPPORT
 
-menuconfig MEDIA_USB_SUPPORT
+menuconfig BACKPORT_MEDIA_USB_SUPPORT
 	bool "Media USB Adapters"
+	depends on !MEDIA_USB_SUPPORT
 	help
 	  Enable media drivers for USB bus.
 	  If you have such devices, say Y.
 
-if MEDIA_USB_SUPPORT
+if BACKPORT_MEDIA_USB_SUPPORT
 
-if MEDIA_CAMERA_SUPPORT
+if BACKPORT_MEDIA_CAMERA_SUPPORT
 	comment "Webcam devices"
 source "$BACKPORT_DIR/drivers/media/usb/uvc/Kconfig"
 source "$BACKPORT_DIR/drivers/media/usb/gspca/Kconfig"
@@ -20,7 +21,7 @@ source "$BACKPORT_DIR/drivers/media/usb/s2255/Kconfig"
 source "$BACKPORT_DIR/drivers/media/usb/usbtv/Kconfig"
 endif
 
-if MEDIA_ANALOG_TV_SUPPORT
+if BACKPORT_MEDIA_ANALOG_TV_SUPPORT
 	comment "Analog TV USB devices"
 source "$BACKPORT_DIR/drivers/media/usb/pvrusb2/Kconfig"
 source "$BACKPORT_DIR/drivers/media/usb/hdpvr/Kconfig"
@@ -37,7 +38,7 @@ source "$BACKPORT_DIR/drivers/media/usb/tm6000/Kconfig"
 endif
 
 
-if I2C && MEDIA_DIGITAL_TV_SUPPORT
+if I2C && BACKPORT_MEDIA_DIGITAL_TV_SUPPORT
 	comment "Digital TV USB devices"
 source "$BACKPORT_DIR/drivers/media/usb/dvb-usb/Kconfig"
 source "$BACKPORT_DIR/drivers/media/usb/dvb-usb-v2/Kconfig"
@@ -53,7 +54,7 @@ if (MEDIA_CAMERA_SUPPORT || MEDIA_ANALOG_TV_SUPPORT || MEDIA_DIGITAL_TV_SUPPORT)
 source "$BACKPORT_DIR/drivers/media/usb/em28xx/Kconfig"
 endif
 
-if MEDIA_SDR_SUPPORT
+if BACKPORT_MEDIA_SDR_SUPPORT
 	comment "Software defined radio USB devices"
 source "$BACKPORT_DIR/drivers/media/usb/airspy/Kconfig"
 source "$BACKPORT_DIR/drivers/media/usb/hackrf/Kconfig"
diff --git a/backports/drivers/media/usb/airspy/Kconfig b/backports/drivers/media/usb/airspy/Kconfig
index e0c8947..d1ae813 100644
--- a/backports/drivers/media/usb/airspy/Kconfig
+++ b/backports/drivers/media/usb/airspy/Kconfig
@@ -1,7 +1,8 @@
-config USB_AIRSPY
+config BACKPORT_USB_AIRSPY
 	tristate "AirSpy"
-	depends on VIDEO_V4L2
-	select VIDEOBUF2_VMALLOC
+	depends on !USB_AIRSPY
+	depends on BACKPORT_VIDEO_V4L2
+	select BACKPORT_VIDEOBUF2_VMALLOC
 	---help---
 	  This is a video4linux2 driver for AirSpy SDR device.
 
diff --git a/backports/drivers/media/usb/as102/Kconfig b/backports/drivers/media/usb/as102/Kconfig
index 28aba00..41aa4c4 100644
--- a/backports/drivers/media/usb/as102/Kconfig
+++ b/backports/drivers/media/usb/as102/Kconfig
@@ -1,6 +1,7 @@
-config DVB_AS102
+config BACKPORT_DVB_AS102
 	tristate "Abilis AS102 DVB receiver"
-	depends on DVB_CORE && USB && I2C && INPUT
+	depends on !DVB_AS102
+	depends on BACKPORT_DVB_CORE && USB && I2C && INPUT
 	select FW_LOADER
 	help
 	  Choose Y or M here if you have a device containing an AS102
diff --git a/backports/drivers/media/usb/au0828/Kconfig b/backports/drivers/media/usb/au0828/Kconfig
index 78b797e..ba72ea2 100644
--- a/backports/drivers/media/usb/au0828/Kconfig
+++ b/backports/drivers/media/usb/au0828/Kconfig
@@ -1,14 +1,15 @@
 
-config VIDEO_AU0828
+config BACKPORT_VIDEO_AU0828
 	tristate "Auvitek AU0828 support"
-	depends on I2C && INPUT && DVB_CORE && USB
+	depends on !VIDEO_AU0828
+	depends on I2C && INPUT && BACKPORT_DVB_CORE && USB
 	select I2C_ALGOBIT
-	select VIDEO_TVEEPROM
-	select VIDEOBUF2_VMALLOC
-	select DVB_AU8522_DTV if MEDIA_SUBDRV_AUTOSELECT
-	select MEDIA_TUNER_XC5000 if MEDIA_SUBDRV_AUTOSELECT
-	select MEDIA_TUNER_MXL5007T if MEDIA_SUBDRV_AUTOSELECT
-	select MEDIA_TUNER_TDA18271 if MEDIA_SUBDRV_AUTOSELECT
+	select BACKPORT_VIDEO_TVEEPROM
+	select BACKPORT_VIDEOBUF2_VMALLOC
+	select DVB_AU8522_DTV if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select MEDIA_TUNER_XC5000 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select MEDIA_TUNER_MXL5007T if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select MEDIA_TUNER_TDA18271 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
 	---help---
 	  This is a hybrid analog/digital tv capture driver for
 	  Auvitek's AU0828 USB device.
@@ -16,11 +17,12 @@ config VIDEO_AU0828
 	  To compile this driver as a module, choose M here: the
 	  module will be called au0828
 
-config VIDEO_AU0828_V4L2
+config BACKPORT_VIDEO_AU0828_V4L2
 	bool "Auvitek AU0828 v4l2 analog video support"
-	depends on VIDEO_AU0828 && VIDEO_V4L2
-	select DVB_AU8522_V4L if MEDIA_SUBDRV_AUTOSELECT
-	select VIDEO_TUNER
+	depends on !VIDEO_AU0828_V4L2
+	depends on VIDEO_AU0828 && BACKPORT_VIDEO_V4L2
+	select DVB_AU8522_V4L if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select BACKPORT_VIDEO_TUNER
 	default y
 	---help---
 	  This is a video4linux driver for Auvitek's USB device.
@@ -28,9 +30,10 @@ config VIDEO_AU0828_V4L2
 	  Choose Y here to include support for v4l2 analog video
 	  capture within the au0828 driver.
 
-config VIDEO_AU0828_RC
+config BACKPORT_VIDEO_AU0828_RC
 	bool "AU0828 Remote Controller support"
-	depends on RC_CORE
-	depends on VIDEO_AU0828
+	depends on !VIDEO_AU0828_RC
+	depends on BACKPORT_RC_CORE
+	depends on BACKPORT_VIDEO_AU0828
 	---help---
 	   Enables Remote Controller support on au0828 driver.
diff --git a/backports/drivers/media/usb/b2c2/Kconfig b/backports/drivers/media/usb/b2c2/Kconfig
index 17d3583..dfafc71 100644
--- a/backports/drivers/media/usb/b2c2/Kconfig
+++ b/backports/drivers/media/usb/b2c2/Kconfig
@@ -1,15 +1,17 @@
-config DVB_B2C2_FLEXCOP_USB
+config BACKPORT_DVB_B2C2_FLEXCOP_USB
 	tristate "Technisat/B2C2 Air/Sky/Cable2PC USB"
-	depends on DVB_CORE && I2C
+	depends on !DVB_B2C2_FLEXCOP_USB
+	depends on BACKPORT_DVB_CORE && I2C
 	help
 	  Support for the Air/Sky/Cable2PC USB1.1 box (DVB/ATSC) by Technisat/B2C2,
 
 	  Say Y if you own such a device and want to use it.
 
-config DVB_B2C2_FLEXCOP_USB_DEBUG
+config BACKPORT_DVB_B2C2_FLEXCOP_USB_DEBUG
 	bool "Enable debug for the B2C2 FlexCop drivers"
-	depends on DVB_B2C2_FLEXCOP_USB
-	select DVB_B2C2_FLEXCOP_DEBUG
+	depends on !DVB_B2C2_FLEXCOP_USB_DEBUG
+	depends on BACKPORT_DVB_B2C2_FLEXCOP_USB
+	select BACKPORT_DVB_B2C2_FLEXCOP_DEBUG
 	   help
 	Say Y if you want to enable the module option to control debug messages
 	of all B2C2 FlexCop drivers.
diff --git a/backports/drivers/media/usb/cpia2/Kconfig b/backports/drivers/media/usb/cpia2/Kconfig
index 66e9283..e381e48 100644
--- a/backports/drivers/media/usb/cpia2/Kconfig
+++ b/backports/drivers/media/usb/cpia2/Kconfig
@@ -1,6 +1,7 @@
-config VIDEO_CPIA2
+config BACKPORT_VIDEO_CPIA2
 	tristate "CPiA2 Video For Linux"
-	depends on VIDEO_DEV && USB && VIDEO_V4L2
+	depends on !VIDEO_CPIA2
+	depends on BACKPORT_VIDEO_DEV && USB && VIDEO_V4L2
 	---help---
 	  This is the video4linux driver for cameras based on Vision's CPiA2
 	  (Colour Processor Interface ASIC), such as the Digital Blue QX5
diff --git a/backports/drivers/media/usb/cx231xx/Kconfig b/backports/drivers/media/usb/cx231xx/Kconfig
index 0cced3e..79e16f6 100644
--- a/backports/drivers/media/usb/cx231xx/Kconfig
+++ b/backports/drivers/media/usb/cx231xx/Kconfig
@@ -1,12 +1,13 @@
-config VIDEO_CX231XX
+config BACKPORT_VIDEO_CX231XX
 	tristate "Conexant cx231xx USB video capture support"
-	depends on VIDEO_DEV && I2C
-	select VIDEO_TUNER
-	select VIDEO_TVEEPROM
-	depends on RC_CORE
-	select VIDEOBUF_VMALLOC
-	select VIDEO_CX25840
-	select VIDEO_CX2341X
+	depends on !VIDEO_CX231XX
+	depends on BACKPORT_VIDEO_DEV && I2C
+	select BACKPORT_VIDEO_TUNER
+	select BACKPORT_VIDEO_TVEEPROM
+	depends on BACKPORT_RC_CORE
+	select BACKPORT_VIDEOBUF_VMALLOC
+	select BACKPORT_VIDEO_CX25840
+	select BACKPORT_VIDEO_CX2341X
 	select I2C_MUX
 
 	---help---
@@ -15,10 +16,11 @@ config VIDEO_CX231XX
 	  To compile this driver as a module, choose M here: the
 	  module will be called cx231xx
 
-config VIDEO_CX231XX_RC
+config BACKPORT_VIDEO_CX231XX_RC
 	bool "Conexant cx231xx Remote Controller additional support"
-	depends on RC_CORE
-	depends on VIDEO_CX231XX
+	depends on !VIDEO_CX231XX_RC
+	depends on BACKPORT_RC_CORE
+	depends on BACKPORT_VIDEO_CX231XX
 	default y
 	---help---
 	  cx231xx hardware has a builtin RX/TX support. However, a few
@@ -28,9 +30,10 @@ config VIDEO_CX231XX_RC
 
 	  On most cases, all you need for IR is mceusb module.
 
-config VIDEO_CX231XX_ALSA
+config BACKPORT_VIDEO_CX231XX_ALSA
 	tristate "Conexant Cx231xx ALSA audio module"
-	depends on VIDEO_CX231XX && SND
+	depends on !VIDEO_CX231XX_ALSA
+	depends on BACKPORT_VIDEO_CX231XX && SND
 	select SND_PCM
 
 	---help---
@@ -39,18 +42,19 @@ config VIDEO_CX231XX_ALSA
 	  To compile this driver as a module, choose M here: the
 	  module will be called cx231xx-alsa
 
-config VIDEO_CX231XX_DVB
+config BACKPORT_VIDEO_CX231XX_DVB
 	tristate "DVB/ATSC Support for Cx231xx based TV cards"
-	depends on VIDEO_CX231XX && DVB_CORE
-	select VIDEOBUF_DVB
-	select MEDIA_TUNER_XC5000 if MEDIA_SUBDRV_AUTOSELECT
-	select MEDIA_TUNER_TDA18271 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_MB86A20S if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_LGDT3305 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_LGDT3306A if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_TDA18271C2DD if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_SI2165 if MEDIA_SUBDRV_AUTOSELECT
-	select MEDIA_TUNER_SI2157 if MEDIA_SUBDRV_AUTOSELECT
+	depends on !VIDEO_CX231XX_DVB
+	depends on VIDEO_CX231XX && BACKPORT_DVB_CORE
+	select BACKPORT_VIDEOBUF_DVB
+	select MEDIA_TUNER_XC5000 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select MEDIA_TUNER_TDA18271 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_MB86A20S if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_LGDT3305 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_LGDT3306A if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_TDA18271C2DD if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_SI2165 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select MEDIA_TUNER_SI2157 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
 
 	---help---
 	  This adds support for DVB cards based on the
diff --git a/backports/drivers/media/usb/dvb-usb-v2/Kconfig b/backports/drivers/media/usb/dvb-usb-v2/Kconfig
index 9facc92..edf1084 100644
--- a/backports/drivers/media/usb/dvb-usb-v2/Kconfig
+++ b/backports/drivers/media/usb/dvb-usb-v2/Kconfig
@@ -1,6 +1,7 @@
-config DVB_USB_V2
+config BACKPORT_DVB_USB_V2
 	tristate "Support for various USB DVB devices v2"
-	depends on DVB_CORE && USB && I2C && (RC_CORE || RC_CORE=n)
+	depends on !DVB_USB_V2
+	depends on BACKPORT_DVB_CORE && USB && I2C && (RC_CORE || RC_CORE=n)
 	help
 	  By enabling this you will be able to choose the various supported
 	  USB1.1 and USB2.0 DVB devices.
@@ -13,141 +14,153 @@ config DVB_USB_V2
 
 	  Say Y if you own a USB DVB device.
 
-config DVB_USB_AF9015
+config BACKPORT_DVB_USB_AF9015
 	tristate "Afatech AF9015 DVB-T USB2.0 support"
-	depends on DVB_USB_V2
-	select DVB_AF9013
-	select DVB_PLL              if MEDIA_SUBDRV_AUTOSELECT
-	select MEDIA_TUNER_MT2060   if MEDIA_SUBDRV_AUTOSELECT
-	select MEDIA_TUNER_QT1010   if MEDIA_SUBDRV_AUTOSELECT
-	select MEDIA_TUNER_TDA18271 if MEDIA_SUBDRV_AUTOSELECT
-	select MEDIA_TUNER_MXL5005S if MEDIA_SUBDRV_AUTOSELECT
-	select MEDIA_TUNER_MC44S803 if MEDIA_SUBDRV_AUTOSELECT
-	select MEDIA_TUNER_TDA18218 if MEDIA_SUBDRV_AUTOSELECT
-	select MEDIA_TUNER_MXL5007T if MEDIA_SUBDRV_AUTOSELECT
+	depends on !DVB_USB_AF9015
+	depends on BACKPORT_DVB_USB_V2
+	select BACKPORT_DVB_AF9013
+	select DVB_PLL              if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select MEDIA_TUNER_MT2060   if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select MEDIA_TUNER_QT1010   if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select MEDIA_TUNER_TDA18271 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select MEDIA_TUNER_MXL5005S if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select MEDIA_TUNER_MC44S803 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select MEDIA_TUNER_TDA18218 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select MEDIA_TUNER_MXL5007T if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
 	help
 	  Say Y here to support the Afatech AF9015 based DVB-T USB2.0 receiver
 
-config DVB_USB_AF9035
+config BACKPORT_DVB_USB_AF9035
 	tristate "Afatech AF9035 DVB-T USB2.0 support"
-	depends on DVB_USB_V2
-	select DVB_AF9033
-	select MEDIA_TUNER_TUA9001 if MEDIA_SUBDRV_AUTOSELECT
-	select MEDIA_TUNER_FC0011 if MEDIA_SUBDRV_AUTOSELECT
-	select MEDIA_TUNER_MXL5007T if MEDIA_SUBDRV_AUTOSELECT
-	select MEDIA_TUNER_TDA18218 if MEDIA_SUBDRV_AUTOSELECT
-	select MEDIA_TUNER_FC2580 if MEDIA_SUBDRV_AUTOSELECT
-	select MEDIA_TUNER_IT913X if MEDIA_SUBDRV_AUTOSELECT
+	depends on !DVB_USB_AF9035
+	depends on BACKPORT_DVB_USB_V2
+	select BACKPORT_DVB_AF9033
+	select MEDIA_TUNER_TUA9001 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select MEDIA_TUNER_FC0011 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select MEDIA_TUNER_MXL5007T if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select MEDIA_TUNER_TDA18218 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select MEDIA_TUNER_FC2580 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select MEDIA_TUNER_IT913X if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
 	help
 	  Say Y here to support the Afatech AF9035 based DVB USB receiver.
 
-config DVB_USB_ANYSEE
+config BACKPORT_DVB_USB_ANYSEE
 	tristate "Anysee DVB-T/C USB2.0 support"
-	depends on DVB_USB_V2
-	select DVB_PLL if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_MT352 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_ZL10353 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_TDA10023 if MEDIA_SUBDRV_AUTOSELECT
-	select MEDIA_TUNER_TDA18212 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_CX24116 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_STV0900 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_STV6110 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_ISL6423 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_CXD2820R if MEDIA_SUBDRV_AUTOSELECT
+	depends on !DVB_USB_ANYSEE
+	depends on BACKPORT_DVB_USB_V2
+	select DVB_PLL if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_MT352 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_ZL10353 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_TDA10023 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select MEDIA_TUNER_TDA18212 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_CX24116 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_STV0900 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_STV6110 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_ISL6423 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_CXD2820R if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
 	help
 	  Say Y here to support the Anysee E30, Anysee E30 Plus or
 	  Anysee E30 C Plus DVB USB2.0 receiver.
 
-config DVB_USB_AU6610
+config BACKPORT_DVB_USB_AU6610
 	tristate "Alcor Micro AU6610 USB2.0 support"
-	depends on DVB_USB_V2
-	select DVB_ZL10353 if MEDIA_SUBDRV_AUTOSELECT
-	select MEDIA_TUNER_QT1010 if MEDIA_SUBDRV_AUTOSELECT
+	depends on !DVB_USB_AU6610
+	depends on BACKPORT_DVB_USB_V2
+	select DVB_ZL10353 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select MEDIA_TUNER_QT1010 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
 	help
 	  Say Y here to support the Sigmatek DVB-110 DVB-T USB2.0 receiver.
 
-config DVB_USB_AZ6007
+config BACKPORT_DVB_USB_AZ6007
 	tristate "AzureWave 6007 and clones DVB-T/C USB2.0 support"
-	depends on DVB_USB_V2
-	select CYPRESS_FIRMWARE
-	select DVB_DRXK if MEDIA_SUBDRV_AUTOSELECT
-	select MEDIA_TUNER_MT2063 if MEDIA_SUBDRV_AUTOSELECT
+	depends on !DVB_USB_AZ6007
+	depends on BACKPORT_DVB_USB_V2
+	select BACKPORT_CYPRESS_FIRMWARE
+	select DVB_DRXK if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select MEDIA_TUNER_MT2063 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
 	help
 	  Say Y here to support the AZ6007 receivers like Terratec H7.
 
-config DVB_USB_CE6230
+config BACKPORT_DVB_USB_CE6230
 	tristate "Intel CE6230 DVB-T USB2.0 support"
-	depends on DVB_USB_V2
-	select DVB_ZL10353
-	select MEDIA_TUNER_MXL5005S if MEDIA_SUBDRV_AUTOSELECT
+	depends on !DVB_USB_CE6230
+	depends on BACKPORT_DVB_USB_V2
+	select BACKPORT_DVB_ZL10353
+	select MEDIA_TUNER_MXL5005S if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
 	help
 	  Say Y here to support the Intel CE6230 DVB-T USB2.0 receiver
 
-config DVB_USB_EC168
+config BACKPORT_DVB_USB_EC168
 	tristate "E3C EC168 DVB-T USB2.0 support"
-	depends on DVB_USB_V2
-	select DVB_EC100
-	select MEDIA_TUNER_MXL5005S if MEDIA_SUBDRV_AUTOSELECT
+	depends on !DVB_USB_EC168
+	depends on BACKPORT_DVB_USB_V2
+	select BACKPORT_DVB_EC100
+	select MEDIA_TUNER_MXL5005S if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
 	help
 	  Say Y here to support the E3C EC168 DVB-T USB2.0 receiver.
 
-config DVB_USB_GL861
+config BACKPORT_DVB_USB_GL861
 	tristate "Genesys Logic GL861 USB2.0 support"
-	depends on DVB_USB_V2
-	select DVB_ZL10353 if MEDIA_SUBDRV_AUTOSELECT
-	select MEDIA_TUNER_QT1010 if MEDIA_SUBDRV_AUTOSELECT
+	depends on !DVB_USB_GL861
+	depends on BACKPORT_DVB_USB_V2
+	select DVB_ZL10353 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select MEDIA_TUNER_QT1010 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
 	help
 	  Say Y here to support the MSI Megasky 580 (55801) DVB-T USB2.0
 	  receiver with USB ID 0db0:5581.
 
-config DVB_USB_LME2510
+config BACKPORT_DVB_USB_LME2510
 	tristate "LME DM04/QQBOX DVB-S USB2.0 support"
-	depends on DVB_USB_V2
-	depends on RC_CORE
-	select DVB_TDA10086 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_TDA826X if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_STV0288 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_IX2505V if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_STV0299 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_PLL if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_M88RS2000 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_TS2020 if MEDIA_SUBDRV_AUTOSELECT
+	depends on !DVB_USB_LME2510
+	depends on BACKPORT_DVB_USB_V2
+	depends on BACKPORT_RC_CORE
+	select DVB_TDA10086 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_TDA826X if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_STV0288 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_IX2505V if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_STV0299 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_PLL if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_M88RS2000 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_TS2020 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
 	help
 	  Say Y here to support the LME DM04/QQBOX DVB-S USB2.0
 
-config DVB_USB_MXL111SF
+config BACKPORT_DVB_USB_MXL111SF
 	tristate "MxL111SF DTV USB2.0 support"
-	depends on DVB_USB_V2
-	select DVB_LGDT3305 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_LG2160 if MEDIA_SUBDRV_AUTOSELECT
-	select VIDEO_TVEEPROM
+	depends on !DVB_USB_MXL111SF
+	depends on BACKPORT_DVB_USB_V2
+	select DVB_LGDT3305 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_LG2160 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select BACKPORT_VIDEO_TVEEPROM
 	help
 	  Say Y here to support the MxL111SF USB2.0 DTV receiver.
 
-config DVB_USB_RTL28XXU
+config BACKPORT_DVB_USB_RTL28XXU
 	tristate "Realtek RTL28xxU DVB USB support"
-	depends on DVB_USB_V2 && I2C_MUX
-	select DVB_RTL2830
-	select DVB_RTL2832
-	select DVB_RTL2832_SDR if (MEDIA_SUBDRV_AUTOSELECT && MEDIA_SDR_SUPPORT)
-	select MEDIA_TUNER_QT1010 if MEDIA_SUBDRV_AUTOSELECT
-	select MEDIA_TUNER_MT2060 if MEDIA_SUBDRV_AUTOSELECT
-	select MEDIA_TUNER_MXL5005S if MEDIA_SUBDRV_AUTOSELECT
-	select MEDIA_TUNER_FC0012 if MEDIA_SUBDRV_AUTOSELECT
-	select MEDIA_TUNER_FC0013 if MEDIA_SUBDRV_AUTOSELECT
-	select MEDIA_TUNER_E4000 if MEDIA_SUBDRV_AUTOSELECT
-	select MEDIA_TUNER_FC2580 if MEDIA_SUBDRV_AUTOSELECT
-	select MEDIA_TUNER_R820T if MEDIA_SUBDRV_AUTOSELECT
+	depends on !DVB_USB_RTL28XXU
+	depends on BACKPORT_DVB_USB_V2 && I2C_MUX
+	select BACKPORT_DVB_RTL2830
+	select BACKPORT_DVB_RTL2832
+	select DVB_RTL2832_SDR if (MEDIA_SUBDRV_AUTOSELECT && BACKPORT_MEDIA_SDR_SUPPORT)
+	select MEDIA_TUNER_QT1010 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select MEDIA_TUNER_MT2060 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select MEDIA_TUNER_MXL5005S if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select MEDIA_TUNER_FC0012 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select MEDIA_TUNER_FC0013 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select MEDIA_TUNER_E4000 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select MEDIA_TUNER_FC2580 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select MEDIA_TUNER_R820T if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
 	help
 	  Say Y here to support the Realtek RTL28xxU DVB USB receiver.
 
-config DVB_USB_DVBSKY
+config BACKPORT_DVB_USB_DVBSKY
 	tristate "DVBSky USB support"
-	depends on DVB_USB_V2
-	select DVB_M88DS3103 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_SI2168 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_TS2020 if MEDIA_SUBDRV_AUTOSELECT
-	select MEDIA_TUNER_SI2157 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_SP2 if MEDIA_SUBDRV_AUTOSELECT
+	depends on !DVB_USB_DVBSKY
+	depends on BACKPORT_DVB_USB_V2
+	select DVB_M88DS3103 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_SI2168 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_TS2020 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select MEDIA_TUNER_SI2157 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_SP2 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
 	help
 	  Say Y here to support the USB receivers from DVBSky.
diff --git a/backports/drivers/media/usb/dvb-usb/Kconfig b/backports/drivers/media/usb/dvb-usb/Kconfig
index 128eee6..4974cf0 100644
--- a/backports/drivers/media/usb/dvb-usb/Kconfig
+++ b/backports/drivers/media/usb/dvb-usb/Kconfig
@@ -1,6 +1,7 @@
-config DVB_USB
+config BACKPORT_DVB_USB
 	tristate "Support for various USB DVB devices"
-	depends on DVB_CORE && USB && I2C && RC_CORE
+	depends on !DVB_USB
+	depends on BACKPORT_DVB_CORE && USB && I2C && RC_CORE
 	help
 	  By enabling this you will be able to choose the various supported
 	  USB1.1 and USB2.0 DVB devices.
@@ -13,28 +14,31 @@ config DVB_USB
 
 	  Say Y if you own a USB DVB device.
 
-config DVB_USB_DEBUG
+config BACKPORT_DVB_USB_DEBUG
 	bool "Enable extended debug support for all DVB-USB devices"
-	depends on DVB_USB
+	depends on !DVB_USB_DEBUG
+	depends on BACKPORT_DVB_USB
 	help
 	  Say Y if you want to enable debugging. See modinfo dvb-usb (and the
 	  appropriate drivers) for debug levels.
 
-config DVB_USB_A800
+config BACKPORT_DVB_USB_A800
 	tristate "AVerMedia AverTV DVB-T USB 2.0 (A800)"
-	depends on DVB_USB
-	select DVB_DIB3000MC
-	select DVB_PLL if MEDIA_SUBDRV_AUTOSELECT
-	select MEDIA_TUNER_MT2060 if MEDIA_SUBDRV_AUTOSELECT
+	depends on !DVB_USB_A800
+	depends on BACKPORT_DVB_USB
+	select BACKPORT_DVB_DIB3000MC
+	select DVB_PLL if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select MEDIA_TUNER_MT2060 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
 	help
 	  Say Y here to support the AVerMedia AverTV DVB-T USB 2.0 (A800) receiver.
 
-config DVB_USB_DIBUSB_MB
+config BACKPORT_DVB_USB_DIBUSB_MB
 	tristate "DiBcom USB DVB-T devices (based on the DiB3000M-B) (see help for device list)"
-	depends on DVB_USB
-	select DVB_PLL if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_DIB3000MB
-	select MEDIA_TUNER_MT2060 if MEDIA_SUBDRV_AUTOSELECT
+	depends on !DVB_USB_DIBUSB_MB
+	depends on BACKPORT_DVB_USB
+	select DVB_PLL if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select BACKPORT_DVB_DIB3000MB
+	select MEDIA_TUNER_MT2060 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
 	help
 	  Support for USB 1.1 and 2.0 DVB-T receivers based on reference designs made by
 	  DiBcom (<http://www.dibcom.fr>) equipped with a DiB3000M-B demodulator.
@@ -45,17 +49,19 @@ config DVB_USB_DIBUSB_MB
 	  Say Y if you own such a device and want to use it. You should build it as
 	  a module.
 
-config DVB_USB_DIBUSB_MB_FAULTY
+config BACKPORT_DVB_USB_DIBUSB_MB_FAULTY
 	bool "Support faulty USB IDs"
-	depends on DVB_USB_DIBUSB_MB
+	depends on !DVB_USB_DIBUSB_MB_FAULTY
+	depends on BACKPORT_DVB_USB_DIBUSB_MB
 	help
 	  Support for faulty USB IDs due to an invalid EEPROM on some Artec devices.
 
-config DVB_USB_DIBUSB_MC
+config BACKPORT_DVB_USB_DIBUSB_MC
 	tristate "DiBcom USB DVB-T devices (based on the DiB3000M-C/P) (see help for device list)"
-	depends on DVB_USB
-	select DVB_DIB3000MC
-	select MEDIA_TUNER_MT2060 if MEDIA_SUBDRV_AUTOSELECT
+	depends on !DVB_USB_DIBUSB_MC
+	depends on BACKPORT_DVB_USB
+	select BACKPORT_DVB_DIB3000MC
+	select MEDIA_TUNER_MT2060 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
 	help
 	  Support for USB2.0 DVB-T receivers based on reference designs made by
 	  DiBcom (<http://www.dibcom.fr>) equipped with a DiB3000M-C/P demodulator.
@@ -66,23 +72,24 @@ config DVB_USB_DIBUSB_MC
 	  Say Y if you own such a device and want to use it. You should build it as
 	  a module.
 
-config DVB_USB_DIB0700
+config BACKPORT_DVB_USB_DIB0700
 	tristate "DiBcom DiB0700 USB DVB devices (see help for supported devices)"
-	depends on DVB_USB
-	select DVB_DIB7000P if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_DIB7000M if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_DIB8000 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_DIB3000MC if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_S5H1411 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_LGDT3305 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_TUNER_DIB0070 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_TUNER_DIB0090 if MEDIA_SUBDRV_AUTOSELECT
-	select MEDIA_TUNER_MT2060 if MEDIA_SUBDRV_AUTOSELECT
-	select MEDIA_TUNER_MT2266 if MEDIA_SUBDRV_AUTOSELECT
-	select MEDIA_TUNER_XC2028 if MEDIA_SUBDRV_AUTOSELECT
-	select MEDIA_TUNER_XC5000 if MEDIA_SUBDRV_AUTOSELECT
-	select MEDIA_TUNER_XC4000 if MEDIA_SUBDRV_AUTOSELECT
-	select MEDIA_TUNER_MXL5007T if MEDIA_SUBDRV_AUTOSELECT
+	depends on !DVB_USB_DIB0700
+	depends on BACKPORT_DVB_USB
+	select DVB_DIB7000P if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_DIB7000M if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_DIB8000 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_DIB3000MC if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_S5H1411 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_LGDT3305 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_TUNER_DIB0070 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_TUNER_DIB0090 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select MEDIA_TUNER_MT2060 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select MEDIA_TUNER_MT2266 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select MEDIA_TUNER_XC2028 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select MEDIA_TUNER_XC5000 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select MEDIA_TUNER_XC4000 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select MEDIA_TUNER_MXL5007T if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
 	help
 	  Support for USB2.0/1.1 DVB receivers based on the DiB0700 USB bridge. The
 	  USB bridge is also present in devices having the DiB7700 DVB-T-USB
@@ -95,34 +102,36 @@ config DVB_USB_DIB0700
 	  Say Y if you own such a device and want to use it. You should build it as
 	  a module.
 
-config DVB_USB_UMT_010
+config BACKPORT_DVB_USB_UMT_010
 	tristate "HanfTek UMT-010 DVB-T USB2.0 support"
-	depends on DVB_USB
-	select DVB_PLL if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_DIB3000MC
-	select MEDIA_TUNER_MT2060 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_MT352 if MEDIA_SUBDRV_AUTOSELECT
+	depends on !DVB_USB_UMT_010
+	depends on BACKPORT_DVB_USB
+	select DVB_PLL if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select BACKPORT_DVB_DIB3000MC
+	select MEDIA_TUNER_MT2060 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_MT352 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
 	help
 	  Say Y here to support the HanfTek UMT-010 USB2.0 stick-sized DVB-T receiver.
 
-config DVB_USB_CXUSB
+config BACKPORT_DVB_USB_CXUSB
 	tristate "Conexant USB2.0 hybrid reference design support"
-	depends on DVB_USB
-	select DVB_PLL if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_CX22702 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_LGDT330X if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_MT352 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_ZL10353 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_DIB7000P if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_TUNER_DIB0070 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_ATBM8830 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_LGS8GXX if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_SI2168 if MEDIA_SUBDRV_AUTOSELECT
-	select MEDIA_TUNER_SIMPLE if MEDIA_SUBDRV_AUTOSELECT
-	select MEDIA_TUNER_XC2028 if MEDIA_SUBDRV_AUTOSELECT
-	select MEDIA_TUNER_MXL5005S if MEDIA_SUBDRV_AUTOSELECT
-	select MEDIA_TUNER_MAX2165 if MEDIA_SUBDRV_AUTOSELECT
-	select MEDIA_TUNER_SI2157 if MEDIA_SUBDRV_AUTOSELECT
+	depends on !DVB_USB_CXUSB
+	depends on BACKPORT_DVB_USB
+	select DVB_PLL if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_CX22702 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_LGDT330X if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_MT352 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_ZL10353 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_DIB7000P if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_TUNER_DIB0070 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_ATBM8830 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_LGS8GXX if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_SI2168 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select MEDIA_TUNER_SIMPLE if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select MEDIA_TUNER_XC2028 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select MEDIA_TUNER_MXL5005S if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select MEDIA_TUNER_MAX2165 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select MEDIA_TUNER_SI2157 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
 	help
 	  Say Y here to support the Conexant USB2.0 hybrid reference design.
 	  Currently, only DVB and ATSC modes are supported, analog mode
@@ -131,32 +140,35 @@ config DVB_USB_CXUSB
 	  Medion MD95700 hybrid USB2.0 device.
 	  DViCO FusionHDTV (Bluebird) USB2.0 devices
 
-config DVB_USB_M920X
+config BACKPORT_DVB_USB_M920X
 	tristate "Uli m920x DVB-T USB2.0 support"
-	depends on DVB_USB
-	select DVB_MT352 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_TDA1004X if MEDIA_SUBDRV_AUTOSELECT
-	select MEDIA_TUNER_QT1010 if MEDIA_SUBDRV_AUTOSELECT
-	select MEDIA_TUNER_TDA827X if MEDIA_SUBDRV_AUTOSELECT
-	select MEDIA_TUNER_SIMPLE if MEDIA_SUBDRV_AUTOSELECT
+	depends on !DVB_USB_M920X
+	depends on BACKPORT_DVB_USB
+	select DVB_MT352 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_TDA1004X if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select MEDIA_TUNER_QT1010 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select MEDIA_TUNER_TDA827X if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select MEDIA_TUNER_SIMPLE if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
 	help
 	  Say Y here to support the MSI Mega Sky 580 USB2.0 DVB-T receiver.
 	  Currently, only devices with a product id of
 	  "DTV USB MINI" (in cold state) are supported.
 	  Firmware required.
 
-config DVB_USB_DIGITV
+config BACKPORT_DVB_USB_DIGITV
 	tristate "Nebula Electronics uDigiTV DVB-T USB2.0 support"
-	depends on DVB_USB
-	select DVB_PLL if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_NXT6000 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_MT352 if MEDIA_SUBDRV_AUTOSELECT
+	depends on !DVB_USB_DIGITV
+	depends on BACKPORT_DVB_USB
+	select DVB_PLL if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_NXT6000 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_MT352 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
 	help
 	  Say Y here to support the Nebula Electronics uDigitV USB2.0 DVB-T receiver.
 
-config DVB_USB_VP7045
+config BACKPORT_DVB_USB_VP7045
 	tristate "TwinhanDTV Alpha/MagicBoxII, DNTV tinyUSB2, Beetle USB2.0 support"
-	depends on DVB_USB
+	depends on !DVB_USB_VP7045
+	depends on BACKPORT_DVB_USB
 	help
 	  Say Y here to support the
 
@@ -168,9 +180,10 @@ config DVB_USB_VP7045
 
 	  DVB-T USB2.0 receivers.
 
-config DVB_USB_VP702X
+config BACKPORT_DVB_USB_VP702X
 	tristate "TwinhanDTV StarBox and clones DVB-S USB2.0 support"
-	depends on DVB_USB
+	depends on !DVB_USB_VP702X
+	depends on BACKPORT_DVB_USB
 	help
 	  Say Y here to support the
 
@@ -180,42 +193,46 @@ config DVB_USB_VP702X
 
 	  DVB-S USB2.0 receivers.
 
-config DVB_USB_GP8PSK
+config BACKPORT_DVB_USB_GP8PSK
 	tristate "GENPIX 8PSK->USB module support"
-	depends on DVB_USB
+	depends on !DVB_USB_GP8PSK
+	depends on BACKPORT_DVB_USB
 	help
 	  Say Y here to support the
 	    GENPIX 8psk module
 
 	  DVB-S USB2.0 receivers.
 
-config DVB_USB_NOVA_T_USB2
+config BACKPORT_DVB_USB_NOVA_T_USB2
 	tristate "Hauppauge WinTV-NOVA-T usb2 DVB-T USB2.0 support"
-	depends on DVB_USB
-	select DVB_DIB3000MC
-	select DVB_PLL if MEDIA_SUBDRV_AUTOSELECT
-	select MEDIA_TUNER_MT2060 if MEDIA_SUBDRV_AUTOSELECT
+	depends on !DVB_USB_NOVA_T_USB2
+	depends on BACKPORT_DVB_USB
+	select BACKPORT_DVB_DIB3000MC
+	select DVB_PLL if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select MEDIA_TUNER_MT2060 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
 	help
 	  Say Y here to support the Hauppauge WinTV-NOVA-T usb2 DVB-T USB2.0 receiver.
 
-config DVB_USB_TTUSB2
+config BACKPORT_DVB_USB_TTUSB2
 	tristate "Pinnacle 400e DVB-S USB2.0 support"
-	depends on DVB_USB
-	select DVB_TDA10086 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_LNBP21 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_TDA826X if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_TDA10023 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_TDA10048 if MEDIA_SUBDRV_AUTOSELECT
-	select MEDIA_TUNER_TDA827X if MEDIA_SUBDRV_AUTOSELECT
+	depends on !DVB_USB_TTUSB2
+	depends on BACKPORT_DVB_USB
+	select DVB_TDA10086 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_LNBP21 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_TDA826X if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_TDA10023 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_TDA10048 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select MEDIA_TUNER_TDA827X if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
 	help
 	  Say Y here to support the Pinnacle 400e DVB-S USB2.0 receiver and
 	  the TechnoTrend CT-3650 CI DVB-C/T USB2.0 receiver. The
 	  firmware protocol used by this module is similar to the one used by the
 	  old ttusb-driver - that's why the module is called dvb-usb-ttusb2.
 
-config DVB_USB_DTT200U
+config BACKPORT_DVB_USB_DTT200U
 	tristate "WideView WT-200U and WT-220U (pen) DVB-T USB2.0 support (Yakumo/Hama/Typhoon/Yuan)"
-	depends on DVB_USB
+	depends on !DVB_USB_DTT200U
+	depends on BACKPORT_DVB_USB
 	help
 	  Say Y here to support the WideView/Yakumo/Hama/Typhoon/Yuan DVB-T USB2.0 receiver.
 
@@ -223,100 +240,110 @@ config DVB_USB_DTT200U
 
 	  The WT-220U and its clones are pen-sized.
 
-config DVB_USB_OPERA1
+config BACKPORT_DVB_USB_OPERA1
 	tristate "Opera1 DVB-S USB2.0 receiver"
-	depends on DVB_USB
-	select DVB_STV0299 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_PLL if MEDIA_SUBDRV_AUTOSELECT
+	depends on !DVB_USB_OPERA1
+	depends on BACKPORT_DVB_USB
+	select DVB_STV0299 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_PLL if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
 	help
 	  Say Y here to support the Opera DVB-S USB2.0 receiver.
 
-config DVB_USB_AF9005
+config BACKPORT_DVB_USB_AF9005
 	tristate "Afatech AF9005 DVB-T USB1.1 support"
-	depends on DVB_USB
-	select MEDIA_TUNER_MT2060 if MEDIA_SUBDRV_AUTOSELECT
-	select MEDIA_TUNER_QT1010 if MEDIA_SUBDRV_AUTOSELECT
+	depends on !DVB_USB_AF9005
+	depends on BACKPORT_DVB_USB
+	select MEDIA_TUNER_MT2060 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select MEDIA_TUNER_QT1010 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
 	help
 	  Say Y here to support the Afatech AF9005 based DVB-T USB1.1 receiver
 	  and the TerraTec Cinergy T USB XE (Rev.1)
 
-config DVB_USB_AF9005_REMOTE
+config BACKPORT_DVB_USB_AF9005_REMOTE
 	tristate "Afatech AF9005 default remote control support"
-	depends on DVB_USB_AF9005
+	depends on !DVB_USB_AF9005_REMOTE
+	depends on BACKPORT_DVB_USB_AF9005
 	help
 	  Say Y here to support the default remote control decoding for the
 	  Afatech AF9005 based receiver.
 
-config DVB_USB_PCTV452E
+config BACKPORT_DVB_USB_PCTV452E
 	tristate "Pinnacle PCTV HDTV Pro USB device/TT Connect S2-3600"
-	depends on DVB_USB
-	select TTPCI_EEPROM
-	select DVB_LNBP22 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_STB0899 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_STB6100 if MEDIA_SUBDRV_AUTOSELECT
+	depends on !DVB_USB_PCTV452E
+	depends on BACKPORT_DVB_USB
+	select BACKPORT_TTPCI_EEPROM
+	select DVB_LNBP22 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_STB0899 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_STB6100 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
 	help
 	  Support for external USB adapter designed by Pinnacle,
 	  shipped under the brand name 'PCTV HDTV Pro USB'.
 	  Also supports TT Connect S2-3600/3650 cards.
 	  Say Y if you own such a device and want to use it.
 
-config DVB_USB_DW2102
+config BACKPORT_DVB_USB_DW2102
 	tristate "DvbWorld & TeVii DVB-S/S2 USB2.0 support"
-	depends on DVB_USB
-	select DVB_PLL if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_STV0299 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_STV0288 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_STB6000 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_CX24116 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_SI21XX if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_TDA10023 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_MT312 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_ZL10039 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_DS3000 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_TS2020 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_STB6100 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_STV6110 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_STV0900 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_M88RS2000 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_M88DS3103 if MEDIA_SUBDRV_AUTOSELECT
+	depends on !DVB_USB_DW2102
+	depends on BACKPORT_DVB_USB
+	select DVB_PLL if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_STV0299 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_STV0288 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_STB6000 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_CX24116 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_SI21XX if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_TDA10023 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_MT312 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_ZL10039 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_DS3000 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_TS2020 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_STB6100 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_STV6110 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_STV0900 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_M88RS2000 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_M88DS3103 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
 	help
 	  Say Y here to support the DvbWorld, TeVii, Prof, TechnoTrend
 	  DVB-S/S2 USB2.0 receivers.
 
-config DVB_USB_CINERGY_T2
+config BACKPORT_DVB_USB_CINERGY_T2
 	tristate "Terratec CinergyT2/qanu USB 2.0 DVB-T receiver"
-	depends on DVB_USB
+	depends on !DVB_USB_CINERGY_T2
+	depends on BACKPORT_DVB_USB
 	help
 	  Support for "TerraTec CinergyT2" USB2.0 Highspeed DVB Receivers
 
 	  Say Y if you own such a device and want to use it.
 
-config DVB_USB_DTV5100
+config BACKPORT_DVB_USB_DTV5100
 	tristate "AME DTV-5100 USB2.0 DVB-T support"
-	depends on DVB_USB
-	select DVB_ZL10353 if MEDIA_SUBDRV_AUTOSELECT
-	select MEDIA_TUNER_QT1010 if MEDIA_SUBDRV_AUTOSELECT
+	depends on !DVB_USB_DTV5100
+	depends on BACKPORT_DVB_USB
+	select DVB_ZL10353 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select MEDIA_TUNER_QT1010 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
 	help
 	  Say Y here to support the AME DTV-5100 USB2.0 DVB-T receiver.
 
-config DVB_USB_FRIIO
+config BACKPORT_DVB_USB_FRIIO
 	tristate "Friio ISDB-T USB2.0 Receiver support"
-	depends on DVB_USB
+	depends on !DVB_USB_FRIIO
+	depends on BACKPORT_DVB_USB
 	help
 	  Say Y here to support the Japanese DTV receiver Friio.
 
-config DVB_USB_AZ6027
+config BACKPORT_DVB_USB_AZ6027
 	tristate "Azurewave DVB-S/S2 USB2.0 AZ6027 support"
-	depends on DVB_USB
-	select DVB_STB0899 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_STB6100 if MEDIA_SUBDRV_AUTOSELECT
+	depends on !DVB_USB_AZ6027
+	depends on BACKPORT_DVB_USB
+	select DVB_STB0899 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_STB6100 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
 	help
 	  Say Y here to support the AZ6027 device
 
-config DVB_USB_TECHNISAT_USB2
+config BACKPORT_DVB_USB_TECHNISAT_USB2
 	tristate "Technisat DVB-S/S2 USB2.0 support"
-	depends on DVB_USB
-	select DVB_STV090x if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_STV6110x if MEDIA_SUBDRV_AUTOSELECT
+	depends on !DVB_USB_TECHNISAT_USB2
+	depends on BACKPORT_DVB_USB
+	select DVB_STV090x if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_STV6110x if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
 	help
 	  Say Y here to support the Technisat USB2 DVB-S/S2 device
diff --git a/backports/drivers/media/usb/em28xx/Kconfig b/backports/drivers/media/usb/em28xx/Kconfig
index e382210..866cee3 100644
--- a/backports/drivers/media/usb/em28xx/Kconfig
+++ b/backports/drivers/media/usb/em28xx/Kconfig
@@ -1,17 +1,19 @@
-config VIDEO_EM28XX
+config BACKPORT_VIDEO_EM28XX
 	tristate "Empia EM28xx USB devices support"
-	depends on VIDEO_DEV && I2C
-	select VIDEO_TUNER
-	select VIDEO_TVEEPROM
+	depends on !VIDEO_EM28XX
+	depends on BACKPORT_VIDEO_DEV && I2C
+	select BACKPORT_VIDEO_TUNER
+	select BACKPORT_VIDEO_TVEEPROM
 
-config VIDEO_EM28XX_V4L2
+config BACKPORT_VIDEO_EM28XX_V4L2
 	tristate "Empia EM28xx analog TV, video capture and/or webcam support"
-	depends on VIDEO_EM28XX
-	select VIDEOBUF2_VMALLOC
-	select VIDEO_SAA711X if MEDIA_SUBDRV_AUTOSELECT
-	select VIDEO_TVP5150 if MEDIA_SUBDRV_AUTOSELECT
-	select VIDEO_MSP3400 if MEDIA_SUBDRV_AUTOSELECT
-	select VIDEO_MT9V011 if MEDIA_SUBDRV_AUTOSELECT
+	depends on !VIDEO_EM28XX_V4L2
+	depends on BACKPORT_VIDEO_EM28XX
+	select BACKPORT_VIDEOBUF2_VMALLOC
+	select VIDEO_SAA711X if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select VIDEO_TVP5150 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select VIDEO_MSP3400 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select VIDEO_MT9V011 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
 
 	---help---
 	  This is a video4linux driver for Empia 28xx based TV cards.
@@ -19,8 +21,9 @@ config VIDEO_EM28XX_V4L2
 	  To compile this driver as a module, choose M here: the
 	  module will be called em28xx
 
-config VIDEO_EM28XX_ALSA
+config BACKPORT_VIDEO_EM28XX_ALSA
 	depends on VIDEO_EM28XX && SND
+	depends on !VIDEO_EM28XX_ALSA
 	select SND_PCM
 	tristate "Empia EM28xx ALSA audio module"
 	---help---
@@ -34,40 +37,42 @@ config VIDEO_EM28XX_ALSA
 	  To compile this driver as a module, choose M here: the
 	  module will be called em28xx-alsa
 
-config VIDEO_EM28XX_DVB
+config BACKPORT_VIDEO_EM28XX_DVB
 	tristate "DVB/ATSC Support for em28xx based TV cards"
-	depends on VIDEO_EM28XX && DVB_CORE
-	select DVB_LGDT330X if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_LGDT3305 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_ZL10353 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_TDA10023 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_S921 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_DRXD if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_CXD2820R if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_DRXK if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_TDA18271C2DD if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_TDA10071 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_A8293 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_MT352 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_S5H1409 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_MB86A20S if MEDIA_SUBDRV_AUTOSELECT
-	select MEDIA_TUNER_QT1010 if MEDIA_SUBDRV_AUTOSELECT
-	select MEDIA_TUNER_TDA18271 if MEDIA_SUBDRV_AUTOSELECT
-	select MEDIA_TUNER_TDA18212 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_M88DS3103 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_TS2020 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_DRX39XYJ if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_SI2168 if MEDIA_SUBDRV_AUTOSELECT
-	select MEDIA_TUNER_SI2157 if MEDIA_SUBDRV_AUTOSELECT
+	depends on !VIDEO_EM28XX_DVB
+	depends on VIDEO_EM28XX && BACKPORT_DVB_CORE
+	select DVB_LGDT330X if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_LGDT3305 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_ZL10353 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_TDA10023 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_S921 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_DRXD if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_CXD2820R if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_DRXK if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_TDA18271C2DD if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_TDA10071 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_A8293 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_MT352 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_S5H1409 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_MB86A20S if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select MEDIA_TUNER_QT1010 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select MEDIA_TUNER_TDA18271 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select MEDIA_TUNER_TDA18212 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_M88DS3103 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_TS2020 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_DRX39XYJ if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_SI2168 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select MEDIA_TUNER_SI2157 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
 	---help---
 	  This adds support for DVB cards based on the
 	  Empiatech em28xx chips.
 
-config VIDEO_EM28XX_RC
+config BACKPORT_VIDEO_EM28XX_RC
         tristate "EM28XX Remote Controller support"
-        depends on RC_CORE
-        depends on VIDEO_EM28XX
+	depends on !VIDEO_EM28XX_RC
+        depends on BACKPORT_RC_CORE
+        depends on BACKPORT_VIDEO_EM28XX
         depends on !(RC_CORE=m && VIDEO_EM28XX=y)
-        default VIDEO_EM28XX
+        default BACKPORT_VIDEO_EM28XX
         ---help---
           Enables Remote Controller support on em28xx driver.
diff --git a/backports/drivers/media/usb/go7007/Kconfig b/backports/drivers/media/usb/go7007/Kconfig
index 95a3af6..3f3cbdd 100644
--- a/backports/drivers/media/usb/go7007/Kconfig
+++ b/backports/drivers/media/usb/go7007/Kconfig
@@ -1,18 +1,19 @@
-config VIDEO_GO7007
+config BACKPORT_VIDEO_GO7007
 	tristate "WIS GO7007 MPEG encoder support"
-	depends on VIDEO_DEV && I2C
+	depends on !VIDEO_GO7007
+	depends on BACKPORT_VIDEO_DEV && I2C
 	depends on SND && USB
-	select VIDEOBUF2_VMALLOC
-	select VIDEO_TUNER
-	select CYPRESS_FIRMWARE
+	select BACKPORT_VIDEOBUF2_VMALLOC
+	select BACKPORT_VIDEO_TUNER
+	select BACKPORT_CYPRESS_FIRMWARE
 	select SND_PCM
 	select VIDEO_SONY_BTF_MPX if MEDIA_SUBDRV_AUTOSELECT
-	select VIDEO_SAA711X if MEDIA_SUBDRV_AUTOSELECT
-	select VIDEO_TW2804 if MEDIA_SUBDRV_AUTOSELECT
-	select VIDEO_TW9903 if MEDIA_SUBDRV_AUTOSELECT
-	select VIDEO_TW9906 if MEDIA_SUBDRV_AUTOSELECT
-	select VIDEO_OV7640 if MEDIA_SUBDRV_AUTOSELECT
-	select VIDEO_UDA1342 if MEDIA_SUBDRV_AUTOSELECT
+	select VIDEO_SAA711X if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select VIDEO_TW2804 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select VIDEO_TW9903 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select VIDEO_TW9906 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select VIDEO_OV7640 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select VIDEO_UDA1342 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
 	---help---
 	  This is a video4linux driver for the WIS GO7007 MPEG
 	  encoder chip.
@@ -20,9 +21,10 @@ config VIDEO_GO7007
 	  To compile this driver as a module, choose M here: the
 	  module will be called go7007.
 
-config VIDEO_GO7007_USB
+config BACKPORT_VIDEO_GO7007_USB
 	tristate "WIS GO7007 USB support"
-	depends on VIDEO_GO7007 && USB
+	depends on !VIDEO_GO7007_USB
+	depends on BACKPORT_VIDEO_GO7007 && USB
 	---help---
 	  This is a video4linux driver for the WIS GO7007 MPEG
 	  encoder chip over USB.
@@ -30,9 +32,10 @@ config VIDEO_GO7007_USB
 	  To compile this driver as a module, choose M here: the
 	  module will be called go7007-usb.
 
-config VIDEO_GO7007_LOADER
+config BACKPORT_VIDEO_GO7007_LOADER
 	tristate "WIS GO7007 Loader support"
-	depends on VIDEO_GO7007
+	depends on !VIDEO_GO7007_LOADER
+	depends on BACKPORT_VIDEO_GO7007
 	default y
 	---help---
 	  This is a go7007 firmware loader driver for the WIS GO7007
@@ -41,9 +44,10 @@ config VIDEO_GO7007_LOADER
 	  To compile this driver as a module, choose M here: the
 	  module will be called go7007-loader.
 
-config VIDEO_GO7007_USB_S2250_BOARD
+config BACKPORT_VIDEO_GO7007_USB_S2250_BOARD
 	tristate "Sensoray 2250/2251 support"
-	depends on VIDEO_GO7007_USB && USB
+	depends on !VIDEO_GO7007_USB_S2250_BOARD
+	depends on BACKPORT_VIDEO_GO7007_USB && USB
 	---help---
 	  This is a video4linux driver for the Sensoray 2250/2251 device.
 
diff --git a/backports/drivers/media/usb/gspca/Kconfig b/backports/drivers/media/usb/gspca/Kconfig
index 6a9c45f..a806b29 100644
--- a/backports/drivers/media/usb/gspca/Kconfig
+++ b/backports/drivers/media/usb/gspca/Kconfig
@@ -1,6 +1,7 @@
-menuconfig USB_GSPCA
+menuconfig BACKPORT_USB_GSPCA
 	tristate "GSPCA based webcams"
-	depends on VIDEO_V4L2
+	depends on !USB_GSPCA
+	depends on BACKPORT_VIDEO_V4L2
 	depends on INPUT || INPUT=n
 	default m
 	---help---
@@ -16,33 +17,36 @@ menuconfig USB_GSPCA
 	  module will be called gspca_main.
 
 
-if USB_GSPCA && VIDEO_V4L2
+if USB_GSPCA && BACKPORT_VIDEO_V4L2
 
 source "$BACKPORT_DIR/drivers/media/usb/gspca/m5602/Kconfig"
 source "$BACKPORT_DIR/drivers/media/usb/gspca/stv06xx/Kconfig"
 source "$BACKPORT_DIR/drivers/media/usb/gspca/gl860/Kconfig"
 
-config USB_GSPCA_BENQ
+config BACKPORT_USB_GSPCA_BENQ
 	tristate "Benq USB Camera Driver"
-	depends on VIDEO_V4L2 && USB_GSPCA
+	depends on !USB_GSPCA_BENQ
+	depends on BACKPORT_VIDEO_V4L2 && USB_GSPCA
 	help
 	  Say Y here if you want support for the Benq DC E300 camera.
 
 	  To compile this driver as a module, choose M here: the
 	  module will be called gspca_benq.
 
-config USB_GSPCA_CONEX
+config BACKPORT_USB_GSPCA_CONEX
 	tristate "Conexant Camera Driver"
-	depends on VIDEO_V4L2 && USB_GSPCA
+	depends on !USB_GSPCA_CONEX
+	depends on BACKPORT_VIDEO_V4L2 && USB_GSPCA
 	help
 	  Say Y here if you want support for cameras based on the Conexant chip.
 
 	  To compile this driver as a module, choose M here: the
 	  module will be called gspca_conex.
 
-config USB_GSPCA_CPIA1
+config BACKPORT_USB_GSPCA_CPIA1
 	tristate "cpia CPiA (version 1) Camera Driver"
-	depends on VIDEO_V4L2 && USB_GSPCA
+	depends on !USB_GSPCA_CPIA1
+	depends on BACKPORT_VIDEO_V4L2 && USB_GSPCA
 	help
 	  Say Y here if you want support for USB cameras based on the cpia
 	  CPiA chip. Note that you need atleast version 0.6.4 of libv4l for
@@ -51,9 +55,10 @@ config USB_GSPCA_CPIA1
 	  To compile this driver as a module, choose M here: the
 	  module will be called gspca_cpia1.
 
-config USB_GSPCA_DTCS033
+config BACKPORT_USB_GSPCA_DTCS033
 	tristate "DTCS033 (Scopium) USB Astro-Camera Driver"
-	depends on VIDEO_V4L2 && USB_GSPCA
+	depends on !USB_GSPCA_DTCS033
+	depends on BACKPORT_VIDEO_V4L2 && USB_GSPCA
 	help
 	  Say Y here if you want support for the Scopium camera
 	  for planetary astrophotography.
@@ -61,36 +66,40 @@ config USB_GSPCA_DTCS033
 	  To compile this driver as a module, choose M here: the
 	  module will be called gspca_dtcs033.
 
-config USB_GSPCA_ETOMS
+config BACKPORT_USB_GSPCA_ETOMS
 	tristate "Etoms USB Camera Driver"
-	depends on VIDEO_V4L2 && USB_GSPCA
+	depends on !USB_GSPCA_ETOMS
+	depends on BACKPORT_VIDEO_V4L2 && USB_GSPCA
 	help
 	  Say Y here if you want support for cameras based on the Etoms chip.
 
 	  To compile this driver as a module, choose M here: the
 	  module will be called gspca_etoms.
 
-config USB_GSPCA_FINEPIX
+config BACKPORT_USB_GSPCA_FINEPIX
 	tristate "Fujifilm FinePix USB V4L2 driver"
-	depends on VIDEO_V4L2 && USB_GSPCA
+	depends on !USB_GSPCA_FINEPIX
+	depends on BACKPORT_VIDEO_V4L2 && USB_GSPCA
 	help
 	  Say Y here if you want support for cameras based on the FinePix chip.
 
 	  To compile this driver as a module, choose M here: the
 	  module will be called gspca_finepix.
 
-config USB_GSPCA_JEILINJ
+config BACKPORT_USB_GSPCA_JEILINJ
 	tristate "Jeilin JPEG USB V4L2 driver"
-	depends on VIDEO_V4L2 && USB_GSPCA
+	depends on !USB_GSPCA_JEILINJ
+	depends on BACKPORT_VIDEO_V4L2 && USB_GSPCA
 	help
 	  Say Y here if you want support for cameras based on this Jeilin chip.
 
 	  To compile this driver as a module, choose M here: the
 	  module will be called gspca_jeilinj.
 
-config USB_GSPCA_JL2005BCD
+config BACKPORT_USB_GSPCA_JL2005BCD
 	tristate "JL2005B/C/D USB V4L2 driver"
-	depends on VIDEO_V4L2 && USB_GSPCA
+	depends on !USB_GSPCA_JL2005BCD
+	depends on BACKPORT_VIDEO_V4L2 && USB_GSPCA
 	help
 	  Say Y here if you want support for cameras based the
 	  JL2005B, JL2005C, or JL2005D chip.
@@ -98,54 +107,60 @@ config USB_GSPCA_JL2005BCD
 	  To compile this driver as a module, choose M here: the
 	  module will be called gspca_jl2005bcd.
 
-config USB_GSPCA_KINECT
+config BACKPORT_USB_GSPCA_KINECT
 	tristate "Kinect sensor device USB Camera Driver"
-	depends on VIDEO_V4L2 && USB_GSPCA
+	depends on !USB_GSPCA_KINECT
+	depends on BACKPORT_VIDEO_V4L2 && USB_GSPCA
 	help
 	  Say Y here if you want support for the Microsoft Kinect sensor device.
 
 	  To compile this driver as a module, choose M here: the
 	  module will be called gspca_kinect.
 
-config USB_GSPCA_KONICA
+config BACKPORT_USB_GSPCA_KONICA
 	tristate "Konica USB Camera V4L2 driver"
-	depends on VIDEO_V4L2 && USB_GSPCA
+	depends on !USB_GSPCA_KONICA
+	depends on BACKPORT_VIDEO_V4L2 && USB_GSPCA
 	help
 	  Say Y here if you want support for cameras based on the Konica chip.
 
 	  To compile this driver as a module, choose M here: the
 	  module will be called gspca_konica.
 
-config USB_GSPCA_MARS
+config BACKPORT_USB_GSPCA_MARS
 	tristate "Mars USB Camera Driver"
-	depends on VIDEO_V4L2 && USB_GSPCA
+	depends on !USB_GSPCA_MARS
+	depends on BACKPORT_VIDEO_V4L2 && USB_GSPCA
 	help
 	  Say Y here if you want support for cameras based on the Mars chip.
 
 	  To compile this driver as a module, choose M here: the
 	  module will be called gspca_mars.
 
-config USB_GSPCA_MR97310A
+config BACKPORT_USB_GSPCA_MR97310A
 	tristate "Mars-Semi MR97310A USB Camera Driver"
-	depends on VIDEO_V4L2 && USB_GSPCA
+	depends on !USB_GSPCA_MR97310A
+	depends on BACKPORT_VIDEO_V4L2 && USB_GSPCA
 	help
 	  Say Y here if you want support for cameras based on the MR97310A chip.
 
 	  To compile this driver as a module, choose M here: the
 	  module will be called gspca_mr97310a.
 
-config USB_GSPCA_NW80X
+config BACKPORT_USB_GSPCA_NW80X
 	tristate "Divio based (NW80x) USB Camera Driver"
-	depends on VIDEO_V4L2 && USB_GSPCA
+	depends on !USB_GSPCA_NW80X
+	depends on BACKPORT_VIDEO_V4L2 && USB_GSPCA
 	help
 	  Say Y here if you want support for cameras based on the NW80x chips.
 
 	  To compile this driver as a module, choose M here: the
 	  module will be called gspca_nw80x.
 
-config USB_GSPCA_OV519
+config BACKPORT_USB_GSPCA_OV519
 	tristate "OV51x / OVFX2 / W996xCF USB Camera Driver"
-	depends on VIDEO_V4L2 && USB_GSPCA
+	depends on !USB_GSPCA_OV519
+	depends on BACKPORT_VIDEO_V4L2 && USB_GSPCA
 	help
 	  Say Y here if you want support for cameras based on one of these:
 	  OV511(+), OV518(+), OV519, OVFX2, W9967CF, W9968CF
@@ -153,9 +168,10 @@ config USB_GSPCA_OV519
 	  To compile this driver as a module, choose M here: the
 	  module will be called gspca_ov519.
 
-config USB_GSPCA_OV534
+config BACKPORT_USB_GSPCA_OV534
 	tristate "OV534 OV772x USB Camera Driver"
-	depends on VIDEO_V4L2 && USB_GSPCA
+	depends on !USB_GSPCA_OV534
+	depends on BACKPORT_VIDEO_V4L2 && USB_GSPCA
 	help
 	  Say Y here if you want support for cameras based on the OV534 chip
 	  and sensor OV772x (e.g. Sony Playstation EYE)
@@ -163,9 +179,10 @@ config USB_GSPCA_OV534
 	  To compile this driver as a module, choose M here: the
 	  module will be called gspca_ov534.
 
-config USB_GSPCA_OV534_9
+config BACKPORT_USB_GSPCA_OV534_9
 	tristate "OV534 OV965x USB Camera Driver"
-	depends on VIDEO_V4L2 && USB_GSPCA
+	depends on !USB_GSPCA_OV534_9
+	depends on BACKPORT_VIDEO_V4L2 && USB_GSPCA
 	help
 	  Say Y here if you want support for cameras based on the OV534 chip
 	  and sensor OV965x (e.g. Hercules Dualpix)
@@ -173,36 +190,40 @@ config USB_GSPCA_OV534_9
 	  To compile this driver as a module, choose M here: the
 	  module will be called gspca_ov534_9.
 
-config USB_GSPCA_PAC207
+config BACKPORT_USB_GSPCA_PAC207
 	tristate "Pixart PAC207 USB Camera Driver"
-	depends on VIDEO_V4L2 && USB_GSPCA
+	depends on !USB_GSPCA_PAC207
+	depends on BACKPORT_VIDEO_V4L2 && USB_GSPCA
 	help
 	  Say Y here if you want support for cameras based on the PAC207 chip.
 
 	  To compile this driver as a module, choose M here: the
 	  module will be called gspca_pac207.
 
-config USB_GSPCA_PAC7302
+config BACKPORT_USB_GSPCA_PAC7302
 	tristate "Pixart PAC7302 USB Camera Driver"
-	depends on VIDEO_V4L2 && USB_GSPCA
+	depends on !USB_GSPCA_PAC7302
+	depends on BACKPORT_VIDEO_V4L2 && USB_GSPCA
 	help
 	  Say Y here if you want support for cameras based on the PAC7302 chip.
 
 	  To compile this driver as a module, choose M here: the
 	  module will be called gspca_pac7302.
 
-config USB_GSPCA_PAC7311
+config BACKPORT_USB_GSPCA_PAC7311
 	tristate "Pixart PAC7311 USB Camera Driver"
-	depends on VIDEO_V4L2 && USB_GSPCA
+	depends on !USB_GSPCA_PAC7311
+	depends on BACKPORT_VIDEO_V4L2 && USB_GSPCA
 	help
 	  Say Y here if you want support for cameras based on the PAC7311 chip.
 
 	  To compile this driver as a module, choose M here: the
 	  module will be called gspca_pac7311.
 
-config USB_GSPCA_SE401
+config BACKPORT_USB_GSPCA_SE401
 	tristate "SE401 USB Camera Driver"
-	depends on VIDEO_V4L2 && USB_GSPCA
+	depends on !USB_GSPCA_SE401
+	depends on BACKPORT_VIDEO_V4L2 && USB_GSPCA
 	help
 	 Say Y here if you want support for cameras based on the
 	 Endpoints (formerly known as AOX) se401 chip.
@@ -210,9 +231,10 @@ config USB_GSPCA_SE401
 	 To compile this driver as a module, choose M here: the
 	 module will be called gspca_se401.
 
-config USB_GSPCA_SN9C2028
+config BACKPORT_USB_GSPCA_SN9C2028
 	tristate "SONIX Dual-Mode USB Camera Driver"
-	depends on VIDEO_V4L2 && USB_GSPCA
+	depends on !USB_GSPCA_SN9C2028
+	depends on BACKPORT_VIDEO_V4L2 && USB_GSPCA
 	help
 	  Say Y here if you want streaming support for Sonix SN9C2028 cameras.
 	  These are supported as stillcams in libgphoto2/camlibs/sonix.
@@ -220,9 +242,10 @@ config USB_GSPCA_SN9C2028
 	  To compile this driver as a module, choose M here: the
 	  module will be called gspca_sn9c2028.
 
-config USB_GSPCA_SN9C20X
+config BACKPORT_USB_GSPCA_SN9C20X
 	tristate "SN9C20X USB Camera Driver"
-	depends on VIDEO_V4L2 && USB_GSPCA
+	depends on !USB_GSPCA_SN9C20X
+	depends on BACKPORT_VIDEO_V4L2 && USB_GSPCA
 	help
 	 Say Y here if you want support for cameras based on the
 	 sn9c20x chips (SN9C201 and SN9C202).
@@ -230,9 +253,10 @@ config USB_GSPCA_SN9C20X
 	 To compile this driver as a module, choose M here: the
 	 module will be called gspca_sn9c20x.
 
-config USB_GSPCA_SONIXB
+config BACKPORT_USB_GSPCA_SONIXB
 	tristate "SONIX Bayer USB Camera Driver"
-	depends on VIDEO_V4L2 && USB_GSPCA
+	depends on !USB_GSPCA_SONIXB
+	depends on BACKPORT_VIDEO_V4L2 && USB_GSPCA
 	help
 	  Say Y here if you want support for cameras based on the Sonix
 	  chips with Bayer format (SN9C101, SN9C102 and SN9C103).
@@ -240,9 +264,10 @@ config USB_GSPCA_SONIXB
 	  To compile this driver as a module, choose M here: the
 	  module will be called gspca_sonixb.
 
-config USB_GSPCA_SONIXJ
+config BACKPORT_USB_GSPCA_SONIXJ
 	tristate "SONIX JPEG USB Camera Driver"
-	depends on VIDEO_V4L2 && USB_GSPCA
+	depends on !USB_GSPCA_SONIXJ
+	depends on BACKPORT_VIDEO_V4L2 && USB_GSPCA
 	help
 	  Say Y here if you want support for cameras based on the Sonix
 	  chips with JPEG format (SN9C102P, SN9C105 and >= SN9C110).
@@ -250,126 +275,140 @@ config USB_GSPCA_SONIXJ
 	  To compile this driver as a module, choose M here: the
 	  module will be called gspca_sonixj
 
-config USB_GSPCA_SPCA500
+config BACKPORT_USB_GSPCA_SPCA500
 	tristate "SPCA500 USB Camera Driver"
-	depends on VIDEO_V4L2 && USB_GSPCA
+	depends on !USB_GSPCA_SPCA500
+	depends on BACKPORT_VIDEO_V4L2 && USB_GSPCA
 	help
 	  Say Y here if you want support for cameras based on the SPCA500 chip.
 
 	  To compile this driver as a module, choose M here: the
 	  module will be called gspca_spca500.
 
-config USB_GSPCA_SPCA501
+config BACKPORT_USB_GSPCA_SPCA501
 	tristate "SPCA501 USB Camera Driver"
-	depends on VIDEO_V4L2 && USB_GSPCA
+	depends on !USB_GSPCA_SPCA501
+	depends on BACKPORT_VIDEO_V4L2 && USB_GSPCA
 	help
 	  Say Y here if you want support for cameras based on the SPCA501 chip.
 
 	  To compile this driver as a module, choose M here: the
 	  module will be called gspca_spca501.
 
-config USB_GSPCA_SPCA505
+config BACKPORT_USB_GSPCA_SPCA505
 	tristate "SPCA505 USB Camera Driver"
-	depends on VIDEO_V4L2 && USB_GSPCA
+	depends on !USB_GSPCA_SPCA505
+	depends on BACKPORT_VIDEO_V4L2 && USB_GSPCA
 	help
 	  Say Y here if you want support for cameras based on the SPCA505 chip.
 
 	  To compile this driver as a module, choose M here: the
 	  module will be called gspca_spca505.
 
-config USB_GSPCA_SPCA506
+config BACKPORT_USB_GSPCA_SPCA506
 	tristate "SPCA506 USB Camera Driver"
-	depends on VIDEO_V4L2 && USB_GSPCA
+	depends on !USB_GSPCA_SPCA506
+	depends on BACKPORT_VIDEO_V4L2 && USB_GSPCA
 	help
 	  Say Y here if you want support for cameras based on the SPCA506 chip.
 
 	  To compile this driver as a module, choose M here: the
 	  module will be called gspca_spca506.
 
-config USB_GSPCA_SPCA508
+config BACKPORT_USB_GSPCA_SPCA508
 	tristate "SPCA508 USB Camera Driver"
-	depends on VIDEO_V4L2 && USB_GSPCA
+	depends on !USB_GSPCA_SPCA508
+	depends on BACKPORT_VIDEO_V4L2 && USB_GSPCA
 	help
 	  Say Y here if you want support for cameras based on the SPCA508 chip.
 
 	  To compile this driver as a module, choose M here: the
 	  module will be called gspca_spca508.
 
-config USB_GSPCA_SPCA561
+config BACKPORT_USB_GSPCA_SPCA561
 	tristate "SPCA561 USB Camera Driver"
-	depends on VIDEO_V4L2 && USB_GSPCA
+	depends on !USB_GSPCA_SPCA561
+	depends on BACKPORT_VIDEO_V4L2 && USB_GSPCA
 	help
 	  Say Y here if you want support for cameras based on the SPCA561 chip.
 
 	  To compile this driver as a module, choose M here: the
 	  module will be called gspca_spca561.
 
-config USB_GSPCA_SPCA1528
+config BACKPORT_USB_GSPCA_SPCA1528
 	tristate "SPCA1528 USB Camera Driver"
-	depends on VIDEO_V4L2 && USB_GSPCA
+	depends on !USB_GSPCA_SPCA1528
+	depends on BACKPORT_VIDEO_V4L2 && USB_GSPCA
 	help
 	  Say Y here if you want support for cameras based on the SPCA1528 chip.
 
 	  To compile this driver as a module, choose M here: the
 	  module will be called gspca_spca1528.
 
-config USB_GSPCA_SQ905
+config BACKPORT_USB_GSPCA_SQ905
 	tristate "SQ Technologies SQ905 based USB Camera Driver"
-	depends on VIDEO_V4L2 && USB_GSPCA
+	depends on !USB_GSPCA_SQ905
+	depends on BACKPORT_VIDEO_V4L2 && USB_GSPCA
 	help
 	  Say Y here if you want support for cameras based on the SQ905 chip.
 
 	  To compile this driver as a module, choose M here: the
 	  module will be called gspca_sq905.
 
-config USB_GSPCA_SQ905C
+config BACKPORT_USB_GSPCA_SQ905C
 	tristate "SQ Technologies SQ905C based USB Camera Driver"
-	depends on VIDEO_V4L2 && USB_GSPCA
+	depends on !USB_GSPCA_SQ905C
+	depends on BACKPORT_VIDEO_V4L2 && USB_GSPCA
 	help
 	  Say Y here if you want support for cameras based on the SQ905C chip.
 
 	  To compile this driver as a module, choose M here: the
 	  module will be called gspca_sq905c.
 
-config USB_GSPCA_SQ930X
+config BACKPORT_USB_GSPCA_SQ930X
 	tristate "SQ Technologies SQ930X based USB Camera Driver"
-	depends on VIDEO_V4L2 && USB_GSPCA
+	depends on !USB_GSPCA_SQ930X
+	depends on BACKPORT_VIDEO_V4L2 && USB_GSPCA
 	help
 	  Say Y here if you want support for cameras based on the SQ930X chip.
 
 	  To compile this driver as a module, choose M here: the
 	  module will be called gspca_sq930x.
 
-config USB_GSPCA_STK014
+config BACKPORT_USB_GSPCA_STK014
 	tristate "Syntek DV4000 (STK014) USB Camera Driver"
-	depends on VIDEO_V4L2 && USB_GSPCA
+	depends on !USB_GSPCA_STK014
+	depends on BACKPORT_VIDEO_V4L2 && USB_GSPCA
 	help
 	  Say Y here if you want support for cameras based on the STK014 chip.
 
 	  To compile this driver as a module, choose M here: the
 	  module will be called gspca_stk014.
 
-config USB_GSPCA_STK1135
+config BACKPORT_USB_GSPCA_STK1135
 	tristate "Syntek STK1135 USB Camera Driver"
-	depends on VIDEO_V4L2 && USB_GSPCA
+	depends on !USB_GSPCA_STK1135
+	depends on BACKPORT_VIDEO_V4L2 && USB_GSPCA
 	help
 	  Say Y here if you want support for cameras based on the STK1135 chip.
 
 	  To compile this driver as a module, choose M here: the
 	  module will be called gspca_stk1135.
 
-config USB_GSPCA_STV0680
+config BACKPORT_USB_GSPCA_STV0680
 	tristate "STV0680 USB Camera Driver"
-	depends on VIDEO_V4L2 && USB_GSPCA
+	depends on !USB_GSPCA_STV0680
+	depends on BACKPORT_VIDEO_V4L2 && USB_GSPCA
 	help
 	  Say Y here if you want support for cameras based on the STV0680 chip.
 
 	  To compile this driver as a module, choose M here: the
 	  module will be called gspca_stv0680.
 
-config USB_GSPCA_SUNPLUS
+config BACKPORT_USB_GSPCA_SUNPLUS
 	tristate "SUNPLUS USB Camera Driver"
-	depends on VIDEO_V4L2 && USB_GSPCA
+	depends on !USB_GSPCA_SUNPLUS
+	depends on BACKPORT_VIDEO_V4L2 && USB_GSPCA
 	help
 	  Say Y here if you want support for cameras based on the Sunplus
 	  SPCA504(abc) SPCA533 SPCA536 chips.
@@ -377,18 +416,20 @@ config USB_GSPCA_SUNPLUS
 	  To compile this driver as a module, choose M here: the
 	  module will be called gspca_sunplus.
 
-config USB_GSPCA_T613
+config BACKPORT_USB_GSPCA_T613
 	tristate "T613 (JPEG Compliance) USB Camera Driver"
-	depends on VIDEO_V4L2 && USB_GSPCA
+	depends on !USB_GSPCA_T613
+	depends on BACKPORT_VIDEO_V4L2 && USB_GSPCA
 	help
 	  Say Y here if you want support for cameras based on the T613 chip.
 
 	  To compile this driver as a module, choose M here: the
 	  module will be called gspca_t613.
 
-config USB_GSPCA_TOPRO
+config BACKPORT_USB_GSPCA_TOPRO
 	tristate "TOPRO USB Camera Driver"
-	depends on VIDEO_V4L2 && USB_GSPCA
+	depends on !USB_GSPCA_TOPRO
+	depends on BACKPORT_VIDEO_V4L2 && USB_GSPCA
 	help
 	  Say Y here if you want support for cameras based on the
 	  TP6800 and TP6810 Topro chips.
@@ -396,9 +437,10 @@ config USB_GSPCA_TOPRO
 	  To compile this driver as a module, choose M here: the
 	  module will be called gspca_topro.
 
-config USB_GSPCA_TOUPTEK
+config BACKPORT_USB_GSPCA_TOUPTEK
 	tristate "Touptek USB Camera Driver"
-	depends on VIDEO_V4L2 && USB_GSPCA
+	depends on !USB_GSPCA_TOUPTEK
+	depends on BACKPORT_VIDEO_V4L2 && USB_GSPCA
 	help
 	  Say Y here if you want support for cameras based on the ToupTek UCMOS
 	  / AmScope MU series camera.
@@ -406,27 +448,30 @@ config USB_GSPCA_TOUPTEK
 	  To compile this driver as a module, choose M here: the
 	  module will be called gspca_touptek.
 
-config USB_GSPCA_TV8532
+config BACKPORT_USB_GSPCA_TV8532
 	tristate "TV8532 USB Camera Driver"
-	depends on VIDEO_V4L2 && USB_GSPCA
+	depends on !USB_GSPCA_TV8532
+	depends on BACKPORT_VIDEO_V4L2 && USB_GSPCA
 	help
 	  Say Y here if you want support for cameras based on the TV8531 chip.
 
 	  To compile this driver as a module, choose M here: the
 	  module will be called gspca_tv8532.
 
-config USB_GSPCA_VC032X
+config BACKPORT_USB_GSPCA_VC032X
 	tristate "VC032X USB Camera Driver"
-	depends on VIDEO_V4L2 && USB_GSPCA
+	depends on !USB_GSPCA_VC032X
+	depends on BACKPORT_VIDEO_V4L2 && USB_GSPCA
 	help
 	  Say Y here if you want support for cameras based on the VC032X chip.
 
 	  To compile this driver as a module, choose M here: the
 	  module will be called gspca_vc032x.
 
-config USB_GSPCA_VICAM
+config BACKPORT_USB_GSPCA_VICAM
 	tristate "ViCam USB Camera Driver"
-	depends on VIDEO_V4L2 && USB_GSPCA
+	depends on !USB_GSPCA_VICAM
+	depends on BACKPORT_VIDEO_V4L2 && USB_GSPCA
 	help
 	  Say Y here if you want support for the 3com homeconnect camera
 	  (vicam).
@@ -434,18 +479,20 @@ config USB_GSPCA_VICAM
 	  To compile this driver as a module, choose M here: the
 	  module will be called gspca_vicam.
 
-config USB_GSPCA_XIRLINK_CIT
+config BACKPORT_USB_GSPCA_XIRLINK_CIT
 	tristate "Xirlink C-It USB Camera Driver"
-	depends on VIDEO_V4L2 && USB_GSPCA
+	depends on !USB_GSPCA_XIRLINK_CIT
+	depends on BACKPORT_VIDEO_V4L2 && USB_GSPCA
 	help
 	  Say Y here if you want support for Xirlink C-It bases cameras.
 
 	  To compile this driver as a module, choose M here: the
 	  module will be called gspca_xirlink_cit.
 
-config USB_GSPCA_ZC3XX
+config BACKPORT_USB_GSPCA_ZC3XX
 	tristate "ZC3XX USB Camera Driver"
-	depends on VIDEO_V4L2 && USB_GSPCA
+	depends on !USB_GSPCA_ZC3XX
+	depends on BACKPORT_VIDEO_V4L2 && USB_GSPCA
 	help
 	  Say Y here if you want support for cameras based on the ZC3XX chip.
 
diff --git a/backports/drivers/media/usb/gspca/gl860/Kconfig b/backports/drivers/media/usb/gspca/gl860/Kconfig
index 22772f5..3ff057a 100644
--- a/backports/drivers/media/usb/gspca/gl860/Kconfig
+++ b/backports/drivers/media/usb/gspca/gl860/Kconfig
@@ -1,6 +1,7 @@
-config USB_GL860
+config BACKPORT_USB_GL860
 	tristate "GL860 USB Camera Driver"
-	depends on VIDEO_V4L2 && USB_GSPCA
+	depends on !USB_GL860
+	depends on BACKPORT_VIDEO_V4L2 && USB_GSPCA
 	help
 	  Say Y here if you want support for cameras based on the GL860 chip.
 
diff --git a/backports/drivers/media/usb/gspca/m5602/Kconfig b/backports/drivers/media/usb/gspca/m5602/Kconfig
index 5a69016..4b3f635 100644
--- a/backports/drivers/media/usb/gspca/m5602/Kconfig
+++ b/backports/drivers/media/usb/gspca/m5602/Kconfig
@@ -1,6 +1,7 @@
-config USB_M5602
+config BACKPORT_USB_M5602
 	tristate "ALi USB m5602 Camera Driver"
-	depends on VIDEO_V4L2 && USB_GSPCA
+	depends on !USB_M5602
+	depends on BACKPORT_VIDEO_V4L2 && USB_GSPCA
 	help
 	  Say Y here if you want support for cameras based on the
 	  ALi m5602 connected to various image sensors.
diff --git a/backports/drivers/media/usb/gspca/stv06xx/Kconfig b/backports/drivers/media/usb/gspca/stv06xx/Kconfig
index 634ad38d..8d8c43c 100644
--- a/backports/drivers/media/usb/gspca/stv06xx/Kconfig
+++ b/backports/drivers/media/usb/gspca/stv06xx/Kconfig
@@ -1,6 +1,7 @@
-config USB_STV06XX
+config BACKPORT_USB_STV06XX
 	tristate "STV06XX USB Camera Driver"
-	depends on USB_GSPCA
+	depends on !USB_STV06XX
+	depends on BACKPORT_USB_GSPCA
 	help
 	  Say Y here if you want support for cameras based on
 	  the ST STV06XX chip.
diff --git a/backports/drivers/media/usb/hackrf/Kconfig b/backports/drivers/media/usb/hackrf/Kconfig
index c9a5af3..f688742 100644
--- a/backports/drivers/media/usb/hackrf/Kconfig
+++ b/backports/drivers/media/usb/hackrf/Kconfig
@@ -1,7 +1,8 @@
-config USB_HACKRF
+config BACKPORT_USB_HACKRF
 	tristate "HackRF"
-	depends on VIDEO_V4L2
-	select VIDEOBUF2_VMALLOC
+	depends on !USB_HACKRF
+	depends on BACKPORT_VIDEO_V4L2
+	select BACKPORT_VIDEOBUF2_VMALLOC
 	---help---
 	  This is a video4linux2 driver for HackRF SDR device.
 
diff --git a/backports/drivers/media/usb/hdpvr/Kconfig b/backports/drivers/media/usb/hdpvr/Kconfig
index ced76cb..c7324d7 100644
--- a/backports/drivers/media/usb/hdpvr/Kconfig
+++ b/backports/drivers/media/usb/hdpvr/Kconfig
@@ -1,7 +1,8 @@
 
-config VIDEO_HDPVR
+config BACKPORT_VIDEO_HDPVR
 	tristate "Hauppauge HD PVR support"
-	depends on VIDEO_DEV && VIDEO_V4L2
+	depends on !VIDEO_HDPVR
+	depends on BACKPORT_VIDEO_DEV && VIDEO_V4L2
 	---help---
 	  This is a video4linux driver for Hauppauge's HD PVR USB device.
 
diff --git a/backports/drivers/media/usb/msi2500/Kconfig b/backports/drivers/media/usb/msi2500/Kconfig
index 9eff8a7..aa80afe 100644
--- a/backports/drivers/media/usb/msi2500/Kconfig
+++ b/backports/drivers/media/usb/msi2500/Kconfig
@@ -1,5 +1,6 @@
-config USB_MSI2500
+config BACKPORT_USB_MSI2500
 	tristate "Mirics MSi2500"
-	depends on VIDEO_V4L2 && SPI
-	select VIDEOBUF2_VMALLOC
-	select MEDIA_TUNER_MSI001
+	depends on !USB_MSI2500
+	depends on BACKPORT_VIDEO_V4L2 && SPI
+	select BACKPORT_VIDEOBUF2_VMALLOC
+	select BACKPORT_MEDIA_TUNER_MSI001
diff --git a/backports/drivers/media/usb/pvrusb2/Kconfig b/backports/drivers/media/usb/pvrusb2/Kconfig
index 60a2604..249fda6 100644
--- a/backports/drivers/media/usb/pvrusb2/Kconfig
+++ b/backports/drivers/media/usb/pvrusb2/Kconfig
@@ -1,14 +1,15 @@
-config VIDEO_PVRUSB2
+config BACKPORT_VIDEO_PVRUSB2
 	tristate "Hauppauge WinTV-PVR USB2 support"
-	depends on VIDEO_V4L2 && I2C
-	select VIDEO_TUNER
-	select VIDEO_TVEEPROM
-	select VIDEO_CX2341X
-	select VIDEO_SAA711X
-	select VIDEO_CX25840
-	select VIDEO_MSP3400
-	select VIDEO_WM8775
-	select VIDEO_CS53L32A
+	depends on !VIDEO_PVRUSB2
+	depends on BACKPORT_VIDEO_V4L2 && I2C
+	select BACKPORT_VIDEO_TUNER
+	select BACKPORT_VIDEO_TVEEPROM
+	select BACKPORT_VIDEO_CX2341X
+	select BACKPORT_VIDEO_SAA711X
+	select BACKPORT_VIDEO_CX25840
+	select BACKPORT_VIDEO_MSP3400
+	select BACKPORT_VIDEO_WM8775
+	select BACKPORT_VIDEO_CS53L32A
 	---help---
 	  This is a video4linux driver for Conexant 23416 based
 	  usb2 personal video recorder devices.
@@ -16,10 +17,11 @@ config VIDEO_PVRUSB2
 	  To compile this driver as a module, choose M here: the
 	  module will be called pvrusb2
 
-config VIDEO_PVRUSB2_SYSFS
+config BACKPORT_VIDEO_PVRUSB2_SYSFS
 	bool "pvrusb2 sysfs support"
+	depends on !VIDEO_PVRUSB2_SYSFS
 	default y
-	depends on VIDEO_PVRUSB2 && SYSFS
+	depends on BACKPORT_VIDEO_PVRUSB2 && SYSFS
 	---help---
 	  This option enables the operation of a sysfs based
 	  interface for query and control of the pvrusb2 driver.
@@ -32,17 +34,18 @@ config VIDEO_PVRUSB2_SYSFS
 
 	  Note: This feature is experimental and subject to change.
 
-config VIDEO_PVRUSB2_DVB
+config BACKPORT_VIDEO_PVRUSB2_DVB
 	bool "pvrusb2 ATSC/DVB support"
+	depends on !VIDEO_PVRUSB2_DVB
 	default y
-	depends on VIDEO_PVRUSB2 && DVB_CORE
-	select DVB_LGDT330X if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_S5H1409 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_S5H1411 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_TDA10048 if MEDIA_SUBDRV_AUTOSELECT
-	select MEDIA_TUNER_TDA18271 if MEDIA_SUBDRV_AUTOSELECT
-	select MEDIA_TUNER_SIMPLE if MEDIA_SUBDRV_AUTOSELECT
-	select MEDIA_TUNER_TDA8290 if MEDIA_SUBDRV_AUTOSELECT
+	depends on VIDEO_PVRUSB2 && BACKPORT_DVB_CORE
+	select DVB_LGDT330X if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_S5H1409 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_S5H1411 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_TDA10048 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select MEDIA_TUNER_TDA18271 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select MEDIA_TUNER_SIMPLE if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select MEDIA_TUNER_TDA8290 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
 	---help---
 
 	  This option enables a DVB interface for the pvrusb2 driver.
@@ -51,9 +54,10 @@ config VIDEO_PVRUSB2_DVB
 
 	  If you are in doubt, say Y.
 
-config VIDEO_PVRUSB2_DEBUGIFC
+config BACKPORT_VIDEO_PVRUSB2_DEBUGIFC
 	bool "pvrusb2 debug interface"
-	depends on VIDEO_PVRUSB2_SYSFS
+	depends on !VIDEO_PVRUSB2_DEBUGIFC
+	depends on BACKPORT_VIDEO_PVRUSB2_SYSFS
 	---help---
 	  This option enables the inclusion of a debug interface
 	  in the pvrusb2 driver, hosted through sysfs.
diff --git a/backports/drivers/media/usb/pwc/Kconfig b/backports/drivers/media/usb/pwc/Kconfig
index d63d0a8..4c2a5f0 100644
--- a/backports/drivers/media/usb/pwc/Kconfig
+++ b/backports/drivers/media/usb/pwc/Kconfig
@@ -1,7 +1,8 @@
-config USB_PWC
+config BACKPORT_USB_PWC
 	tristate "USB Philips Cameras"
-	depends on VIDEO_V4L2
-	select VIDEOBUF2_VMALLOC
+	depends on !USB_PWC
+	depends on BACKPORT_VIDEO_V4L2
+	select BACKPORT_VIDEOBUF2_VMALLOC
 	---help---
 	  Say Y or M here if you want to use one of these Philips & OEM
 	  webcams:
@@ -29,18 +30,20 @@ config USB_PWC
 	  To compile this driver as a module, choose M here: the
 	  module will be called pwc.
 
-config USB_PWC_DEBUG
+config BACKPORT_USB_PWC_DEBUG
 	bool "USB Philips Cameras verbose debug"
-	depends on USB_PWC
+	depends on !USB_PWC_DEBUG
+	depends on BACKPORT_USB_PWC
 	help
 	  Say Y here in order to have the pwc driver generate verbose debugging
 	  messages.
 	  A special module options 'trace' is used to control the verbosity.
 
-config USB_PWC_INPUT_EVDEV
+config BACKPORT_USB_PWC_INPUT_EVDEV
 	bool "USB Philips Cameras input events device support"
+	depends on !USB_PWC_INPUT_EVDEV
 	default y
-	depends on USB_PWC && (USB_PWC=INPUT || INPUT=y)
+	depends on BACKPORT_USB_PWC && (USB_PWC=INPUT || INPUT=y)
 	---help---
 	  This option makes USB Philips cameras register the snapshot button as
 	  an input device to report button events.
diff --git a/backports/drivers/media/usb/s2255/Kconfig b/backports/drivers/media/usb/s2255/Kconfig
index 6335453..df05f15 100644
--- a/backports/drivers/media/usb/s2255/Kconfig
+++ b/backports/drivers/media/usb/s2255/Kconfig
@@ -1,7 +1,8 @@
-config USB_S2255
+config BACKPORT_USB_S2255
 	tristate "USB Sensoray 2255 video capture device"
-	depends on VIDEO_V4L2
-	select VIDEOBUF2_VMALLOC
+	depends on !USB_S2255
+	depends on BACKPORT_VIDEO_V4L2
+	select BACKPORT_VIDEOBUF2_VMALLOC
 	default n
 	help
 	  Say Y here if you want support for the Sensoray 2255 USB device.
diff --git a/backports/drivers/media/usb/siano/Kconfig b/backports/drivers/media/usb/siano/Kconfig
index 0568585..3283762 100644
--- a/backports/drivers/media/usb/siano/Kconfig
+++ b/backports/drivers/media/usb/siano/Kconfig
@@ -2,11 +2,12 @@
 # Siano Mobile Silicon Digital TV device configuration
 #
 
-config SMS_USB_DRV
+config BACKPORT_SMS_USB_DRV
 	tristate "Siano SMS1xxx based MDTV receiver"
-	depends on DVB_CORE && HAS_DMA
-	depends on !RC_CORE || RC_CORE
-	select MEDIA_COMMON_OPTIONS
-	select SMS_SIANO_MDTV
+	depends on !SMS_USB_DRV
+	depends on BACKPORT_DVB_CORE && HAS_DMA
+	depends on !RC_CORE || BACKPORT_RC_CORE
+	select BACKPORT_MEDIA_COMMON_OPTIONS
+	select BACKPORT_SMS_SIANO_MDTV
 	---help---
 	  Choose if you would like to have Siano's support for USB interface
diff --git a/backports/drivers/media/usb/stk1160/Kconfig b/backports/drivers/media/usb/stk1160/Kconfig
index 95584c1..0bffae6 100644
--- a/backports/drivers/media/usb/stk1160/Kconfig
+++ b/backports/drivers/media/usb/stk1160/Kconfig
@@ -1,6 +1,7 @@
-config VIDEO_STK1160_COMMON
+config BACKPORT_VIDEO_STK1160_COMMON
 	tristate "STK1160 USB video capture support"
-	depends on VIDEO_DEV && I2C
+	depends on !VIDEO_STK1160_COMMON
+	depends on BACKPORT_VIDEO_DEV && I2C
 
 	---help---
 	  This is a video4linux driver for STK1160 based video capture devices.
@@ -8,17 +9,19 @@ config VIDEO_STK1160_COMMON
 	  To compile this driver as a module, choose M here: the
 	  module will be called stk1160
 
-config VIDEO_STK1160_AC97
+config BACKPORT_VIDEO_STK1160_AC97
 	bool "STK1160 AC97 codec support"
-	depends on VIDEO_STK1160_COMMON && SND
+	depends on !VIDEO_STK1160_AC97
+	depends on BACKPORT_VIDEO_STK1160_COMMON && SND
 
 	---help---
 	  Enables AC97 codec support for stk1160 driver.
 
-config VIDEO_STK1160
+config BACKPORT_VIDEO_STK1160
 	tristate
-	depends on (!VIDEO_STK1160_AC97 || (SND='n') || SND) && VIDEO_STK1160_COMMON
+	depends on !VIDEO_STK1160
+	depends on (!VIDEO_STK1160_AC97 || (SND='n') || SND) && BACKPORT_VIDEO_STK1160_COMMON
 	default y
-	select VIDEOBUF2_VMALLOC
-	select VIDEO_SAA711X
+	select BACKPORT_VIDEOBUF2_VMALLOC
+	select BACKPORT_VIDEO_SAA711X
 	select SND_AC97_CODEC if SND
diff --git a/backports/drivers/media/usb/stkwebcam/Kconfig b/backports/drivers/media/usb/stkwebcam/Kconfig
index 54c7ce7..a5ef517 100644
--- a/backports/drivers/media/usb/stkwebcam/Kconfig
+++ b/backports/drivers/media/usb/stkwebcam/Kconfig
@@ -1,6 +1,7 @@
-config USB_STKWEBCAM
+config BACKPORT_USB_STKWEBCAM
 	tristate "USB Syntek DC1125 Camera support"
-	depends on VIDEO_V4L2
+	depends on !USB_STKWEBCAM
+	depends on BACKPORT_VIDEO_V4L2
 	---help---
 	  Say Y here if you want to use this type of camera.
 	  Supported devices are typically found in some Asus laptops,
diff --git a/backports/drivers/media/usb/tm6000/Kconfig b/backports/drivers/media/usb/tm6000/Kconfig
index a43b77a..b50182a 100644
--- a/backports/drivers/media/usb/tm6000/Kconfig
+++ b/backports/drivers/media/usb/tm6000/Kconfig
@@ -1,10 +1,11 @@
-config VIDEO_TM6000
+config BACKPORT_VIDEO_TM6000
 	tristate "TV Master TM5600/6000/6010 driver"
-	depends on VIDEO_DEV && I2C && INPUT && RC_CORE && USB
-	select VIDEO_TUNER
-	select MEDIA_TUNER_XC2028
-	select MEDIA_TUNER_XC5000
-	select VIDEOBUF_VMALLOC
+	depends on !VIDEO_TM6000
+	depends on BACKPORT_VIDEO_DEV && I2C && INPUT && RC_CORE && USB
+	select BACKPORT_VIDEO_TUNER
+	select BACKPORT_MEDIA_TUNER_XC2028
+	select BACKPORT_MEDIA_TUNER_XC5000
+	select BACKPORT_VIDEOBUF_VMALLOC
 	help
 	  Support for TM5600/TM6000/TM6010 USB Device
 
@@ -14,9 +15,10 @@ config VIDEO_TM6000
 
 	  Say Y if you own such a device and want to use it.
 
-config VIDEO_TM6000_ALSA
+config BACKPORT_VIDEO_TM6000_ALSA
 	tristate "TV Master TM5600/6000/6010 audio support"
-	depends on VIDEO_TM6000 && SND
+	depends on !VIDEO_TM6000_ALSA
+	depends on BACKPORT_VIDEO_TM6000 && SND
 	select SND_PCM
 	---help---
 	  This is a video4linux driver for direct (DMA) audio for
@@ -25,9 +27,10 @@ config VIDEO_TM6000_ALSA
 	  To compile this driver as a module, choose M here: the
 	  module will be called tm6000-alsa.
 
-config VIDEO_TM6000_DVB
+config BACKPORT_VIDEO_TM6000_DVB
 	tristate "DVB Support for tm6000 based TV cards"
-	depends on VIDEO_TM6000 && DVB_CORE && USB
-	select DVB_ZL10353
+	depends on !VIDEO_TM6000_DVB
+	depends on VIDEO_TM6000 && BACKPORT_DVB_CORE && USB
+	select BACKPORT_DVB_ZL10353
 	---help---
 	  This adds support for DVB cards based on the tm5600/tm6000 chip.
diff --git a/backports/drivers/media/usb/ttusb-budget/Kconfig b/backports/drivers/media/usb/ttusb-budget/Kconfig
index 97bad7d..28b3dd7 100644
--- a/backports/drivers/media/usb/ttusb-budget/Kconfig
+++ b/backports/drivers/media/usb/ttusb-budget/Kconfig
@@ -1,13 +1,14 @@
-config DVB_TTUSB_BUDGET
+config BACKPORT_DVB_TTUSB_BUDGET
 	tristate "Technotrend/Hauppauge Nova-USB devices"
-	depends on DVB_CORE && USB && I2C && PCI
-	select DVB_CX22700 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_TDA1004X if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_VES1820 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_TDA8083 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_STV0299 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_STV0297 if MEDIA_SUBDRV_AUTOSELECT
-	select DVB_LNBP21 if MEDIA_SUBDRV_AUTOSELECT
+	depends on !DVB_TTUSB_BUDGET
+	depends on BACKPORT_DVB_CORE && USB && I2C && PCI
+	select DVB_CX22700 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_TDA1004X if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_VES1820 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_TDA8083 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_STV0299 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_STV0297 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
+	select DVB_LNBP21 if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
 	help
 	  Support for external USB adapters designed by Technotrend and
 	  produced by Hauppauge, shipped under the brand name 'Nova-USB'.
diff --git a/backports/drivers/media/usb/ttusb-dec/Kconfig b/backports/drivers/media/usb/ttusb-dec/Kconfig
index 290254a..ec7b963 100644
--- a/backports/drivers/media/usb/ttusb-dec/Kconfig
+++ b/backports/drivers/media/usb/ttusb-dec/Kconfig
@@ -1,6 +1,7 @@
-config DVB_TTUSB_DEC
+config BACKPORT_DVB_TTUSB_DEC
 	tristate "Technotrend/Hauppauge USB DEC devices"
-	depends on DVB_CORE && USB && INPUT && PCI
+	depends on !DVB_TTUSB_DEC
+	depends on BACKPORT_DVB_CORE && USB && INPUT && PCI
 	select CRC32
 	help
 	  Support for external USB adapters designed by Technotrend and
diff --git a/backports/drivers/media/usb/usbtv/Kconfig b/backports/drivers/media/usb/usbtv/Kconfig
index b833c5b..94b73d8 100644
--- a/backports/drivers/media/usb/usbtv/Kconfig
+++ b/backports/drivers/media/usb/usbtv/Kconfig
@@ -1,8 +1,9 @@
-config VIDEO_USBTV
+config BACKPORT_VIDEO_USBTV
         tristate "USBTV007 video capture support"
-        depends on VIDEO_V4L2 && SND
+	depends on !VIDEO_USBTV
+        depends on BACKPORT_VIDEO_V4L2 && SND
         select SND_PCM
-        select VIDEOBUF2_VMALLOC
+        select BACKPORT_VIDEOBUF2_VMALLOC
 
         ---help---
           This is a video4linux2 driver for USBTV007 based video capture devices.
diff --git a/backports/drivers/media/usb/usbvision/Kconfig b/backports/drivers/media/usb/usbvision/Kconfig
index 6b6afc5..1f18ceb 100644
--- a/backports/drivers/media/usb/usbvision/Kconfig
+++ b/backports/drivers/media/usb/usbvision/Kconfig
@@ -1,8 +1,9 @@
-config VIDEO_USBVISION
+config BACKPORT_VIDEO_USBVISION
 	tristate "USB video devices based on Nogatech NT1003/1004/1005"
-	depends on I2C && VIDEO_V4L2
-	select VIDEO_TUNER
-	select VIDEO_SAA711X if MEDIA_SUBDRV_AUTOSELECT
+	depends on !VIDEO_USBVISION
+	depends on I2C && BACKPORT_VIDEO_V4L2
+	select BACKPORT_VIDEO_TUNER
+	select VIDEO_SAA711X if BACKPORT_MEDIA_SUBDRV_AUTOSELECT
 	---help---
 	  There are more than 50 different USB video devices based on
 	  NT1003/1004/1005 USB Bridges. This driver enables using those
diff --git a/backports/drivers/media/usb/uvc/Kconfig b/backports/drivers/media/usb/uvc/Kconfig
index 6ed85efa..270ceb7 100644
--- a/backports/drivers/media/usb/uvc/Kconfig
+++ b/backports/drivers/media/usb/uvc/Kconfig
@@ -1,18 +1,20 @@
-config USB_VIDEO_CLASS
+config BACKPORT_USB_VIDEO_CLASS
 	tristate "USB Video Class (UVC)"
-	depends on VIDEO_V4L2
-	select VIDEOBUF2_VMALLOC
+	depends on !USB_VIDEO_CLASS
+	depends on BACKPORT_VIDEO_V4L2
+	select BACKPORT_VIDEOBUF2_VMALLOC
 	---help---
 	  Support for the USB Video Class (UVC).  Currently only video
 	  input devices, such as webcams, are supported.
 
 	  For more information see: <http://linux-uvc.berlios.de/>
 
-config USB_VIDEO_CLASS_INPUT_EVDEV
+config BACKPORT_USB_VIDEO_CLASS_INPUT_EVDEV
 	bool "UVC input events device support"
+	depends on !USB_VIDEO_CLASS_INPUT_EVDEV
 	default y
-	depends on USB_VIDEO_CLASS
-	depends on USB_VIDEO_CLASS=INPUT || INPUT=y
+	depends on BACKPORT_USB_VIDEO_CLASS
+	depends on BACKPORT_USB_VIDEO_CLASS=INPUT || INPUT=y
 	---help---
 	  This option makes USB Video Class devices register an input device
 	  to report button events.
diff --git a/backports/drivers/media/usb/zr364xx/Kconfig b/backports/drivers/media/usb/zr364xx/Kconfig
index 78e20a5..99edcfa 100644
--- a/backports/drivers/media/usb/zr364xx/Kconfig
+++ b/backports/drivers/media/usb/zr364xx/Kconfig
@@ -1,8 +1,9 @@
-config USB_ZR364XX
+config BACKPORT_USB_ZR364XX
 	tristate "USB ZR364XX Camera support"
-	depends on VIDEO_V4L2
-	select VIDEOBUF_GEN
-	select VIDEOBUF_VMALLOC
+	depends on !USB_ZR364XX
+	depends on BACKPORT_VIDEO_V4L2
+	select BACKPORT_VIDEOBUF_GEN
+	select BACKPORT_VIDEOBUF_VMALLOC
 	---help---
 	  Say Y here if you want to connect this type of camera to your
 	  computer's USB port.
diff --git a/backports/drivers/media/v4l2-core/Kconfig b/backports/drivers/media/v4l2-core/Kconfig
index 04e6e18..4e002b6 100644
--- a/backports/drivers/media/v4l2-core/Kconfig
+++ b/backports/drivers/media/v4l2-core/Kconfig
@@ -3,21 +3,24 @@
 #
 
 # Enable the V4L2 core and API
-config VIDEO_V4L2
+config BACKPORT_VIDEO_V4L2
 	tristate
-	depends on (I2C || I2C=n) && VIDEO_DEV
-	default (I2C || I2C=n) && VIDEO_DEV
+	depends on !VIDEO_V4L2
+	depends on (I2C || I2C=n) && BACKPORT_VIDEO_DEV
+	default (I2C || I2C=n) && BACKPORT_VIDEO_DEV
 
-config VIDEO_ADV_DEBUG
+config BACKPORT_VIDEO_ADV_DEBUG
 	bool "Enable advanced debug functionality on V4L2 drivers"
+	depends on !VIDEO_ADV_DEBUG
 	default n
 	---help---
 	  Say Y here to enable advanced debugging functionality on some
 	  V4L devices.
 	  In doubt, say N.
 
-config VIDEO_FIXED_MINOR_RANGES
+config BACKPORT_VIDEO_FIXED_MINOR_RANGES
 	bool "Enable old-style fixed minor ranges on drivers/video devices"
+	depends on !VIDEO_FIXED_MINOR_RANGES
 	default n
 	---help---
 	  Say Y here to enable the old-style fixed-range minor assignments.
@@ -25,74 +28,88 @@ config VIDEO_FIXED_MINOR_RANGES
 
 	  When in doubt, say N.
 
-config VIDEO_PCI_SKELETON
+config BACKPORT_VIDEO_PCI_SKELETON
 	tristate "Skeleton PCI V4L2 driver"
+	depends on !VIDEO_PCI_SKELETON
 	depends on PCI && BUILD_DOCSRC
-	depends on VIDEO_V4L2 && VIDEOBUF2_CORE
-	depends on VIDEOBUF2_MEMOPS && VIDEOBUF2_DMA_CONTIG
+	depends on BACKPORT_VIDEO_V4L2 && VIDEOBUF2_CORE
+	depends on BACKPORT_VIDEOBUF2_MEMOPS && VIDEOBUF2_DMA_CONTIG
 	---help---
 	  Enable build of the skeleton PCI driver, used as a reference
 	  when developing new drivers.
 
 # Used by drivers that need tuner.ko
-config VIDEO_TUNER
+config BACKPORT_VIDEO_TUNER
 	tristate
-	depends on MEDIA_TUNER
+	depends on !VIDEO_TUNER
+	depends on BACKPORT_MEDIA_TUNER
 
 # Used by drivers that need v4l2-mem2mem.ko
-config V4L2_MEM2MEM_DEV
+config BACKPORT_V4L2_MEM2MEM_DEV
         tristate
-        depends on VIDEOBUF2_CORE
+	depends on !V4L2_MEM2MEM_DEV
+        depends on BACKPORT_VIDEOBUF2_CORE
 
 # Used by drivers that need Videobuf modules
-config VIDEOBUF_GEN
+config BACKPORT_VIDEOBUF_GEN
 	tristate
+	depends on !VIDEOBUF_GEN
 
-config VIDEOBUF_DMA_SG
+config BACKPORT_VIDEOBUF_DMA_SG
 	tristate
+	depends on !VIDEOBUF_DMA_SG
 	depends on HAS_DMA
-	select VIDEOBUF_GEN
+	select BACKPORT_VIDEOBUF_GEN
 
-config VIDEOBUF_VMALLOC
+config BACKPORT_VIDEOBUF_VMALLOC
 	tristate
-	select VIDEOBUF_GEN
+	depends on !VIDEOBUF_VMALLOC
+	select BACKPORT_VIDEOBUF_GEN
 
-config VIDEOBUF_DMA_CONTIG
+config BACKPORT_VIDEOBUF_DMA_CONTIG
 	tristate
+	depends on !VIDEOBUF_DMA_CONTIG
 	depends on HAS_DMA
-	select VIDEOBUF_GEN
+	select BACKPORT_VIDEOBUF_GEN
 
-config VIDEOBUF_DVB
+config BACKPORT_VIDEOBUF_DVB
 	tristate
-	select VIDEOBUF_GEN
+	depends on !VIDEOBUF_DVB
+	select BACKPORT_VIDEOBUF_GEN
 
 # Used by drivers that need Videobuf2 modules
-config VIDEOBUF2_CORE
+config BACKPORT_VIDEOBUF2_CORE
 	select DMA_SHARED_BUFFER if !KERNEL_3_5
+	depends on !VIDEOBUF2_CORE
 	tristate
 
-config VIDEOBUF2_MEMOPS
+config BACKPORT_VIDEOBUF2_MEMOPS
 	tristate
+	depends on !VIDEOBUF2_MEMOPS
 
-config VIDEOBUF2_DMA_CONTIG
+config BACKPORT_VIDEOBUF2_DMA_CONTIG
 	tristate
+	depends on !VIDEOBUF2_DMA_CONTIG
 	depends on HAS_DMA
-	select VIDEOBUF2_CORE
-	select VIDEOBUF2_MEMOPS
+	select BACKPORT_VIDEOBUF2_CORE
+	select BACKPORT_VIDEOBUF2_MEMOPS
 	select DMA_SHARED_BUFFER if !KERNEL_3_5
 
-config VIDEOBUF2_VMALLOC
+config BACKPORT_VIDEOBUF2_VMALLOC
 	tristate
-	select VIDEOBUF2_CORE
-	select VIDEOBUF2_MEMOPS
+	depends on !VIDEOBUF2_VMALLOC
+	select BACKPORT_VIDEOBUF2_CORE
+	select BACKPORT_VIDEOBUF2_MEMOPS
 	select DMA_SHARED_BUFFER if !KERNEL_3_5
 
-config VIDEOBUF2_DMA_SG
+config BACKPORT_VIDEOBUF2_DMA_SG
 	tristate
+	depends on !VIDEOBUF2_DMA_SG
 	#depends on HAS_DMA
-	select VIDEOBUF2_CORE
-	select VIDEOBUF2_MEMOPS
+	select BACKPORT_VIDEOBUF2_CORE
+	select BACKPORT_VIDEOBUF2_MEMOPS
 
-config VIDEOBUF2_DVB
+config BACKPORT_VIDEOBUF2_DVB
 	tristate
-	select VIDEOBUF2_CORE
+	depends on !VIDEOBUF2_DVB
+	select BACKPORT_VIDEOBUF2_CORE
diff --git a/backports/drivers/net/ethernet/Kconfig b/backports/drivers/net/ethernet/Kconfig
index b025298..1cdc03f 100644
--- a/backports/drivers/net/ethernet/Kconfig
+++ b/backports/drivers/net/ethernet/Kconfig
@@ -2,20 +2,23 @@
 # Ethernet LAN device configuration
 #
 
-menuconfig ETHERNET
+menuconfig BACKPORT_ETHERNET
 	bool "Ethernet driver support"
+	depends on !ETHERNET
 	depends on NET
 	default y
 	---help---
 	  This section contains all the Ethernet device drivers.
 
-if ETHERNET
+if BACKPORT_ETHERNET
 
-config MDIO
+config BACKPORT_MDIO
 	tristate
+	depends on !MDIO
 
-config SUNGEM_PHY
+config BACKPORT_SUNGEM_PHY
 	tristate
+	depends on !SUNGEM_PHY
 
 #source "drivers/net/ethernet/3com/Kconfig"
 #source "drivers/net/ethernet/adaptec/Kconfig"
@@ -38,8 +41,9 @@ source "$BACKPORT_DIR/drivers/net/ethernet/broadcom/Kconfig"
 #source "drivers/net/ethernet/cirrus/Kconfig"
 #source "drivers/net/ethernet/cisco/Kconfig"
 
-config CX_ECAT
+config BACKPORT_CX_ECAT
 	tristate "Beckhoff CX5020 EtherCAT master support"
+	depends on !CX_ECAT
 	depends on PCI
 	depends on X86 || COMPILE_TEST
 	---help---
@@ -52,13 +56,14 @@ config CX_ECAT
 
 #source "drivers/net/ethernet/davicom/Kconfig"
 
-config DNET
+config BACKPORT_DNET
 	tristate "Dave ethernet support (DNET)"
+	depends on !DNET
 	depends on HAS_IOMEM
 	select PHYLIB
 	---help---
 	  The Dave ethernet interface (DNET) is found on Qong Board FPGA.
-	  Say Y to include support for the DNET chip.
+	  Say Y to include support for the BACKPORT_DNET chip.
 
 	  To compile this driver as a module, choose M here: the module
 	  will be called dnet.
@@ -78,8 +83,9 @@ source "$BACKPORT_DIR/drivers/net/ethernet/intel/Kconfig"
 #source "drivers/net/ethernet/xscale/Kconfig"
 #source "drivers/net/ethernet/icplus/Kconfig"
 
-config JME
+config BACKPORT_JME
 	tristate "JMicron(R) PCI-Express Gigabit Ethernet support"
+	depends on !JME
 	depends on PCI
 	select CRC32
 	select MII
@@ -90,15 +96,17 @@ config JME
 	  To compile this driver as a module, choose M here. The module
 	  will be called jme.
 
-config KORINA
+config BACKPORT_KORINA
 	tristate "Korina (IDT RC32434) Ethernet support"
+	depends on !KORINA
 	depends on MIKROTIK_RB532
 	---help---
 	  If you have a Mikrotik RouterBoard 500 or IDT RC32434
 	  based system say Y. Otherwise say N.
 
-config LANTIQ_ETOP
+config BACKPORT_LANTIQ_ETOP
 	tristate "Lantiq SoC ETOP driver"
+	depends on !LANTIQ_ETOP
 	depends on SOC_TYPE_XWAY
 	---help---
 	  Support for the MII0 inside the Lantiq SoC
@@ -110,8 +118,9 @@ config LANTIQ_ETOP
 #source "drivers/net/ethernet/moxa/Kconfig"
 #source "drivers/net/ethernet/myricom/Kconfig"
 
-config FEALNX
+config BACKPORT_FEALNX
 	tristate "Myson MTD-8xx PCI Ethernet support"
+	depends on !FEALNX
 	depends on PCI
 	select CRC32
 	select MII
@@ -122,8 +131,9 @@ config FEALNX
 #source "drivers/net/ethernet/natsemi/Kconfig"
 #source "drivers/net/ethernet/8390/Kconfig"
 
-config NET_NETX
+config BACKPORT_NET_NETX
 	tristate "NetX Ethernet support"
+	depends on !NET_NETX
 	select MII
 	depends on ARCH_NETX
 	---help---
@@ -138,8 +148,9 @@ config NET_NETX
 #source "drivers/net/ethernet/octeon/Kconfig"
 #source "drivers/net/ethernet/oki-semi/Kconfig"
 
-config ETHOC
+config BACKPORT_ETHOC
 	tristate "OpenCores 10/100 Mbps Ethernet MAC support"
+	depends on !ETHOC
 	depends on HAS_IOMEM && HAS_DMA
 	select MII
 	select PHYLIB
@@ -175,4 +186,4 @@ config ETHOC
 #source "drivers/net/ethernet/xilinx/Kconfig"
 #source "drivers/net/ethernet/xircom/Kconfig"
 
-endif # ETHERNET
+endif # BACKPORT_ETHERNET
diff --git a/backports/drivers/net/ethernet/atheros/Kconfig b/backports/drivers/net/ethernet/atheros/Kconfig
index 58ad37c..a388f53 100644
--- a/backports/drivers/net/ethernet/atheros/Kconfig
+++ b/backports/drivers/net/ethernet/atheros/Kconfig
@@ -2,8 +2,9 @@
 # Atheros device configuration
 #
 
-config NET_VENDOR_ATHEROS
+config BACKPORT_NET_VENDOR_ATHEROS
 	bool "Atheros devices"
+	depends on !NET_VENDOR_ATHEROS
 	default y
 	depends on PCI
 	---help---
@@ -16,10 +17,11 @@ config NET_VENDOR_ATHEROS
 	  the questions about Atheros devices. If you say Y, you will be asked
 	  for your specific card in the following questions.
 
-if NET_VENDOR_ATHEROS
+if BACKPORT_NET_VENDOR_ATHEROS
 
-config ATL2
+config BACKPORT_ATL2
 	tristate "Atheros L2 Fast Ethernet support"
+	depends on !ATL2
 	depends on PCI
 	select CRC32
 	select MII
@@ -29,8 +31,9 @@ config ATL2
 	  To compile this driver as a module, choose M here.  The module
 	  will be called atl2.
 
-config ATL1
+config BACKPORT_ATL1
 	tristate "Atheros/Attansic L1 Gigabit Ethernet support"
+	depends on !ATL1
 	depends on PCI
 	select CRC32
 	select MII
@@ -41,8 +44,9 @@ config ATL1
 	  To compile this driver as a module, choose M here.  The module
 	  will be called atl1.
 
-config ATL1E
+config BACKPORT_ATL1E
 	tristate "Atheros L1E Gigabit Ethernet support"
+	depends on !ATL1E
 	depends on PCI
 	select CRC32
 	select MII
@@ -52,8 +56,9 @@ config ATL1E
 	  To compile this driver as a module, choose M here.  The module
 	  will be called atl1e.
 
-config ATL1C
+config BACKPORT_ATL1C
 	tristate "Atheros L1C Gigabit Ethernet support"
+	depends on !ATL1C
 	depends on PCI
 	select CRC32
 	select MII
@@ -63,11 +68,12 @@ config ATL1C
 	  To compile this driver as a module, choose M here.  The module
 	  will be called atl1c.
 
-config ALX
+config BACKPORT_ALX
 	tristate "Qualcomm Atheros AR816x/AR817x support"
+	depends on !ALX
 	depends on PCI
 	select CRC32
-	select MDIO
+	select BACKPORT_MDIO
 	help
 	  This driver supports the Qualcomm Atheros L1F ethernet adapter,
 	  i.e. the following chipsets:
@@ -80,4 +86,4 @@ config ALX
 	  To compile this driver as a module, choose M here.  The module
 	  will be called alx.
 
-endif # NET_VENDOR_ATHEROS
+endif # BACKPORT_NET_VENDOR_ATHEROS
diff --git a/backports/drivers/net/ethernet/broadcom/Kconfig b/backports/drivers/net/ethernet/broadcom/Kconfig
index a6f9142..eaeb166 100644
--- a/backports/drivers/net/ethernet/broadcom/Kconfig
+++ b/backports/drivers/net/ethernet/broadcom/Kconfig
@@ -2,8 +2,9 @@
 # Broadcom device configuration
 #
 
-config NET_VENDOR_BROADCOM
+config BACKPORT_NET_VENDOR_BROADCOM
 	bool "Broadcom devices"
+	depends on !NET_VENDOR_BROADCOM
 	default y
 	depends on (SSB_POSSIBLE && HAS_DMA) || PCI || BCM63XX || \
 		   SIBYTE_SB1xxx_SOC
@@ -16,12 +17,13 @@ config NET_VENDOR_BROADCOM
 	  the questions regarding AMD chipsets. If you say Y, you will be asked
 	  for your specific chipset/driver in the following questions.
 
-if NET_VENDOR_BROADCOM
+if BACKPORT_NET_VENDOR_BROADCOM
 
-config B44
+config BACKPORT_B44
 	tristate "Broadcom 440x/47xx ethernet support"
-	depends on SSB_POSSIBLE && HAS_DMA
-	select SSB
+	depends on !B44
+	depends on BACKPORT_SSB_POSSIBLE && HAS_DMA
+	select BACKPORT_SSB
 	select MII
 	select PHYLIB
 	---help---
@@ -32,27 +34,31 @@ config B44
 	  To compile this driver as a module, choose M here. The module
 	  will be called b44.
 
-# Auto-select SSB PCI-HOST support, if possible
-config B44_PCI_AUTOSELECT
+# Auto-select BACKPORT_SSB PCI-HOST support, if possible
+config BACKPORT_B44_PCI_AUTOSELECT
 	bool
-	depends on B44 && SSB_PCIHOST_POSSIBLE
-	select SSB_PCIHOST
+	depends on !B44_PCI_AUTOSELECT
+	depends on BACKPORT_B44 && SSB_PCIHOST_POSSIBLE
+	select BACKPORT_SSB_PCIHOST
 	default y
 
-# Auto-select SSB PCICORE driver, if possible
-config B44_PCICORE_AUTOSELECT
+# Auto-select BACKPORT_SSB PCICORE driver, if possible
+config BACKPORT_B44_PCICORE_AUTOSELECT
 	bool
-	depends on B44 && SSB_DRIVER_PCICORE_POSSIBLE
-	select SSB_DRIVER_PCICORE
+	depends on !B44_PCICORE_AUTOSELECT
+	depends on BACKPORT_B44 && SSB_DRIVER_PCICORE_POSSIBLE
+	select BACKPORT_SSB_DRIVER_PCICORE
 	default y
 
-config B44_PCI
+config BACKPORT_B44_PCI
 	bool
-	depends on B44_PCI_AUTOSELECT && B44_PCICORE_AUTOSELECT
+	depends on !B44_PCI
+	depends on BACKPORT_B44_PCI_AUTOSELECT && BACKPORT_B44_PCICORE_AUTOSELECT
 	default y
 
-config BCM63XX_ENET
+config BACKPORT_BCM63XX_ENET
 	tristate "Broadcom 63xx internal mac support"
+	depends on !BCM63XX_ENET
 	depends on BCM63XX
 	select MII
 	select PHYLIB
@@ -60,8 +66,9 @@ config BCM63XX_ENET
 	  This driver supports the ethernet MACs in the Broadcom 63xx
 	  MIPS chipset family (BCM63XX).
 
-config BCMGENET
+config BACKPORT_BCMGENET
 	tristate "Broadcom GENET internal MAC support"
+	depends on !BCMGENET
 	select MII
 	select PHYLIB
 	select FIXED_PHY
@@ -70,8 +77,9 @@ config BCMGENET
 	  This driver supports the built-in Ethernet MACs found in the
 	  Broadcom BCM7xxx Set Top Box family chipset.
 
-config BNX2
+config BACKPORT_BNX2
 	tristate "QLogic bnx2 support"
+	depends on !BNX2
 	depends on PCI
 	select CRC32
 	select FW_LOADER
@@ -81,10 +89,11 @@ config BNX2
 	  To compile this driver as a module, choose M here: the module
 	  will be called bnx2.  This is recommended.
 
-config CNIC
+config BACKPORT_CNIC
 	tristate "QLogic CNIC support"
+	depends on !CNIC
 	depends on PCI && (IPV6 || IPV6=n)
-	select BNX2
+	select BACKPORT_BNX2
 	select UIO
 	---help---
 	  This driver supports offload features of QLogic bnx2 gigabit
@@ -93,8 +102,9 @@ config CNIC
 	  To compile this driver as a module, choose M here: the module
 	  will be called cnic.  This is recommended.
 
-config SB1250_MAC
+config BACKPORT_SB1250_MAC
 	tristate "SB1250 Gigabit Ethernet support"
+	depends on !SB1250_MAC
 	depends on SIBYTE_SB1xxx_SOC
 	select PHYLIB
 	---help---
@@ -106,8 +116,9 @@ config SB1250_MAC
 	  To compile this driver as a module, choose M here: the module
 	  will be called sb1250-mac.
 
-config TIGON3
+config BACKPORT_TIGON3
 	tristate "Broadcom Tigon3 support"
+	depends on !TIGON3
 	depends on PCI
 	select PHYLIB
 	select HWMON
@@ -118,40 +129,44 @@ config TIGON3
 	  To compile this driver as a module, choose M here: the module
 	  will be called tg3.  This is recommended.
 
-config BNX2X
+config BACKPORT_BNX2X
 	tristate "Broadcom NetXtremeII 10Gb support"
+	depends on !BNX2X
 	depends on PCI
 	select PTP_1588_CLOCK
 	select FW_LOADER
 	select ZLIB_INFLATE
 	select LIBCRC32C
-	select MDIO
+	select BACKPORT_MDIO
 	---help---
 	  This driver supports Broadcom NetXtremeII 10 gigabit Ethernet cards.
 	  To compile this driver as a module, choose M here: the module
 	  will be called bnx2x.  This is recommended.
 
-config BNX2X_SRIOV
+config BACKPORT_BNX2X_SRIOV
 	bool "Broadcom 578xx and 57712 SR-IOV support"
-	depends on BNX2X && PCI_IOV
+	depends on !BNX2X_SRIOV
+	depends on BACKPORT_BNX2X && PCI_IOV
 	default y
 	---help---
 	  This configuration parameter enables Single Root Input Output
 	  Virtualization support in the 578xx and 57712 products. This
 	  allows for virtual function acceleration in virtual environments.
 
-config BGMAC
+config BACKPORT_BGMAC
 	tristate "BCMA bus GBit core support"
-	depends on BCMA_HOST_SOC && HAS_DMA && (BCM47XX || ARCH_BCM_5301X)
+	depends on !BGMAC
+	depends on BACKPORT_BCMA_HOST_SOC && HAS_DMA && (BCM47XX || ARCH_BCM_5301X)
 	select PHYLIB
 	---help---
-	  This driver supports GBit MAC and BCM4706 GBit MAC cores on BCMA bus.
+	  This driver supports GBit MAC and BCM4706 GBit MAC cores on BACKPORT_BCMA bus.
 	  They can be found on BCM47xx SoCs and provide gigabit ethernet.
 	  In case of using this driver on BCM4706 it's also requires to enable
-	  BCMA_DRIVER_GMAC_CMN to make it work.
+	  BACKPORT_BCMA_DRIVER_GMAC_CMN to make it work.
 
-config SYSTEMPORT
+config BACKPORT_SYSTEMPORT
 	tristate "Broadcom SYSTEMPORT internal MAC support"
+	depends on !SYSTEMPORT
 	depends on OF
 	select MII
 	select PHYLIB
@@ -161,4 +176,4 @@ config SYSTEMPORT
 	  Broadcom BCM7xxx Set Top Box family chipset using an internal
 	  Ethernet switch.
 
-endif # NET_VENDOR_BROADCOM
+endif # BACKPORT_NET_VENDOR_BROADCOM
diff --git a/backports/drivers/net/ethernet/intel/Kconfig b/backports/drivers/net/ethernet/intel/Kconfig
index f4ff465..9111cea 100644
--- a/backports/drivers/net/ethernet/intel/Kconfig
+++ b/backports/drivers/net/ethernet/intel/Kconfig
@@ -2,8 +2,9 @@
 # Intel network device configuration
 #
 
-config NET_VENDOR_INTEL
+config BACKPORT_NET_VENDOR_INTEL
 	bool "Intel devices"
+	depends on !NET_VENDOR_INTEL
 	default y
 	---help---
 	  If you have a network (Ethernet) card belonging to this class, say Y
@@ -15,10 +16,11 @@ config NET_VENDOR_INTEL
 	  the questions about Intel cards. If you say Y, you will be asked for
 	  your specific card in the following questions.
 
-if NET_VENDOR_INTEL
+if BACKPORT_NET_VENDOR_INTEL
 
-config E100
+config BACKPORT_E100
 	tristate "Intel(R) PRO/100+ support"
+	depends on !E100
 	depends on PCI
 	select MII
 	---help---
@@ -43,8 +45,9 @@ config E100
 	  To compile this driver as a module, choose M here. The module
 	  will be called e100.
 
-config E1000
+config BACKPORT_E1000
 	tristate "Intel(R) PRO/1000 Gigabit Ethernet support"
+	depends on !E1000
 	depends on PCI
 	---help---
 	  This driver supports Intel(R) PRO/1000 gigabit ethernet family of
@@ -64,8 +67,9 @@ config E1000
 	  To compile this driver as a module, choose M here. The module
 	  will be called e1000.
 
-config E1000E
+config BACKPORT_E1000E
 	tristate "Intel(R) PRO/1000 PCI-Express Gigabit Ethernet support"
+	depends on !E1000E
 	depends on PCI && (!SPARC32 || BROKEN)
 	select CRC32
 	select PTP_1588_CLOCK
@@ -85,8 +89,9 @@ config E1000E
 	  To compile this driver as a module, choose M here. The module
 	  will be called e1000e.
 
-config IGB
+config BACKPORT_IGB
 	tristate "Intel(R) 82575/82576 PCI-Express Gigabit Ethernet support"
+	depends on !IGB
 	depends on PCI
 	select PTP_1588_CLOCK
 	select I2C
@@ -109,10 +114,11 @@ config IGB
 	  To compile this driver as a module, choose M here. The module
 	  will be called igb.
 
-config IGB_HWMON
+config BACKPORT_IGB_HWMON
 	bool "Intel(R) PCI-Express Gigabit adapters HWMON support"
+	depends on !IGB_HWMON
 	default y
-	depends on IGB && HWMON && !(IGB=y && HWMON=m)
+	depends on BACKPORT_IGB && HWMON && !(IGB=y && HWMON=m)
 	---help---
 	  Say Y if you want to expose thermal sensor data on Intel devices.
 
@@ -120,17 +126,19 @@ config IGB_HWMON
 	  This data is available via the hwmon sysfs interface and exposes
 	  the onboard sensors.
 
-config IGB_DCA
+config BACKPORT_IGB_DCA
 	bool "Direct Cache Access (DCA) Support"
+	depends on !IGB_DCA
 	default y
-	depends on IGB && DCA && !(IGB=y && DCA=m)
+	depends on BACKPORT_IGB && DCA && !(IGB=y && DCA=m)
 	---help---
 	  Say Y here if you want to use Direct Cache Access (DCA) in the
 	  driver.  DCA is a method for warming the CPU cache before data
 	  is used, with the intent of lessening the impact of cache misses.
 
-config IGBVF
+config BACKPORT_IGBVF
 	tristate "Intel(R) 82576 Virtual Function Ethernet support"
+	depends on !IGBVF
 	depends on PCI
 	---help---
 	  This driver supports Intel(R) 82576 virtual functions.  For more
@@ -150,8 +158,9 @@ config IGBVF
 	  To compile this driver as a module, choose M here. The module
 	  will be called igbvf.
 
-config IXGB
+config BACKPORT_IXGB
 	tristate "Intel(R) PRO/10GbE support"
+	depends on !IXGB
 	depends on PCI
 	---help---
 	  This driver supports Intel(R) PRO/10GbE family of adapters for
@@ -172,10 +181,11 @@ config IXGB
 	  To compile this driver as a module, choose M here. The module
 	  will be called ixgb.
 
-config IXGBE
+config BACKPORT_IXGBE
 	tristate "Intel(R) 10GbE PCI Express adapters support"
+	depends on !IXGBE
 	depends on PCI
-	select MDIO
+	select BACKPORT_MDIO
 	select PTP_1588_CLOCK
 	---help---
 	  This driver supports Intel(R) 10GbE PCI Express family of
@@ -192,10 +202,11 @@ config IXGBE
 	  To compile this driver as a module, choose M here. The module
 	  will be called ixgbe.
 
-config IXGBE_VXLAN
+config BACKPORT_IXGBE_VXLAN
 	bool "Virtual eXtensible Local Area Network Support"
+	depends on !IXGBE_VXLAN
 	default n
-	depends on IXGBE && VXLAN && !(IXGBE=y && VXLAN=m)
+	depends on BACKPORT_IXGBE && VXLAN && !(IXGBE=y && VXLAN=m)
 	---help---
 	  This allows one to create VXLAN virtual interfaces that provide
 	  Layer 2 Networks over Layer 3 Networks. VXLAN is often used
@@ -203,35 +214,39 @@ config IXGBE_VXLAN
 	  Say Y here if you want to use Virtual eXtensible Local Area Network
 	  (VXLAN) in the driver.
 
-config IXGBE_HWMON
+config BACKPORT_IXGBE_HWMON
 	bool "Intel(R) 10GbE PCI Express adapters HWMON support"
+	depends on !IXGBE_HWMON
 	default y
-	depends on IXGBE && HWMON && !(IXGBE=y && HWMON=m)
+	depends on BACKPORT_IXGBE && HWMON && !(IXGBE=y && HWMON=m)
 	---help---
 	  Say Y if you want to expose the thermal sensor data on some of
 	  our cards, via a hwmon sysfs interface.
 
-config IXGBE_DCA
+config BACKPORT_IXGBE_DCA
 	bool "Direct Cache Access (DCA) Support"
+	depends on !IXGBE_DCA
 	default y
-	depends on IXGBE && DCA && !(IXGBE=y && DCA=m)
+	depends on BACKPORT_IXGBE && DCA && !(IXGBE=y && DCA=m)
 	---help---
 	  Say Y here if you want to use Direct Cache Access (DCA) in the
 	  driver.  DCA is a method for warming the CPU cache before data
 	  is used, with the intent of lessening the impact of cache misses.
 
-config IXGBE_DCB
+config BACKPORT_IXGBE_DCB
 	bool "Data Center Bridging (DCB) Support"
+	depends on !IXGBE_DCB
 	default n
-	depends on IXGBE && DCB
+	depends on BACKPORT_IXGBE && DCB
 	---help---
 	  Say Y here if you want to use Data Center Bridging (DCB) in the
 	  driver.
 
 	  If unsure, say N.
 
-config IXGBEVF
+config BACKPORT_IXGBEVF
 	tristate "Intel(R) 10GbE PCI Express Virtual Function Ethernet support"
+	depends on !IXGBEVF
 	depends on PCI_MSI
 	---help---
 	  This driver supports Intel(R) PCI Express virtual functions for the
@@ -252,8 +267,9 @@ config IXGBEVF
 	  will be called ixgbevf.  MSI-X interrupt support is required
 	  for this driver to work correctly.
 
-config I40E
+config BACKPORT_I40E
 	tristate "Intel(R) Ethernet Controller XL710 Family support"
+	depends on !I40E
 	select PTP_1588_CLOCK
 	depends on PCI
 	---help---
@@ -271,10 +287,11 @@ config I40E
 	  To compile this driver as a module, choose M here. The module
 	  will be called i40e.
 
-config I40E_VXLAN
+config BACKPORT_I40E_VXLAN
 	bool "Virtual eXtensible Local Area Network Support"
+	depends on !I40E_VXLAN
 	default n
-	depends on I40E && VXLAN && !(I40E=y && VXLAN=m)
+	depends on BACKPORT_I40E && VXLAN && !(I40E=y && VXLAN=m)
 	---help---
 	  This allows one to create VXLAN virtual interfaces that provide
 	  Layer 2 Networks over Layer 3 Networks. VXLAN is often used
@@ -282,20 +299,22 @@ config I40E_VXLAN
 	  Say Y here if you want to use Virtual eXtensible Local Area Network
 	  (VXLAN) in the driver.
 
-config I40E_DCB
+config BACKPORT_I40E_DCB
 	bool "Data Center Bridging (DCB) Support"
+	depends on !I40E_DCB
 	default n
-	depends on I40E && DCB
+	depends on BACKPORT_I40E && DCB
 	---help---
 	  Say Y here if you want to use Data Center Bridging (DCB) in the
 	  driver.
 
 	  If unsure, say N.
 
-config I40E_FCOE
+config BACKPORT_I40E_FCOE
 	bool "Fibre Channel over Ethernet (FCoE)"
+	depends on !I40E_FCOE
 	default n
-	depends on I40E && DCB && FCOE
+	depends on BACKPORT_I40E && DCB && FCOE
 	---help---
 	  Say Y here if you want to use Fibre Channel over Ethernet (FCoE)
 	  in the driver. This will create new netdev for exclusive FCoE
@@ -303,8 +322,9 @@ config I40E_FCOE
 
 	  If unsure, say N.
 
-config I40EVF
+config BACKPORT_I40EVF
 	tristate "Intel(R) XL710 X710 Virtual Function Ethernet support"
+	depends on !I40EVF
 	depends on PCI_MSI
 	---help---
 	  This driver supports Intel(R) XL710 and X710 virtual functions.
@@ -322,8 +342,9 @@ config I40EVF
 	  will be called i40evf.  MSI-X interrupt support is required
 	  for this driver to work correctly.
 
-config FM10K
+config BACKPORT_FM10K
 	tristate "Intel(R) FM10000 Ethernet Switch Host Interface Support"
+	depends on !FM10K
 	default n
 	depends on PCI_MSI
 	select PTP_1588_CLOCK
@@ -342,10 +363,11 @@ config FM10K
 	  To compile this driver as a module, choose M here. The module
 	  will be called fm10k.  MSI-X interrupt support is required
 
-config FM10K_VXLAN
+config BACKPORT_FM10K_VXLAN
 	bool "Virtual eXtensible Local Area Network Support"
+	depends on !FM10K_VXLAN
 	default n
-	depends on FM10K && VXLAN && !(FM10K=y && VXLAN=m)
+	depends on BACKPORT_FM10K && VXLAN && !(FM10K=y && VXLAN=m)
 	---help---
 	  This allows one to create VXLAN virtual interfaces that provide
 	  Layer 2 Networks over Layer 3 Networks. VXLAN is often used
@@ -353,4 +375,4 @@ config FM10K_VXLAN
 	  Say Y here if you want to use Virtual eXtensible Local Area Network
 	  (VXLAN) in the driver.
 
-endif # NET_VENDOR_INTEL
+endif # BACKPORT_NET_VENDOR_INTEL
diff --git a/backports/drivers/net/ieee802154/Kconfig b/backports/drivers/net/ieee802154/Kconfig
index 1a3c3e5..87e82ba 100644
--- a/backports/drivers/net/ieee802154/Kconfig
+++ b/backports/drivers/net/ieee802154/Kconfig
@@ -1,6 +1,7 @@
-menuconfig IEEE802154_DRIVERS
+menuconfig BACKPORT_IEEE802154_DRIVERS
 	tristate "IEEE 802.15.4 drivers"
-	depends on NETDEVICES && IEEE802154
+	depends on !IEEE802154_DRIVERS
+	depends on NETDEVICES && BACKPORT_IEEE802154
 	default y
 	---help---
 	  Say Y here to get to see options for IEEE 802.15.4 Low-Rate
@@ -10,8 +11,9 @@ menuconfig IEEE802154_DRIVERS
 	  If you say N, all options in this submenu will be skipped and
 	  disabled.
 
-config IEEE802154_FAKELB
+config BACKPORT_IEEE802154_FAKELB
 	depends on IEEE802154_DRIVERS && MAC802154
+	depends on !IEEE802154_FAKELB
 	tristate "IEEE 802.15.4 loopback driver"
 	---help---
 	  Say Y here to enable the fake driver that can emulate a net
@@ -20,8 +22,9 @@ config IEEE802154_FAKELB
 	  This driver can also be built as a module. To do so say M here.
 	  The module will be called 'fakelb'.
 
-config IEEE802154_AT86RF230
+config BACKPORT_IEEE802154_AT86RF230
 	depends on IEEE802154_DRIVERS && MAC802154
+	depends on !IEEE802154_AT86RF230
 	tristate "AT86RF230/231/233/212 transceiver driver"
 	depends on SPI
 	select REGMAP_SPI
@@ -32,9 +35,10 @@ config IEEE802154_AT86RF230
 	  This driver can also be built as a module. To do so, say M here.
 	  the module will be called 'at86rf230'.
 
-config IEEE802154_MRF24J40
+config BACKPORT_IEEE802154_MRF24J40
 	tristate "Microchip MRF24J40 transceiver driver"
-	depends on IEEE802154_DRIVERS && MAC802154
+	depends on !IEEE802154_MRF24J40
+	depends on BACKPORT_IEEE802154_DRIVERS && MAC802154
 	depends on SPI
 	---help---
 	  Say Y here to enable the MRF24J20 SPI 802.15.4 wireless
@@ -43,8 +47,9 @@ config IEEE802154_MRF24J40
 	  This driver can also be built as a module. To do so, say M here.
 	  the module will be called 'mrf24j40'.
 
-config IEEE802154_CC2520
+config BACKPORT_IEEE802154_CC2520
 	depends on IEEE802154_DRIVERS && MAC802154
+	depends on !IEEE802154_CC2520
 	tristate "CC2520 transceiver driver"
 	depends on SPI
 	---help---
diff --git a/backports/drivers/net/usb/Kconfig b/backports/drivers/net/usb/Kconfig
index c5133ac..f03a668 100644
--- a/backports/drivers/net/usb/Kconfig
+++ b/backports/drivers/net/usb/Kconfig
@@ -4,15 +4,17 @@
 comment "Host-side USB support is needed for USB Network Adapter support"
 	depends on !USB && NET
 
-menuconfig USB_NET_DRIVERS
+menuconfig BACKPORT_USB_NET_DRIVERS
 	tristate "USB Network Adapters"
+	depends on !USB_NET_DRIVERS
 	default USB if USB
 	depends on USB && NET
 
-if USB_NET_DRIVERS
+if BACKPORT_USB_NET_DRIVERS
 
-config USB_CATC
+config BACKPORT_USB_CATC
 	tristate "USB CATC NetMate-based Ethernet device support"
+	depends on !USB_CATC
 	select CRC32
 	---help---
 	  Say Y if you want to use one of the following 10Mbps USB Ethernet
@@ -30,8 +32,9 @@ config USB_CATC
 	  To compile this driver as a module, choose M here: the
 	  module will be called catc.
 
-config USB_KAWETH
+config BACKPORT_USB_KAWETH
 	tristate "USB KLSI KL5USB101-based ethernet device support"
+	depends on !USB_KAWETH
 	---help---
 	  Say Y here if you want to use one of the following 10Mbps only
 	  USB Ethernet adapters based on the KLSI KL5KUSB101B chipset:
@@ -69,8 +72,9 @@ config USB_KAWETH
 	  To compile this driver as a module, choose M here: the
 	  module will be called kaweth.
 
-config USB_PEGASUS
+config BACKPORT_USB_PEGASUS
 	tristate "USB Pegasus/Pegasus-II based ethernet device support"
+	depends on !USB_PEGASUS
 	select MII
 	---help---
 	  Say Y here if you know you have Pegasus or Pegasus-II based adapter.
@@ -84,8 +88,9 @@ config USB_PEGASUS
 	  To compile this driver as a module, choose M here: the
 	  module will be called pegasus.
 
-config USB_RTL8150
+config BACKPORT_USB_RTL8150
 	tristate "USB RTL8150 based ethernet device support"
+	depends on !USB_RTL8150
 	select MII
 	help
 	  Say Y here if you have RTL8150 based usb-ethernet adapter.
@@ -95,8 +100,9 @@ config USB_RTL8150
 	  To compile this driver as a module, choose M here: the
 	  module will be called rtl8150.
 
-config USB_RTL8152
+config BACKPORT_USB_RTL8152
 	tristate "Realtek RTL8152/RTL8153 Based USB Ethernet Adapters"
+	depends on !USB_RTL8152
 	select MII
 	help
 	  This option adds support for Realtek RTL8152 based USB 2.0
@@ -106,8 +112,9 @@ config USB_RTL8152
 	  To compile this driver as a module, choose M here: the
 	  module will be called r8152.
 
-config USB_USBNET
+config BACKPORT_USB_USBNET
 	tristate "Multi-purpose USB Networking Framework"
+	depends on !USB_USBNET
 	select MII
 	---help---
 	  This driver supports several kinds of network links over USB,
@@ -140,9 +147,10 @@ config USB_USBNET
 	  To compile this driver as a module, choose M here: the
 	  module will be called usbnet.
 
-config USB_NET_AX8817X
+config BACKPORT_USB_NET_AX8817X
 	tristate "ASIX AX88xxx Based USB 2.0 Ethernet Adapters"
-	depends on USB_USBNET
+	depends on !USB_NET_AX8817X
+	depends on BACKPORT_USB_USBNET
 	select CRC32
 	select PHYLIB
 	default y
@@ -169,9 +177,10 @@ config USB_NET_AX8817X
 	  This driver creates an interface named "ethX", where X depends on
 	  what other networking devices you have in use.
 
-config USB_NET_AX88179_178A
+config BACKPORT_USB_NET_AX88179_178A
 	tristate "ASIX AX88179/178A USB 3.0/2.0 to Gigabit Ethernet"
-	depends on USB_USBNET
+	depends on !USB_NET_AX88179_178A
+	depends on BACKPORT_USB_USBNET
 	select CRC32
 	select PHYLIB
 	default y
@@ -187,9 +196,10 @@ config USB_NET_AX88179_178A
 	  This driver creates an interface named "ethX", where X depends on
 	  what other networking devices you have in use.
 
-config USB_NET_CDCETHER
+config BACKPORT_USB_NET_CDCETHER
 	tristate "CDC Ethernet support (smart devices such as cable modems)"
-	depends on USB_USBNET
+	depends on !USB_NET_CDCETHER
+	depends on BACKPORT_USB_USBNET
 	default y
 	help
 	  This option supports devices conforming to the Communication Device
@@ -215,9 +225,10 @@ config USB_NET_CDCETHER
 	  IEEE 802 "local assignment" bit is set in the address, a "usbX"
 	  name is used instead.
 
-config USB_NET_CDC_EEM
+config BACKPORT_USB_NET_CDC_EEM
 	tristate "CDC EEM support"
-	depends on USB_USBNET
+	depends on !USB_NET_CDC_EEM
+	depends on BACKPORT_USB_USBNET
 	help
 	  This option supports devices conforming to the Communication Device
 	  Class (CDC) Ethernet Emulation Model, a specification that's easy to
@@ -229,9 +240,10 @@ config USB_NET_CDC_EEM
 	  IEEE 802 "local assignment" bit is set in the address, a "usbX"
 	  name is used instead.
 
-config USB_NET_CDC_NCM
+config BACKPORT_USB_NET_CDC_NCM
 	tristate "CDC NCM support"
-	depends on USB_USBNET
+	depends on !USB_NET_CDC_NCM
+	depends on BACKPORT_USB_USBNET
 	default y
 	help
 	  This driver provides support for CDC NCM (Network Control Model
@@ -248,11 +260,12 @@ config USB_NET_CDC_NCM
 	    * ST-Ericsson M343 HSPA Mobile Broadband Modem (reference design)
 	    * Ericsson F5521gw Mobile Broadband Module
 
-config USB_NET_HUAWEI_CDC_NCM
+config BACKPORT_USB_NET_HUAWEI_CDC_NCM
 	tristate "Huawei NCM embedded AT channel support"
-	depends on USB_USBNET
-	select USB_WDM
-	select USB_NET_CDC_NCM
+	depends on !USB_NET_HUAWEI_CDC_NCM
+	depends on BACKPORT_USB_USBNET
+	select BACKPORT_USB_WDM
+	select BACKPORT_USB_NET_CDC_NCM
 	help
 		This driver supports huawei-style NCM devices, that use NCM as a
 		transport for other protocols, usually an embedded AT channel.
@@ -263,11 +276,12 @@ config USB_NET_HUAWEI_CDC_NCM
 		To compile this driver as a module, choose M here: the module will be
 		called huawei_cdc_ncm.ko.
 
-config USB_NET_CDC_MBIM
+config BACKPORT_USB_NET_CDC_MBIM
 	tristate "CDC MBIM support"
-	depends on USB_USBNET
-	select USB_WDM
-	select USB_NET_CDC_NCM
+	depends on !USB_NET_CDC_MBIM
+	depends on BACKPORT_USB_USBNET
+	select BACKPORT_USB_WDM
+	select BACKPORT_USB_NET_CDC_NCM
 	help
 	  This driver provides support for CDC MBIM (Mobile Broadband
 	  Interface Model) devices. The CDC MBIM specification is
@@ -281,25 +295,28 @@ config USB_NET_CDC_MBIM
 	  To compile this driver as a module, choose M here: the
 	  module will be called cdc_mbim.
 
-config USB_NET_DM9601
+config BACKPORT_USB_NET_DM9601
 	tristate "Davicom DM96xx based USB 10/100 ethernet devices"
-	depends on USB_USBNET
+	depends on !USB_NET_DM9601
+	depends on BACKPORT_USB_USBNET
 	select CRC32
 	help
 	  This option adds support for Davicom DM9601/DM9620/DM9621A
 	  based USB 10/100 Ethernet adapters.
 
-config USB_NET_SR9700
+config BACKPORT_USB_NET_SR9700
 	tristate "CoreChip-sz SR9700 based USB 1.1 10/100 ethernet devices"
-	depends on USB_USBNET
+	depends on !USB_NET_SR9700
+	depends on BACKPORT_USB_USBNET
 	select CRC32
 	help
 	  This option adds support for CoreChip-sz SR9700 based USB 1.1
 	  10/100 Ethernet adapters.
 
-config USB_NET_SR9800
+config BACKPORT_USB_NET_SR9800
 	tristate "CoreChip-sz SR9800 based USB 2.0 10/100 ethernet devices"
-	depends on USB_USBNET
+	depends on !USB_NET_SR9800
+	depends on BACKPORT_USB_USBNET
 	select CRC32
 	---help---
 	  Say Y if you want to use one of the following 100Mbps USB Ethernet
@@ -312,9 +329,10 @@ config USB_NET_SR9800
 	  To compile this driver as a module, choose M here: the
 	  module will be called sr9800.
 
-config USB_NET_SMSC75XX
+config BACKPORT_USB_NET_SMSC75XX
 	tristate "SMSC LAN75XX based USB 2.0 gigabit ethernet devices"
-	depends on USB_USBNET
+	depends on !USB_NET_SMSC75XX
+	depends on BACKPORT_USB_USBNET
 	select BITREVERSE
 	select CRC16
 	select CRC32
@@ -322,9 +340,10 @@ config USB_NET_SMSC75XX
 	  This option adds support for SMSC LAN75XX based USB 2.0
 	  Gigabit Ethernet adapters.
 
-config USB_NET_SMSC95XX
+config BACKPORT_USB_NET_SMSC95XX
 	tristate "SMSC LAN95XX based USB 2.0 10/100 ethernet devices"
-	depends on USB_USBNET
+	depends on !USB_NET_SMSC95XX
+	depends on BACKPORT_USB_USBNET
 	select BITREVERSE
 	select CRC16
 	select CRC32
@@ -332,45 +351,50 @@ config USB_NET_SMSC95XX
 	  This option adds support for SMSC LAN95XX based USB 2.0
 	  10/100 Ethernet adapters.
 
-config USB_NET_GL620A
+config BACKPORT_USB_NET_GL620A
 	tristate "GeneSys GL620USB-A based cables"
-	depends on USB_USBNET
+	depends on !USB_NET_GL620A
+	depends on BACKPORT_USB_USBNET
 	help
 	  Choose this option if you're using a host-to-host cable,
 	  or PC2PC motherboard, with this chip.
 
 	  Note that the half-duplex "GL620USB" is not supported.
 
-config USB_NET_NET1080
+config BACKPORT_USB_NET_NET1080
 	tristate "NetChip 1080 based cables (Laplink, ...)"
+	depends on !USB_NET_NET1080
 	default y
-	depends on USB_USBNET
+	depends on BACKPORT_USB_USBNET
 	help
 	  Choose this option if you're using a host-to-host cable based
 	  on this design:  one NetChip 1080 chip and supporting logic,
 	  optionally with LEDs that indicate traffic
 
-config USB_NET_PLUSB
+config BACKPORT_USB_NET_PLUSB
 	tristate "Prolific PL-2301/2302/25A1 based cables"
+	depends on !USB_NET_PLUSB
 	# if the handshake/init/reset problems, from original 'plusb',
 	# are ever resolved ... then remove "experimental"
-	depends on USB_USBNET
+	depends on BACKPORT_USB_USBNET
 	help
 	  Choose this option if you're using a host-to-host cable
 	  with one of these chips.
 
-config USB_NET_MCS7830
+config BACKPORT_USB_NET_MCS7830
 	tristate "MosChip MCS7830 based Ethernet adapters"
-	depends on USB_USBNET
+	depends on !USB_NET_MCS7830
+	depends on BACKPORT_USB_USBNET
 	help
 	  Choose this option if you're using a 10/100 Ethernet USB2
 	  adapter based on the MosChip 7830 controller. This includes
 	  adapters marketed under the DeLOCK brand.
 
-config USB_NET_RNDIS_HOST
+config BACKPORT_USB_NET_RNDIS_HOST
 	tristate "Host for RNDIS and ActiveSync devices"
-	depends on USB_USBNET
-	select USB_NET_CDCETHER
+	depends on !USB_NET_RNDIS_HOST
+	depends on BACKPORT_USB_USBNET
+	select BACKPORT_USB_NET_CDCETHER
 	help
 	  This option enables hosting "Remote NDIS" USB networking links,
 	  as encouraged by Microsoft (instead of CDC Ethernet!) for use in
@@ -382,9 +406,10 @@ config USB_NET_RNDIS_HOST
 	  The protocol specification is incomplete, and is controlled by
 	  (and for) Microsoft; it isn't an "Open" ecosystem or market.
 
-config USB_NET_CDC_SUBSET
+config BACKPORT_USB_NET_CDC_SUBSET
 	tristate "Simple USB Network Links (CDC Ethernet subset)"
-	depends on USB_USBNET
+	depends on !USB_NET_CDC_SUBSET
+	depends on BACKPORT_USB_USBNET
 	default y
 	help
 	  This driver module supports USB network devices that can work
@@ -397,33 +422,37 @@ config USB_NET_CDC_SUBSET
 	  the host on the other end) very well.  Also, these devices will
 	  not generally have permanently assigned Ethernet addresses.
 
-config USB_ALI_M5632
+config BACKPORT_USB_ALI_M5632
 	bool "ALi M5632 based 'USB 2.0 Data Link' cables"
-	depends on USB_NET_CDC_SUBSET
+	depends on !USB_ALI_M5632
+	depends on BACKPORT_USB_NET_CDC_SUBSET
 	help
 	  Choose this option if you're using a host-to-host cable
 	  based on this design, which supports USB 2.0 high speed.
 
-config USB_AN2720
+config BACKPORT_USB_AN2720
 	bool "AnchorChips 2720 based cables (Xircom PGUNET, ...)"
-	depends on USB_NET_CDC_SUBSET
+	depends on !USB_AN2720
+	depends on BACKPORT_USB_NET_CDC_SUBSET
 	help
 	  Choose this option if you're using a host-to-host cable
 	  based on this design.  Note that AnchorChips is now a
 	  Cypress brand.
 
-config USB_BELKIN
+config BACKPORT_USB_BELKIN
 	bool "eTEK based host-to-host cables (Advance, Belkin, ...)"
-	depends on USB_NET_CDC_SUBSET
+	depends on !USB_BELKIN
+	depends on BACKPORT_USB_NET_CDC_SUBSET
 	default y
 	help
 	  Choose this option if you're using a host-to-host cable
 	  based on this design:  two NetChip 2890 chips and an Atmel
 	  microcontroller, with LEDs that indicate traffic.
 
-config USB_ARMLINUX
+config BACKPORT_USB_ARMLINUX
 	bool "Embedded ARM Linux links (iPaq, ...)"
-	depends on USB_NET_CDC_SUBSET
+	depends on !USB_ARMLINUX
+	depends on BACKPORT_USB_NET_CDC_SUBSET
 	default y
 	help
 	  Choose this option to support the "usb-eth" networking driver
@@ -438,24 +467,27 @@ config USB_ARMLINUX
 	  different link level framing protocol, you can have them use
 	  this simpler protocol by installing a different kernel.
 
-config USB_EPSON2888
+config BACKPORT_USB_EPSON2888
 	bool "Epson 2888 based firmware (DEVELOPMENT)"
-	depends on USB_NET_CDC_SUBSET
+	depends on !USB_EPSON2888
+	depends on BACKPORT_USB_NET_CDC_SUBSET
 	help
 	  Choose this option to support the usb networking links used
 	  by some sample firmware from Epson.
 
-config USB_KC2190
+config BACKPORT_USB_KC2190
 	bool "KT Technology KC2190 based cables (InstaNet)"
-	depends on USB_NET_CDC_SUBSET
+	depends on !USB_KC2190
+	depends on BACKPORT_USB_NET_CDC_SUBSET
 	help
 	  Choose this option if you're using a host-to-host cable
 	  with one of these chips.
 
-config USB_NET_ZAURUS
+config BACKPORT_USB_NET_ZAURUS
 	tristate "Sharp Zaurus (stock ROMs) and compatible"
-	depends on USB_USBNET
-	select USB_NET_CDCETHER
+	depends on !USB_NET_ZAURUS
+	depends on BACKPORT_USB_USBNET
+	select BACKPORT_USB_NET_CDCETHER
 	select CRC32
 	default y
 	help
@@ -470,17 +502,19 @@ config USB_NET_ZAURUS
 	  really need this non-conformant variant of CDC Ethernet (or in
 	  some cases CDC MDLM) protocol, not "g_ether".
 
-config USB_NET_CX82310_ETH
+config BACKPORT_USB_NET_CX82310_ETH
 	tristate "Conexant CX82310 USB ethernet port"
-	depends on USB_USBNET
+	depends on !USB_NET_CX82310_ETH
+	depends on BACKPORT_USB_USBNET
 	help
 	  Choose this option if you're using a Conexant CX82310-based ADSL
 	  router with USB ethernet port. This driver is for routers only,
 	  it will not work with ADSL modems (use cxacru driver instead).
 
-config USB_NET_KALMIA
+config BACKPORT_USB_NET_KALMIA
 	tristate "Samsung Kalmia based LTE USB modem"
-	depends on USB_USBNET
+	depends on !USB_NET_KALMIA
+	depends on BACKPORT_USB_USBNET
 	help
 	  Choose this option if you have a Samsung Kalmia based USB modem
 	  as Samsung GT-B3730.
@@ -488,10 +522,11 @@ config USB_NET_KALMIA
 	  To compile this driver as a module, choose M here: the
 	  module will be called kalmia.
 
-config USB_NET_QMI_WWAN
+config BACKPORT_USB_NET_QMI_WWAN
 	tristate "QMI WWAN driver for Qualcomm MSM based 3G and LTE modems"
-	depends on USB_USBNET
-	select USB_WDM
+	depends on !USB_NET_QMI_WWAN
+	depends on BACKPORT_USB_USBNET
+	select BACKPORT_USB_WDM
 	help
 	  Support WWAN LTE/3G devices based on Qualcomm Mobile Data Modem
 	  (MDM) chipsets.  Examples of such devices are
@@ -510,8 +545,9 @@ config USB_NET_QMI_WWAN
 	  To compile this driver as a module, choose M here: the
 	  module will be called qmi_wwan.
 
-config USB_HSO
+config BACKPORT_USB_HSO
 	tristate "Option USB High Speed Mobile Devices"
+	depends on !USB_HSO
 	depends on USB && RFKILL && TTY
 	default n
 	help
@@ -521,24 +557,27 @@ config USB_HSO
 	  To compile this driver as a module, choose M here: the
 	  module will be called hso.
 
-config USB_NET_INT51X1
+config BACKPORT_USB_NET_INT51X1
 	tristate "Intellon PLC based usb adapter"
-	depends on USB_USBNET
+	depends on !USB_NET_INT51X1
+	depends on BACKPORT_USB_USBNET
 	help
 	  Choose this option if you're using a 14Mb USB-based PLC
 	  (Powerline Communications) solution with an Intellon
 	  INT51x1/INT5200 chip, like the "devolo dLan duo".
 
-config USB_CDC_PHONET
+config BACKPORT_USB_CDC_PHONET
 	tristate "CDC Phonet support"
+	depends on !USB_CDC_PHONET
 	depends on PHONET
 	help
 	  Choose this option to support the Phonet interface to a Nokia
 	  cellular modem, as found on most Nokia handsets with the
 	  "PC suite" USB profile.
 
-config USB_IPHETH
+config BACKPORT_USB_IPHETH
 	tristate "Apple iPhone USB Ethernet driver"
+	depends on !USB_IPHETH
 	default n
 	---help---
 	  Module used to share Internet connection (tethering) from your
@@ -549,19 +588,21 @@ config USB_IPHETH
 
 	  For more information: http://giagio.com/wiki/moin.cgi/iPhoneEthernetDriver
 
-config USB_SIERRA_NET
+config BACKPORT_USB_SIERRA_NET
 	tristate "USB-to-WWAN Driver for Sierra Wireless modems"
-	depends on USB_USBNET
+	depends on !USB_SIERRA_NET
+	depends on BACKPORT_USB_USBNET
 	help
 	  Choose this option if you have a Sierra Wireless USB-to-WWAN device.
 
 	  To compile this driver as a module, choose M here: the
 	  module will be called sierra_net.
 
-config USB_VL600
+config BACKPORT_USB_VL600
 	tristate "LG VL600 modem dongle"
-	depends on USB_NET_CDCETHER && TTY
-	select USB_ACM
+	depends on !USB_VL600
+	depends on BACKPORT_USB_NET_CDCETHER && TTY
+	select BACKPORT_USB_ACM
 	help
 	  Select this if you want to use an LG Electronics 4G/LTE usb modem
 	  called VL600.  This driver only handles the ethernet
@@ -573,4 +614,4 @@ config USB_VL600
 
 	  http://ubuntuforums.org/showpost.php?p=10589647&postcount=17
 
-endif # USB_NET_DRIVERS
+endif # BACKPORT_USB_NET_DRIVERS
diff --git a/backports/drivers/net/wireless/Kconfig b/backports/drivers/net/wireless/Kconfig
index 818c54a..e4c2d79 100644
--- a/backports/drivers/net/wireless/Kconfig
+++ b/backports/drivers/net/wireless/Kconfig
@@ -2,8 +2,9 @@
 # Wireless LAN device configuration
 #
 
-menuconfig WLAN
+menuconfig BACKPORT_WLAN
 	bool "Wireless LAN"
+	depends on !WLAN
 	depends on !S390
 	depends on NET
 	select WIRELESS
@@ -15,10 +16,11 @@ menuconfig WLAN
 
 	  http://wireless.kernel.org/en/users/Drivers
 
-if WLAN
+if BACKPORT_WLAN
 
-config PCMCIA_RAYCS
+config BACKPORT_PCMCIA_RAYCS
 	tristate "Aviator/Raytheon 2.4GHz wireless support"
+	depends on !PCMCIA_RAYCS
 	depends on PCMCIA
 	select WIRELESS_EXT
 	select WEXT_SPY
@@ -32,28 +34,32 @@ config PCMCIA_RAYCS
 	  To compile this driver as a module, choose M here: the module will be
 	  called ray_cs.  If unsure, say N.
 
-config LIBERTAS_THINFIRM
+config BACKPORT_LIBERTAS_THINFIRM
 	tristate "Marvell 8xxx Libertas WLAN driver support with thin firmware"
-	depends on MAC80211
+	depends on !LIBERTAS_THINFIRM
+	depends on BACKPORT_MAC80211
 	select FW_LOADER
 	---help---
 	  A library for Marvell Libertas 8xxx devices using thinfirm.
 
-config LIBERTAS_THINFIRM_DEBUG
+config BACKPORT_LIBERTAS_THINFIRM_DEBUG
 	bool "Enable full debugging output in the Libertas thin firmware module."
-	depends on LIBERTAS_THINFIRM
+	depends on !LIBERTAS_THINFIRM_DEBUG
+	depends on BACKPORT_LIBERTAS_THINFIRM
 	---help---
 	  Debugging support.
 
-config LIBERTAS_THINFIRM_USB
+config BACKPORT_LIBERTAS_THINFIRM_USB
 	tristate "Marvell Libertas 8388 USB 802.11b/g cards with thin firmware"
-	depends on LIBERTAS_THINFIRM && USB
+	depends on !LIBERTAS_THINFIRM_USB
+	depends on BACKPORT_LIBERTAS_THINFIRM && USB
 	---help---
 	  A driver for Marvell Libertas 8388 USB devices using thinfirm.
 
-config AIRO
+config BACKPORT_AIRO
 	tristate "Cisco/Aironet 34X/35X/4500/4800 ISA and PCI cards"
-	depends on CFG80211 && ISA_DMA_API && (PCI || BROKEN)
+	depends on !AIRO
+	depends on BACKPORT_CFG80211 && ISA_DMA_API && (PCI || BROKEN)
 	select WIRELESS_EXT
 	select CRYPTO
 	select WEXT_SPY
@@ -71,9 +77,10 @@ config AIRO
 
 	  The driver can be compiled as a module and will be named "airo".
 
-config ATMEL
+config BACKPORT_ATMEL
       tristate "Atmel at76c50x chipset  802.11b support"
-      depends on CFG80211 && (PCI || PCMCIA)
+	depends on !ATMEL
+      depends on BACKPORT_CFG80211 && (PCI || PCMCIA)
       select WIRELESS_EXT
       select WEXT_PRIV
       select FW_LOADER
@@ -89,16 +96,18 @@ config ATMEL
         firmware package can be downloaded from
         <http://www.thekelleys.org.uk/atmel>
 
-config PCI_ATMEL
+config BACKPORT_PCI_ATMEL
       tristate "Atmel at76c506 PCI cards"
-      depends on ATMEL && PCI
+	depends on !PCI_ATMEL
+      depends on BACKPORT_ATMEL && PCI
        ---help---
         Enable support for PCI and mini-PCI cards containing the
         Atmel at76c506 chip.
 
-config PCMCIA_ATMEL
+config BACKPORT_PCMCIA_ATMEL
 	tristate "Atmel at76c502/at76c504 PCMCIA cards"
-	depends on ATMEL && PCMCIA
+	depends on !PCMCIA_ATMEL
+	depends on BACKPORT_ATMEL && PCMCIA
 	select WIRELESS_EXT
 	select FW_LOADER
 	select CRC32
@@ -106,17 +115,19 @@ config PCMCIA_ATMEL
 	  Enable support for PCMCIA cards containing the
 	  Atmel at76c502 and at76c504 chips.
 
-config AT76C50X_USB
+config BACKPORT_AT76C50X_USB
         tristate "Atmel at76c503/at76c505/at76c505a USB cards"
-        depends on MAC80211 && USB
+	depends on !AT76C50X_USB
+        depends on BACKPORT_MAC80211 && USB
         select FW_LOADER
         ---help---
           Enable support for USB Wireless devices using Atmel at76c503,
           at76c505 or at76c505a chips.
 
-config AIRO_CS
+config BACKPORT_AIRO_CS
 	tristate "Cisco/Aironet 34X/35X/4500/4800 PCMCIA cards"
-	depends on CFG80211 && PCMCIA && (BROKEN || !M32R)
+	depends on !AIRO_CS
+	depends on BACKPORT_CFG80211 && PCMCIA && (BROKEN || !M32R)
 	select WIRELESS_EXT
 	select WEXT_SPY
 	select WEXT_PRIV
@@ -136,9 +147,10 @@ config AIRO_CS
 	  and Cisco proprietary API, so both the Linux Wireless Tools and the
 	  Cisco Linux utilities can be used to configure the card.
 
-config PCMCIA_WL3501
+config BACKPORT_PCMCIA_WL3501
 	tristate "Planet WL3501 PCMCIA cards"
-	depends on CFG80211 && PCMCIA
+	depends on !PCMCIA_WL3501
+	depends on BACKPORT_CFG80211 && PCMCIA
 	select WIRELESS_EXT
 	select WEXT_SPY
 	help
@@ -146,8 +158,9 @@ config PCMCIA_WL3501
 	  It has basic support for Linux wireless extensions and initial
 	  micro support for ethtool.
 
-config PRISM54
+config BACKPORT_PRISM54
 	tristate 'Intersil Prism GT/Duette/Indigo PCI/Cardbus (DEPRECATED)'
+	depends on !PRISM54
 	depends on PCI
 	select WIRELESS_EXT
 	select WEXT_SPY
@@ -166,9 +179,10 @@ config PRISM54
 
 	  When built as module you get the module prism54
 
-config USB_ZD1201
+config BACKPORT_USB_ZD1201
 	tristate "USB ZD1201 based Wireless device support"
-	depends on CFG80211 && USB
+	depends on !USB_ZD1201
+	depends on BACKPORT_CFG80211 && USB
 	select WIRELESS_EXT
 	select WEXT_PRIV
 	select FW_LOADER
@@ -185,14 +199,15 @@ config USB_ZD1201
 	  To compile this driver as a module, choose M here: the
 	  module will be called zd1201.
 
-config USB_NET_RNDIS_WLAN
+config BACKPORT_USB_NET_RNDIS_WLAN
 	tristate "Wireless RNDIS USB support"
+	depends on !USB_NET_RNDIS_WLAN
 	depends on USB
-	depends on CFG80211
-	select USB_NET_DRIVERS
-	select USB_USBNET
-	select USB_NET_CDCETHER
-	select USB_NET_RNDIS_HOST
+	depends on BACKPORT_CFG80211
+	select BACKPORT_USB_NET_DRIVERS
+	select BACKPORT_USB_USBNET
+	select BACKPORT_USB_NET_CDCETHER
+	select BACKPORT_USB_NET_RNDIS_HOST
 	---help---
 	  This is a driver for wireless RNDIS devices.
 	  These are USB based adapters found in devices such as:
@@ -204,7 +219,7 @@ config USB_NET_RNDIS_WLAN
 	  Linksys WUSB54GSC
 	  Asus WL169gE
 	  Eminent EM4045
-	  BT Voyager 1055
+	  BACKPORT_BT Voyager 1055
 	  Linksys WUSB54GSv1
 	  U.S. Robotics USR5420
 	  BUFFALO WLI-USB-G54
@@ -216,13 +231,14 @@ config USB_NET_RNDIS_WLAN
 
 source "$BACKPORT_DIR/drivers/net/wireless/rtl818x/Kconfig"
 
-config ADM8211
+config BACKPORT_ADM8211
 	tristate "ADMtek ADM8211 support"
-	depends on MAC80211 && PCI
+	depends on !ADM8211
+	depends on BACKPORT_MAC80211 && PCI
 	select CRC32
 	select EEPROM_93CX6
 	---help---
-	  This driver is for ADM8211A, ADM8211B, and ADM8211C based cards.
+	  This driver is for BACKPORT_ADM8211A, BACKPORT_ADM8211B, and BACKPORT_ADM8211C based cards.
 	  These are PCI/mini-PCI/Cardbus 802.11b chips found in cards such as:
 
 	  Xterasys Cardbus XN-2411b
@@ -238,14 +254,15 @@ config ADM8211
 	  D-Link DWL-520 Revision C
 
 	  However, some of these cards have been replaced with other chips
-	  like the RTL8180L (Xterasys Cardbus XN-2411b, Belkin F5D6001) or
+	  like the BACKPORT_RTL8180L (Xterasys Cardbus XN-2411b, Belkin F5D6001) or
 	  the Ralink RT2400 (SMC2635W) without a model number change.
 
 	  Thanks to Infineon-ADMtek for their support of this driver.
 
-config MAC80211_HWSIM
+config BACKPORT_MAC80211_HWSIM
 	tristate "Simulated radio testing tool for mac80211"
-	depends on MAC80211
+	depends on !MAC80211_HWSIM
+	depends on BACKPORT_MAC80211
 	---help---
 	  This driver is a developer testing tool that can be used to test
 	  IEEE 802.11 networking stack (mac80211) functionality. This is not
@@ -256,9 +273,10 @@ config MAC80211_HWSIM
 	  To compile this driver as a module, choose M here: the module will be
 	  called mac80211_hwsim.  If unsure, say N.
 
-config MWL8K
+config BACKPORT_MWL8K
 	tristate "Marvell 88W8xxx PCI/PCIe Wireless support"
-	depends on MAC80211 && PCI
+	depends on !MWL8K
+	depends on BACKPORT_MAC80211 && PCI
 	---help---
 	  This driver supports Marvell TOPDOG 802.11 wireless cards.
 
@@ -284,4 +302,4 @@ source "$BACKPORT_DIR/drivers/net/wireless/mwifiex/Kconfig"
 source "$BACKPORT_DIR/drivers/net/wireless/cw1200/Kconfig"
 source "$BACKPORT_DIR/drivers/net/wireless/rsi/Kconfig"
 
-endif # WLAN
+endif # BACKPORT_WLAN
diff --git a/backports/drivers/net/wireless/ath/Kconfig b/backports/drivers/net/wireless/ath/Kconfig
index 9f759a5..83ccc54 100644
--- a/backports/drivers/net/wireless/ath/Kconfig
+++ b/backports/drivers/net/wireless/ath/Kconfig
@@ -1,9 +1,11 @@
-config ATH_COMMON
+config BACKPORT_ATH_COMMON
 	tristate
+	depends on !ATH_COMMON
 
-menuconfig ATH_CARDS
+menuconfig BACKPORT_ATH_CARDS
 	tristate "Atheros Wireless Cards"
-	depends on CFG80211 && (!UML || BROKEN)
+	depends on !ATH_CARDS
+	depends on BACKPORT_CFG80211 && (!UML || BROKEN)
 	---help---
 	  This will enable the support for the Atheros wireless drivers.
 	  ath5k, ath9k, ath9k_htc and ar9170 drivers share some common code, this option
@@ -17,34 +19,38 @@ menuconfig ATH_CARDS
 
 	  http://wireless.kernel.org/en/users/Drivers/Atheros
 
-if ATH_CARDS
+if BACKPORT_ATH_CARDS
 
-config ATH_DEBUG
+config BACKPORT_ATH_DEBUG
 	bool "Atheros wireless debugging"
+	depends on !ATH_DEBUG
 	---help---
 	  Say Y, if you want to debug atheros wireless drivers.
 	  Right now only ath9k makes use of this.
 
-config ATH_TRACEPOINTS
+config BACKPORT_ATH_TRACEPOINTS
        bool "Atheros wireless tracing"
-       depends on ATH_DEBUG
+	depends on !ATH_TRACEPOINTS
+       depends on BACKPORT_ATH_DEBUG
        depends on EVENT_TRACING
        ---help---
          This option enables tracepoints for atheros wireless drivers.
 	 Currently, ath9k makes use of this facility.
 
-config ATH_REG_DYNAMIC_USER_REG_HINTS
+config BACKPORT_ATH_REG_DYNAMIC_USER_REG_HINTS
 	bool "Atheros dynamic user regulatory hints"
-	depends on CFG80211_CERTIFICATION_ONUS
+	depends on !ATH_REG_DYNAMIC_USER_REG_HINTS
+	depends on BACKPORT_CFG80211_CERTIFICATION_ONUS
 	default n
 	---help---
 	  Say N. This should only be enabled in countries where
 	  this feature is explicitly allowed and only on cards that
 	  specifically have been tested for this.
 
-config ATH_REG_DYNAMIC_USER_CERT_TESTING
+config BACKPORT_ATH_REG_DYNAMIC_USER_CERT_TESTING
 	bool "Atheros dynamic user regulatory testing"
-	depends on ATH_REG_DYNAMIC_USER_REG_HINTS && CFG80211_CERTIFICATION_ONUS
+	depends on !ATH_REG_DYNAMIC_USER_CERT_TESTING
+	depends on ATH_REG_DYNAMIC_USER_REG_HINTS && BACKPORT_CFG80211_CERTIFICATION_ONUS
 	default n
 	---help---
 	  Say N. This should only be enabled on systems
diff --git a/backports/drivers/net/wireless/ath/ar5523/Kconfig b/backports/drivers/net/wireless/ath/ar5523/Kconfig
index 0d320cc..bfb52f4 100644
--- a/backports/drivers/net/wireless/ath/ar5523/Kconfig
+++ b/backports/drivers/net/wireless/ath/ar5523/Kconfig
@@ -1,8 +1,9 @@
-config AR5523
+config BACKPORT_AR5523
        tristate "Atheros AR5523 wireless driver support"
-       depends on MAC80211 && USB
-       select ATH_COMMON
+	depends on !AR5523
+       depends on BACKPORT_MAC80211 && USB
+       select BACKPORT_ATH_COMMON
        select FW_LOADER
        ---help---
-         This module add support for AR5523 based USB dongles such as D-Link
+         This module add support for BACKPORT_AR5523 based USB dongles such as D-Link
          DWL-G132, Netgear WPN111 and many more.
diff --git a/backports/drivers/net/wireless/ath/ath10k/Kconfig b/backports/drivers/net/wireless/ath/ath10k/Kconfig
index 72acb82..483af44 100644
--- a/backports/drivers/net/wireless/ath/ath10k/Kconfig
+++ b/backports/drivers/net/wireless/ath/ath10k/Kconfig
@@ -1,46 +1,52 @@
-config ATH10K
+config BACKPORT_ATH10K
         tristate "Atheros 802.11ac wireless cards support"
-        depends on MAC80211 && HAS_DMA
-	select ATH_COMMON
+	depends on !ATH10K
+        depends on BACKPORT_MAC80211 && HAS_DMA
+	select BACKPORT_ATH_COMMON
         ---help---
           This module adds support for wireless adapters based on
           Atheros IEEE 802.11ac family of chipsets.
 
           If you choose to build a module, it'll be called ath10k.
 
-config ATH10K_PCI
+config BACKPORT_ATH10K_PCI
 	tristate "Atheros ath10k PCI support"
-	depends on ATH10K && PCI
+	depends on !ATH10K_PCI
+	depends on BACKPORT_ATH10K && PCI
 	---help---
 	  This module adds support for PCIE bus
 
-config ATH10K_DEBUG
+config BACKPORT_ATH10K_DEBUG
 	bool "Atheros ath10k debugging"
-	depends on ATH10K
+	depends on !ATH10K_DEBUG
+	depends on BACKPORT_ATH10K
 	---help---
 	  Enables debug support
 
 	  If unsure, say Y to make it easier to debug problems.
 
-config ATH10K_DEBUGFS
+config BACKPORT_ATH10K_DEBUGFS
 	bool "Atheros ath10k debugfs support"
-	depends on ATH10K && DEBUG_FS
+	depends on !ATH10K_DEBUGFS
+	depends on BACKPORT_ATH10K && DEBUG_FS
 	select RELAY
 	---help---
 	  Enabled debugfs support
 
 	  If unsure, say Y to make it easier to debug problems.
 
-config ATH10K_TRACING
+config BACKPORT_ATH10K_TRACING
 	bool "Atheros ath10k tracing support"
-	depends on ATH10K
+	depends on !ATH10K_TRACING
+	depends on BACKPORT_ATH10K
 	depends on EVENT_TRACING
 	---help---
 	  Select this to ath10k use tracing infrastructure.
 
-config ATH10K_DFS_CERTIFIED
+config BACKPORT_ATH10K_DFS_CERTIFIED
 	bool "Atheros DFS support for certified platforms"
-	depends on ATH10K && CFG80211_CERTIFICATION_ONUS
+	depends on !ATH10K_DFS_CERTIFIED
+	depends on ATH10K && BACKPORT_CFG80211_CERTIFICATION_ONUS
 	default n
 	---help---
 	This option enables DFS support for initiating radiation on
diff --git a/backports/drivers/net/wireless/ath/ath5k/Kconfig b/backports/drivers/net/wireless/ath/ath5k/Kconfig
index 2399a39..7660a91 100644
--- a/backports/drivers/net/wireless/ath/ath5k/Kconfig
+++ b/backports/drivers/net/wireless/ath/ath5k/Kconfig
@@ -1,13 +1,14 @@
-config ATH5K
+config BACKPORT_ATH5K
 	tristate "Atheros 5xxx wireless cards support"
-	depends on (PCI || ATH25) && MAC80211
-	select ATH_COMMON
-	select MAC80211_LEDS
+	depends on !ATH5K
+	depends on (PCI || ATH25) && BACKPORT_MAC80211
+	select BACKPORT_ATH_COMMON
+	select BACKPORT_MAC80211_LEDS
 	select LEDS_CLASS
 	select NEW_LEDS
 	select AVERAGE
-	select ATH5K_AHB if ATH25
-	select ATH5K_PCI if !ATH25
+	select BACKPORT_ATH5K_AHB if ATH25
+	select BACKPORT_ATH5K_PCI if !ATH25
 	---help---
 	  This module adds support for wireless adapters based on
 	  Atheros 5xxx chipset.
@@ -22,9 +23,10 @@ config ATH5K
 	  If you choose to build a module, it'll be called ath5k. Say M if
 	  unsure.
 
-config ATH5K_DEBUG
+config BACKPORT_ATH5K_DEBUG
 	bool "Atheros 5xxx debugging"
-	depends on ATH5K
+	depends on !ATH5K_DEBUG
+	depends on BACKPORT_ATH5K
 	---help---
 	  Atheros 5xxx debugging messages.
 
@@ -41,9 +43,10 @@ config ATH5K_DEBUG
 
 	  modprobe ath5k debug=0x00000400
 
-config ATH5K_TRACER
+config BACKPORT_ATH5K_TRACER
 	bool "Atheros 5xxx tracer"
-	depends on ATH5K
+	depends on !ATH5K_TRACER
+	depends on BACKPORT_ATH5K
 	depends on EVENT_TRACING
 	---help---
 	  Say Y here to enable tracepoints for the ath5k driver
@@ -52,23 +55,26 @@ config ATH5K_TRACER
 
 	  If unsure, say N.
 
-config ATH5K_AHB
+config BACKPORT_ATH5K_AHB
 	bool "Atheros 5xxx AHB bus support"
+	depends on !ATH5K_AHB
 	depends on ATH25
 	---help---
 	  This adds support for WiSoC type chipsets of the 5xxx Atheros
 	  family.
 
-config ATH5K_PCI
+config BACKPORT_ATH5K_PCI
 	bool "Atheros 5xxx PCI bus support"
+	depends on !ATH5K_PCI
 	depends on (!ATH25 && PCI)
 	---help---
 	  This adds support for PCI type chipsets of the 5xxx Atheros
 	  family.
 
-config ATH5K_TEST_CHANNELS
+config BACKPORT_ATH5K_TEST_CHANNELS
 	bool "Enables testing channels on ath5k"
-	depends on ATH5K && CFG80211_CERTIFICATION_ONUS
+	depends on !ATH5K_TEST_CHANNELS
+	depends on ATH5K && BACKPORT_CFG80211_CERTIFICATION_ONUS
 	---help---
 	  This enables non-standard IEEE 802.11 channels on ath5k, which
 	  can be used for research purposes. This option should be disabled
diff --git a/backports/drivers/net/wireless/ath/ath6kl/Kconfig b/backports/drivers/net/wireless/ath/ath6kl/Kconfig
index 9c125ff..df0a495 100644
--- a/backports/drivers/net/wireless/ath/ath6kl/Kconfig
+++ b/backports/drivers/net/wireless/ath/ath6kl/Kconfig
@@ -1,6 +1,7 @@
-config ATH6KL
+config BACKPORT_ATH6KL
 	tristate "Atheros mobile chipsets support"
-	depends on CFG80211
+	depends on !ATH6KL
+	depends on BACKPORT_CFG80211
         ---help---
 	  This module adds core support for wireless adapters based on
 	  Atheros AR6003 and AR6004 chipsets. You still need separate
@@ -10,9 +11,10 @@ config ATH6KL
 	  ath6kl_core. Please note that AR6002 and AR6001 are not
 	  supported by this driver.
 
-config ATH6KL_SDIO
+config BACKPORT_ATH6KL_SDIO
 	tristate "Atheros ath6kl SDIO support"
-	depends on ATH6KL
+	depends on !ATH6KL_SDIO
+	depends on BACKPORT_ATH6KL
 	depends on MMC
 	---help---
 	  This module adds support for wireless adapters based on
@@ -21,9 +23,10 @@ config ATH6KL_SDIO
 	  Please note that AR6002 and AR6001 are not supported by this
 	  driver.
 
-config ATH6KL_USB
+config BACKPORT_ATH6KL_USB
 	tristate "Atheros ath6kl USB support"
-	depends on ATH6KL
+	depends on !ATH6KL_USB
+	depends on BACKPORT_ATH6KL
 	depends on USB
 	---help---
 	  This module adds support for wireless adapters based on
@@ -31,9 +34,10 @@ config ATH6KL_USB
 	  USB. If you choose to build it as a module, it will be
 	  called ath6kl_usb.
 
-config ATH6KL_DEBUG
+config BACKPORT_ATH6KL_DEBUG
 	bool "Atheros ath6kl debugging"
-	depends on ATH6KL
+	depends on !ATH6KL_DEBUG
+	depends on BACKPORT_ATH6KL
 	---help---
 	  Enables ath6kl debug support, including debug messages
 	  enabled with debug_mask module parameter and debugfs
@@ -41,9 +45,10 @@ config ATH6KL_DEBUG
 
 	  If unsure, say Y to make it easier to debug problems.
 
-config ATH6KL_TRACING
+config BACKPORT_ATH6KL_TRACING
 	bool "Atheros ath6kl tracing support"
-	depends on ATH6KL
+	depends on !ATH6KL_TRACING
+	depends on BACKPORT_ATH6KL
 	depends on EVENT_TRACING
 	---help---
 	  Select this to ath6kl use tracing infrastructure which, for
@@ -53,10 +58,11 @@ config ATH6KL_TRACING
 
 	  If unsure, say Y to make it easier to debug problems.
 
-config ATH6KL_REGDOMAIN
+config BACKPORT_ATH6KL_REGDOMAIN
 	bool "Atheros ath6kl regdomain support"
-	depends on ATH6KL
-	depends on CFG80211_CERTIFICATION_ONUS
+	depends on !ATH6KL_REGDOMAIN
+	depends on BACKPORT_ATH6KL
+	depends on BACKPORT_CFG80211_CERTIFICATION_ONUS
 	---help---
 	  Enabling this makes it possible to change the regdomain in
 	  the firmware. This can be only enabled if regulatory requirements
diff --git a/backports/drivers/net/wireless/ath/ath9k/Kconfig b/backports/drivers/net/wireless/ath/ath9k/Kconfig
index fee0cad..220ffdf 100644
--- a/backports/drivers/net/wireless/ath/ath9k/Kconfig
+++ b/backports/drivers/net/wireless/ath/ath9k/Kconfig
@@ -1,30 +1,35 @@
-config ATH9K_HW
+config BACKPORT_ATH9K_HW
 	tristate
-config ATH9K_COMMON
+	depends on !ATH9K_HW
+config BACKPORT_ATH9K_COMMON
 	tristate
-	select ATH_COMMON
+	depends on !ATH9K_COMMON
+	select BACKPORT_ATH_COMMON
 	select DEBUG_FS
 	select RELAY
-config ATH9K_DFS_DEBUGFS
+config BACKPORT_ATH9K_DFS_DEBUGFS
 	def_bool y
-	depends on ATH9K_DEBUGFS && ATH9K_DFS_CERTIFIED
+	depends on !ATH9K_DFS_DEBUGFS
+	depends on BACKPORT_ATH9K_DEBUGFS && BACKPORT_ATH9K_DFS_CERTIFIED
 
-config ATH9K_BTCOEX_SUPPORT
+config BACKPORT_ATH9K_BTCOEX_SUPPORT
 	bool "Atheros bluetooth coexistence support"
-	depends on (ATH9K || ATH9K_HTC)
+	depends on !ATH9K_BTCOEX_SUPPORT
+	depends on (ATH9K || BACKPORT_ATH9K_HTC)
 	default y
 	---help---
 	  Say Y, if you want to use the ath9k/ath9k_htc radios together with
 	  Bluetooth modules in the same system.
 
-config ATH9K
+config BACKPORT_ATH9K
 	tristate "Atheros 802.11n wireless cards support"
-	depends on MAC80211 && HAS_DMA
-	select ATH9K_HW
-	select MAC80211_LEDS
+	depends on !ATH9K
+	depends on BACKPORT_MAC80211 && HAS_DMA
+	select BACKPORT_ATH9K_HW
+	select BACKPORT_MAC80211_LEDS
 	select LEDS_CLASS
 	select NEW_LEDS
-	select ATH9K_COMMON
+	select BACKPORT_ATH9K_COMMON
 	---help---
 	  This module adds support for wireless adapters based on
 	  Atheros IEEE 802.11n AR5008, AR9001 and AR9002 family
@@ -37,18 +42,20 @@ config ATH9K
 
 	  If you choose to build a module, it'll be called ath9k.
 
-config ATH9K_PCI
+config BACKPORT_ATH9K_PCI
 	bool "Atheros ath9k PCI/PCIe bus support"
+	depends on !ATH9K_PCI
 	default y
-	depends on ATH9K && PCI
+	depends on BACKPORT_ATH9K && PCI
 	---help---
 	  This option enables the PCI bus support in ath9k.
 
 	  Say Y, if you have a compatible PCI/PCIe wireless card.
 
-config ATH9K_AHB
+config BACKPORT_ATH9K_AHB
 	bool "Atheros ath9k AHB bus support"
-	depends on ATH9K
+	depends on !ATH9K_AHB
+	depends on BACKPORT_ATH9K
 	default n
 	---help---
 	  This option enables the AHB bus support in ath9k.
@@ -56,10 +63,11 @@ config ATH9K_AHB
 	  Say Y, if you have a SoC with a compatible built-in
 	  wireless MAC. Say N if unsure.
 
-config ATH9K_DEBUGFS
+config BACKPORT_ATH9K_DEBUGFS
 	bool "Atheros ath9k debugging"
-	depends on ATH9K && DEBUG_FS
-	select MAC80211_DEBUGFS
+	depends on !ATH9K_DEBUGFS
+	depends on BACKPORT_ATH9K && DEBUG_FS
+	select BACKPORT_MAC80211_DEBUGFS
 	select RELAY
 	---help---
 	  Say Y, if you need access to ath9k's statistics for
@@ -67,17 +75,19 @@ config ATH9K_DEBUGFS
 
 	  Also required for changing debug message flags at run time.
 
-config ATH9K_STATION_STATISTICS
+config BACKPORT_ATH9K_STATION_STATISTICS
 	bool "Detailed station statistics"
-	depends on ATH9K && ATH9K_DEBUGFS && DEBUG_FS
-	select MAC80211_DEBUGFS
+	depends on !ATH9K_STATION_STATISTICS
+	depends on BACKPORT_ATH9K && BACKPORT_ATH9K_DEBUGFS && DEBUG_FS
+	select BACKPORT_MAC80211_DEBUGFS
 	default n
 	---help---
 	  This option enables detailed statistics for association stations.
 
-config ATH9K_DFS_CERTIFIED
+config BACKPORT_ATH9K_DFS_CERTIFIED
 	bool "Atheros DFS support for certified platforms"
-	depends on ATH9K && CFG80211_CERTIFICATION_ONUS
+	depends on !ATH9K_DFS_CERTIFIED
+	depends on ATH9K && BACKPORT_CFG80211_CERTIFICATION_ONUS
 	default n
 	---help---
 	  This option enables DFS support for initiating radiation on
@@ -94,18 +104,20 @@ config ATH9K_DFS_CERTIFIED
 	  developed. At this point enabling this option won't do anything
 	  except increase code size.
 
-config ATH9K_DYNACK
+config BACKPORT_ATH9K_DYNACK
 	bool "Atheros ath9k ACK timeout estimation algorithm (EXPERIMENTAL)"
-	depends on ATH9K
+	depends on !ATH9K_DYNACK
+	depends on BACKPORT_ATH9K
 	default n
 	---help---
 	  This option enables ath9k dynamic ACK timeout estimation algorithm
 	  based on ACK frame RX timestamp, TX frame timestamp and frame
 	  duration
 
-config ATH9K_TX99
+config BACKPORT_ATH9K_TX99
 	bool "Atheros ath9k TX99 testing support"
-	depends on ATH9K_DEBUGFS && CFG80211_CERTIFICATION_ONUS
+	depends on !ATH9K_TX99
+	depends on ATH9K_DEBUGFS && BACKPORT_CFG80211_CERTIFICATION_ONUS
 	default n
 	---help---
 	  Say N. This should only be enabled on systems undergoing
@@ -123,17 +135,19 @@ config ATH9K_TX99
 	  be evaluated to meet the RF exposure limits set forth in the
 	  governmental SAR regulations.
 
-config ATH9K_WOW
+config BACKPORT_ATH9K_WOW
 	bool "Wake on Wireless LAN support (EXPERIMENTAL)"
-	depends on ATH9K && PM
+	depends on !ATH9K_WOW
+	depends on BACKPORT_ATH9K && PM
 	default n
 	---help---
 	  This option enables Wake on Wireless LAN support for certain cards.
 	  Currently, AR9462 is supported.
 
-config ATH9K_RFKILL
+config BACKPORT_ATH9K_RFKILL
 	bool "Atheros ath9k rfkill support" if EXPERT
-	depends on ATH9K
+	depends on !ATH9K_RFKILL
+	depends on BACKPORT_ATH9K
 	depends on RFKILL=y || RFKILL=ATH9K
 	default y
 	help
@@ -141,28 +155,31 @@ config ATH9K_RFKILL
 	  seconds. Turn off to save power, but enable it if you have
 	  a platform that can toggle the RF-Kill GPIO.
 
-config ATH9K_CHANNEL_CONTEXT
+config BACKPORT_ATH9K_CHANNEL_CONTEXT
        bool "Channel Context support"
-       depends on ATH9K
+	depends on !ATH9K_CHANNEL_CONTEXT
+       depends on BACKPORT_ATH9K
        default n
        ---help---
          This option enables channel context support in ath9k, which is needed
 	 for multi-channel concurrency. Enable this if P2P PowerSave support
 	 is required.
 
-config ATH9K_PCOEM
+config BACKPORT_ATH9K_PCOEM
 	bool "Atheros ath9k support for PC OEM cards" if EXPERT
-	depends on ATH9K
+	depends on !ATH9K_PCOEM
+	depends on BACKPORT_ATH9K
 	default y
 
-config ATH9K_HTC
+config BACKPORT_ATH9K_HTC
        tristate "Atheros HTC based wireless cards support"
-       depends on USB && MAC80211
-       select ATH9K_HW
-       select MAC80211_LEDS
+	depends on !ATH9K_HTC
+       depends on USB && BACKPORT_MAC80211
+       select BACKPORT_ATH9K_HW
+       select BACKPORT_MAC80211_LEDS
        select LEDS_CLASS
        select NEW_LEDS
-       select ATH9K_COMMON
+       select BACKPORT_ATH9K_COMMON
        ---help---
 	 Support for Atheros HTC based cards.
 	 Chipsets supported: AR9271
@@ -171,8 +188,9 @@ config ATH9K_HTC
 
 	 The built module will be ath9k_htc.
 
-config ATH9K_HTC_DEBUGFS
+config BACKPORT_ATH9K_HTC_DEBUGFS
 	bool "Atheros ath9k_htc debugging"
-	depends on ATH9K_HTC && DEBUG_FS
+	depends on !ATH9K_HTC_DEBUGFS
+	depends on BACKPORT_ATH9K_HTC && DEBUG_FS
 	---help---
 	  Say Y, if you need access to ath9k_htc's statistics.
diff --git a/backports/drivers/net/wireless/ath/carl9170/Kconfig b/backports/drivers/net/wireless/ath/carl9170/Kconfig
index 1a796e5..da54292 100644
--- a/backports/drivers/net/wireless/ath/carl9170/Kconfig
+++ b/backports/drivers/net/wireless/ath/carl9170/Kconfig
@@ -1,7 +1,8 @@
-config CARL9170
+config BACKPORT_CARL9170
 	tristate "Linux Community AR9170 802.11n USB support"
-	depends on USB && MAC80211
-	select ATH_COMMON
+	depends on !CARL9170
+	depends on USB && BACKPORT_MAC80211
+	select BACKPORT_ATH_COMMON
 	select FW_LOADER
 	select CRC32
 	help
@@ -15,10 +16,11 @@ config CARL9170
 
 	  If you choose to build a module, it'll be called carl9170.
 
-config CARL9170_LEDS
+config BACKPORT_CARL9170_LEDS
 	bool "SoftLED Support"
-	depends on CARL9170
-	select MAC80211_LEDS
+	depends on !CARL9170_LEDS
+	depends on BACKPORT_CARL9170
+	select BACKPORT_MAC80211_LEDS
 	select LEDS_CLASS
 	select NEW_LEDS
 	default y
@@ -27,23 +29,26 @@ config CARL9170_LEDS
 
 	  Say Y, unless you need the LEDs for firmware debugging.
 
-config CARL9170_DEBUGFS
+config BACKPORT_CARL9170_DEBUGFS
 	bool "DebugFS Support"
-	depends on CARL9170 && DEBUG_FS && MAC80211_DEBUGFS
+	depends on !CARL9170_DEBUGFS
+	depends on CARL9170 && DEBUG_FS && BACKPORT_MAC80211_DEBUGFS
 	default n
 	help
 	  Export several driver and device internals to user space.
 
 	  Say N.
 
-config CARL9170_WPC
+config BACKPORT_CARL9170_WPC
 	bool
-	depends on CARL9170 && (INPUT = y || INPUT = CARL9170)
+	depends on !CARL9170_WPC
+	depends on BACKPORT_CARL9170 && (INPUT = y || INPUT = BACKPORT_CARL9170)
 	default y
 
-config CARL9170_HWRNG
+config BACKPORT_CARL9170_HWRNG
         bool "Random number generator"
-        depends on CARL9170 && (HW_RANDOM = y || HW_RANDOM = CARL9170)
+	depends on !CARL9170_HWRNG
+        depends on BACKPORT_CARL9170 && (HW_RANDOM = y || HW_RANDOM = BACKPORT_CARL9170)
         default n
 	help
 	  Provides a hardware random number generator to the kernel.
diff --git a/backports/drivers/net/wireless/ath/wcn36xx/Kconfig b/backports/drivers/net/wireless/ath/wcn36xx/Kconfig
index 591ebae..bb362bf 100644
--- a/backports/drivers/net/wireless/ath/wcn36xx/Kconfig
+++ b/backports/drivers/net/wireless/ath/wcn36xx/Kconfig
@@ -1,15 +1,17 @@
-config WCN36XX
+config BACKPORT_WCN36XX
 	tristate "Qualcomm Atheros WCN3660/3680 support"
-	depends on MAC80211 && HAS_DMA
+	depends on !WCN36XX
+	depends on BACKPORT_MAC80211 && HAS_DMA
 	---help---
 	  This module adds support for wireless adapters based on
 	  Qualcomm Atheros WCN3660 and WCN3680 mobile chipsets.
 
 	  If you choose to build a module, it'll be called wcn36xx.
 
-config WCN36XX_DEBUGFS
+config BACKPORT_WCN36XX_DEBUGFS
 	bool "WCN36XX debugfs support"
-	depends on WCN36XX
+	depends on !WCN36XX_DEBUGFS
+	depends on BACKPORT_WCN36XX
 	---help---
 	  Enabled debugfs support
 
diff --git a/backports/drivers/net/wireless/ath/wil6210/Kconfig b/backports/drivers/net/wireless/ath/wil6210/Kconfig
index ce8c038..5feeba5 100644
--- a/backports/drivers/net/wireless/ath/wil6210/Kconfig
+++ b/backports/drivers/net/wireless/ath/wil6210/Kconfig
@@ -1,6 +1,7 @@
-config WIL6210
+config BACKPORT_WIL6210
 	tristate "Wilocity 60g WiFi card wil6210 support"
-	depends on CFG80211
+	depends on !WIL6210
+	depends on BACKPORT_CFG80211
 	depends on PCI
 	default n
 	---help---
@@ -13,9 +14,10 @@ config WIL6210
 	  If you choose to build it as a module, it will be called
 	  wil6210
 
-config WIL6210_ISR_COR
+config BACKPORT_WIL6210_ISR_COR
 	bool "Use Clear-On-Read mode for ISR registers for wil6210"
-	depends on WIL6210
+	depends on !WIL6210_ISR_COR
+	depends on BACKPORT_WIL6210
 	default y
 	---help---
 	  ISR registers on wil6210 chip may operate in either
@@ -28,9 +30,10 @@ config WIL6210_ISR_COR
 	  such monitoring impossible.
 	  Say y unless you debug interrupts
 
-config WIL6210_TRACING
+config BACKPORT_WIL6210_TRACING
 	bool "wil6210 tracing support"
-	depends on WIL6210
+	depends on !WIL6210_TRACING
+	depends on BACKPORT_WIL6210
 	depends on EVENT_TRACING
 	default y
 	---help---
diff --git a/backports/drivers/net/wireless/b43/Kconfig b/backports/drivers/net/wireless/b43/Kconfig
index 759fb8d..7958fbb 100644
--- a/backports/drivers/net/wireless/b43/Kconfig
+++ b/backports/drivers/net/wireless/b43/Kconfig
@@ -1,8 +1,9 @@
-config B43
+config BACKPORT_B43
 	tristate "Broadcom 43xx wireless support (mac80211 stack)"
-	depends on (BCMA_POSSIBLE || SSB_POSSIBLE) && MAC80211 && HAS_DMA
-	select BCMA if B43_BCMA
-	select SSB if B43_SSB
+	depends on !B43
+	depends on (BCMA_POSSIBLE || SSB_POSSIBLE) && BACKPORT_MAC80211 && HAS_DMA
+	select BCMA if BACKPORT_B43_BCMA
+	select SSB if BACKPORT_B43_SSB
 	select FW_LOADER
 	---help---
 	  b43 is a driver for the Broadcom 43xx series wireless devices.
@@ -27,54 +28,62 @@ config B43
 	  This driver can be built as a module (recommended) that will be called "b43".
 	  If unsure, say M.
 
-config B43_BCMA
+config BACKPORT_B43_BCMA
 	bool
+	depends on !B43_BCMA
 
-config B43_SSB
+config BACKPORT_B43_SSB
 	bool
+	depends on !B43_SSB
 
 choice
 	prompt "Supported bus types"
-	depends on B43
-	default B43_BUSES_BCMA_AND_SSB
+	depends on BACKPORT_B43
+	default BACKPORT_B43_BUSES_BCMA_AND_SSB
 
-config B43_BUSES_BCMA_AND_SSB
+config BACKPORT_B43_BUSES_BCMA_AND_SSB
 	bool "BCMA and SSB"
-	depends on BCMA_POSSIBLE && SSB_POSSIBLE
-	select B43_BCMA
-	select B43_SSB
+	depends on !B43_BUSES_BCMA_AND_SSB
+	depends on BCMA_POSSIBLE && BACKPORT_SSB_POSSIBLE
+	select BACKPORT_B43_BCMA
+	select BACKPORT_B43_SSB
 
-config B43_BUSES_BCMA
+config BACKPORT_B43_BUSES_BCMA
 	bool "BCMA only"
-	depends on BCMA_POSSIBLE
-	select B43_BCMA
+	depends on !B43_BUSES_BCMA
+	depends on BACKPORT_BCMA_POSSIBLE
+	select BACKPORT_B43_BCMA
 
-config B43_BUSES_SSB
+config BACKPORT_B43_BUSES_SSB
 	bool "SSB only"
-	depends on SSB_POSSIBLE
-	select B43_SSB
+	depends on !B43_BUSES_SSB
+	depends on BACKPORT_SSB_POSSIBLE
+	select BACKPORT_B43_SSB
 
 endchoice
 
-# Auto-select SSB PCI-HOST support, if possible
-config B43_PCI_AUTOSELECT
+# Auto-select BACKPORT_SSB PCI-HOST support, if possible
+config BACKPORT_B43_PCI_AUTOSELECT
 	bool
-	depends on B43 && SSB_PCIHOST_POSSIBLE
-	select SSB_PCIHOST
+	depends on !B43_PCI_AUTOSELECT
+	depends on BACKPORT_B43 && SSB_PCIHOST_POSSIBLE
+	select BACKPORT_SSB_PCIHOST
 	select SSB_B43_PCI_BRIDGE
 	default y
 
-# Auto-select SSB PCICORE driver, if possible
-config B43_PCICORE_AUTOSELECT
+# Auto-select BACKPORT_SSB PCICORE driver, if possible
+config BACKPORT_B43_PCICORE_AUTOSELECT
 	bool
-	depends on B43 && SSB_DRIVER_PCICORE_POSSIBLE
-	select SSB_DRIVER_PCICORE
+	depends on !B43_PCICORE_AUTOSELECT
+	depends on BACKPORT_B43 && SSB_DRIVER_PCICORE_POSSIBLE
+	select BACKPORT_SSB_DRIVER_PCICORE
 	default y
 
-config B43_PCMCIA
+config BACKPORT_B43_PCMCIA
 	bool "Broadcom 43xx PCMCIA device support"
-	depends on B43 && B43_SSB && SSB_PCMCIAHOST_POSSIBLE
-	select SSB_PCMCIAHOST
+	depends on !B43_PCMCIA
+	depends on BACKPORT_B43 && BACKPORT_B43_SSB && SSB_PCMCIAHOST_POSSIBLE
+	select BACKPORT_SSB_PCMCIAHOST
 	---help---
 	  Broadcom 43xx PCMCIA device support.
 
@@ -87,53 +96,58 @@ config B43_PCMCIA
 	  CompactFlash formfactor in a PCMCIA adaptor.
 	  CF b43 cards can sometimes be found in handheld PCs.
 
-	  It's safe to select Y here, even if you don't have a B43 PCMCIA device.
+	  It's safe to select Y here, even if you don't have a BACKPORT_B43 PCMCIA device.
 
 	  If unsure, say N.
 
-config B43_SDIO
+config BACKPORT_B43_SDIO
 	bool "Broadcom 43xx SDIO device support"
-	depends on B43 && B43_SSB && SSB_SDIOHOST_POSSIBLE
-	select SSB_SDIOHOST
+	depends on !B43_SDIO
+	depends on BACKPORT_B43 && BACKPORT_B43_SSB && SSB_SDIOHOST_POSSIBLE
+	select BACKPORT_SSB_SDIOHOST
 	---help---
 	  Broadcom 43xx device support for Soft-MAC SDIO devices.
 
 	  With this config option you can drive Soft-MAC b43 cards with a
 	  Secure Digital I/O interface.
-	  This includes the WLAN daughter card found on the Nintendo Wii
+	  This includes the BACKPORT_WLAN daughter card found on the Nintendo Wii
 	  video game console.
 	  Note that this does not support Broadcom 43xx Full-MAC devices.
 
-	  It's safe to select Y here, even if you don't have a B43 SDIO device.
+	  It's safe to select Y here, even if you don't have a BACKPORT_B43 SDIO device.
 
 	  If unsure, say N.
 
 #Data transfers to the device via PIO. We want it as a fallback even
 # if we can do DMA.
-config B43_BCMA_PIO
+config BACKPORT_B43_BCMA_PIO
 	bool
-	depends on B43 && B43_BCMA
-	select BCMA_BLOCKIO
+	depends on !B43_BCMA_PIO
+	depends on BACKPORT_B43 && BACKPORT_B43_BCMA
+	select BACKPORT_BCMA_BLOCKIO
 	default y
 
-config B43_PIO
+config BACKPORT_B43_PIO
 	bool
-	depends on B43 && B43_SSB
-	select SSB_BLOCKIO
+	depends on !B43_PIO
+	depends on BACKPORT_B43 && BACKPORT_B43_SSB
+	select BACKPORT_SSB_BLOCKIO
 	default y
 
-config B43_PHY_G
+config BACKPORT_B43_PHY_G
 	bool "Support for G-PHY (802.11g) devices"
-	depends on B43 && B43_SSB
+	depends on !B43_PHY_G
+	depends on BACKPORT_B43 && BACKPORT_B43_SSB
 	default y
 	---help---
 	  This PHY type can be found in the following chipsets:
 	  PCI: BCM4306, BCM4311, BCM4318
 	  SoC: BCM4712, BCM5352E
 
-config B43_PHY_N
+config BACKPORT_B43_PHY_N
 	bool "Support for N-PHY (the main 802.11n series) devices"
-	depends on B43
+	depends on !B43_PHY_N
+	depends on BACKPORT_B43
 	default y
 	---help---
 	  This PHY type can be found in the following chipsets:
@@ -142,33 +156,37 @@ config B43_PHY_N
 	       BCM43131, BCM43217, BCM43227, BCM43228
 	  SoC: BCM4716, BCM4717, BCM4718, BCM5356, BCM5357, BCM5358
 
-config B43_PHY_LP
+config BACKPORT_B43_PHY_LP
 	bool "Support for LP-PHY (low-power 802.11g) devices"
-	depends on B43 && B43_SSB
+	depends on !B43_PHY_LP
+	depends on BACKPORT_B43 && BACKPORT_B43_SSB
 	default y
 	---help---
 	  The LP-PHY is a low-power PHY built into some notebooks
 	  and embedded devices. It supports 802.11a/b/g
 	  (802.11a support is optional, and currently disabled).
 
-config B43_PHY_HT
+config BACKPORT_B43_PHY_HT
 	bool "Support for HT-PHY (high throughput 802.11n) devices"
-	depends on B43 && B43_BCMA
+	depends on !B43_PHY_HT
+	depends on BACKPORT_B43 && BACKPORT_B43_BCMA
 	default y
 	---help---
 	  This PHY type with 3x3:3 MIMO can be found in the BCM4331 PCI chipset.
 
-config B43_PHY_LCN
+config BACKPORT_B43_PHY_LCN
 	bool "Support for LCN-PHY devices (BROKEN)"
-	depends on B43 && BROKEN
+	depends on !B43_PHY_LCN
+	depends on BACKPORT_B43 && BROKEN
 	---help---
 	  Support for the LCN-PHY.
 
 	  Say N, this is BROKEN and crashes driver.
 
-config B43_PHY_AC
+config BACKPORT_B43_PHY_AC
 	bool "Support for AC-PHY (802.11ac) devices (BROKEN)"
-	depends on B43 && B43_BCMA && BROKEN
+	depends on !B43_PHY_AC
+	depends on BACKPORT_B43 && BACKPORT_B43_BCMA && BROKEN
 	---help---
 	  This PHY type can be found in the following chipsets:
 	  PCI: BCM4352, BCM4360
@@ -177,21 +195,24 @@ config B43_PHY_AC
 
 # This config option automatically enables b43 LEDS support,
 # if it's possible.
-config B43_LEDS
+config BACKPORT_B43_LEDS
 	bool
-	depends on B43 && MAC80211_LEDS && (LEDS_CLASS = y || LEDS_CLASS = B43)
+	depends on !B43_LEDS
+	depends on B43 && BACKPORT_MAC80211_LEDS && (LEDS_CLASS = y || LEDS_CLASS = B43)
 	default y
 
 # This config option automatically enables b43 HW-RNG support,
 # if the HW-RNG core is enabled.
-config B43_HWRNG
+config BACKPORT_B43_HWRNG
 	bool
-	depends on B43 && (HW_RANDOM = y || HW_RANDOM = B43)
+	depends on !B43_HWRNG
+	depends on BACKPORT_B43 && (HW_RANDOM = y || HW_RANDOM = BACKPORT_B43)
 	default y
 
-config B43_DEBUG
+config BACKPORT_B43_DEBUG
 	bool "Broadcom 43xx debugging"
-	depends on B43
+	depends on !B43_DEBUG
+	depends on BACKPORT_B43
 	---help---
 	  Broadcom 43xx debugging.
 
diff --git a/backports/drivers/net/wireless/b43legacy/Kconfig b/backports/drivers/net/wireless/b43legacy/Kconfig
index 1ffa288..5778837 100644
--- a/backports/drivers/net/wireless/b43legacy/Kconfig
+++ b/backports/drivers/net/wireless/b43legacy/Kconfig
@@ -1,7 +1,8 @@
-config B43LEGACY
+config BACKPORT_B43LEGACY
 	tristate "Broadcom 43xx-legacy wireless support (mac80211 stack)"
-	depends on SSB_POSSIBLE && MAC80211 && HAS_DMA
-	select SSB
+	depends on !B43LEGACY
+	depends on SSB_POSSIBLE && BACKPORT_MAC80211 && HAS_DMA
+	select BACKPORT_SSB
 	select FW_LOADER
 	---help---
 	  b43legacy is a driver for 802.11b devices from Broadcom (BCM4301 and
@@ -19,61 +20,69 @@ config B43LEGACY
 	  This driver can be built as a module (recommended) that will be
 	  called "b43legacy". If unsure, say M.
 
-# Auto-select SSB PCI-HOST support, if possible
-config B43LEGACY_PCI_AUTOSELECT
+# Auto-select BACKPORT_SSB PCI-HOST support, if possible
+config BACKPORT_B43LEGACY_PCI_AUTOSELECT
 	bool
-	depends on B43LEGACY && SSB_PCIHOST_POSSIBLE
-	select SSB_PCIHOST
+	depends on !B43LEGACY_PCI_AUTOSELECT
+	depends on BACKPORT_B43LEGACY && SSB_PCIHOST_POSSIBLE
+	select BACKPORT_SSB_PCIHOST
 	select SSB_B43_PCI_BRIDGE
 	default y
 
-# Auto-select SSB PCICORE driver, if possible
-config B43LEGACY_PCICORE_AUTOSELECT
+# Auto-select BACKPORT_SSB PCICORE driver, if possible
+config BACKPORT_B43LEGACY_PCICORE_AUTOSELECT
 	bool
-	depends on B43LEGACY && SSB_DRIVER_PCICORE_POSSIBLE
-	select SSB_DRIVER_PCICORE
+	depends on !B43LEGACY_PCICORE_AUTOSELECT
+	depends on BACKPORT_B43LEGACY && SSB_DRIVER_PCICORE_POSSIBLE
+	select BACKPORT_SSB_DRIVER_PCICORE
 	default y
 
 # LED support
 # This config option automatically enables b43legacy LEDS support,
 # if it's possible.
-config B43LEGACY_LEDS
+config BACKPORT_B43LEGACY_LEDS
 	bool
-	depends on B43LEGACY && MAC80211_LEDS && (LEDS_CLASS = y || LEDS_CLASS = B43LEGACY)
+	depends on !B43LEGACY_LEDS
+	depends on B43LEGACY && BACKPORT_MAC80211_LEDS && (LEDS_CLASS = y || LEDS_CLASS = B43LEGACY)
 	default y
 
 # This config option automatically enables b43 HW-RNG support,
 # if the HW-RNG core is enabled.
-config B43LEGACY_HWRNG
+config BACKPORT_B43LEGACY_HWRNG
 	bool
-	depends on B43LEGACY && (HW_RANDOM = y || HW_RANDOM = B43LEGACY)
+	depends on !B43LEGACY_HWRNG
+	depends on BACKPORT_B43LEGACY && (HW_RANDOM = y || HW_RANDOM = BACKPORT_B43LEGACY)
 	default y
 
-config B43LEGACY_DEBUG
+config BACKPORT_B43LEGACY_DEBUG
 	bool "Broadcom 43xx-legacy debugging"
-	depends on B43LEGACY
+	depends on !B43LEGACY_DEBUG
+	depends on BACKPORT_B43LEGACY
 	default y
 	---help---
 	  Say Y, because this information will help you get the driver running.
 	  This option generates a minimum of log output.
 
-config B43LEGACY_DMA
+config BACKPORT_B43LEGACY_DMA
 	bool
-	depends on B43LEGACY
+	depends on !B43LEGACY_DMA
+	depends on BACKPORT_B43LEGACY
 
-config B43LEGACY_PIO
+config BACKPORT_B43LEGACY_PIO
 	bool
-	depends on B43LEGACY
+	depends on !B43LEGACY_PIO
+	depends on BACKPORT_B43LEGACY
 
 choice
 	prompt "Broadcom 43xx-legacy data transfer mode"
-	depends on B43LEGACY
-	default B43LEGACY_DMA_AND_PIO_MODE
+	depends on BACKPORT_B43LEGACY
+	default BACKPORT_B43LEGACY_DMA_AND_PIO_MODE
 
-config B43LEGACY_DMA_AND_PIO_MODE
+config BACKPORT_B43LEGACY_DMA_AND_PIO_MODE
 	bool "DMA + PIO"
-	select B43LEGACY_DMA
-	select B43LEGACY_PIO
+	depends on !B43LEGACY_DMA_AND_PIO_MODE
+	select BACKPORT_B43LEGACY_DMA
+	select BACKPORT_B43LEGACY_PIO
 	---help---
 	  Include both, Direct Memory Access (DMA) and Programmed I/O (PIO)
 	  data transfer modes. The mode actually used is selectable through
@@ -82,16 +91,18 @@ config B43LEGACY_DMA_AND_PIO_MODE
 
 	  If unsure, choose this option.
 
-config B43LEGACY_DMA_MODE
+config BACKPORT_B43LEGACY_DMA_MODE
 	bool "DMA (Direct Memory Access) only"
-	select B43LEGACY_DMA
+	depends on !B43LEGACY_DMA_MODE
+	select BACKPORT_B43LEGACY_DMA
 	---help---
 	  Only include Direct Memory Access (DMA).
 	  This reduces the size of the driver module, by omitting the PIO code.
 
-config B43LEGACY_PIO_MODE
+config BACKPORT_B43LEGACY_PIO_MODE
 	bool "PIO (Programmed I/O) only"
-	select B43LEGACY_PIO
+	depends on !B43LEGACY_PIO_MODE
+	select BACKPORT_B43LEGACY_PIO
 	---help---
 	  Only include Programmed I/O (PIO).
 	  This reduces the size of the driver module, by omitting the DMA code.
diff --git a/backports/drivers/net/wireless/brcm80211/Kconfig b/backports/drivers/net/wireless/brcm80211/Kconfig
index fe3dc12..d57b24f 100644
--- a/backports/drivers/net/wireless/brcm80211/Kconfig
+++ b/backports/drivers/net/wireless/brcm80211/Kconfig
@@ -1,76 +1,85 @@
-config BRCMUTIL
+config BACKPORT_BRCMUTIL
 	tristate
+	depends on !BRCMUTIL
 
-config BRCMSMAC
+config BACKPORT_BRCMSMAC
 	tristate "Broadcom IEEE802.11n PCIe SoftMAC WLAN driver"
-	depends on MAC80211
-	depends on BCMA_POSSIBLE
-	select BCMA
-	select NEW_LEDS if BCMA_DRIVER_GPIO
-	select LEDS_CLASS if BCMA_DRIVER_GPIO
-	select BRCMUTIL
+	depends on !BRCMSMAC
+	depends on BACKPORT_MAC80211
+	depends on BACKPORT_BCMA_POSSIBLE
+	select BACKPORT_BCMA
+	select NEW_LEDS if BACKPORT_BCMA_DRIVER_GPIO
+	select LEDS_CLASS if BACKPORT_BCMA_DRIVER_GPIO
+	select BACKPORT_BRCMUTIL
 	select FW_LOADER
 	select CORDIC
 	---help---
 	  This module adds support for PCIe wireless adapters based on Broadcom
-	  IEEE802.11n SoftMAC chipsets. It also has WLAN led support, which will
-	  be available if you select BCMA_DRIVER_GPIO. If you choose to build a
+	  IEEE802.11n SoftMAC chipsets. It also has BACKPORT_WLAN led support, which will
+	  be available if you select BACKPORT_BCMA_DRIVER_GPIO. If you choose to build a
 	  module, the driver will be called brcmsmac.ko.
 
-config BRCMFMAC
+config BACKPORT_BRCMFMAC
 	tristate "Broadcom IEEE802.11n embedded FullMAC WLAN driver"
-	depends on CFG80211
-	select BRCMUTIL
+	depends on !BRCMFMAC
+	depends on BACKPORT_CFG80211
+	select BACKPORT_BRCMUTIL
 	---help---
 	  This module adds support for embedded wireless adapters based on
 	  Broadcom IEEE802.11n FullMAC chipsets. It has to work with at least
 	  one of the bus interface support. If you choose to build a module,
 	  it'll be called brcmfmac.ko.
 
-config BRCMFMAC_PROTO_BCDC
+config BACKPORT_BRCMFMAC_PROTO_BCDC
 	bool
+	depends on !BRCMFMAC_PROTO_BCDC
 
-config BRCMFMAC_PROTO_MSGBUF
+config BACKPORT_BRCMFMAC_PROTO_MSGBUF
 	bool
+	depends on !BRCMFMAC_PROTO_MSGBUF
 
-config BRCMFMAC_SDIO
+config BACKPORT_BRCMFMAC_SDIO
 	bool "SDIO bus interface support for FullMAC driver"
-	depends on (MMC = y || MMC = BRCMFMAC)
-	depends on BRCMFMAC
-	select BRCMFMAC_PROTO_BCDC
+	depends on !BRCMFMAC_SDIO
+	depends on (MMC = y || MMC = BACKPORT_BRCMFMAC)
+	depends on BACKPORT_BRCMFMAC
+	select BACKPORT_BRCMFMAC_PROTO_BCDC
 	select FW_LOADER
 	default y
 	---help---
 	  This option enables the SDIO bus interface support for Broadcom
-	  IEEE802.11n embedded FullMAC WLAN driver. Say Y if you want to
+	  IEEE802.11n embedded FullMAC BACKPORT_WLAN driver. Say Y if you want to
 	  use the driver for a SDIO wireless card.
 
-config BRCMFMAC_USB
+config BACKPORT_BRCMFMAC_USB
 	bool "USB bus interface support for FullMAC driver"
-	depends on (USB = y || USB = BRCMFMAC)
-	depends on BRCMFMAC
-	select BRCMFMAC_PROTO_BCDC
+	depends on !BRCMFMAC_USB
+	depends on (USB = y || USB = BACKPORT_BRCMFMAC)
+	depends on BACKPORT_BRCMFMAC
+	select BACKPORT_BRCMFMAC_PROTO_BCDC
 	select FW_LOADER
 	---help---
 	  This option enables the USB bus interface support for Broadcom
-	  IEEE802.11n embedded FullMAC WLAN driver. Say Y if you want to
+	  IEEE802.11n embedded FullMAC BACKPORT_WLAN driver. Say Y if you want to
 	  use the driver for an USB wireless card.
 
-config BRCMFMAC_PCIE
+config BACKPORT_BRCMFMAC_PCIE
 	bool "PCIE bus interface support for FullMAC driver"
-	depends on BRCMFMAC
+	depends on !BRCMFMAC_PCIE
+	depends on BACKPORT_BRCMFMAC
 	depends on PCI
 	depends on HAS_DMA
-	select BRCMFMAC_PROTO_MSGBUF
+	select BACKPORT_BRCMFMAC_PROTO_MSGBUF
 	select FW_LOADER
 	---help---
 	  This option enables the PCIE bus interface support for Broadcom
-	  IEEE802.11ac embedded FullMAC WLAN driver. Say Y if you want to
+	  IEEE802.11ac embedded FullMAC BACKPORT_WLAN driver. Say Y if you want to
 	  use the driver for an PCIE wireless card.
 
-config BRCM_TRACING
+config BACKPORT_BRCM_TRACING
 	bool "Broadcom device tracing"
-	depends on BRCMSMAC || BRCMFMAC
+	depends on !BRCM_TRACING
+	depends on BACKPORT_BRCMSMAC || BRCMFMAC
 	---help---
 	  If you say Y here, the Broadcom wireless drivers will register
 	  with ftrace to dump event information into the trace ringbuffer.
@@ -79,8 +88,9 @@ config BRCM_TRACING
 	  is disabled. If unsure, say Y to allow developers to better help
 	  you when wireless problems occur.
 
-config BRCMDBG
+config BACKPORT_BRCMDBG
 	bool "Broadcom driver debug functions"
-	depends on BRCMSMAC || BRCMFMAC
+	depends on !BRCMDBG
+	depends on BACKPORT_BRCMSMAC || BRCMFMAC
 	---help---
 	  Selecting this enables additional code for debug purposes.
diff --git a/backports/drivers/net/wireless/cw1200/Kconfig b/backports/drivers/net/wireless/cw1200/Kconfig
index 0880742..1c231c5 100644
--- a/backports/drivers/net/wireless/cw1200/Kconfig
+++ b/backports/drivers/net/wireless/cw1200/Kconfig
@@ -1,29 +1,32 @@
-config CW1200
+config BACKPORT_CW1200
 	tristate "CW1200 WLAN support"
-	depends on MAC80211 && CFG80211
+	depends on !CW1200
+	depends on MAC80211 && BACKPORT_CFG80211
 	help
-	  This is a driver for the ST-E CW1100 & CW1200 WLAN chipsets.
+	  This is a driver for the ST-E CW1100 & CW1200 BACKPORT_WLAN chipsets.
 	  This option just enables the driver core, see below for
 	  specific bus support.
 
-if CW1200
+if BACKPORT_CW1200
 
-config CW1200_WLAN_SDIO
+config BACKPORT_CW1200_WLAN_SDIO
 	tristate "Support SDIO platforms"
-	depends on CW1200 && MMC
+	depends on !CW1200_WLAN_SDIO
+	depends on BACKPORT_CW1200 && MMC
 	help
-	  Enable support for the CW1200 connected via an SDIO bus.
+	  Enable support for the BACKPORT_CW1200 connected via an SDIO bus.
 	  By default this driver only supports the Sagrad SG901-1091/1098 EVK
 	  and similar designs that utilize a hardware reset circuit. To
-	  support different CW1200 SDIO designs you will need to override
+	  support different BACKPORT_CW1200 SDIO designs you will need to override
 	  the default platform data by calling cw1200_sdio_set_platform_data()
 	  in your board setup file.
 
-config CW1200_WLAN_SPI
+config BACKPORT_CW1200_WLAN_SPI
 	tristate "Support SPI platforms"
-	depends on CW1200 && SPI
+	depends on !CW1200_WLAN_SPI
+	depends on BACKPORT_CW1200 && SPI
 	help
-	  Enables support for the CW1200 connected via a SPI bus.  You will
+	  Enables support for the BACKPORT_CW1200 connected via a SPI bus.  You will
 	  need to add appropriate platform data glue in your board setup
 	  file.
 
diff --git a/backports/drivers/net/wireless/ipw2x00/Kconfig b/backports/drivers/net/wireless/ipw2x00/Kconfig
index df83220..90d851f 100644
--- a/backports/drivers/net/wireless/ipw2x00/Kconfig
+++ b/backports/drivers/net/wireless/ipw2x00/Kconfig
@@ -2,15 +2,16 @@
 # Intel Centrino wireless drivers
 #
 
-config IPW2100
+config BACKPORT_IPW2100
 	tristate "Intel PRO/Wireless 2100 Network Connection"
-	depends on PCI && CFG80211
+	depends on !IPW2100
+	depends on PCI && BACKPORT_CFG80211
 	select WIRELESS_EXT
 	select WEXT_SPY
 	select WEXT_PRIV
 	select FW_LOADER
-	select LIB80211
-	select LIBIPW
+	select BACKPORT_LIB80211
+	select BACKPORT_LIBIPW
 	---help---
           A driver for the Intel PRO/Wireless 2100 Network
 	  Connection 802.11b wireless network adapter.
@@ -37,20 +38,22 @@ config IPW2100
           this driver into your kernel image, you can avoid this problem by
           including the firmware and a firmware loader in an initramfs.
 
-config IPW2100_MONITOR
+config BACKPORT_IPW2100_MONITOR
         bool "Enable promiscuous mode"
-        depends on IPW2100
+	depends on !IPW2100_MONITOR
+        depends on BACKPORT_IPW2100
         ---help---
 	  Enables promiscuous/monitor mode support for the ipw2100 driver.
 	  With this feature compiled into the driver, you can switch to
 	  promiscuous mode via the Wireless Tool's Monitor mode.  While in this
 	  mode, no packets can be sent.
 
-config IPW2100_DEBUG
+config BACKPORT_IPW2100_DEBUG
 	bool "Enable full debugging output in IPW2100 module."
-	depends on IPW2100
+	depends on !IPW2100_DEBUG
+	depends on BACKPORT_IPW2100
 	---help---
-	  This option will enable debug tracing output for the IPW2100.
+	  This option will enable debug tracing output for the BACKPORT_IPW2100.
 
 	  This will result in the kernel module being ~60k larger.  You can
 	  control which debug output is sent to the kernel log by setting the
@@ -60,19 +63,20 @@ config IPW2100_DEBUG
 
 	  This entry will only exist if this option is enabled.
 
-	  If you are not trying to debug or develop the IPW2100 driver, you
+	  If you are not trying to debug or develop the BACKPORT_IPW2100 driver, you
 	  most likely want to say N here.
 
-config IPW2200
+config BACKPORT_IPW2200
 	tristate "Intel PRO/Wireless 2200BG and 2915ABG Network Connection"
-	depends on PCI && CFG80211
-	select CFG80211_WEXT_EXPORT
+	depends on !IPW2200
+	depends on PCI && BACKPORT_CFG80211
+	select BACKPORT_CFG80211_WEXT_EXPORT
 	select WIRELESS_EXT
 	select WEXT_SPY
 	select WEXT_PRIV
 	select FW_LOADER
-	select LIB80211
-	select LIBIPW
+	select BACKPORT_LIB80211
+	select BACKPORT_LIBIPW
 	---help---
           A driver for the Intel PRO/Wireless 2200BG and 2915ABG Network
 	  Connection adapters.
@@ -99,23 +103,26 @@ config IPW2200
           this driver into your kernel image, you can avoid this problem by
           including the firmware and a firmware loader in an initramfs.
 
-config IPW2200_MONITOR
+config BACKPORT_IPW2200_MONITOR
         bool "Enable promiscuous mode"
-        depends on IPW2200
+	depends on !IPW2200_MONITOR
+        depends on BACKPORT_IPW2200
         ---help---
 	  Enables promiscuous/monitor mode support for the ipw2200 driver.
 	  With this feature compiled into the driver, you can switch to
 	  promiscuous mode via the Wireless Tool's Monitor mode.  While in this
 	  mode, no packets can be sent.
 
-config IPW2200_RADIOTAP
+config BACKPORT_IPW2200_RADIOTAP
 	bool "Enable radiotap format 802.11 raw packet support"
-	depends on IPW2200_MONITOR
+	depends on !IPW2200_RADIOTAP
+	depends on BACKPORT_IPW2200_MONITOR
 
-config IPW2200_PROMISCUOUS
+config BACKPORT_IPW2200_PROMISCUOUS
 	bool "Enable creation of a RF radiotap promiscuous interface"
-	depends on IPW2200_MONITOR
-	select IPW2200_RADIOTAP
+	depends on !IPW2200_PROMISCUOUS
+	depends on BACKPORT_IPW2200_MONITOR
+	select BACKPORT_IPW2200_RADIOTAP
 	---help---
           Enables the creation of a second interface prefixed 'rtap'.
           This second interface will provide every received in radiotap
@@ -136,15 +143,17 @@ config IPW2200_PROMISCUOUS
 
             % echo 1 > /sys/bus/pci/drivers/ipw2200/*/rtap_iface
 
-config IPW2200_QOS
+config BACKPORT_IPW2200_QOS
         bool "Enable QoS support"
-        depends on IPW2200
+	depends on !IPW2200_QOS
+        depends on BACKPORT_IPW2200
 
-config IPW2200_DEBUG
+config BACKPORT_IPW2200_DEBUG
 	bool "Enable full debugging output in IPW2200 module."
-	depends on IPW2200
+	depends on !IPW2200_DEBUG
+	depends on BACKPORT_IPW2200
 	---help---
-	  This option will enable low level debug tracing output for IPW2200.
+	  This option will enable low level debug tracing output for BACKPORT_IPW2200.
 
 	  Note, normal debug code is already compiled in. This low level
 	  debug option enables debug on hot paths (e.g Tx, Rx, ISR) and
@@ -153,9 +162,10 @@ config IPW2200_DEBUG
 
 	  If you are not sure, say N here.
 
-config LIBIPW
+config BACKPORT_LIBIPW
 	tristate
-	depends on PCI && CFG80211
+	depends on !LIBIPW
+	depends on PCI && BACKPORT_CFG80211
 	select WIRELESS_EXT
 	select WEXT_SPY
 	select CRYPTO
@@ -165,18 +175,19 @@ config LIBIPW
 	select CRYPTO_MICHAEL_MIC
 	select CRYPTO_ECB
 	select CRC32
-	select LIB80211
-	select LIB80211_CRYPT_WEP
-	select LIB80211_CRYPT_TKIP
-	select LIB80211_CRYPT_CCMP
+	select BACKPORT_LIB80211
+	select BACKPORT_LIB80211_CRYPT_WEP
+	select BACKPORT_LIB80211_CRYPT_TKIP
+	select BACKPORT_LIB80211_CRYPT_CCMP
 	---help---
 	This option enables the hardware independent IEEE 802.11
 	networking stack.  This component is deprecated in favor of the
 	mac80211 component.
 
-config LIBIPW_DEBUG
+config BACKPORT_LIBIPW_DEBUG
 	bool "Full debugging output for the LIBIPW component"
-	depends on LIBIPW
+	depends on !LIBIPW_DEBUG
+	depends on BACKPORT_LIBIPW
 	---help---
 	  This option will enable debug tracing output for the
 	  libipw component.
diff --git a/backports/drivers/net/wireless/iwlegacy/Kconfig b/backports/drivers/net/wireless/iwlegacy/Kconfig
index fb91972..0ac2a58 100644
--- a/backports/drivers/net/wireless/iwlegacy/Kconfig
+++ b/backports/drivers/net/wireless/iwlegacy/Kconfig
@@ -1,15 +1,17 @@
-config IWLEGACY
+config BACKPORT_IWLEGACY
 	tristate
+	depends on !IWLEGACY
 	select FW_LOADER
 	select NEW_LEDS
 	select LEDS_CLASS
 	select LEDS_TRIGGERS
-	select MAC80211_LEDS
+	select BACKPORT_MAC80211_LEDS
 
-config IWL4965
+config BACKPORT_IWL4965
 	tristate "Intel Wireless WiFi 4965AGN (iwl4965)"
-	depends on PCI && MAC80211
-	select IWLEGACY
+	depends on !IWL4965
+	depends on PCI && BACKPORT_MAC80211
+	select BACKPORT_IWLEGACY
 	---help---
 	  This option enables support for
 
@@ -34,10 +36,11 @@ config IWL4965
 	  say M here and read <file:Documentation/kbuild/modules.txt>.  The
 	  module will be called iwl4965.
 
-config IWL3945
+config BACKPORT_IWL3945
 	tristate "Intel PRO/Wireless 3945ABG/BG Network Connection (iwl3945)"
-	depends on PCI && MAC80211
-	select IWLEGACY
+	depends on !IWL3945
+	depends on PCI && BACKPORT_MAC80211
+	select BACKPORT_IWLEGACY
 	---help---
 	  Select to build the driver supporting the:
 
@@ -61,11 +64,12 @@ config IWL3945
 	  module will be called iwl3945.
 
 menu "iwl3945 / iwl4965 Debugging Options"
-	depends on IWLEGACY
+	depends on BACKPORT_IWLEGACY
 
-config IWLEGACY_DEBUG
+config BACKPORT_IWLEGACY_DEBUG
 	bool "Enable full debugging output in iwlegacy (iwl 3945/4965) drivers"
-	depends on IWLEGACY
+	depends on !IWLEGACY_DEBUG
+	depends on BACKPORT_IWLEGACY
 	---help---
 	  This option will enable debug tracing output for the iwlegacy
 	  drivers.
@@ -89,9 +93,10 @@ config IWLEGACY_DEBUG
 	  as the debug information can assist others in helping you resolve
 	  any problems you may encounter.
 
-config IWLEGACY_DEBUGFS
+config BACKPORT_IWLEGACY_DEBUGFS
         bool "iwlegacy (iwl 3945/4965) debugfs support"
-        depends on IWLEGACY && MAC80211_DEBUGFS
+	depends on !IWLEGACY_DEBUGFS
+        depends on IWLEGACY && BACKPORT_MAC80211_DEBUGFS
         ---help---
 	  Enable creation of debugfs files for the iwlegacy drivers. This
 	  is a low-impact option that allows getting insight into the
diff --git a/backports/drivers/net/wireless/iwlwifi/Kconfig b/backports/drivers/net/wireless/iwlwifi/Kconfig
index f89f446..bb3fde8 100644
--- a/backports/drivers/net/wireless/iwlwifi/Kconfig
+++ b/backports/drivers/net/wireless/iwlwifi/Kconfig
@@ -1,6 +1,7 @@
-config IWLWIFI
+config BACKPORT_IWLWIFI
 	tristate "Intel Wireless WiFi Next Gen AGN - Wireless-N/Advanced-N/Ultimate-N (iwlwifi) "
-	depends on PCI && MAC80211 && HAS_IOMEM
+	depends on !IWLWIFI
+	depends on PCI && BACKPORT_MAC80211 && HAS_IOMEM
 	select FW_LOADER
 	---help---
 	  Select to build the driver supporting the:
@@ -41,42 +42,47 @@ config IWLWIFI
 	  say M here and read <file:Documentation/kbuild/modules.txt>.  The
 	  module will be called iwlwifi.
 
-if IWLWIFI
+if BACKPORT_IWLWIFI
 
-config IWLWIFI_LEDS
+config BACKPORT_IWLWIFI_LEDS
 	bool
+	depends on !IWLWIFI_LEDS
 	depends on LEDS_CLASS=y || LEDS_CLASS=IWLWIFI
 	select LEDS_TRIGGERS
-	select MAC80211_LEDS
+	select BACKPORT_MAC80211_LEDS
 	default y
 
-config IWLDVM
+config BACKPORT_IWLDVM
 	tristate "Intel Wireless WiFi DVM Firmware support"
-	default IWLWIFI
+	depends on !IWLDVM
+	default BACKPORT_IWLWIFI
 	help
 	  This is the driver that supports the DVM firmware which is
 	  used by most existing devices (with the exception of 7260
 	  and 3160).
 
-config IWLMVM
+config BACKPORT_IWLMVM
 	tristate "Intel Wireless WiFi MVM Firmware support"
+	depends on !IWLMVM
 	select WANT_DEV_COREDUMP
 	help
 	  This is the driver that supports the MVM firmware which is
 	  currently only available for 7260 and 3160 devices.
 
 # don't call it _MODULE -- will confuse Kconfig/fixdep/...
-config IWLWIFI_OPMODE_MODULAR
+config BACKPORT_IWLWIFI_OPMODE_MODULAR
 	bool
-	default y if IWLDVM=m
-	default y if IWLMVM=m
+	depends on !IWLWIFI_OPMODE_MODULAR
+	default y if BACKPORT_IWLDVM=m
+	default y if BACKPORT_IWLMVM=m
 
-comment "WARNING: iwlwifi is useless without IWLDVM or IWLMVM"
-	depends on IWLDVM=n && IWLMVM=n
+comment "WARNING: iwlwifi is useless without BACKPORT_IWLDVM or IWLMVM"
+	depends on BACKPORT_IWLDVM=n && IWLMVM=n
 
-config IWLWIFI_BCAST_FILTERING
+config BACKPORT_IWLWIFI_BCAST_FILTERING
 	bool "Enable broadcast filtering"
-	depends on IWLMVM
+	depends on !IWLWIFI_BCAST_FILTERING
+	depends on BACKPORT_IWLMVM
 	help
 	  Say Y here to enable default bcast filtering configuration.
 
@@ -87,9 +93,10 @@ config IWLWIFI_BCAST_FILTERING
 	  If unsure, don't enable this option, as some programs might
 	  expect incoming broadcasts for their normal operations.
 
-config IWLWIFI_UAPSD
+config BACKPORT_IWLWIFI_UAPSD
 	bool "enable U-APSD by default"
-	depends on IWLMVM
+	depends on !IWLWIFI_UAPSD
+	depends on BACKPORT_IWLMVM
 	help
 	  Say Y here to enable U-APSD by default. This may cause
 	  interoperability problems with some APs, manifesting in lower than
@@ -99,8 +106,9 @@ config IWLWIFI_UAPSD
 
 menu "Debugging Options"
 
-config IWLWIFI_DEBUG
+config BACKPORT_IWLWIFI_DEBUG
 	bool "Enable full debugging output in the iwlwifi driver"
+	depends on !IWLWIFI_DEBUG
 	---help---
 	  This option will enable debug tracing output for the iwlwifi drivers
 
@@ -123,22 +131,25 @@ config IWLWIFI_DEBUG
 	  as the debug information can assist others in helping you resolve
 	  any problems you may encounter.
 
-config IWLWIFI_DEBUGFS
+config BACKPORT_IWLWIFI_DEBUGFS
         bool "iwlwifi debugfs support"
-        depends on MAC80211_DEBUGFS
+	depends on !IWLWIFI_DEBUGFS
+        depends on BACKPORT_MAC80211_DEBUGFS
         ---help---
 	  Enable creation of debugfs files for the iwlwifi drivers. This
 	  is a low-impact option that allows getting insight into the
 	  driver's state at runtime.
 
-config IWLWIFI_DEBUG_EXPERIMENTAL_UCODE
+config BACKPORT_IWLWIFI_DEBUG_EXPERIMENTAL_UCODE
         bool "Experimental uCode support"
-        depends on IWLWIFI_DEBUG
+	depends on !IWLWIFI_DEBUG_EXPERIMENTAL_UCODE
+        depends on BACKPORT_IWLWIFI_DEBUG
         ---help---
 	  Enable use of experimental ucode for testing and debugging.
 
-config IWLWIFI_DEVICE_TRACING
+config BACKPORT_IWLWIFI_DEVICE_TRACING
 	bool "iwlwifi device access tracing"
+	depends on !IWLWIFI_DEVICE_TRACING
 	depends on EVENT_TRACING
 	help
 	  Say Y here to trace all commands, including TX frames and IO
diff --git a/backports/drivers/net/wireless/libertas/Kconfig b/backports/drivers/net/wireless/libertas/Kconfig
index e6268ce..ad639aa 100644
--- a/backports/drivers/net/wireless/libertas/Kconfig
+++ b/backports/drivers/net/wireless/libertas/Kconfig
@@ -1,45 +1,52 @@
-config LIBERTAS
+config BACKPORT_LIBERTAS
 	tristate "Marvell 8xxx Libertas WLAN driver support"
-	depends on CFG80211
+	depends on !LIBERTAS
+	depends on BACKPORT_CFG80211
 	select WIRELESS_EXT
 	select WEXT_SPY
-	select LIB80211
+	select BACKPORT_LIB80211
 	select FW_LOADER
 	---help---
 	  A library for Marvell Libertas 8xxx devices.
 
-config LIBERTAS_USB
+config BACKPORT_LIBERTAS_USB
 	tristate "Marvell Libertas 8388 USB 802.11b/g cards"
-	depends on LIBERTAS && USB
+	depends on !LIBERTAS_USB
+	depends on BACKPORT_LIBERTAS && USB
 	---help---
 	  A driver for Marvell Libertas 8388 USB devices.
 
-config LIBERTAS_CS
+config BACKPORT_LIBERTAS_CS
 	tristate "Marvell Libertas 8385 CompactFlash 802.11b/g cards"
-	depends on LIBERTAS && PCMCIA && HAS_IOPORT_MAP
+	depends on !LIBERTAS_CS
+	depends on BACKPORT_LIBERTAS && PCMCIA && HAS_IOPORT_MAP
 	---help---
 	  A driver for Marvell Libertas 8385 CompactFlash devices.
 
-config LIBERTAS_SDIO
+config BACKPORT_LIBERTAS_SDIO
 	tristate "Marvell Libertas 8385/8686/8688 SDIO 802.11b/g cards"
-	depends on LIBERTAS && MMC
+	depends on !LIBERTAS_SDIO
+	depends on BACKPORT_LIBERTAS && MMC
 	---help---
 	  A driver for Marvell Libertas 8385/8686/8688 SDIO devices.
 
-config LIBERTAS_SPI
+config BACKPORT_LIBERTAS_SPI
 	tristate "Marvell Libertas 8686 SPI 802.11b/g cards"
-	depends on LIBERTAS && SPI
+	depends on !LIBERTAS_SPI
+	depends on BACKPORT_LIBERTAS && SPI
 	---help---
 	  A driver for Marvell Libertas 8686 SPI devices.
 
-config LIBERTAS_DEBUG
+config BACKPORT_LIBERTAS_DEBUG
 	bool "Enable full debugging output in the Libertas module."
-	depends on LIBERTAS
+	depends on !LIBERTAS_DEBUG
+	depends on BACKPORT_LIBERTAS
 	---help---
 	  Debugging support.
 
-config LIBERTAS_MESH
+config BACKPORT_LIBERTAS_MESH
 	bool "Enable mesh support"
-	depends on LIBERTAS
+	depends on !LIBERTAS_MESH
+	depends on BACKPORT_LIBERTAS
 	help
 	  This enables Libertas' MESH support, used by e.g. the OLPC people.
diff --git a/backports/drivers/net/wireless/mwifiex/Kconfig b/backports/drivers/net/wireless/mwifiex/Kconfig
index aa01c9b..489cdf1 100644
--- a/backports/drivers/net/wireless/mwifiex/Kconfig
+++ b/backports/drivers/net/wireless/mwifiex/Kconfig
@@ -1,6 +1,7 @@
-config MWIFIEX
+config BACKPORT_MWIFIEX
 	tristate "Marvell WiFi-Ex Driver"
-	depends on CFG80211
+	depends on !MWIFIEX
+	depends on BACKPORT_CFG80211
 	---help---
 	  This adds support for wireless adapters based on Marvell
 	  802.11n/ac chipsets.
@@ -8,9 +9,10 @@ config MWIFIEX
 	  If you choose to build it as a module, it will be called
 	  mwifiex.
 
-config MWIFIEX_SDIO
+config BACKPORT_MWIFIEX_SDIO
 	tristate "Marvell WiFi-Ex Driver for SD8786/SD8787/SD8797/SD8887/SD8897"
-	depends on MWIFIEX && MMC
+	depends on !MWIFIEX_SDIO
+	depends on BACKPORT_MWIFIEX && MMC
 	select FW_LOADER
 	---help---
 	  This adds support for wireless adapters based on Marvell
@@ -19,9 +21,10 @@ config MWIFIEX_SDIO
 	  If you choose to build it as a module, it will be called
 	  mwifiex_sdio.
 
-config MWIFIEX_PCIE
+config BACKPORT_MWIFIEX_PCIE
 	tristate "Marvell WiFi-Ex Driver for PCIE 8766/8897"
-	depends on MWIFIEX && PCI
+	depends on !MWIFIEX_PCIE
+	depends on BACKPORT_MWIFIEX && PCI
 	select FW_LOADER
 	---help---
 	  This adds support for wireless adapters based on Marvell
@@ -30,9 +33,10 @@ config MWIFIEX_PCIE
 	  If you choose to build it as a module, it will be called
 	  mwifiex_pcie.
 
-config MWIFIEX_USB
+config BACKPORT_MWIFIEX_USB
 	tristate "Marvell WiFi-Ex Driver for USB8766/8797/8897"
-	depends on MWIFIEX && USB
+	depends on !MWIFIEX_USB
+	depends on BACKPORT_MWIFIEX && USB
 	select FW_LOADER
 	---help---
 	  This adds support for wireless adapters based on Marvell
diff --git a/backports/drivers/net/wireless/orinoco/Kconfig b/backports/drivers/net/wireless/orinoco/Kconfig
index f6fa3f4..21ec6a8 100644
--- a/backports/drivers/net/wireless/orinoco/Kconfig
+++ b/backports/drivers/net/wireless/orinoco/Kconfig
@@ -1,8 +1,9 @@
-config HERMES
+config BACKPORT_HERMES
 	tristate "Hermes chipset 802.11b support (Orinoco/Prism2/Symbol)"
+	depends on !HERMES
 	depends on (PPC_PMAC || PCI || PCMCIA)
-	depends on CFG80211
-	select CFG80211_WEXT_EXPORT
+	depends on BACKPORT_CFG80211
+	select BACKPORT_CFG80211_WEXT_EXPORT
 	select WIRELESS_EXT
 	select WEXT_SPY
 	select WEXT_PRIV
@@ -28,9 +29,10 @@ config HERMES
 	  configure your card and that /etc/pcmcia/wireless.opts works :
 	  <http://www.hpl.hp.com/personal/Jean_Tourrilhes/Linux/Tools.html>
 
-config HERMES_PRISM
+config BACKPORT_HERMES_PRISM
 	bool "Support Prism 2/2.5 chipset"
-	depends on HERMES
+	depends on !HERMES_PRISM
+	depends on BACKPORT_HERMES
 	---help---
 
 	  Say Y to enable support for Prism 2 and 2.5 chipsets.  These
@@ -39,9 +41,10 @@ config HERMES_PRISM
 
 	  If you are not sure, say N.
 
-config HERMES_CACHE_FW_ON_INIT
+config BACKPORT_HERMES_CACHE_FW_ON_INIT
 	bool "Cache Hermes firmware on driver initialisation"
-	depends on HERMES
+	depends on !HERMES_CACHE_FW_ON_INIT
+	depends on BACKPORT_HERMES
 	default y
 	---help---
 	  Say Y to cache any firmware required by the Hermes drivers
@@ -54,9 +57,10 @@ config HERMES_CACHE_FW_ON_INIT
 
 	  If you are not sure, say Y.
 
-config APPLE_AIRPORT
+config BACKPORT_APPLE_AIRPORT
 	tristate "Apple Airport support (built-in)"
-	depends on PPC_PMAC && HERMES
+	depends on !APPLE_AIRPORT
+	depends on PPC_PMAC && BACKPORT_HERMES
 	help
 	  Say Y here to support the Airport 802.11b wireless Ethernet hardware
 	  built into the Macintosh iBook and other recent PowerPC-based
@@ -66,9 +70,10 @@ config APPLE_AIRPORT
 	  This driver does not support the Airport Extreme (802.11b/g). Use
 	  the BCM43xx driver for Airport Extreme cards.
 
-config PLX_HERMES
+config BACKPORT_PLX_HERMES
 	tristate "Hermes in PLX9052 based PCI adaptor support (Netgear MA301 etc.)"
-	depends on PCI && HERMES
+	depends on !PLX_HERMES
+	depends on PCI && BACKPORT_HERMES
 	help
 	  Enable support for PCMCIA cards supported by the "Hermes" (aka
 	  orinoco) driver when used in PLX9052 based PCI adaptors.  These
@@ -77,9 +82,10 @@ config PLX_HERMES
 	  802.11b PCMCIA cards can be used in desktop machines.  The Netgear
 	  MA301 is such an adaptor.
 
-config TMD_HERMES
+config BACKPORT_TMD_HERMES
 	tristate "Hermes in TMD7160 based PCI adaptor support"
-	depends on PCI && HERMES
+	depends on !TMD_HERMES
+	depends on PCI && BACKPORT_HERMES
 	help
 	  Enable support for PCMCIA cards supported by the "Hermes" (aka
 	  orinoco) driver when used in TMD7160 based PCI adaptors.  These
@@ -87,18 +93,20 @@ config TMD_HERMES
 	  PCI <-> PCMCIA bridge.  Several vendors sell such adaptors so that
 	  802.11b PCMCIA cards can be used in desktop machines.
 
-config NORTEL_HERMES
+config BACKPORT_NORTEL_HERMES
 	tristate "Nortel emobility PCI adaptor support"
-	depends on PCI && HERMES
+	depends on !NORTEL_HERMES
+	depends on PCI && BACKPORT_HERMES
 	help
 	  Enable support for PCMCIA cards supported by the "Hermes" (aka
 	  orinoco) driver when used in Nortel emobility PCI adaptors.  These
 	  adaptors are not full PCMCIA controllers, but act as a more limited
 	  PCI <-> PCMCIA bridge.
 
-config PCI_HERMES
+config BACKPORT_PCI_HERMES
 	tristate "Prism 2.5 PCI 802.11b adaptor support"
-	depends on PCI && HERMES && HERMES_PRISM
+	depends on !PCI_HERMES
+	depends on PCI && BACKPORT_HERMES && BACKPORT_HERMES_PRISM
 	help
 	  Enable support for PCI and mini-PCI 802.11b wireless NICs based on
 	  the Prism 2.5 chipset.  These are true PCI cards, not the 802.11b
@@ -106,24 +114,26 @@ config PCI_HERMES
 	  common.  Some of the built-in wireless adaptors in laptops are of
 	  this variety.
 
-config PCMCIA_HERMES
+config BACKPORT_PCMCIA_HERMES
 	tristate "Hermes PCMCIA card support"
-	depends on PCMCIA && HERMES && HAS_IOPORT_MAP
+	depends on !PCMCIA_HERMES
+	depends on PCMCIA && BACKPORT_HERMES && HAS_IOPORT_MAP
 	---help---
 	  A driver for "Hermes" chipset based PCMCIA wireless adaptors, such
 	  as the Lucent WavelanIEEE/Orinoco cards and their OEM (Cabletron/
 	  EnteraSys RoamAbout 802.11, ELSA Airlancer, Melco Buffalo and
 	  others).  It should also be usable on various Prism II based cards
 	  such as the Linksys, D-Link and Farallon Skyline.  It should also
-	  work on Symbol cards such as the 3Com AirConnect and Ericsson WLAN.
+	  work on Symbol cards such as the 3Com AirConnect and Ericsson BACKPORT_WLAN.
 
 	  You will very likely need the Wireless Tools in order to
 	  configure your card and that /etc/pcmcia/wireless.opts works:
 	  <http://www.hpl.hp.com/personal/Jean_Tourrilhes/Linux/Tools.html>.
 
-config PCMCIA_SPECTRUM
+config BACKPORT_PCMCIA_SPECTRUM
 	tristate "Symbol Spectrum24 Trilogy PCMCIA card support"
-	depends on PCMCIA && HERMES && HAS_IOPORT_MAP
+	depends on !PCMCIA_SPECTRUM
+	depends on PCMCIA && BACKPORT_HERMES && HAS_IOPORT_MAP
 	---help---
 
 	  This is a driver for 802.11b cards using RAM-loadable Symbol
@@ -134,9 +144,10 @@ config PCMCIA_SPECTRUM
 	  for downloading Symbol firmware are available at
 	  <http://sourceforge.net/projects/orinoco/>
 
-config ORINOCO_USB
+config BACKPORT_ORINOCO_USB
 	tristate "Agere Orinoco USB support"
-	depends on USB && HERMES
+	depends on !ORINOCO_USB
+	depends on USB && BACKPORT_HERMES
 	select FW_LOADER
 	---help---
 	  This driver is for USB versions of the Agere Orinoco card.
diff --git a/backports/drivers/net/wireless/p54/Kconfig b/backports/drivers/net/wireless/p54/Kconfig
index cdafb8c..2f19da0 100644
--- a/backports/drivers/net/wireless/p54/Kconfig
+++ b/backports/drivers/net/wireless/p54/Kconfig
@@ -1,6 +1,7 @@
-config P54_COMMON
+config BACKPORT_P54_COMMON
 	tristate "Softmac Prism54 support"
-	depends on MAC80211
+	depends on !P54_COMMON
+	depends on BACKPORT_MAC80211
 	select FW_LOADER
 	select CRC_CCITT
 	---help---
@@ -13,9 +14,10 @@ config P54_COMMON
 
 	  If you choose to build a module, it'll be called p54common.
 
-config P54_USB
+config BACKPORT_P54_USB
 	tristate "Prism54 USB support"
-	depends on P54_COMMON && USB
+	depends on !P54_USB
+	depends on BACKPORT_P54_COMMON && USB
 	select CRC32
 	---help---
 	  This driver is for USB isl38xx based wireless cards.
@@ -25,9 +27,10 @@ config P54_USB
 
 	  If you choose to build a module, it'll be called p54usb.
 
-config P54_PCI
+config BACKPORT_P54_PCI
 	tristate "Prism54 PCI support"
-	depends on P54_COMMON && PCI
+	depends on !P54_PCI
+	depends on BACKPORT_P54_COMMON && PCI
 	---help---
 	  This driver is for PCI isl38xx based wireless cards.
 	  This driver supports most devices that are supported by the
@@ -39,18 +42,20 @@ config P54_PCI
 
 	  If you choose to build a module, it'll be called p54pci.
 
-config P54_SPI
+config BACKPORT_P54_SPI
 	tristate "Prism54 SPI (stlc45xx) support"
-	depends on P54_COMMON && SPI_MASTER
+	depends on !P54_SPI
+	depends on BACKPORT_P54_COMMON && SPI_MASTER
 	---help---
 	  This driver is for stlc4550 or stlc4560 based wireless chips
 	  such as Nokia's N800/N810 Portable Internet Tablet.
 
 	  If you choose to build a module, it'll be called p54spi.
 
-config P54_SPI_DEFAULT_EEPROM
+config BACKPORT_P54_SPI_DEFAULT_EEPROM
 	bool "Include fallback EEPROM blob"
-	depends on P54_SPI
+	depends on !P54_SPI_DEFAULT_EEPROM
+	depends on BACKPORT_P54_SPI
 	default n
 	---help---
 	 Unlike the PCI or USB devices, the SPI variants don't have
@@ -65,7 +70,8 @@ config P54_SPI_DEFAULT_EEPROM
 
 	 Enabling this option adds about 4k to p54spi.
 
-config P54_LEDS
+config BACKPORT_P54_LEDS
 	bool
-	depends on P54_COMMON && MAC80211_LEDS && (LEDS_CLASS = y || LEDS_CLASS = P54_COMMON)
+	depends on !P54_LEDS
+	depends on P54_COMMON && BACKPORT_MAC80211_LEDS && (LEDS_CLASS = y || LEDS_CLASS = P54_COMMON)
 	default y
diff --git a/backports/drivers/net/wireless/rsi/Kconfig b/backports/drivers/net/wireless/rsi/Kconfig
index 35245f9..43a5b1e 100644
--- a/backports/drivers/net/wireless/rsi/Kconfig
+++ b/backports/drivers/net/wireless/rsi/Kconfig
@@ -1,29 +1,33 @@
-config RSI_91X
+config BACKPORT_RSI_91X
 	tristate "Redpine Signals Inc 91x WLAN driver support"
-	depends on MAC80211
+	depends on !RSI_91X
+	depends on BACKPORT_MAC80211
 	---help---
 	  This option enabes support for RSI 1x1 devices.
 	  Select M (recommended), if you have a RSI 1x1 wireless module.
 
-config RSI_DEBUGFS
+config BACKPORT_RSI_DEBUGFS
 	bool "Redpine Signals Inc debug support"
-	depends on RSI_91X
+	depends on !RSI_DEBUGFS
+	depends on BACKPORT_RSI_91X
 	default y
 	---help---
 	 Say Y, if you would like to enable debug support. This option
 	 creates debugfs entries
 
-config RSI_SDIO
+config BACKPORT_RSI_SDIO
 	tristate "Redpine Signals SDIO bus support"
-	depends on MMC && RSI_91X
+	depends on !RSI_SDIO
+	depends on MMC && BACKPORT_RSI_91X
 	default m
 	---help---
 	  This option enables the SDIO bus support in rsi drivers.
 	  Select M (recommended), if you have a RSI 1x1 wireless module.
 
-config RSI_USB
+config BACKPORT_RSI_USB
 	tristate "Redpine Signals USB bus support"
-	depends on USB && RSI_91X
+	depends on !RSI_USB
+	depends on USB && BACKPORT_RSI_91X
 	default m
 	---help---
 	  This option enables the USB bus support in rsi drivers.
diff --git a/backports/drivers/net/wireless/rt2x00/Kconfig b/backports/drivers/net/wireless/rt2x00/Kconfig
index 2b4ef25..efdce83 100644
--- a/backports/drivers/net/wireless/rt2x00/Kconfig
+++ b/backports/drivers/net/wireless/rt2x00/Kconfig
@@ -1,6 +1,7 @@
-menuconfig RT2X00
+menuconfig BACKPORT_RT2X00
 	tristate "Ralink driver support"
-	depends on MAC80211 && HAS_DMA
+	depends on !RT2X00
+	depends on BACKPORT_MAC80211 && HAS_DMA
 	---help---
 	  This will enable the support for the Ralink drivers,
 	  developed in the rt2x00 project <http://rt2x00.serialmonkey.com>.
@@ -15,13 +16,14 @@ menuconfig RT2X00
 	  on the types of drivers being selected, these libraries will be
 	  called rt2x00pci and rt2x00usb.
 
-if RT2X00
+if BACKPORT_RT2X00
 
-config RT2400PCI
+config BACKPORT_RT2400PCI
 	tristate "Ralink rt2400 (PCI/PCMCIA) support"
+	depends on !RT2400PCI
 	depends on PCI
-	select RT2X00_LIB_MMIO
-	select RT2X00_LIB_PCI
+	select BACKPORT_RT2X00_LIB_MMIO
+	select BACKPORT_RT2X00_LIB_PCI
 	select EEPROM_93CX6
 	---help---
 	  This adds support for rt2400 wireless chipset family.
@@ -29,11 +31,12 @@ config RT2400PCI
 
 	  When compiled as a module, this driver will be called rt2400pci.
 
-config RT2500PCI
+config BACKPORT_RT2500PCI
 	tristate "Ralink rt2500 (PCI/PCMCIA) support"
+	depends on !RT2500PCI
 	depends on PCI
-	select RT2X00_LIB_MMIO
-	select RT2X00_LIB_PCI
+	select BACKPORT_RT2X00_LIB_MMIO
+	select BACKPORT_RT2X00_LIB_PCI
 	select EEPROM_93CX6
 	---help---
 	  This adds support for rt2500 wireless chipset family.
@@ -41,13 +44,14 @@ config RT2500PCI
 
 	  When compiled as a module, this driver will be called rt2500pci.
 
-config RT61PCI
+config BACKPORT_RT61PCI
 	tristate "Ralink rt2501/rt61 (PCI/PCMCIA) support"
+	depends on !RT61PCI
 	depends on PCI
-	select RT2X00_LIB_PCI
-	select RT2X00_LIB_MMIO
-	select RT2X00_LIB_FIRMWARE
-	select RT2X00_LIB_CRYPTO
+	select BACKPORT_RT2X00_LIB_PCI
+	select BACKPORT_RT2X00_LIB_MMIO
+	select BACKPORT_RT2X00_LIB_FIRMWARE
+	select BACKPORT_RT2X00_LIB_CRYPTO
 	select CRC_ITU_T
 	select EEPROM_93CX6
 	---help---
@@ -56,15 +60,16 @@ config RT61PCI
 
 	  When compiled as a module, this driver will be called rt61pci.
 
-config RT2800PCI
+config BACKPORT_RT2800PCI
 	tristate "Ralink rt27xx/rt28xx/rt30xx (PCI/PCIe/PCMCIA) support"
+	depends on !RT2800PCI
 	depends on PCI
-	select RT2800_LIB
-	select RT2800_LIB_MMIO
-	select RT2X00_LIB_MMIO
-	select RT2X00_LIB_PCI
-	select RT2X00_LIB_FIRMWARE
-	select RT2X00_LIB_CRYPTO
+	select BACKPORT_RT2800_LIB
+	select BACKPORT_RT2800_LIB_MMIO
+	select BACKPORT_RT2X00_LIB_MMIO
+	select BACKPORT_RT2X00_LIB_PCI
+	select BACKPORT_RT2X00_LIB_FIRMWARE
+	select BACKPORT_RT2X00_LIB_CRYPTO
 	select CRC_CCITT
 	select EEPROM_93CX6
 	---help---
@@ -74,18 +79,20 @@ config RT2800PCI
 
 	  When compiled as a module, this driver will be called "rt2800pci.ko".
 
-if RT2800PCI
+if BACKPORT_RT2800PCI
 
-config RT2800PCI_RT33XX
+config BACKPORT_RT2800PCI_RT33XX
 	bool "rt2800pci - Include support for rt33xx devices"
+	depends on !RT2800PCI_RT33XX
 	default y
 	---help---
 	  This adds support for rt33xx wireless chipset family to the
 	  rt2800pci driver.
 	  Supported chips: RT3390
 
-config RT2800PCI_RT35XX
+config BACKPORT_RT2800PCI_RT35XX
 	bool "rt2800pci - Include support for rt35xx devices (EXPERIMENTAL)"
+	depends on !RT2800PCI_RT35XX
 	default y
 	---help---
 	  This adds support for rt35xx wireless chipset family to the
@@ -93,16 +100,18 @@ config RT2800PCI_RT35XX
 	  Supported chips: RT3060, RT3062, RT3562, RT3592
 
 
-config RT2800PCI_RT53XX
+config BACKPORT_RT2800PCI_RT53XX
        bool "rt2800pci - Include support for rt53xx devices (EXPERIMENTAL)"
+	depends on !RT2800PCI_RT53XX
        default y
        ---help---
          This adds support for rt53xx wireless chipset family to the
          rt2800pci driver.
          Supported chips: RT5390
 
-config RT2800PCI_RT3290
+config BACKPORT_RT2800PCI_RT3290
        bool "rt2800pci - Include support for rt3290 devices (EXPERIMENTAL)"
+	depends on !RT2800PCI_RT3290
        default y
        ---help---
          This adds support for rt3290 wireless chipset family to the
@@ -110,23 +119,25 @@ config RT2800PCI_RT3290
          Supported chips: RT3290
 endif
 
-config RT2500USB
+config BACKPORT_RT2500USB
 	tristate "Ralink rt2500 (USB) support"
+	depends on !RT2500USB
 	depends on USB
-	select RT2X00_LIB_USB
-	select RT2X00_LIB_CRYPTO
+	select BACKPORT_RT2X00_LIB_USB
+	select BACKPORT_RT2X00_LIB_CRYPTO
 	---help---
 	  This adds support for rt2500 wireless chipset family.
 	  Supported chips: RT2571 & RT2572.
 
 	  When compiled as a module, this driver will be called rt2500usb.
 
-config RT73USB
+config BACKPORT_RT73USB
 	tristate "Ralink rt2501/rt73 (USB) support"
+	depends on !RT73USB
 	depends on USB
-	select RT2X00_LIB_USB
-	select RT2X00_LIB_FIRMWARE
-	select RT2X00_LIB_CRYPTO
+	select BACKPORT_RT2X00_LIB_USB
+	select BACKPORT_RT2X00_LIB_FIRMWARE
+	select BACKPORT_RT2X00_LIB_CRYPTO
 	select CRC_ITU_T
 	---help---
 	  This adds support for rt2501 wireless chipset family.
@@ -134,13 +145,14 @@ config RT73USB
 
 	  When compiled as a module, this driver will be called rt73usb.
 
-config RT2800USB
+config BACKPORT_RT2800USB
 	tristate "Ralink rt27xx/rt28xx/rt30xx (USB) support"
+	depends on !RT2800USB
 	depends on USB
-	select RT2800_LIB
-	select RT2X00_LIB_USB
-	select RT2X00_LIB_FIRMWARE
-	select RT2X00_LIB_CRYPTO
+	select BACKPORT_RT2800_LIB
+	select BACKPORT_RT2X00_LIB_USB
+	select BACKPORT_RT2X00_LIB_FIRMWARE
+	select BACKPORT_RT2X00_LIB_CRYPTO
 	select CRC_CCITT
 	---help---
 	  This adds support for rt27xx/rt28xx/rt30xx wireless chipset family.
@@ -148,46 +160,52 @@ config RT2800USB
 
 	  When compiled as a module, this driver will be called "rt2800usb.ko".
 
-if RT2800USB
+if BACKPORT_RT2800USB
 
-config RT2800USB_RT33XX
+config BACKPORT_RT2800USB_RT33XX
 	bool "rt2800usb - Include support for rt33xx devices"
+	depends on !RT2800USB_RT33XX
 	default y
 	---help---
 	  This adds support for rt33xx wireless chipset family to the
 	  rt2800usb driver.
 	  Supported chips: RT3370
 
-config RT2800USB_RT35XX
+config BACKPORT_RT2800USB_RT35XX
 	bool "rt2800usb - Include support for rt35xx devices (EXPERIMENTAL)"
+	depends on !RT2800USB_RT35XX
 	default y
 	---help---
 	  This adds support for rt35xx wireless chipset family to the
 	  rt2800usb driver.
 	  Supported chips: RT3572
 
-config RT2800USB_RT3573
+config BACKPORT_RT2800USB_RT3573
 	bool "rt2800usb - Include support for rt3573 devices (EXPERIMENTAL)"
+	depends on !RT2800USB_RT3573
 	---help---
 	  This enables support for RT3573 chipset based wireless USB devices
 	  in the rt2800usb driver.
 
-config RT2800USB_RT53XX
+config BACKPORT_RT2800USB_RT53XX
        bool "rt2800usb - Include support for rt53xx devices (EXPERIMENTAL)"
+	depends on !RT2800USB_RT53XX
        ---help---
          This adds support for rt53xx wireless chipset family to the
          rt2800usb driver.
          Supported chips: RT5370
 
-config RT2800USB_RT55XX
+config BACKPORT_RT2800USB_RT55XX
        bool "rt2800usb - Include support for rt55xx devices (EXPERIMENTAL)"
+	depends on !RT2800USB_RT55XX
        ---help---
          This adds support for rt55xx wireless chipset family to the
          rt2800usb driver.
          Supported chips: RT5572
 
-config RT2800USB_UNKNOWN
+config BACKPORT_RT2800USB_UNKNOWN
 	bool "rt2800usb - Include support for unknown (USB) devices"
+	depends on !RT2800USB_UNKNOWN
 	default n
 	---help---
 	  This adds support for rt2800usb devices that are known to
@@ -199,15 +217,16 @@ config RT2800USB_UNKNOWN
 
 endif
 
-config RT2800SOC
+config BACKPORT_RT2800SOC
 	tristate "Ralink WiSoC support"
+	depends on !RT2800SOC
 	depends on SOC_RT288X || SOC_RT305X
-	select RT2X00_LIB_SOC
-	select RT2X00_LIB_MMIO
-	select RT2X00_LIB_CRYPTO
-	select RT2X00_LIB_FIRMWARE
-	select RT2800_LIB
-	select RT2800_LIB_MMIO
+	select BACKPORT_RT2X00_LIB_SOC
+	select BACKPORT_RT2X00_LIB_MMIO
+	select BACKPORT_RT2X00_LIB_CRYPTO
+	select BACKPORT_RT2X00_LIB_FIRMWARE
+	select BACKPORT_RT2800_LIB
+	select BACKPORT_RT2800_LIB_MMIO
 	---help---
 	  This adds support for Ralink WiSoC devices.
 	  Supported chips: RT2880, RT3050, RT3052, RT3350, RT3352.
@@ -215,55 +234,67 @@ config RT2800SOC
 	  When compiled as a module, this driver will be called rt2800soc.
 
 
-config RT2800_LIB
+config BACKPORT_RT2800_LIB
 	tristate
+	depends on !RT2800_LIB
 
-config RT2800_LIB_MMIO
+config BACKPORT_RT2800_LIB_MMIO
 	tristate
-	select RT2X00_LIB_MMIO
-	select RT2800_LIB
+	depends on !RT2800_LIB_MMIO
+	select BACKPORT_RT2X00_LIB_MMIO
+	select BACKPORT_RT2800_LIB
 
-config RT2X00_LIB_MMIO
+config BACKPORT_RT2X00_LIB_MMIO
 	tristate
+	depends on !RT2X00_LIB_MMIO
 
-config RT2X00_LIB_PCI
+config BACKPORT_RT2X00_LIB_PCI
 	tristate
-	select RT2X00_LIB
+	depends on !RT2X00_LIB_PCI
+	select BACKPORT_RT2X00_LIB
 
-config RT2X00_LIB_SOC
+config BACKPORT_RT2X00_LIB_SOC
 	tristate
-	select RT2X00_LIB
+	depends on !RT2X00_LIB_SOC
+	select BACKPORT_RT2X00_LIB
 
-config RT2X00_LIB_USB
+config BACKPORT_RT2X00_LIB_USB
 	tristate
-	select RT2X00_LIB
+	depends on !RT2X00_LIB_USB
+	select BACKPORT_RT2X00_LIB
 
-config RT2X00_LIB
+config BACKPORT_RT2X00_LIB
 	tristate
+	depends on !RT2X00_LIB
 	select AVERAGE
 
-config RT2X00_LIB_FIRMWARE
+config BACKPORT_RT2X00_LIB_FIRMWARE
 	bool
+	depends on !RT2X00_LIB_FIRMWARE
 	select FW_LOADER
 
-config RT2X00_LIB_CRYPTO
+config BACKPORT_RT2X00_LIB_CRYPTO
 	bool
+	depends on !RT2X00_LIB_CRYPTO
 
-config RT2X00_LIB_LEDS
+config BACKPORT_RT2X00_LIB_LEDS
 	bool
+	depends on !RT2X00_LIB_LEDS
 	default y if (RT2X00_LIB=y && LEDS_CLASS=y) || (RT2X00_LIB=m && LEDS_CLASS!=n)
 
-config RT2X00_LIB_DEBUGFS
+config BACKPORT_RT2X00_LIB_DEBUGFS
 	bool "Ralink debugfs support"
-	depends on RT2X00_LIB && MAC80211_DEBUGFS
+	depends on !RT2X00_LIB_DEBUGFS
+	depends on RT2X00_LIB && BACKPORT_MAC80211_DEBUGFS
 	---help---
 	  Enable creation of debugfs files for the rt2x00 drivers.
 	  These debugfs files support both reading and writing of the
 	  most important register types of the rt2x00 hardware.
 
-config RT2X00_DEBUG
+config BACKPORT_RT2X00_DEBUG
 	bool "Ralink debug output"
-	depends on RT2X00_LIB
+	depends on !RT2X00_DEBUG
+	depends on BACKPORT_RT2X00_LIB
 	---help---
 	  Enable debugging output for all rt2x00 modules
 
diff --git a/backports/drivers/net/wireless/rtl818x/Kconfig b/backports/drivers/net/wireless/rtl818x/Kconfig
index d47221d..420c29d 100644
--- a/backports/drivers/net/wireless/rtl818x/Kconfig
+++ b/backports/drivers/net/wireless/rtl818x/Kconfig
@@ -1,12 +1,13 @@
 #
 # RTL818X Wireless LAN device configuration
 #
-config RTL8180
+config BACKPORT_RTL8180
 	tristate "Realtek 8180/8185/8187SE PCI support"
-	depends on MAC80211 && PCI
+	depends on !RTL8180
+	depends on BACKPORT_MAC80211 && PCI
 	select EEPROM_93CX6
 	---help---
-	  This is a driver for RTL8180, RTL8185 and RTL8187SE based cards.
+	  This is a driver for BACKPORT_RTL8180, RTL8185 and RTL8187SE based cards.
 	  These are PCI based chips found in cards such as:
 
 	  (RTL8185 802.11g)
@@ -57,12 +58,13 @@ config RTL8180
 
 	  Thanks to Realtek for their support!
 
-config RTL8187
+config BACKPORT_RTL8187
 	tristate "Realtek 8187 and 8187B USB support"
-	depends on MAC80211 && USB
+	depends on !RTL8187
+	depends on BACKPORT_MAC80211 && USB
 	select EEPROM_93CX6
 	---help---
-	  This is a driver for RTL8187 and RTL8187B based cards.
+	  This is a driver for BACKPORT_RTL8187 and BACKPORT_RTL8187B based cards.
 	  These are USB based chips found in devices such as:
 
 	  Netgear WG111v2
@@ -79,9 +81,10 @@ config RTL8187
 
 	  Thanks to Realtek for their support!
 
-# If possible, automatically enable LEDs for RTL8187.
+# If possible, automatically enable LEDs for BACKPORT_RTL8187.
 
-config RTL8187_LEDS
+config BACKPORT_RTL8187_LEDS
 	bool
-	depends on RTL8187 && MAC80211_LEDS && (LEDS_CLASS = y || LEDS_CLASS = RTL8187)
+	depends on !RTL8187_LEDS
+	depends on RTL8187 && BACKPORT_MAC80211_LEDS && (LEDS_CLASS = y || LEDS_CLASS = RTL8187)
 	default y
diff --git a/backports/drivers/net/wireless/rtlwifi/Kconfig b/backports/drivers/net/wireless/rtlwifi/Kconfig
index 5cf509d..f8cf996 100644
--- a/backports/drivers/net/wireless/rtlwifi/Kconfig
+++ b/backports/drivers/net/wireless/rtlwifi/Kconfig
@@ -1,6 +1,7 @@
-menuconfig RTL_CARDS
+menuconfig BACKPORT_RTL_CARDS
 	tristate "Realtek rtlwifi family of devices"
-	depends on MAC80211 && (PCI || USB)
+	depends on !RTL_CARDS
+	depends on BACKPORT_MAC80211 && (PCI || USB)
 	default y
 	---help---
 	  This option will enable support for the Realtek mac80211-based
@@ -8,96 +9,104 @@ menuconfig RTL_CARDS
 	  rtl8723ae, rtl8723be, rtl8188ee, rtl8192ee, and rtl8821ae share
 	  some common code.
 
-if RTL_CARDS
+if BACKPORT_RTL_CARDS
 
-config RTL8192CE
+config BACKPORT_RTL8192CE
 	tristate "Realtek RTL8192CE/RTL8188CE Wireless Network Adapter"
+	depends on !RTL8192CE
 	depends on PCI
-	select RTL8192C_COMMON
-	select RTLWIFI
-	select RTLWIFI_PCI
+	select BACKPORT_RTL8192C_COMMON
+	select BACKPORT_RTLWIFI
+	select BACKPORT_RTLWIFI_PCI
 	---help---
-	This is the driver for Realtek RTL8192CE/RTL8188CE 802.11n PCIe
+	This is the driver for Realtek BACKPORT_RTL8192CE/RTL8188CE 802.11n PCIe
 	wireless network adapters.
 
 	If you choose to build it as a module, it will be called rtl8192ce
 
-config RTL8192SE
+config BACKPORT_RTL8192SE
 	tristate "Realtek RTL8192SE/RTL8191SE PCIe Wireless Network Adapter"
+	depends on !RTL8192SE
 	depends on PCI
-	select RTLWIFI
-	select RTLWIFI_PCI
+	select BACKPORT_RTLWIFI
+	select BACKPORT_RTLWIFI_PCI
 	---help---
-	This is the driver for Realtek RTL8192SE/RTL8191SE 802.11n PCIe
+	This is the driver for Realtek BACKPORT_RTL8192SE/RTL8191SE 802.11n PCIe
 	wireless network adapters.
 
 	If you choose to build it as a module, it will be called rtl8192se
 
-config RTL8192DE
+config BACKPORT_RTL8192DE
 	tristate "Realtek RTL8192DE/RTL8188DE PCIe Wireless Network Adapter"
+	depends on !RTL8192DE
 	depends on PCI
-	select RTLWIFI
-	select RTLWIFI_PCI
+	select BACKPORT_RTLWIFI
+	select BACKPORT_RTLWIFI_PCI
 	---help---
-	This is the driver for Realtek RTL8192DE/RTL8188DE 802.11n PCIe
+	This is the driver for Realtek BACKPORT_RTL8192DE/RTL8188DE 802.11n PCIe
 	wireless network adapters.
 
 	If you choose to build it as a module, it will be called rtl8192de
 
-config RTL8723AE
+config BACKPORT_RTL8723AE
 	tristate "Realtek RTL8723AE PCIe Wireless Network Adapter"
+	depends on !RTL8723AE
 	depends on PCI
-	select RTLWIFI
-	select RTLWIFI_PCI
-	select RTL8723_COMMON
+	select BACKPORT_RTLWIFI
+	select BACKPORT_RTLWIFI_PCI
+	select BACKPORT_RTL8723_COMMON
 	select RTLBTCOEXIST
 	---help---
-	This is the driver for Realtek RTL8723AE 802.11n PCIe
+	This is the driver for Realtek BACKPORT_RTL8723AE 802.11n PCIe
 	wireless network adapters.
 
 	If you choose to build it as a module, it will be called rtl8723ae
 
-config RTL8723BE
+config BACKPORT_RTL8723BE
 	tristate "Realtek RTL8723BE PCIe Wireless Network Adapter"
+	depends on !RTL8723BE
 	depends on PCI
-	select RTLWIFI
-	select RTLWIFI_PCI
-	select RTL8723_COMMON
+	select BACKPORT_RTLWIFI
+	select BACKPORT_RTLWIFI_PCI
+	select BACKPORT_RTL8723_COMMON
 	select RTLBTCOEXIST
 	---help---
-	This is the driver for Realtek RTL8723BE 802.11n PCIe
+	This is the driver for Realtek BACKPORT_RTL8723BE 802.11n PCIe
 	wireless network adapters.
 
 	If you choose to build it as a module, it will be called rtl8723be
 
-config RTL8188EE
+config BACKPORT_RTL8188EE
 	tristate "Realtek RTL8188EE Wireless Network Adapter"
+	depends on !RTL8188EE
 	depends on PCI
-	select RTLWIFI
-	select RTLWIFI_PCI
+	select BACKPORT_RTLWIFI
+	select BACKPORT_RTLWIFI_PCI
 	---help---
-	This is the driver for Realtek RTL8188EE 802.11n PCIe
+	This is the driver for Realtek BACKPORT_RTL8188EE 802.11n PCIe
 	wireless network adapters.
 
 	If you choose to build it as a module, it will be called rtl8188ee
 
-config RTL8192EE
+config BACKPORT_RTL8192EE
 	tristate "Realtek RTL8192EE Wireless Network Adapter"
+	depends on !RTL8192EE
 	depends on PCI
-	select RTLWIFI
-	select RTLWIFI_PCI
+	select BACKPORT_RTLWIFI
+	select BACKPORT_RTLWIFI_PCI
 	select RTLBTCOEXIST
 	---help---
-	This is the driver for Realtek RTL8192EE 802.11n PCIe
+	This is the driver for Realtek BACKPORT_RTL8192EE 802.11n PCIe
 	wireless network adapters.
 
 	If you choose to build it as a module, it will be called rtl8192ee
 
-config RTL8821AE
+config BACKPORT_RTL8821AE
 	tristate "Realtek RTL8821AE/RTL8812AE Wireless Network Adapter"
+	depends on !RTL8821AE
 	depends on PCI
-	select RTLWIFI
-	select RTLWIFI_PCI
+	select BACKPORT_RTLWIFI
+	select BACKPORT_RTLWIFI_PCI
 	select RTLBTCOEXIST
 	---help---
 	This is the driver for Realtek RTL8i821AE/RTL8812AE 802.11av PCIe
@@ -105,50 +114,58 @@ config RTL8821AE
 
 	If you choose to build it as a module, it will be called rtl8821ae
 
-config RTL8192CU
+config BACKPORT_RTL8192CU
 	tristate "Realtek RTL8192CU/RTL8188CU USB Wireless Network Adapter"
+	depends on !RTL8192CU
 	depends on USB
-	select RTLWIFI
-	select RTLWIFI_USB
-	select RTL8192C_COMMON
+	select BACKPORT_RTLWIFI
+	select BACKPORT_RTLWIFI_USB
+	select BACKPORT_RTL8192C_COMMON
 	---help---
-	This is the driver for Realtek RTL8192CU/RTL8188CU 802.11n USB
+	This is the driver for Realtek BACKPORT_RTL8192CU/RTL8188CU 802.11n USB
 	wireless network adapters.
 
 	If you choose to build it as a module, it will be called rtl8192cu
 
-config RTLWIFI
+config BACKPORT_RTLWIFI
 	tristate
+	depends on !RTLWIFI
 	select FW_LOADER
 
-config RTLWIFI_PCI
+config BACKPORT_RTLWIFI_PCI
 	tristate
+	depends on !RTLWIFI_PCI
 
-config RTLWIFI_USB
+config BACKPORT_RTLWIFI_USB
 	tristate
+	depends on !RTLWIFI_USB
 
-config RTLWIFI_DEBUG
+config BACKPORT_RTLWIFI_DEBUG
 	bool "Debugging output for rtlwifi driver family"
-	depends on RTLWIFI
+	depends on !RTLWIFI_DEBUG
+	depends on BACKPORT_RTLWIFI
 	default y
 	---help---
 	To use the module option that sets the dynamic-debugging level for,
 	the front-end driver, this parameter must be "Y". For memory-limited
 	systems, choose "N". If in doubt, choose "Y".
 
-config RTL8192C_COMMON
+config BACKPORT_RTL8192C_COMMON
 	tristate
-	depends on RTL8192CE || RTL8192CU
+	depends on !RTL8192C_COMMON
+	depends on BACKPORT_RTL8192CE || RTL8192CU
 	default y
 
-config RTL8723_COMMON
+config BACKPORT_RTL8723_COMMON
 	tristate
-	depends on RTL8723AE || RTL8723BE
+	depends on !RTL8723_COMMON
+	depends on BACKPORT_RTL8723AE || RTL8723BE
 	default y
 
-config RTLBTCOEXIST
+config BACKPORT_RTLBTCOEXIST
 	tristate
-	depends on RTL8723AE || RTL8723BE || RTL8821AE || RTL8192EE
+	depends on !RTLBTCOEXIST
+	depends on BACKPORT_RTL8723AE || RTL8723BE || RTL8821AE || RTL8192EE
 	default y
 
 endif
diff --git a/backports/drivers/net/wireless/ti/Kconfig b/backports/drivers/net/wireless/ti/Kconfig
index b5ef017..b6d1e59 100644
--- a/backports/drivers/net/wireless/ti/Kconfig
+++ b/backports/drivers/net/wireless/ti/Kconfig
@@ -1,11 +1,12 @@
-menuconfig WL_TI
+menuconfig BACKPORT_WL_TI
 	bool "TI Wireless LAN support"
+	depends on !WL_TI
 	---help---
 	  This section contains support for all the wireless drivers
-	  for Texas Instruments WLAN chips, such as wl1251 and the wl12xx
+	  for Texas Instruments BACKPORT_WLAN chips, such as wl1251 and the wl12xx
 	  family.
 
-if WL_TI
+if BACKPORT_WL_TI
 source "$BACKPORT_DIR/drivers/net/wireless/ti/wl1251/Kconfig"
 source "$BACKPORT_DIR/drivers/net/wireless/ti/wl12xx/Kconfig"
 source "$BACKPORT_DIR/drivers/net/wireless/ti/wl18xx/Kconfig"
@@ -13,12 +14,13 @@ source "$BACKPORT_DIR/drivers/net/wireless/ti/wl18xx/Kconfig"
 # keep last for automatic dependencies
 source "$BACKPORT_DIR/drivers/net/wireless/ti/wlcore/Kconfig"
 
-config WILINK_PLATFORM_DATA
+config BACKPORT_WILINK_PLATFORM_DATA
 	bool "TI WiLink platform data"
-	depends on WLCORE_SDIO || WL1251_SDIO
+	depends on !WILINK_PLATFORM_DATA
+	depends on WLCORE_SDIO || BACKPORT_WL1251_SDIO
 	default y
 	---help---
 	Small platform data bit needed to pass data to the sdio modules.
 
 
-endif # WL_TI
+endif # BACKPORT_WL_TI
diff --git a/backports/drivers/net/wireless/ti/wl1251/Kconfig b/backports/drivers/net/wireless/ti/wl1251/Kconfig
index 477a206..12574b9 100644
--- a/backports/drivers/net/wireless/ti/wl1251/Kconfig
+++ b/backports/drivers/net/wireless/ti/wl1251/Kconfig
@@ -1,6 +1,7 @@
-menuconfig WL1251
+menuconfig BACKPORT_WL1251
 	tristate "TI wl1251 driver support"
-	depends on MAC80211
+	depends on !WL1251
+	depends on BACKPORT_MAC80211
 	select FW_LOADER
 	select CRC7
 	---help---
@@ -10,9 +11,10 @@ menuconfig WL1251
 	  If you choose to build a module, it'll be called wl1251. Say
 	  N if unsure.
 
-config WL1251_SPI
+config BACKPORT_WL1251_SPI
 	tristate "TI wl1251 SPI support"
-	depends on WL1251 && SPI_MASTER
+	depends on !WL1251_SPI
+	depends on BACKPORT_WL1251 && SPI_MASTER
 	---help---
 	  This module adds support for the SPI interface of adapters using
 	  TI wl1251 chipset.  Select this if your platform is using
@@ -21,9 +23,10 @@ config WL1251_SPI
 	  If you choose to build a module, it'll be called wl1251_spi.
 	  Say N if unsure.
 
-config WL1251_SDIO
+config BACKPORT_WL1251_SDIO
 	tristate "TI wl1251 SDIO support"
-	depends on WL1251 && MMC
+	depends on !WL1251_SDIO
+	depends on BACKPORT_WL1251 && MMC
 	---help---
 	  This module adds support for the SDIO interface of adapters using
 	  TI wl1251 chipset.  Select this if your platform is using
diff --git a/backports/drivers/net/wireless/ti/wl12xx/Kconfig b/backports/drivers/net/wireless/ti/wl12xx/Kconfig
index c218359..b807952 100644
--- a/backports/drivers/net/wireless/ti/wl12xx/Kconfig
+++ b/backports/drivers/net/wireless/ti/wl12xx/Kconfig
@@ -1,7 +1,8 @@
-config WL12XX
+config BACKPORT_WL12XX
        tristate "TI wl12xx support"
-	depends on MAC80211
-       select WLCORE
+	depends on !WL12XX
+	depends on BACKPORT_MAC80211
+       select BACKPORT_WLCORE
        ---help---
 	  This module adds support for wireless adapters based on TI wl1271,
 	  wl1273, wl1281 and wl1283 chipsets. This module does *not* include
diff --git a/backports/drivers/net/wireless/ti/wl18xx/Kconfig b/backports/drivers/net/wireless/ti/wl18xx/Kconfig
index 1cfdb25..498e79d 100644
--- a/backports/drivers/net/wireless/ti/wl18xx/Kconfig
+++ b/backports/drivers/net/wireless/ti/wl18xx/Kconfig
@@ -1,7 +1,8 @@
-config WL18XX
+config BACKPORT_WL18XX
 	tristate "TI wl18xx support"
-	depends on MAC80211
-	select WLCORE
+	depends on !WL18XX
+	depends on BACKPORT_MAC80211
+	select BACKPORT_WLCORE
 	---help---
 	  This module adds support for wireless adapters based on TI
 	  WiLink 8 chipsets.
diff --git a/backports/drivers/net/wireless/ti/wlcore/Kconfig b/backports/drivers/net/wireless/ti/wlcore/Kconfig
index 7c09954..4e40f2b 100644
--- a/backports/drivers/net/wireless/ti/wlcore/Kconfig
+++ b/backports/drivers/net/wireless/ti/wlcore/Kconfig
@@ -1,9 +1,10 @@
-config WLCORE
+config BACKPORT_WLCORE
 	tristate "TI wlcore support"
-	depends on WL_TI && MAC80211
+	depends on !WLCORE
+	depends on WL_TI && BACKPORT_MAC80211
 	select FW_LOADER
 	---help---
-	  This module contains the main code for TI WLAN chips.  It abstracts
+	  This module contains the main code for TI BACKPORT_WLAN chips.  It abstracts
 	  hardware-specific differences among different chipset families.
 	  Each chipset family needs to implement its own lower-level module
 	  that will depend on this module for the common code.
@@ -11,24 +12,26 @@ config WLCORE
 	  If you choose to build a module, it will be called wlcore. Say N if
 	  unsure.
 
-config WLCORE_SPI
+config BACKPORT_WLCORE_SPI
 	tristate "TI wlcore SPI support"
-	depends on WLCORE && SPI_MASTER
+	depends on !WLCORE_SPI
+	depends on BACKPORT_WLCORE && SPI_MASTER
 	select CRC7
 	---help---
 	  This module adds support for the SPI interface of adapters using
-	  TI WLAN chipsets.  Select this if your platform is using
+	  TI BACKPORT_WLAN chipsets.  Select this if your platform is using
 	  the SPI bus.
 
 	  If you choose to build a module, it'll be called wlcore_spi.
 	  Say N if unsure.
 
-config WLCORE_SDIO
+config BACKPORT_WLCORE_SDIO
 	tristate "TI wlcore SDIO support"
-	depends on WLCORE && MMC
+	depends on !WLCORE_SDIO
+	depends on BACKPORT_WLCORE && MMC
 	---help---
 	  This module adds support for the SDIO interface of adapters using
-	  TI WLAN chipsets.  Select this if your platform is using
+	  TI BACKPORT_WLAN chipsets.  Select this if your platform is using
 	  the SDIO bus.
 
 	  If you choose to build a module, it'll be called wlcore_sdio.
diff --git a/backports/drivers/net/wireless/zd1211rw/Kconfig b/backports/drivers/net/wireless/zd1211rw/Kconfig
index 40d5061..482913c 100644
--- a/backports/drivers/net/wireless/zd1211rw/Kconfig
+++ b/backports/drivers/net/wireless/zd1211rw/Kconfig
@@ -1,6 +1,7 @@
-config ZD1211RW
+config BACKPORT_ZD1211RW
 	tristate "ZyDAS ZD1211/ZD1211B USB-wireless support"
-	depends on USB && MAC80211
+	depends on !ZD1211RW
+	depends on USB && BACKPORT_MAC80211
 	select FW_LOADER
 	---help---
 	  This is a driver for the ZyDAS ZD1211/ZD1211B wireless
@@ -9,9 +10,10 @@ config ZD1211RW
 	  Device firmware is required alongside this driver. You can download
 	  the firmware distribution from http://sf.net/projects/zd1211/files/
 
-config ZD1211RW_DEBUG
+config BACKPORT_ZD1211RW_DEBUG
 	bool "ZyDAS ZD1211 debugging"
-	depends on ZD1211RW
+	depends on !ZD1211RW_DEBUG
+	depends on BACKPORT_ZD1211RW
 	---help---
 	  ZD1211 debugging messages. Choosing Y will result in additional debug
 	  messages being saved to your kernel logs, which may help debug any
diff --git a/backports/drivers/nfc/Kconfig b/backports/drivers/nfc/Kconfig
index b6be9ec..c8ea6d5 100644
--- a/backports/drivers/nfc/Kconfig
+++ b/backports/drivers/nfc/Kconfig
@@ -3,34 +3,37 @@
 #
 
 menu "Near Field Communication (NFC) devices"
-	depends on NFC
+	depends on BACKPORT_NFC
 
-config NFC_PN533
+config BACKPORT_NFC_PN533
 	tristate "NXP PN533 USB driver"
+	depends on !NFC_PN533
 	depends on USB
 	help
 	  NXP PN533 USB driver.
-	  This driver provides support for NFC NXP PN533 devices.
+	  This driver provides support for BACKPORT_NFC NXP PN533 devices.
 
 	  Say Y here to compile support for PN533 devices into the
 	  kernel or say M to compile it as module (pn533).
 
-config NFC_WILINK
+config BACKPORT_NFC_WILINK
 	tristate "Texas Instruments NFC WiLink driver"
-	depends on TI_ST && NFC_NCI
+	depends on !NFC_WILINK
+	depends on TI_ST && BACKPORT_NFC_NCI
 	help
-	  This enables the NFC driver for Texas Instrument's BT/FM/GPS/NFC
+	  This enables the NFC driver for Texas Instrument's BACKPORT_BT/FM/GPS/NFC
 	  combo devices. This makes use of shared transport line discipline
-	  core driver to communicate with the NFC core of the combo chip.
+	  core driver to communicate with the BACKPORT_NFC core of the combo chip.
 
-	  Say Y here to compile support for Texas Instrument's NFC WiLink driver
+	  Say Y here to compile support for Texas Instrument's BACKPORT_NFC WiLink driver
 	  into the kernel or say M to compile it as module.
 
-config NFC_TRF7970A
+config BACKPORT_NFC_TRF7970A
 	tristate "Texas Instruments TRF7970a NFC driver"
-	depends on SPI && NFC_DIGITAL
+	depends on !NFC_TRF7970A
+	depends on SPI && BACKPORT_NFC_DIGITAL
 	help
-	  This option enables the NFC driver for Texas Instruments' TRF7970a
+	  This option enables the BACKPORT_NFC driver for Texas Instruments' TRF7970a
 	  device. Such device supports 5 different protocols: ISO14443A,
 	  ISO14443B, FeLiCa, ISO15693 and ISO18000-3.
 
@@ -38,30 +41,33 @@ config NFC_TRF7970A
 	  say M  to compile it as a module. The module will be called
 	  trf7970a.ko.
 
-config NFC_MEI_PHY
+config BACKPORT_NFC_MEI_PHY
 	tristate "MEI bus NFC device support"
-	depends on INTEL_MEI && NFC_HCI
+	depends on !NFC_MEI_PHY
+	depends on INTEL_MEI && BACKPORT_NFC_HCI
 	help
 	  This adds support to use an mei bus nfc device. Select this if you
-	  will use an HCI NFC driver for an NFC chip connected behind an
+	  will use an HCI BACKPORT_NFC driver for an BACKPORT_NFC chip connected behind an
 	  Intel's Management Engine chip.
 
 	  If unsure, say N.
 
-config NFC_SIM
+config BACKPORT_NFC_SIM
 	tristate "NFC hardware simulator driver"
+	depends on !NFC_SIM
 	help
-	  This driver declares two virtual NFC devices supporting NFC-DEP
+	  This driver declares two virtual BACKPORT_NFC devices supporting BACKPORT_NFC-DEP
 	  protocol. An LLCP connection can be established between them and
 	  all packets sent from one device is sent back to the other, acting as
 	  loopback devices.
 
 	  If unsure, say N.
 
-config NFC_PORT100
+config BACKPORT_NFC_PORT100
 	tristate "Sony NFC Port-100 Series USB device support"
+	depends on !NFC_PORT100
 	depends on USB
-	depends on NFC_DIGITAL
+	depends on BACKPORT_NFC_DIGITAL
 	help
 	  This adds support for Sony Port-100 chip based USB devices such as the
 	  RC-S380 dongle.
diff --git a/backports/drivers/nfc/microread/Kconfig b/backports/drivers/nfc/microread/Kconfig
index 951d554..9b7d6e5 100644
--- a/backports/drivers/nfc/microread/Kconfig
+++ b/backports/drivers/nfc/microread/Kconfig
@@ -1,20 +1,22 @@
-config NFC_MICROREAD
+config BACKPORT_NFC_MICROREAD
 	tristate "Inside Secure microread NFC driver"
-	depends on NFC_HCI
+	depends on !NFC_MICROREAD
+	depends on BACKPORT_NFC_HCI
 	select CRC_CCITT
 	default n
 	---help---
 	  This module contains the main code for Inside Secure microread
-	  NFC chipsets. It implements the chipset HCI logic and hooks into
-	  the NFC kernel APIs. Physical layers will register against it.
+	  BACKPORT_NFC chipsets. It implements the chipset HCI logic and hooks into
+	  the BACKPORT_NFC kernel APIs. Physical layers will register against it.
 
 	  To compile this driver as a module, choose m here. The module will
 	  be called microread.
 	  Say N if unsure.
 
-config NFC_MICROREAD_I2C
+config BACKPORT_NFC_MICROREAD_I2C
 	tristate "NFC Microread i2c support"
-	depends on NFC_MICROREAD && I2C && NFC_SHDLC
+	depends on !NFC_MICROREAD_I2C
+	depends on BACKPORT_NFC_MICROREAD && I2C && BACKPORT_NFC_SHDLC
 	---help---
 	  This module adds support for the i2c interface of adapters using
 	  Inside microread chipsets.  Select this if your platform is using
@@ -23,9 +25,10 @@ config NFC_MICROREAD_I2C
 	  If you choose to build a module, it'll be called microread_i2c.
 	  Say N if unsure.
 
-config NFC_MICROREAD_MEI
+config BACKPORT_NFC_MICROREAD_MEI
 	tristate "NFC Microread MEI support"
-	depends on NFC_MICROREAD && NFC_MEI_PHY
+	depends on !NFC_MICROREAD_MEI
+	depends on BACKPORT_NFC_MICROREAD && BACKPORT_NFC_MEI_PHY
 	---help---
 	  This module adds support for the mei interface of adapters using
 	  Inside microread chipsets.  Select this if your microread chipset
diff --git a/backports/drivers/nfc/nfcmrvl/Kconfig b/backports/drivers/nfc/nfcmrvl/Kconfig
index 5e18afd..a53ffcb 100644
--- a/backports/drivers/nfc/nfcmrvl/Kconfig
+++ b/backports/drivers/nfc/nfcmrvl/Kconfig
@@ -1,23 +1,25 @@
-config NFC_MRVL
+config BACKPORT_NFC_MRVL
 	tristate "Marvell NFC driver support"
-	depends on NFC_NCI
+	depends on !NFC_MRVL
+	depends on BACKPORT_NFC_NCI
 	help
-	  The core driver to support Marvell NFC devices.
+	  The core driver to support Marvell BACKPORT_NFC devices.
 
 	  This driver is required if you want to support
-	  Marvell NFC device 8897.
+	  Marvell BACKPORT_NFC device 8897.
 
-	  Say Y here to compile Marvell NFC driver into the kernel or
+	  Say Y here to compile Marvell BACKPORT_NFC driver into the kernel or
 	  say M to compile it as module.
 
-config NFC_MRVL_USB
+config BACKPORT_NFC_MRVL_USB
 	tristate "Marvell NFC-over-USB driver"
-	depends on NFC_MRVL && USB
+	depends on !NFC_MRVL_USB
+	depends on BACKPORT_NFC_MRVL && USB
 	help
-	  Marvell NFC-over-USB driver.
+	  Marvell BACKPORT_NFC-over-USB driver.
 
-	  This driver provides support for Marvell NFC-over-USB devices:
+	  This driver provides support for Marvell BACKPORT_NFC-over-USB devices:
           8897.
 
-	  Say Y here to compile support for Marvell NFC-over-USB driver
+	  Say Y here to compile support for Marvell BACKPORT_NFC-over-USB driver
 	  into the kernel or say M to compile it as module.
diff --git a/backports/drivers/nfc/nxp-nci/Kconfig b/backports/drivers/nfc/nxp-nci/Kconfig
index 37b4061..d921dc1 100644
--- a/backports/drivers/nfc/nxp-nci/Kconfig
+++ b/backports/drivers/nfc/nxp-nci/Kconfig
@@ -1,20 +1,22 @@
-config NFC_NXP_NCI
+config BACKPORT_NFC_NXP_NCI
 	tristate "NXP-NCI NFC driver"
-	depends on NFC_NCI
+	depends on !NFC_NXP_NCI
+	depends on BACKPORT_NFC_NCI
 	default n
 	---help---
 	  Generic core driver for NXP NCI chips such as the NPC100
 	  or PN7150 families.
-	  This is a driver based on the NCI NFC kernel layers and
+	  This is a driver based on the NCI BACKPORT_NFC kernel layers and
 	  will thus not work with NXP libnfc library.
 
 	  To compile this driver as a module, choose m here. The module will
 	  be called nxp_nci.
 	  Say N if unsure.
 
-config NFC_NXP_NCI_I2C
+config BACKPORT_NFC_NXP_NCI_I2C
 	tristate "NXP-NCI I2C support"
-	depends on NFC_NXP_NCI && I2C
+	depends on !NFC_NXP_NCI_I2C
+	depends on BACKPORT_NFC_NXP_NCI && I2C
 	---help---
 	  This module adds support for an I2C interface to the NXP NCI
 	  chips.
diff --git a/backports/drivers/nfc/pn544/Kconfig b/backports/drivers/nfc/pn544/Kconfig
index ccf06f5..d5511bb 100644
--- a/backports/drivers/nfc/pn544/Kconfig
+++ b/backports/drivers/nfc/pn544/Kconfig
@@ -1,20 +1,22 @@
-config NFC_PN544
+config BACKPORT_NFC_PN544
 	tristate "NXP PN544 NFC driver"
-	depends on NFC_HCI
+	depends on !NFC_PN544
+	depends on BACKPORT_NFC_HCI
 	select CRC_CCITT
 	default n
 	---help---
 	  NXP PN544 core driver.
-	  This is a driver based on the HCI NFC kernel layers and
+	  This is a driver based on the HCI BACKPORT_NFC kernel layers and
 	  will thus not work with NXP libnfc library.
 
 	  To compile this driver as a module, choose m here. The module will
 	  be called pn544.
 	  Say N if unsure.
 
-config NFC_PN544_I2C
+config BACKPORT_NFC_PN544_I2C
 	tristate "NFC PN544 i2c support"
-	depends on NFC_PN544 && I2C && NFC_SHDLC
+	depends on !NFC_PN544_I2C
+	depends on BACKPORT_NFC_PN544 && I2C && BACKPORT_NFC_SHDLC
 	---help---
 	  This module adds support for the NXP pn544 i2c interface.
 	  Select this if your platform is using the i2c bus.
@@ -22,9 +24,10 @@ config NFC_PN544_I2C
 	  If you choose to build a module, it'll be called pn544_i2c.
 	  Say N if unsure.
 
-config NFC_PN544_MEI
+config BACKPORT_NFC_PN544_MEI
 	tristate "NFC PN544 MEI support"
-	depends on NFC_PN544 && NFC_MEI_PHY
+	depends on !NFC_PN544_MEI
+	depends on BACKPORT_NFC_PN544 && BACKPORT_NFC_MEI_PHY
 	---help---
 	  This module adds support for the mei interface of adapters using
 	  NXP pn544 chipsets.  Select this if your pn544 chipset
diff --git a/backports/drivers/nfc/st21nfca/Kconfig b/backports/drivers/nfc/st21nfca/Kconfig
index ee459f0..55af7e7 100644
--- a/backports/drivers/nfc/st21nfca/Kconfig
+++ b/backports/drivers/nfc/st21nfca/Kconfig
@@ -1,20 +1,22 @@
-config NFC_ST21NFCA
+config BACKPORT_NFC_ST21NFCA
 	tristate "STMicroelectronics ST21NFCA NFC driver"
-	depends on NFC_HCI
+	depends on !NFC_ST21NFCA
+	depends on BACKPORT_NFC_HCI
 	select CRC_CCITT
 	default n
 	---help---
 	  STMicroelectronics ST21NFCA core driver. It implements the chipset
-	  HCI logic and hooks into the NFC kernel APIs. Physical layers will
+	  HCI logic and hooks into the BACKPORT_NFC kernel APIs. Physical layers will
 	  register against it.
 
 	  To compile this driver as a module, choose m here. The module will
 	  be called st21nfca.
 	  Say N if unsure.
 
-config NFC_ST21NFCA_I2C
+config BACKPORT_NFC_ST21NFCA_I2C
 	tristate "NFC ST21NFCA i2c support"
-	depends on NFC_ST21NFCA && I2C && NFC_SHDLC
+	depends on !NFC_ST21NFCA_I2C
+	depends on BACKPORT_NFC_ST21NFCA && I2C && BACKPORT_NFC_SHDLC
 	---help---
 	  This module adds support for the STMicroelectronics st21nfca i2c interface.
 	  Select this if your platform is using the i2c bus.
diff --git a/backports/drivers/nfc/st21nfcb/Kconfig b/backports/drivers/nfc/st21nfcb/Kconfig
index e0322dd..f794eb6 100644
--- a/backports/drivers/nfc/st21nfcb/Kconfig
+++ b/backports/drivers/nfc/st21nfcb/Kconfig
@@ -1,19 +1,21 @@
-config NFC_ST21NFCB
+config BACKPORT_NFC_ST21NFCB
 	tristate "STMicroelectronics ST21NFCB NFC driver"
-	depends on NFC_NCI
+	depends on !NFC_ST21NFCB
+	depends on BACKPORT_NFC_NCI
 	default n
 	---help---
 	  STMicroelectronics ST21NFCB core driver. It implements the chipset
-	  NCI logic and hooks into the NFC kernel APIs. Physical layers will
+	  NCI logic and hooks into the BACKPORT_NFC kernel APIs. Physical layers will
 	  register against it.
 
 	  To compile this driver as a module, choose m here. The module will
 	  be called st21nfcb.
 	  Say N if unsure.
 
-config NFC_ST21NFCB_I2C
+config BACKPORT_NFC_ST21NFCB_I2C
 	tristate "NFC ST21NFCB i2c support"
-	depends on NFC_ST21NFCB && I2C
+	depends on !NFC_ST21NFCB_I2C
+	depends on BACKPORT_NFC_ST21NFCB && I2C
 	---help---
 	  This module adds support for the STMicroelectronics st21nfcb i2c interface.
 	  Select this if your platform is using the i2c bus.
diff --git a/backports/drivers/ssb/Kconfig b/backports/drivers/ssb/Kconfig
index f0d22cd..034351c 100644
--- a/backports/drivers/ssb/Kconfig
+++ b/backports/drivers/ssb/Kconfig
@@ -1,14 +1,16 @@
-config SSB_POSSIBLE
+config BACKPORT_SSB_POSSIBLE
 	bool
+	depends on !SSB_POSSIBLE
 	depends on HAS_IOMEM && HAS_DMA
 	default y
 
 menu "Sonics Silicon Backplane"
-	depends on SSB_POSSIBLE
+	depends on BACKPORT_SSB_POSSIBLE
 
-config SSB
+config BACKPORT_SSB
 	tristate "Sonics Silicon Backplane support"
-	depends on SSB_POSSIBLE
+	depends on !SSB
+	depends on BACKPORT_SSB_POSSIBLE
 	help
 	  Support for the Sonics Silicon Backplane bus.
 	  You only need to enable this option, if you are
@@ -22,23 +24,27 @@ config SSB
 	  If unsure, say N.
 
 # Common SPROM support routines
-config SSB_SPROM
+config BACKPORT_SSB_SPROM
 	bool
+	depends on !SSB_SPROM
 
 # Support for Block-I/O. SELECT this from the driver that needs it.
-config SSB_BLOCKIO
+config BACKPORT_SSB_BLOCKIO
 	bool
-	depends on SSB
+	depends on !SSB_BLOCKIO
+	depends on BACKPORT_SSB
 
-config SSB_PCIHOST_POSSIBLE
+config BACKPORT_SSB_PCIHOST_POSSIBLE
 	bool
-	depends on SSB && (PCI = y || PCI = SSB)
+	depends on !SSB_PCIHOST_POSSIBLE
+	depends on BACKPORT_SSB && (PCI = y || PCI = BACKPORT_SSB)
 	default y
 
-config SSB_PCIHOST
+config BACKPORT_SSB_PCIHOST
 	bool "Support for SSB on PCI-bus host"
-	depends on SSB_PCIHOST_POSSIBLE
-	select SSB_SPROM
+	depends on !SSB_PCIHOST
+	depends on BACKPORT_SSB_PCIHOST_POSSIBLE
+	select BACKPORT_SSB_SPROM
 	default y
 	help
 	  Support for a Sonics Silicon Backplane on top
@@ -46,43 +52,49 @@ config SSB_PCIHOST
 
 	  If unsure, say Y
 
-config SSB_B43_PCI_BRIDGE
+config BACKPORT_SSB_B43_PCI_BRIDGE
 	bool
-	depends on SSB_PCIHOST
+	depends on !SSB_B43_PCI_BRIDGE
+	depends on BACKPORT_SSB_PCIHOST
 	default n
 
-config SSB_PCMCIAHOST_POSSIBLE
+config BACKPORT_SSB_PCMCIAHOST_POSSIBLE
 	bool
-	depends on SSB && (PCMCIA = y || PCMCIA = SSB)
+	depends on !SSB_PCMCIAHOST_POSSIBLE
+	depends on BACKPORT_SSB && (PCMCIA = y || PCMCIA = BACKPORT_SSB)
 	default y
 
-config SSB_PCMCIAHOST
+config BACKPORT_SSB_PCMCIAHOST
 	bool "Support for SSB on PCMCIA-bus host"
-	depends on SSB_PCMCIAHOST_POSSIBLE
-	select SSB_SPROM
+	depends on !SSB_PCMCIAHOST
+	depends on BACKPORT_SSB_PCMCIAHOST_POSSIBLE
+	select BACKPORT_SSB_SPROM
 	help
 	  Support for a Sonics Silicon Backplane on top
 	  of a PCMCIA device.
 
 	  If unsure, say N
 
-config SSB_SDIOHOST_POSSIBLE
+config BACKPORT_SSB_SDIOHOST_POSSIBLE
 	bool
-	depends on SSB && (MMC = y || MMC = SSB)
+	depends on !SSB_SDIOHOST_POSSIBLE
+	depends on BACKPORT_SSB && (MMC = y || MMC = BACKPORT_SSB)
 	default y
 
-config SSB_SDIOHOST
+config BACKPORT_SSB_SDIOHOST
 	bool "Support for SSB on SDIO-bus host"
-	depends on SSB_SDIOHOST_POSSIBLE
+	depends on !SSB_SDIOHOST
+	depends on BACKPORT_SSB_SDIOHOST_POSSIBLE
 	help
 	  Support for a Sonics Silicon Backplane on top
 	  of a SDIO device.
 
 	  If unsure, say N
 
-config SSB_SILENT
+config BACKPORT_SSB_SILENT
 	bool "No SSB kernel messages"
-	depends on SSB && EXPERT
+	depends on !SSB_SILENT
+	depends on BACKPORT_SSB && EXPERT
 	help
 	  This option turns off all Sonics Silicon Backplane printks.
 	  Note that you won't be able to identify problems, once
@@ -92,84 +104,95 @@ config SSB_SILENT
 
 	  Say N
 
-config SSB_DEBUG
+config BACKPORT_SSB_DEBUG
 	bool "SSB debugging"
-	depends on SSB && !SSB_SILENT
+	depends on !SSB_DEBUG
+	depends on BACKPORT_SSB && !SSB_SILENT
 	help
 	  This turns on additional runtime checks and debugging
-	  messages. Turn this on for SSB troubleshooting.
+	  messages. Turn this on for BACKPORT_SSB troubleshooting.
 
 	  If unsure, say N
 
-config SSB_SERIAL
+config BACKPORT_SSB_SERIAL
 	bool
-	depends on SSB
+	depends on !SSB_SERIAL
+	depends on BACKPORT_SSB
 	# ChipCommon and ExtIf serial support routines.
 
-config SSB_DRIVER_PCICORE_POSSIBLE
+config BACKPORT_SSB_DRIVER_PCICORE_POSSIBLE
 	bool
-	depends on SSB_PCIHOST
+	depends on !SSB_DRIVER_PCICORE_POSSIBLE
+	depends on BACKPORT_SSB_PCIHOST
 	default y
 
-config SSB_DRIVER_PCICORE
+config BACKPORT_SSB_DRIVER_PCICORE
 	bool "SSB PCI core driver"
-	depends on SSB_DRIVER_PCICORE_POSSIBLE
+	depends on !SSB_DRIVER_PCICORE
+	depends on BACKPORT_SSB_DRIVER_PCICORE_POSSIBLE
 	help
 	  Driver for the Sonics Silicon Backplane attached
 	  Broadcom PCI core.
 
 	  If unsure, say Y
 
-config SSB_PCICORE_HOSTMODE
+config BACKPORT_SSB_PCICORE_HOSTMODE
 	bool "Hostmode support for SSB PCI core"
-	depends on SSB_DRIVER_PCICORE && SSB_DRIVER_MIPS
+	depends on !SSB_PCICORE_HOSTMODE
+	depends on BACKPORT_SSB_DRIVER_PCICORE && BACKPORT_SSB_DRIVER_MIPS
 	help
 	  PCIcore hostmode operation (external PCI bus).
 
-config SSB_DRIVER_MIPS
+config BACKPORT_SSB_DRIVER_MIPS
 	bool "SSB Broadcom MIPS core driver"
-	depends on SSB && MIPS
-	select SSB_SERIAL
-	select SSB_SFLASH
+	depends on !SSB_DRIVER_MIPS
+	depends on BACKPORT_SSB && MIPS
+	select BACKPORT_SSB_SERIAL
+	select BACKPORT_SSB_SFLASH
 	help
 	  Driver for the Sonics Silicon Backplane attached
 	  Broadcom MIPS core.
 
 	  If unsure, say N
 
-config SSB_SFLASH
+config BACKPORT_SSB_SFLASH
 	bool "SSB serial flash support"
-	depends on SSB_DRIVER_MIPS
+	depends on !SSB_SFLASH
+	depends on BACKPORT_SSB_DRIVER_MIPS
 	default y
 
 # Assumption: We are on embedded, if we compile the MIPS core.
-config SSB_EMBEDDED
+config BACKPORT_SSB_EMBEDDED
 	bool
-	depends on SSB_DRIVER_MIPS && SSB_PCICORE_HOSTMODE
+	depends on !SSB_EMBEDDED
+	depends on BACKPORT_SSB_DRIVER_MIPS && BACKPORT_SSB_PCICORE_HOSTMODE
 	default y
 
-config SSB_DRIVER_EXTIF
+config BACKPORT_SSB_DRIVER_EXTIF
 	bool "SSB Broadcom EXTIF core driver"
-	depends on SSB_DRIVER_MIPS
+	depends on !SSB_DRIVER_EXTIF
+	depends on BACKPORT_SSB_DRIVER_MIPS
 	help
 	  Driver for the Sonics Silicon Backplane attached
 	  Broadcom EXTIF core.
 
 	  If unsure, say N
 
-config SSB_DRIVER_GIGE
+config BACKPORT_SSB_DRIVER_GIGE
 	bool "SSB Broadcom Gigabit Ethernet driver"
-	depends on SSB_PCIHOST_POSSIBLE && SSB_EMBEDDED && MIPS
+	depends on !SSB_DRIVER_GIGE
+	depends on BACKPORT_SSB_PCIHOST_POSSIBLE && BACKPORT_SSB_EMBEDDED && MIPS
 	help
 	  Driver for the Sonics Silicon Backplane attached
 	  Broadcom Gigabit Ethernet.
 
 	  If unsure, say N
 
-config SSB_DRIVER_GPIO
+config BACKPORT_SSB_DRIVER_GPIO
 	bool "SSB GPIO driver"
-	depends on SSB && GPIOLIB
-	select IRQ_DOMAIN if SSB_EMBEDDED
+	depends on !SSB_DRIVER_GPIO
+	depends on BACKPORT_SSB && GPIOLIB
+	select IRQ_DOMAIN if BACKPORT_SSB_EMBEDDED
 	help
 	  Driver to provide access to the GPIO pins on the bus.
 
diff --git a/backports/drivers/usb/class/Kconfig b/backports/drivers/usb/class/Kconfig
index bb8b736..dfb6e0d 100644
--- a/backports/drivers/usb/class/Kconfig
+++ b/backports/drivers/usb/class/Kconfig
@@ -3,8 +3,9 @@
 #
 comment "USB Device Class drivers"
 
-config USB_ACM
+config BACKPORT_USB_ACM
 	tristate "USB Modem (CDC ACM) support"
+	depends on !USB_ACM
 	depends on TTY
 	---help---
 	  This driver supports USB modems and ISDN adapters which support the
@@ -18,8 +19,9 @@ config USB_ACM
 	  To compile this driver as a module, choose M here: the
 	  module will be called cdc-acm.
 
-config USB_PRINTER
+config BACKPORT_USB_PRINTER
 	tristate "USB Printer support"
+	depends on !USB_PRINTER
 	help
 	  Say Y here if you want to connect a USB printer to your computer's
 	  USB port.
@@ -27,8 +29,9 @@ config USB_PRINTER
 	  To compile this driver as a module, choose M here: the
 	  module will be called usblp.
 
-config USB_WDM
+config BACKPORT_USB_WDM
 	tristate "USB Wireless Device Management support"
+	depends on !USB_WDM
 	---help---
 	  This driver supports the WMC Device Management functionality
 	  of cell phones compliant to the CDC WMC specification. You can use
@@ -37,8 +40,9 @@ config USB_WDM
 	  To compile this driver as a module, choose M here: the
 	  module will be called cdc-wdm.
 
-config USB_TMC
+config BACKPORT_USB_TMC
 	tristate "USB Test and Measurement Class support"
+	depends on !USB_TMC
 	help
 	  Say Y here if you want to connect a USB device that follows
 	  the USB.org specification for USB Test and Measurement devices
diff --git a/backports/net/6lowpan/Kconfig b/backports/net/6lowpan/Kconfig
index 7fa0f38..7465ca4 100644
--- a/backports/net/6lowpan/Kconfig
+++ b/backports/net/6lowpan/Kconfig
@@ -1,59 +1,68 @@
-menuconfig 6LOWPAN
+menuconfig BACKPORT_6LOWPAN
 	tristate "6LoWPAN Support"
+	depends on !6LOWPAN
 	depends on IPV6
 	---help---
 	  This enables IPv6 over Low power Wireless Personal Area Network -
 	  "6LoWPAN" which is supported by IEEE 802.15.4 or Bluetooth stacks.
 
-menuconfig 6LOWPAN_NHC
+menuconfig BACKPORT_6LOWPAN_NHC
 	tristate "Next Header Compression Support"
-	depends on 6LOWPAN
+	depends on !6LOWPAN_NHC
+	depends on BACKPORT_6LOWPAN
 	default y
 	---help---
 	  Support for next header compression.
 
-if 6LOWPAN_NHC
+if BACKPORT_6LOWPAN_NHC
 
-config 6LOWPAN_NHC_DEST
+config BACKPORT_6LOWPAN_NHC_DEST
 	tristate "Destination Options Header Support"
+	depends on !6LOWPAN_NHC_DEST
 	default y
 	---help---
 	  6LoWPAN IPv6 Destination Options Header compression according to
 	  RFC6282.
 
-config 6LOWPAN_NHC_FRAGMENT
+config BACKPORT_6LOWPAN_NHC_FRAGMENT
 	tristate "Fragment Header Support"
+	depends on !6LOWPAN_NHC_FRAGMENT
 	default y
 	---help---
 	  6LoWPAN IPv6 Fragment Header compression according to RFC6282.
 
-config 6LOWPAN_NHC_HOP
+config BACKPORT_6LOWPAN_NHC_HOP
 	tristate "Hop-by-Hop Options Header Support"
+	depends on !6LOWPAN_NHC_HOP
 	default y
 	---help---
 	  6LoWPAN IPv6 Hop-by-Hop Options Header compression according to
 	  RFC6282.
 
-config 6LOWPAN_NHC_IPV6
+config BACKPORT_6LOWPAN_NHC_IPV6
 	tristate "IPv6 Header Support"
+	depends on !6LOWPAN_NHC_IPV6
 	default y
 	---help---
 	  6LoWPAN IPv6 Header compression according to RFC6282.
 
-config 6LOWPAN_NHC_MOBILITY
+config BACKPORT_6LOWPAN_NHC_MOBILITY
 	tristate "Mobility Header Support"
+	depends on !6LOWPAN_NHC_MOBILITY
 	default y
 	---help---
 	  6LoWPAN IPv6 Mobility Header compression according to RFC6282.
 
-config 6LOWPAN_NHC_ROUTING
+config BACKPORT_6LOWPAN_NHC_ROUTING
 	tristate "Routing Header Support"
+	depends on !6LOWPAN_NHC_ROUTING
 	default y
 	---help---
 	  6LoWPAN IPv6 Routing Header compression according to RFC6282.
 
-config 6LOWPAN_NHC_UDP
+config BACKPORT_6LOWPAN_NHC_UDP
 	tristate "UDP Header Support"
+	depends on !6LOWPAN_NHC_UDP
 	default y
 	---help---
 	  6LoWPAN IPv6 UDP Header compression according to RFC6282.
diff --git a/backports/net/bluetooth/Kconfig b/backports/net/bluetooth/Kconfig
index 1b1ecb9..a7d4fa6 100644
--- a/backports/net/bluetooth/Kconfig
+++ b/backports/net/bluetooth/Kconfig
@@ -2,8 +2,9 @@
 # Bluetooth subsystem configuration
 #
 
-menuconfig BT
+menuconfig BACKPORT_BT
 	tristate "Bluetooth subsystem support"
+	depends on !BT
 	depends on NET && !S390
 	depends on RFKILL || !RFKILL
 	select CRC16
@@ -40,9 +41,10 @@ menuconfig BT
 	  to Bluetooth kernel modules are provided in the BlueZ packages.  For
 	  more information, see <http://www.bluez.org/>.
 
-config BT_BREDR
+config BACKPORT_BT_BREDR
 	bool "Bluetooth Classic (BR/EDR) features"
-	depends on BT
+	depends on !BT_BREDR
+	depends on BACKPORT_BT
 	default y
 
 source "$BACKPORT_DIR/net/bluetooth/rfcomm/Kconfig"
@@ -53,20 +55,23 @@ source "$BACKPORT_DIR/net/bluetooth/cmtp/Kconfig"
 
 source "$BACKPORT_DIR/net/bluetooth/hidp/Kconfig"
 
-config BT_LE
+config BACKPORT_BT_LE
 	bool "Bluetooth Low Energy (LE) features"
-	depends on BT
+	depends on !BT_LE
+	depends on BACKPORT_BT
 	default y
 
-config BT_6LOWPAN
+config BACKPORT_BT_6LOWPAN
 	tristate "Bluetooth 6LoWPAN support"
-	depends on BT_LE && 6LOWPAN
+	depends on !BT_6LOWPAN
+	depends on BACKPORT_BT_LE && 6LOWPAN
 	help
 	  IPv6 compression over Bluetooth Low Energy.
 
-config BT_SELFTEST
+config BACKPORT_BT_SELFTEST
 	bool "Bluetooth self testing support"
-	depends on BT && DEBUG_KERNEL
+	depends on !BT_SELFTEST
+	depends on BACKPORT_BT && DEBUG_KERNEL
 	help
 	  Run self tests when initializing the Bluetooth subsystem.  This
 	  is a developer option and can cause significant delay when booting
@@ -77,23 +82,26 @@ config BT_SELFTEST
 	  subsystem is compiled into the kernel image, then the test cases
 	  are run late in the initcall hierarchy.
 
-config BT_SELFTEST_ECDH
+config BACKPORT_BT_SELFTEST_ECDH
 	bool "ECDH test cases"
-	depends on BT_LE && BT_SELFTEST
+	depends on !BT_SELFTEST_ECDH
+	depends on BACKPORT_BT_LE && BACKPORT_BT_SELFTEST
 	help
 	  Run test cases for ECDH cryptographic functionality used by the
 	  Bluetooth Low Energy Secure Connections feature.
 
-config BT_SELFTEST_SMP
+config BACKPORT_BT_SELFTEST_SMP
 	bool "SMP test cases"
-	depends on BT_LE && BT_SELFTEST
+	depends on !BT_SELFTEST_SMP
+	depends on BACKPORT_BT_LE && BACKPORT_BT_SELFTEST
 	help
 	  Run test cases for SMP cryptographic functionality, including both
 	  legacy SMP as well as the Secure Connections features.
 
-config BT_DEBUGFS
+config BACKPORT_BT_DEBUGFS
 	bool "Export Bluetooth internals in debugfs"
-	depends on BT && DEBUG_FS
+	depends on !BT_DEBUGFS
+	depends on BACKPORT_BT && DEBUG_FS
 	default y
 	help
 	  Provide extensive information about internal Bluetooth states
diff --git a/backports/net/bluetooth/bnep/Kconfig b/backports/net/bluetooth/bnep/Kconfig
index 9657125..ec210c1 100644
--- a/backports/net/bluetooth/bnep/Kconfig
+++ b/backports/net/bluetooth/bnep/Kconfig
@@ -1,6 +1,7 @@
-config BT_BNEP
+config BACKPORT_BT_BNEP
 	tristate "BNEP protocol support"
-	depends on BT_BREDR
+	depends on !BT_BNEP
+	depends on BACKPORT_BT_BREDR
 	select CRC32
 	help
 	  BNEP (Bluetooth Network Encapsulation Protocol) is Ethernet
@@ -10,14 +11,16 @@ config BT_BNEP
 	  Say Y here to compile BNEP support into the kernel or say M to
 	  compile it as module (bnep).
 
-config BT_BNEP_MC_FILTER
+config BACKPORT_BT_BNEP_MC_FILTER
 	bool "Multicast filter support"
-	depends on BT_BNEP
+	depends on !BT_BNEP_MC_FILTER
+	depends on BACKPORT_BT_BNEP
 	help
 	  This option enables the multicast filter support for BNEP.
 
-config BT_BNEP_PROTO_FILTER
+config BACKPORT_BT_BNEP_PROTO_FILTER
 	bool "Protocol filter support"
-	depends on BT_BNEP
+	depends on !BT_BNEP_PROTO_FILTER
+	depends on BACKPORT_BT_BNEP
 	help
 	  This option enables the protocol filter support for BNEP.
diff --git a/backports/net/bluetooth/cmtp/Kconfig b/backports/net/bluetooth/cmtp/Kconfig
index 049e2d10..6600bcc 100644
--- a/backports/net/bluetooth/cmtp/Kconfig
+++ b/backports/net/bluetooth/cmtp/Kconfig
@@ -1,6 +1,7 @@
-config BT_CMTP
+config BACKPORT_BT_CMTP
 	tristate "CMTP protocol support"
-	depends on BT_BREDR && ISDN_CAPI
+	depends on !BT_CMTP
+	depends on BACKPORT_BT_BREDR && ISDN_CAPI
 	help
 	  CMTP (CAPI Message Transport Protocol) is a transport layer
 	  for CAPI messages.  CMTP is required for the Bluetooth Common
diff --git a/backports/net/bluetooth/hidp/Kconfig b/backports/net/bluetooth/hidp/Kconfig
index f092dbe..353013a 100644
--- a/backports/net/bluetooth/hidp/Kconfig
+++ b/backports/net/bluetooth/hidp/Kconfig
@@ -1,6 +1,7 @@
-config BT_HIDP
+config BACKPORT_BT_HIDP
 	tristate "HIDP protocol support"
-	depends on BT_BREDR && INPUT
+	depends on !BT_HIDP
+	depends on BACKPORT_BT_BREDR && INPUT
 	select HID
 	help
 	  HIDP (Human Interface Device Protocol) is a transport layer
diff --git a/backports/net/bluetooth/rfcomm/Kconfig b/backports/net/bluetooth/rfcomm/Kconfig
index 3274f35..55f90da 100644
--- a/backports/net/bluetooth/rfcomm/Kconfig
+++ b/backports/net/bluetooth/rfcomm/Kconfig
@@ -1,6 +1,7 @@
-config BT_RFCOMM
+config BACKPORT_BT_RFCOMM
 	tristate "RFCOMM protocol support"
-	depends on BT_BREDR
+	depends on !BT_RFCOMM
+	depends on BACKPORT_BT_BREDR
 	help
 	  RFCOMM provides connection oriented stream transport.  RFCOMM
 	  support is required for Dialup Networking, OBEX and other Bluetooth
@@ -9,9 +10,10 @@ config BT_RFCOMM
 	  Say Y here to compile RFCOMM support into the kernel or say M to
 	  compile it as module (rfcomm).
 
-config BT_RFCOMM_TTY
+config BACKPORT_BT_RFCOMM_TTY
 	bool "RFCOMM TTY support"
-	depends on BT_RFCOMM
+	depends on !BT_RFCOMM_TTY
+	depends on BACKPORT_BT_RFCOMM
 	depends on TTY
 	help
 	  This option enables TTY emulation support for RFCOMM channels.
diff --git a/backports/net/ieee802154/6lowpan/Kconfig b/backports/net/ieee802154/6lowpan/Kconfig
index d24f985..107bd41 100644
--- a/backports/net/ieee802154/6lowpan/Kconfig
+++ b/backports/net/ieee802154/6lowpan/Kconfig
@@ -1,5 +1,6 @@
-config IEEE802154_6LOWPAN
+config BACKPORT_IEEE802154_6LOWPAN
 	tristate "6lowpan support over IEEE 802.15.4"
-	depends on 6LOWPAN
+	depends on !IEEE802154_6LOWPAN
+	depends on BACKPORT_6LOWPAN
 	---help---
 	  IPv6 compression over IEEE 802.15.4.
diff --git a/backports/net/ieee802154/Kconfig b/backports/net/ieee802154/Kconfig
index 8f85f73c..92c8163 100644
--- a/backports/net/ieee802154/Kconfig
+++ b/backports/net/ieee802154/Kconfig
@@ -1,5 +1,6 @@
-menuconfig IEEE802154
+menuconfig BACKPORT_IEEE802154
 	tristate "IEEE Std 802.15.4 Low-Rate Wireless Personal Area Networks support"
+	depends on !IEEE802154
 	---help---
 	  IEEE Std 802.15.4 defines a low data rate, low power and low
 	  complexity short range wireless personal area networks. It was
@@ -10,10 +11,11 @@ menuconfig IEEE802154
 	  Say Y here to compile LR-WPAN support into the kernel or say M to
 	  compile it as modules.
 
-if IEEE802154
+if BACKPORT_IEEE802154
 
-config IEEE802154_SOCKET
+config BACKPORT_IEEE802154_SOCKET
 	tristate "IEEE 802.15.4 socket interface"
+	depends on !IEEE802154_SOCKET
 	default y
 	---help---
 	  Socket interface for IEEE 802.15.4. Contains DGRAM sockets interface
diff --git a/backports/net/mac80211/Kconfig b/backports/net/mac80211/Kconfig
index 64a012a..9270837 100644
--- a/backports/net/mac80211/Kconfig
+++ b/backports/net/mac80211/Kconfig
@@ -1,6 +1,7 @@
-config MAC80211
+config BACKPORT_MAC80211
 	tristate "Generic IEEE 802.11 Networking Stack (mac80211)"
-	depends on CFG80211
+	depends on !MAC80211
+	depends on BACKPORT_CFG80211
 	select CRYPTO
 	select CRYPTO_ARC4
 	select CRYPTO_AES
@@ -13,67 +14,74 @@ config MAC80211
 	  networking stack.
 
 comment "CFG80211 needs to be enabled for MAC80211"
-	depends on CFG80211=n
+	depends on BACKPORT_CFG80211=n
 
-if MAC80211 != n
+if BACKPORT_MAC80211 != n
 
-config MAC80211_HAS_RC
+config BACKPORT_MAC80211_HAS_RC
 	bool
+	depends on !MAC80211_HAS_RC
 
-config MAC80211_RC_MINSTREL
+config BACKPORT_MAC80211_RC_MINSTREL
 	bool "Minstrel" if EXPERT
-	select MAC80211_HAS_RC
+	depends on !MAC80211_RC_MINSTREL
+	select BACKPORT_MAC80211_HAS_RC
 	default y
 	---help---
 	  This option enables the 'minstrel' TX rate control algorithm
 
-config MAC80211_RC_MINSTREL_HT
+config BACKPORT_MAC80211_RC_MINSTREL_HT
 	bool "Minstrel 802.11n support" if EXPERT
-	depends on MAC80211_RC_MINSTREL
+	depends on !MAC80211_RC_MINSTREL_HT
+	depends on BACKPORT_MAC80211_RC_MINSTREL
 	default y
 	---help---
 	  This option enables the 'minstrel_ht' TX rate control algorithm
 
-config MAC80211_RC_MINSTREL_VHT
+config BACKPORT_MAC80211_RC_MINSTREL_VHT
 	bool "Minstrel 802.11ac support" if EXPERT
-	depends on MAC80211_RC_MINSTREL_HT
+	depends on !MAC80211_RC_MINSTREL_VHT
+	depends on BACKPORT_MAC80211_RC_MINSTREL_HT
 	default n
 	---help---
 	  This option enables VHT in the 'minstrel_ht' TX rate control algorithm
 
 choice
 	prompt "Default rate control algorithm"
-	depends on MAC80211_HAS_RC
-	default MAC80211_RC_DEFAULT_MINSTREL
+	depends on BACKPORT_MAC80211_HAS_RC
+	default BACKPORT_MAC80211_RC_DEFAULT_MINSTREL
 	---help---
 	  This option selects the default rate control algorithm
 	  mac80211 will use. Note that this default can still be
 	  overridden through the ieee80211_default_rc_algo module
 	  parameter if different algorithms are available.
 
-config MAC80211_RC_DEFAULT_MINSTREL
+config BACKPORT_MAC80211_RC_DEFAULT_MINSTREL
 	bool "Minstrel"
-	depends on MAC80211_RC_MINSTREL
+	depends on !MAC80211_RC_DEFAULT_MINSTREL
+	depends on BACKPORT_MAC80211_RC_MINSTREL
 	---help---
 	  Select Minstrel as the default rate control algorithm.
 
 
 endchoice
 
-config MAC80211_RC_DEFAULT
+config BACKPORT_MAC80211_RC_DEFAULT
 	string
-	default "minstrel_ht" if MAC80211_RC_DEFAULT_MINSTREL && MAC80211_RC_MINSTREL_HT
-	default "minstrel" if MAC80211_RC_DEFAULT_MINSTREL
+	depends on !MAC80211_RC_DEFAULT
+	default "minstrel_ht" if BACKPORT_MAC80211_RC_DEFAULT_MINSTREL && BACKPORT_MAC80211_RC_MINSTREL_HT
+	default "minstrel" if BACKPORT_MAC80211_RC_DEFAULT_MINSTREL
 	default ""
 
 endif
 
 comment "Some wireless drivers require a rate control algorithm"
-	depends on MAC80211 && MAC80211_HAS_RC=n
+	depends on BACKPORT_MAC80211 && BACKPORT_MAC80211_HAS_RC=n
 
-config MAC80211_MESH
+config BACKPORT_MAC80211_MESH
 	bool "Enable mac80211 mesh networking (pre-802.11s) support"
-	depends on MAC80211
+	depends on !MAC80211_MESH
+	depends on BACKPORT_MAC80211
 	---help---
 	 This options enables support of Draft 802.11s mesh networking.
 	 The implementation is based on Draft 2.08 of the Mesh Networking
@@ -81,27 +89,30 @@ config MAC80211_MESH
 	 possible, as drafts leave a number of identifiers to be defined after
 	 ratification.  For more information visit http://o11s.org/.
 
-config MAC80211_LEDS
+config BACKPORT_MAC80211_LEDS
 	bool "Enable LED triggers"
-	depends on MAC80211
+	depends on !MAC80211_LEDS
+	depends on BACKPORT_MAC80211
 	depends on LEDS_CLASS
 	select LEDS_TRIGGERS
 	---help---
 	  This option enables a few LED triggers for different
 	  packet receive/transmit events.
 
-config MAC80211_DEBUGFS
+config BACKPORT_MAC80211_DEBUGFS
 	bool "Export mac80211 internals in DebugFS"
-	depends on MAC80211 && DEBUG_FS
+	depends on !MAC80211_DEBUGFS
+	depends on BACKPORT_MAC80211 && DEBUG_FS
 	---help---
 	  Select this to see extensive information about
 	  the internal state of mac80211 in debugfs.
 
 	  Say N unless you know you need this.
 
-config MAC80211_MESSAGE_TRACING
+config BACKPORT_MAC80211_MESSAGE_TRACING
 	bool "Trace all mac80211 debug messages"
-	depends on MAC80211
+	depends on !MAC80211_MESSAGE_TRACING
+	depends on BACKPORT_MAC80211
 	---help---
 	  Select this option to have mac80211 register the
 	  mac80211_msg trace subsystem with tracepoints to
@@ -112,15 +123,17 @@ config MAC80211_MESSAGE_TRACING
 	  need to be present in the binary and formatted at
 	  runtime for tracing.
 
-menuconfig MAC80211_DEBUG_MENU
+menuconfig BACKPORT_MAC80211_DEBUG_MENU
 	bool "Select mac80211 debugging features"
-	depends on MAC80211
+	depends on !MAC80211_DEBUG_MENU
+	depends on BACKPORT_MAC80211
 	---help---
 	  This option collects various mac80211 debug settings.
 
-config MAC80211_NOINLINE
+config BACKPORT_MAC80211_NOINLINE
 	bool "Do not inline TX/RX handlers"
-	depends on MAC80211_DEBUG_MENU
+	depends on !MAC80211_NOINLINE
+	depends on BACKPORT_MAC80211_DEBUG_MENU
 	---help---
 	  This option affects code generation in mac80211, when
 	  selected some functions are marked "noinline" to allow
@@ -134,9 +147,10 @@ config MAC80211_NOINLINE
 	  If unsure, say N unless you expect to be finding problems
 	  in mac80211.
 
-config MAC80211_VERBOSE_DEBUG
+config BACKPORT_MAC80211_VERBOSE_DEBUG
 	bool "Verbose debugging output"
-	depends on MAC80211_DEBUG_MENU
+	depends on !MAC80211_VERBOSE_DEBUG
+	depends on BACKPORT_MAC80211_DEBUG_MENU
 	---help---
 	  Selecting this option causes mac80211 to print out
 	  many debugging messages. It should not be selected
@@ -145,9 +159,10 @@ config MAC80211_VERBOSE_DEBUG
 
 	  Do not select this option.
 
-config MAC80211_MLME_DEBUG
+config BACKPORT_MAC80211_MLME_DEBUG
 	bool "Verbose managed MLME output"
-	depends on MAC80211_DEBUG_MENU
+	depends on !MAC80211_MLME_DEBUG
+	depends on BACKPORT_MAC80211_DEBUG_MENU
 	---help---
 	  Selecting this option causes mac80211 to print out
 	  debugging messages for the managed-mode MLME. It
@@ -156,18 +171,20 @@ config MAC80211_MLME_DEBUG
 
 	  Do not select this option.
 
-config MAC80211_STA_DEBUG
+config BACKPORT_MAC80211_STA_DEBUG
 	bool "Verbose station debugging"
-	depends on MAC80211_DEBUG_MENU
+	depends on !MAC80211_STA_DEBUG
+	depends on BACKPORT_MAC80211_DEBUG_MENU
 	---help---
 	  Selecting this option causes mac80211 to print out
 	  debugging messages for station addition/removal.
 
 	  Do not select this option.
 
-config MAC80211_HT_DEBUG
+config BACKPORT_MAC80211_HT_DEBUG
 	bool "Verbose HT debugging"
-	depends on MAC80211_DEBUG_MENU
+	depends on !MAC80211_HT_DEBUG
+	depends on BACKPORT_MAC80211_DEBUG_MENU
 	---help---
 	  This option enables 802.11n High Throughput features
 	  debug tracing output.
@@ -177,9 +194,10 @@ config MAC80211_HT_DEBUG
 
 	  Do not select this option.
 
-config MAC80211_OCB_DEBUG
+config BACKPORT_MAC80211_OCB_DEBUG
 	bool "Verbose OCB debugging"
-	depends on MAC80211_DEBUG_MENU
+	depends on !MAC80211_OCB_DEBUG
+	depends on BACKPORT_MAC80211_DEBUG_MENU
 	---help---
 	  Selecting this option causes mac80211 to print out
 	  very verbose OCB debugging messages. It should not
@@ -188,9 +206,10 @@ config MAC80211_OCB_DEBUG
 
 	  Do not select this option.
 
-config MAC80211_IBSS_DEBUG
+config BACKPORT_MAC80211_IBSS_DEBUG
 	bool "Verbose IBSS debugging"
-	depends on MAC80211_DEBUG_MENU
+	depends on !MAC80211_IBSS_DEBUG
+	depends on BACKPORT_MAC80211_DEBUG_MENU
 	---help---
 	  Selecting this option causes mac80211 to print out
 	  very verbose IBSS debugging messages. It should not
@@ -199,9 +218,10 @@ config MAC80211_IBSS_DEBUG
 
 	  Do not select this option.
 
-config MAC80211_PS_DEBUG
+config BACKPORT_MAC80211_PS_DEBUG
 	bool "Verbose powersave mode debugging"
-	depends on MAC80211_DEBUG_MENU
+	depends on !MAC80211_PS_DEBUG
+	depends on BACKPORT_MAC80211_DEBUG_MENU
 	---help---
 	  Selecting this option causes mac80211 to print out very
 	  verbose power save mode debugging messages (when mac80211
@@ -211,10 +231,11 @@ config MAC80211_PS_DEBUG
 
 	  Do not select this option.
 
-config MAC80211_MPL_DEBUG
+config BACKPORT_MAC80211_MPL_DEBUG
 	bool "Verbose mesh peer link debugging"
-	depends on MAC80211_DEBUG_MENU
-	depends on MAC80211_MESH
+	depends on !MAC80211_MPL_DEBUG
+	depends on BACKPORT_MAC80211_DEBUG_MENU
+	depends on BACKPORT_MAC80211_MESH
 	---help---
 	  Selecting this option causes mac80211 to print out very
 	  verbose mesh peer link debugging messages (when mac80211
@@ -224,10 +245,11 @@ config MAC80211_MPL_DEBUG
 
 	  Do not select this option.
 
-config MAC80211_MPATH_DEBUG
+config BACKPORT_MAC80211_MPATH_DEBUG
 	bool "Verbose mesh path debugging"
-	depends on MAC80211_DEBUG_MENU
-	depends on MAC80211_MESH
+	depends on !MAC80211_MPATH_DEBUG
+	depends on BACKPORT_MAC80211_DEBUG_MENU
+	depends on BACKPORT_MAC80211_MESH
 	---help---
 	  Selecting this option causes mac80211 to print out very
 	  verbose mesh path selection debugging messages (when mac80211
@@ -237,10 +259,11 @@ config MAC80211_MPATH_DEBUG
 
 	  Do not select this option.
 
-config MAC80211_MHWMP_DEBUG
+config BACKPORT_MAC80211_MHWMP_DEBUG
 	bool "Verbose mesh HWMP routing debugging"
-	depends on MAC80211_DEBUG_MENU
-	depends on MAC80211_MESH
+	depends on !MAC80211_MHWMP_DEBUG
+	depends on BACKPORT_MAC80211_DEBUG_MENU
+	depends on BACKPORT_MAC80211_MESH
 	---help---
 	  Selecting this option causes mac80211 to print out very
 	  verbose mesh routing (HWMP) debugging messages (when mac80211
@@ -250,10 +273,11 @@ config MAC80211_MHWMP_DEBUG
 
 	  Do not select this option.
 
-config MAC80211_MESH_SYNC_DEBUG
+config BACKPORT_MAC80211_MESH_SYNC_DEBUG
 	bool "Verbose mesh synchronization debugging"
-	depends on MAC80211_DEBUG_MENU
-	depends on MAC80211_MESH
+	depends on !MAC80211_MESH_SYNC_DEBUG
+	depends on BACKPORT_MAC80211_DEBUG_MENU
+	depends on BACKPORT_MAC80211_MESH
 	---help---
 	  Selecting this option causes mac80211 to print out very verbose mesh
 	  synchronization debugging messages (when mac80211 is taking part in a
@@ -261,10 +285,11 @@ config MAC80211_MESH_SYNC_DEBUG
 
 	  Do not select this option.
 
-config MAC80211_MESH_CSA_DEBUG
+config BACKPORT_MAC80211_MESH_CSA_DEBUG
 	bool "Verbose mesh channel switch debugging"
-	depends on MAC80211_DEBUG_MENU
-	depends on MAC80211_MESH
+	depends on !MAC80211_MESH_CSA_DEBUG
+	depends on BACKPORT_MAC80211_DEBUG_MENU
+	depends on BACKPORT_MAC80211_MESH
 	---help---
 	  Selecting this option causes mac80211 to print out very verbose mesh
 	  channel switch debugging messages (when mac80211 is taking part in a
@@ -272,10 +297,11 @@ config MAC80211_MESH_CSA_DEBUG
 
 	  Do not select this option.
 
-config MAC80211_MESH_PS_DEBUG
+config BACKPORT_MAC80211_MESH_PS_DEBUG
 	bool "Verbose mesh powersave debugging"
-	depends on MAC80211_DEBUG_MENU
-	depends on MAC80211_MESH
+	depends on !MAC80211_MESH_PS_DEBUG
+	depends on BACKPORT_MAC80211_DEBUG_MENU
+	depends on BACKPORT_MAC80211_MESH
 	---help---
 	  Selecting this option causes mac80211 to print out very verbose mesh
 	  powersave debugging messages (when mac80211 is taking part in a
@@ -283,9 +309,10 @@ config MAC80211_MESH_PS_DEBUG
 
 	  Do not select this option.
 
-config MAC80211_TDLS_DEBUG
+config BACKPORT_MAC80211_TDLS_DEBUG
 	bool "Verbose TDLS debugging"
-	depends on MAC80211_DEBUG_MENU
+	depends on !MAC80211_TDLS_DEBUG
+	depends on BACKPORT_MAC80211_DEBUG_MENU
 	---help---
 	  Selecting this option causes mac80211 to print out very
 	  verbose TDLS selection debugging messages (when mac80211
@@ -295,10 +322,11 @@ config MAC80211_TDLS_DEBUG
 
 	  Do not select this option.
 
-config MAC80211_DEBUG_COUNTERS
+config BACKPORT_MAC80211_DEBUG_COUNTERS
 	bool "Extra statistics for TX/RX debugging"
-	depends on MAC80211_DEBUG_MENU
-	depends on MAC80211_DEBUGFS
+	depends on !MAC80211_DEBUG_COUNTERS
+	depends on BACKPORT_MAC80211_DEBUG_MENU
+	depends on BACKPORT_MAC80211_DEBUGFS
 	---help---
 	  Selecting this option causes mac80211 to keep additional
 	  and very verbose statistics about TX and RX handler use
diff --git a/backports/net/mac802154/Kconfig b/backports/net/mac802154/Kconfig
index aa462b4..c88c497 100644
--- a/backports/net/mac802154/Kconfig
+++ b/backports/net/mac802154/Kconfig
@@ -1,6 +1,7 @@
-config MAC802154
+config BACKPORT_MAC802154
 	tristate "Generic IEEE 802.15.4 Soft Networking Stack (mac802154)"
-	depends on IEEE802154
+	depends on !MAC802154
+	depends on BACKPORT_IEEE802154
 	select CRC_CCITT
 	select CRYPTO_AUTHENC
 	select CRYPTO_CCM
diff --git a/backports/net/nfc/Kconfig b/backports/net/nfc/Kconfig
index ca65809..87f1738 100644
--- a/backports/net/nfc/Kconfig
+++ b/backports/net/nfc/Kconfig
@@ -1,29 +1,31 @@
 #
-# NFC sybsystem configuration
+# BACKPORT_NFC sybsystem configuration
 #
 
-menuconfig NFC
+menuconfig BACKPORT_NFC
 	depends on NET
+	depends on !NFC
 	depends on RFKILL || !RFKILL
 	tristate "NFC subsystem support"
 	default n
 	help
-	  Say Y here if you want to build support for NFC (Near field
+	  Say Y here if you want to build support for BACKPORT_NFC (Near field
 	  communication) devices.
 
 	  To compile this support as a module, choose M here: the module will
 	  be called nfc.
 
-config NFC_DIGITAL
+config BACKPORT_NFC_DIGITAL
 	depends on NFC
+	depends on !NFC_DIGITAL
 	select CRC_CCITT
 	select CRC_ITU_T
 	tristate "NFC Digital Protocol stack support"
 	default n
 	help
-	  Say Y if you want to build NFC digital protocol stack support.
-	  This is needed by NFC chipsets whose firmware only implement
-	  the NFC analog layer.
+	  Say Y if you want to build BACKPORT_NFC digital protocol stack support.
+	  This is needed by BACKPORT_NFC chipsets whose firmware only implement
+	  the BACKPORT_NFC analog layer.
 
 	  To compile this support as a module, choose M here: the module will
 	  be called nfc_digital.
diff --git a/backports/net/nfc/hci/Kconfig b/backports/net/nfc/hci/Kconfig
index fd67f51..4766853 100644
--- a/backports/net/nfc/hci/Kconfig
+++ b/backports/net/nfc/hci/Kconfig
@@ -1,17 +1,19 @@
-config NFC_HCI
+config BACKPORT_NFC_HCI
        depends on NFC
+	depends on !NFC_HCI
        tristate "NFC HCI implementation"
        default n
        help
-	 Say Y here if you want to build support for a kernel NFC HCI
+	 Say Y here if you want to build support for a kernel BACKPORT_NFC HCI
 	 implementation. This is mostly needed for devices that only process
 	 HCI frames, like for example the NXP pn544.
 
-config NFC_SHDLC
+config BACKPORT_NFC_SHDLC
 	depends on NFC_HCI
+	depends on !NFC_SHDLC
 	select CRC_CCITT
-	bool "SHDLC link layer for HCI based NFC drivers"
+	bool "SHDLC link layer for HCI based BACKPORT_NFC drivers"
 	default n
 	---help---
-	  Say yes if you use an NFC HCI driver that requires SHDLC link layer.
+	  Say yes if you use an BACKPORT_NFC HCI driver that requires SHDLC link layer.
 	  If unsure, say N here.
diff --git a/backports/net/nfc/nci/Kconfig b/backports/net/nfc/nci/Kconfig
index a4f1e42..c10eae0 100644
--- a/backports/net/nfc/nci/Kconfig
+++ b/backports/net/nfc/nci/Kconfig
@@ -1,21 +1,23 @@
-config NFC_NCI
+config BACKPORT_NFC_NCI
 	depends on NFC
+	depends on !NFC_NCI
 	tristate "NCI protocol support"
 	default n
 	help
 	  NCI (NFC Controller Interface) is a communication protocol between
-	  an NFC Controller (NFCC) and a Device Host (DH).
+	  an BACKPORT_NFC Controller (NFCC) and a Device Host (DH).
 
 	  Say Y here to compile NCI support into the kernel or say M to
 	  compile it as module (nci).
 
-config NFC_NCI_SPI
+config BACKPORT_NFC_NCI_SPI
 	depends on NFC_NCI && SPI
+	depends on !NFC_NCI_SPI
 	select CRC_CCITT
 	bool "NCI over SPI protocol support"
 	default n
 	help
 	  NCI (NFC Controller Interface) is a communication protocol between
-	  an NFC Controller (NFCC) and a Device Host (DH).
+	  an BACKPORT_NFC Controller (NFCC) and a Device Host (DH).
 
 	  Say yes if you use an NCI driver that requires SPI link layer.
diff --git a/backports/net/wireless/Kconfig b/backports/net/wireless/Kconfig
index c8370f5..e6880a3 100644
--- a/backports/net/wireless/Kconfig
+++ b/backports/net/wireless/Kconfig
@@ -1,5 +1,6 @@
-config CFG80211
+config BACKPORT_CFG80211
 	tristate "cfg80211 - wireless configuration API"
+	depends on !CFG80211
 	depends on RFKILL || !RFKILL
 	---help---
 	  cfg80211 is the Linux wireless LAN (802.11) configuration API.
@@ -11,9 +12,10 @@ config CFG80211
 
 	  When built as a module it will be called cfg80211.
 
-config NL80211_TESTMODE
+config BACKPORT_NL80211_TESTMODE
 	bool "nl80211 testmode command"
-	depends on CFG80211
+	depends on !NL80211_TESTMODE
+	depends on BACKPORT_CFG80211
 	help
 	  The nl80211 testmode command helps implementing things like
 	  factory calibration or validation tools for wireless chips.
@@ -26,9 +28,10 @@ config NL80211_TESTMODE
 
 	  Say N.
 
-config CFG80211_DEVELOPER_WARNINGS
+config BACKPORT_CFG80211_DEVELOPER_WARNINGS
 	bool "enable developer warnings"
-	depends on CFG80211
+	depends on !CFG80211_DEVELOPER_WARNINGS
+	depends on BACKPORT_CFG80211
 	default n
 	help
 	  This option enables some additional warnings that help
@@ -43,9 +46,10 @@ config CFG80211_DEVELOPER_WARNINGS
 	  on it (or mac80211).
 
 
-config CFG80211_REG_DEBUG
+config BACKPORT_CFG80211_REG_DEBUG
 	bool "cfg80211 regulatory debugging"
-	depends on CFG80211
+	depends on !CFG80211_REG_DEBUG
+	depends on BACKPORT_CFG80211
 	default n
 	---help---
 	  You can enable this if you want to debug regulatory changes.
@@ -56,9 +60,10 @@ config CFG80211_REG_DEBUG
 
 	  If unsure, say N.
 
-config CFG80211_CERTIFICATION_ONUS
+config BACKPORT_CFG80211_CERTIFICATION_ONUS
 	bool "cfg80211 certification onus"
-	depends on CFG80211 && EXPERT
+	depends on !CFG80211_CERTIFICATION_ONUS
+	depends on BACKPORT_CFG80211 && EXPERT
 	default n
 	---help---
 	  You should disable this option unless you are both capable
@@ -77,9 +82,10 @@ config CFG80211_CERTIFICATION_ONUS
 	  you are a wireless researcher and are working in a controlled
 	  and approved environment by your local regulatory agency.
 
-config CFG80211_REG_CELLULAR_HINTS
+config BACKPORT_CFG80211_REG_CELLULAR_HINTS
 	bool "cfg80211 regulatory support for cellular base station hints"
-	depends on CFG80211_CERTIFICATION_ONUS
+	depends on !CFG80211_REG_CELLULAR_HINTS
+	depends on BACKPORT_CFG80211_CERTIFICATION_ONUS
 	---help---
 	  This option enables support for parsing regulatory hints
 	  from cellular base stations. If enabled and at least one driver
@@ -90,9 +96,10 @@ config CFG80211_REG_CELLULAR_HINTS
 	  feature if you have tested and validated this feature on your
 	  systems.
 
-config CFG80211_REG_RELAX_NO_IR
+config BACKPORT_CFG80211_REG_RELAX_NO_IR
 	bool "cfg80211 support for NO_IR relaxation"
-	depends on CFG80211_CERTIFICATION_ONUS
+	depends on !CFG80211_REG_RELAX_NO_IR
+	depends on BACKPORT_CFG80211_CERTIFICATION_ONUS
 	---help---
 	 This option enables support for relaxation of the NO_IR flag for
 	 situations that certain regulatory bodies have provided clarifications
@@ -114,9 +121,10 @@ config CFG80211_REG_RELAX_NO_IR
 	 support this feature by declaring the appropriate channel flags and
 	 capabilities in their registration flow.
 
-config CFG80211_DEFAULT_PS
+config BACKPORT_CFG80211_DEFAULT_PS
 	bool "enable powersave by default"
-	depends on CFG80211
+	depends on !CFG80211_DEFAULT_PS
+	depends on BACKPORT_CFG80211
 	default y
 	help
 	  This option enables powersave mode by default.
@@ -125,19 +133,21 @@ config CFG80211_DEFAULT_PS
 	  applications instead -- they need to register their network
 	  latency requirement, see Documentation/power/pm_qos_interface.txt.
 
-config CFG80211_DEBUGFS
+config BACKPORT_CFG80211_DEBUGFS
 	bool "cfg80211 DebugFS entries"
-	depends on CFG80211
+	depends on !CFG80211_DEBUGFS
+	depends on BACKPORT_CFG80211
 	depends on DEBUG_FS
 	---help---
 	  You can enable this if you want to debugfs entries for cfg80211.
 
 	  If unsure, say N.
 
-config CFG80211_INTERNAL_REGDB
+config BACKPORT_CFG80211_INTERNAL_REGDB
 	bool "use statically compiled regulatory rules database" if EXPERT
+	depends on !CFG80211_INTERNAL_REGDB
 	default n
-	depends on CFG80211
+	depends on BACKPORT_CFG80211
 	---help---
 	  This option generates an internal data structure representing
 	  the wireless regulatory rules described in net/wireless/db.txt
@@ -156,24 +166,27 @@ config CFG80211_INTERNAL_REGDB
 
 	  Most distributions have a CRDA package.  So if unsure, say N.
 
-config CFG80211_WEXT
+config BACKPORT_CFG80211_WEXT
 	bool "cfg80211 wireless extensions compatibility" if !CFG80211_WEXT_EXPORT
-	depends on CFG80211
+	depends on !CFG80211_WEXT
+	depends on BACKPORT_CFG80211
 	depends on WEXT_CORE
-	default y if CFG80211_WEXT_EXPORT
+	default y if BACKPORT_CFG80211_WEXT_EXPORT
 	help
 	  Enable this option if you need old userspace for wireless
 	  extensions with cfg80211-based drivers.
 
-config CFG80211_WEXT_EXPORT
+config BACKPORT_CFG80211_WEXT_EXPORT
 	bool
-	depends on CFG80211
+	depends on !CFG80211_WEXT_EXPORT
+	depends on BACKPORT_CFG80211
 	help
 	  Drivers should select this option if they require cfg80211's
 	  wext compatibility symbols to be exported.
 
-config LIB80211
+config BACKPORT_LIB80211
 	tristate
+	depends on !LIB80211
 	default n
 	help
 	  This options enables a library of common routines used
@@ -181,18 +194,22 @@ config LIB80211
 
 	  Drivers should select this themselves if needed.
 
-config LIB80211_CRYPT_WEP
+config BACKPORT_LIB80211_CRYPT_WEP
 	tristate
+	depends on !LIB80211_CRYPT_WEP
 
-config LIB80211_CRYPT_CCMP
+config BACKPORT_LIB80211_CRYPT_CCMP
 	tristate
+	depends on !LIB80211_CRYPT_CCMP
 
-config LIB80211_CRYPT_TKIP
+config BACKPORT_LIB80211_CRYPT_TKIP
 	tristate
+	depends on !LIB80211_CRYPT_TKIP
 
-config LIB80211_DEBUG
+config BACKPORT_LIB80211_DEBUG
 	bool "lib80211 debugging messages"
-	depends on LIB80211
+	depends on !LIB80211_DEBUG
+	depends on BACKPORT_LIB80211
 	default n
 	---help---
 	  You can enable this if you want verbose debugging messages
-- 
2.5.1

